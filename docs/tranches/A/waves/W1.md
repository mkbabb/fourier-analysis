# A.W1 — Attribute and land the glass-ui migration cohort

## State

**Name**: W1 — Attribute and land the glass-ui migration cohort.
**Opens after**: W0 (the *Open, challenge, hygiene, numerical-test repair* wave — build verified green; submodule wiring committed; challenge discharged).
**Agents**: 3 parallel.
**Hard gate**: working tree clean; `vue-tsc -b --force` and `npm run build` green; `uv run pytest` green (the W0 brittleness window restored — see `A.md §10`); every retired file's glass-ui replacement wired and rendering (deletion proof + browser observation).
**Status**: planned.

## Goal criterion

W1 succeeds when fourier-analysis's working tree is clean for the first time across the constellation's recent history — every file the in-flight glass-ui v1.8.5 migration touched is committed under this plan with its retirement empirically verified against a wired, rendering glass-ui replacement (the substrate-with-consumer discipline at `docs/precepts/glossary/meta-terms.md §"Substrate-with-consumer"`). The aim is to convert the K-invariant-3 shadow-execution anti-pattern's fourth recurrence (the C1 chronic-deferral item) into an attributed, reviewable commit sequence, with the lone exception of `BouncyToggle.vue` named explicitly as flagged-for-rework rather than silently deferred.

## Scope

Numbered scope bullets carry Goal + What + Why + Completion (the agent-unit explication discipline at `docs/precepts/instructions/STYLE.md §"Wave item explication"`):

1. **Cohort attribution.**
   - *Goal*: bind the 109-file in-flight cohort to tranche A so the work can be reviewed under a plan rather than carried as a perpetual "dirty working tree" entry.
   - *What*: stage and commit the 109-file in-flight cohort (62 modified + 31 deleted + 16 untracked, per the H1 hardening observation) — a near-complete, internally clean glass-ui v1.8.5 migration — under A.W1.
   - *Why*: this is the C1 chronic-deferral closure (the cohort has sat uncommitted across glass-ui's O, P, and Q tranches, named "working tree DIRTY — same in-flight refactor" at every constellation audit), and the K-invariant-3 fourth-recurrence remedy.
   - *Completion*: `git status` clean after W1; every file from the open-time cohort attributed to a W1 commit (or escalated as a defect, not patched).

2. **Replacement verification before commit.**
   - *Goal*: convert each deletion from an implicit deletion into a witnessed retirement — no deletion lands without a wired, rendering glass-ui replacement.
   - *What*: confirm, for every deleted file, that the glass-ui replacement is wired and rendering. The research pass found this clean across most files (`web/src/components/ui/select/*`, `GlassDock.vue`, `ToastContainer.vue`, `UnderlineTabs.vue`, `Collapsible.vue`, `usePaperSearch.ts`, `paperSearchIndex.ts`, `useHoverCard.ts`, root-level `FourierMorphDemo.vue` / `FourierShapeExtractor.vue`). **`BouncyToggle.vue` is flagged-for-rework** per H1 hardening — its replacement is not empirically discharged; W1 either confirms a glass-ui replacement renders, or rolls back the deletion pending W3 (the *Interactive-primitive adoption* wave).
   - *Why*: invariant 4 (substrate lands with its consumer) — a deletion without a verified replacement is the same anti-pattern in reverse, a substrate retired without proof the consumer reroute landed.
   - *Completion*: the deletion ledger (scope item 5 below) renders one row per deletion with the `disposition` column populated; the lone `flagged-for-rework` row is `BouncyToggle.vue`.

3. **Coherent reviewable commit sequence.**
   - *Goal*: leave behind a commit history a reviewer can read by surface rather than a single landslide commit.
   - *What*: commit in this fixed sequence — (i) the W0 wiring chunk; (ii) the W1.a.1 web deletions + rewires; (iii) the W1.a.2 `style.css` decomposition; (iv) the W1.b api admin / auth / gallery feature; (v) the W1.c Docker / nginx infra. Every commit subject cites `A.W1.{a..c}`.
   - *Why*: a single mega-commit defeats every subsequent invariant-6 (gates close on evidence) check that needs to bisect to a specific surface.
   - *Completion*: five commits land in the named order, each tagged by tranche-letter + agent unit in the subject line; PROGRESS.md records the hashes.

4. **No new work inside W1.**
   - *Goal*: keep W1 a pure attribution wave — no scope creep into refactor or abrogation that would muddy the cohort attribution evidence.
   - *What*: refactors, abrogations, and new primitives are out of W1; W1 attributes and lands what already exists in the working tree.
   - *Why*: every other A wave depends on a clean post-W1 tree; mixing new work into W1 conflates the cohort attribution with new mutations and breaks downstream wave bounds.
   - *Completion*: no commit in this wave introduces a file outside the open-time `git status` cohort or the named ledger artefact.

5. **Deletion ledger as a committed artefact.**
   - *Goal*: every retirement carries a row a future auditor can read without re-running the W1 dispatch.
   - *What*: ship the deletion ledger as a committed artefact at `docs/tranches/A/audit/W1-deletion-ledger.md` (not as a pointer to the audit doc). The format is 11 columns per row: `path | kind | category | replacement_path | replacement_kind | consumer_import_site(s) | confirming_route(s) | verification_command | disposition | audit_source | commit_chunk`. The `disposition` column is typed: `verified-clean` / `verified-with-route-evidence` / `flagged-for-rework` / `flagged-for-retire`.
   - *Why*: invariant 6 (gates close on evidence) — a deletion proof that lives only in audit chatter is not an artefact a close-honesty checklist can verify.
   - *Completion*: the ledger file exists with one row per deletion; the pre-populated H1 breakdown discharges (20 glass-ui-shadow-copy + 4 directory-relocation + 4 module-fold + 3 auth-store-replacement); `BouncyToggle.vue` carries `flagged-for-rework`.

## File bounds

| File | Access |
|---|---|
| the 109 paths reported by `git status` at W1 open | modify (commit as-is) |
| `docs/tranches/A/PROGRESS.md` | modify |
| `docs/tranches/A/audit/W1-deletion-ledger.md` | create |

Do NOT touch: `web/src/styles/fourier-overrides.css`, `ios-fixes.css`, `buttons.css` (W2 — the *Override-stylesheet abrogation* wave — owns abrogation; W1 commits these in their current state); any file not already dirty at W1 open.

## Triumvirate dispatch

The triumvirate (the recovery pattern at `docs/precepts/glossary/meta-terms.md §"Triumvirate"`) fires if:
- the file bounds expand beyond the open-time `git status` cohort (a scope-reveal — see `glossary/meta-terms.md §"Scope reveal"`);
- a deletion's replacement-verification fails for more than `BouncyToggle.vue` (a hard-gate failure not local-edit-recoverable);
- the build fails on the post-cohort tree after three sequential local-edit attempts (the diagnostic-loop trigger).

## Agent units

### A.W1.a — Web migration cohort

- Goal: every deleted `web/` component or composable has its glass-ui replacement rendering in the dev server before its deletion commit lands.
- Mechanism: walk each deleted `web/` path; confirm its glass-ui replacement (the router target, the root-barrel export, the rendered surface in-browser); stage and commit the web component + `style.css` decomposition chunk.
- Files: `web/src/components/**`, `web/src/composables/**`, `web/src/lib/**`, `web/src/style.css`, `web/index.html`, `web/src/App.vue`.
- Sub-gate: `vue-tsc -b --force` exits 0; the dev server renders every migrated route without console error.

### A.W1.b — API feature cohort

- Goal: the +778-line admin / auth / gallery api delta lands attributed, with no stub, shadow endpoint, or unconsumed model surviving the commit.
- Mechanism: review the api delta for stubs, shadow endpoints, or unconsumed models (invariants 2 and 4); commit the api chunk.
- Files: `api/**` — routers, services, models, `main.py`, `dependencies.py`, `Dockerfile`.
- Sub-gate: `uv run pytest` green; `uv run ruff check api` clean; no endpoint reachable from the changed routers ships without a test.

### A.W1.c — Infra surface cohort

- Goal: the Docker / nginx / `.env.example` delta lands attributed and validates under `docker compose config`, with the deployment-behaviour-changing rule (local instructions) satisfied.
- Mechanism: commit the Docker / nginx / env-example delta; confirm it changes deployment behaviour only within the wave that owns it (per `docs/instructions/README.md`) — W1 owns it because the cohort already contains it.
- Files: `docker-compose.yml`, `docker-compose.prod.yml`, `nginx/fourier.conf`, `.env.example`, `.gitignore`, `scripts/dev.sh`.
- Sub-gate: `docker compose config` validates both compose files.

## Hard gate (completion criterion, item-by-item)

1. `git status` reports a clean working tree after W1's commits.
2. `vue-tsc -b --force` exits 0; `npm run build` in `web/` succeeds — output captured in `PROGRESS.md`.
3. `uv run pytest` green — output captured.
4. Deletion proof: `docs/tranches/A/audit/W1-deletion-ledger.md` exists with one row per deletion in the 11-column format `path | kind | category | replacement_path | replacement_kind | consumer_import_site(s) | confirming_route(s) | verification_command | disposition | audit_source | commit_chunk`. The `disposition` column is typed: `verified-clean` / `verified-with-route-evidence` / `flagged-for-rework` / `flagged-for-retire`. The pre-populated H1 breakdown discharges: 20 glass-ui-shadow-copy + 4 directory-relocation + 4 module-fold + 3 auth-store-replacement; `BouncyToggle.vue` is the lone `flagged-for-rework` row pending W3.
5. No stub, shadow API, or `*_v2` sibling was introduced — the cohort lands as-is, or a defect is escalated rather than patched.

## Verification artefacts

- `docs/tranches/A/audit/W1-deletion-ledger.md` — deleted file → replacement → confirming route, in the 11-column format.
- Build / test output captured in `PROGRESS.md`.
- Commit hashes for each cohort chunk recorded in `PROGRESS.md`.

## Dependencies

- **Depends on**: W0 (the *Open, challenge, hygiene, numerical-test repair* wave).
- **Blocks**: W2, W3, W4, W5, W6 — every subsequent wave edits a post-W1 clean tree.

## Archaeology

This cohort has sat uncommitted across glass-ui's O, P, and Q tranches, named "working tree DIRTY — same in-flight refactor" at each constellation audit (the C1 chronic item, audit `a-plan-archaeology.md`). Prior failure mode: no plan folder existed to attribute it to, so it was never committed and never verified against a stable build. New guardrail: W1 commits it under this plan; A invariant 7 (no silent deferral) forbids it recurring as a fourier AB+1.

## Discharge

W1 closed at commit `83e3a14` on 2026-05-26. Seven sub-agent commits constitute the wave work (per `PROGRESS.md §"2026-05-26 — W1 close ceremony"`):

| # | Hard-gate item | Evidence |
|---|---|---|
| 1 | `git status` clean post-W1 | sole residual `?? web/src/styles/` intentionally preserved per `A.md §6` (W2 scope); discharged |
| 2 | `vue-tsc -b --force` + `npm run build` green | 2.99 s build at W1 close |
| 3 | `uv run pytest` green | 89 passed (W0.c brittleness window held) |
| 4 | 11-column deletion ledger | `audit/W1-deletion-ledger.md` — 31 rows: 17 verified-clean / 13 verified-with-route-evidence / 1 flagged-for-rework (`BouncyToggle.vue`, discharged at W3.b via `<Switch>` adoption) / 0 flagged-for-retire |
| 5 | No stub / shadow API / `*_v2` sibling | three preserved bugs (contour-hash, Mongo password, batch-contract) carried to named W4 / W5 successors per invariant 7 |

AMEND absorptions inherited from W0-challenge §4: row 1 (cohort count 109 → 110 under `--untracked-files=all`) and the W1.a sub-agent commit-order observation (sibling `3926205` over-staging, rebased away; W1.a.1 → W1.a.3 → log → W1.a.2 → reconcile) — both recorded in PROGRESS log entries and discharged at W6 close (`FINAL.md §4`). C1 chronic-deferral closed; K-invariant-3 fourth-recurrence remedy lands.
