# A.W3 — Interactive-primitive adoption

## State

**Name**: W3 — Interactive-primitive adoption.
**Opens after**: W2 (the *Override-stylesheet abrogation* wave — override layer gone; the button system has a canonical target).
**Agents**: 4 parallel.
**Hard gate**: native interactive `<button>` count → 0 (or per-site justified in a ledger); `AnimatedDigit` / `MetricRow` / `MetricStack` / `MetricCell` / `StatusDot` / `Skeleton` adopted at every audited site; browser evidence; build green.
**Status**: planned.

## Goal criterion

W3 succeeds when fourier-analysis adopts glass-ui's interactive vocabulary across the consumer tree: native `<button>` retires in favour of `<Button>` + `buttonVariants`; the AB+1 primitive cohort (`AnimatedDigit`, `MetricRow / Stack`, `MetricCell`, `StatusDot`, `Skeleton`) renders at every audited site; the duplicate `@keyframes` shadowing glass-ui's canon retires; and the hand-rolled hover / press / focus / disabled idiom routes through `.interactive-item` / `.focus-ring` / `.active-scale` / `.disabled-base`. The aim is to close the fourth-recurrence anti-pattern in CSS form (the reinvented button system, the unadopted AB+1 cohort across glass-ui's P and Q tranches), discharging constellation P12 (the cross-repo carry catalogued at `coordination/CONSTELLATION.md`) and the C4-residual composable folds.

## Scope

1. **Native button migration.**
   - *Goal*: every interactive `<button>` in `web/src/` lands as `<Button>` (or a justified residue named in the W3 ledger).
   - *What*: migrate the 89 native `<button>` elements to glass-ui's `<Button>` + `buttonVariants`. H2 hardening confirmed the count: 79 git-tracked + 10 in `morph/*` working-tree files committed by W1 (the *Attribute and land the glass-ui migration cohort* wave). **Non-trivial sites named by H2**: `ImageUpload.vue:121` and `VisualizationView.vue:220` need `<Button as="label">` for the file-upload labels (not plain `<Button>`); `MobileFloatingToc.vue` summary triggers use `<Collapsible.Trigger asChild>` wrapping a `<Button>`, not plain `<Button>`.
   - *Why*: this is why `buttons.css` existed; W3 finishes what W2 (the *Override-stylesheet abrogation* wave) began by removing the reinvented button system entirely. Invariant 10 (component-over-CSS-class) and invariant 1 (KISS / DRY) both bind here.
   - *Completion*: `git grep` for interactive native `<button>` in `web/src/` returns zero or the remaining sites are each justified in `audit/W3-button-ledger.md`; `buttons.css` deleted.

2. **Canonical interactive vocabulary.**
   - *Goal*: hover / press / disabled / focus idiom across the consumer tree comes from glass-ui's canonical utilities, with no surviving ad-hoc bezier strings.
   - *What*: adopt `.interactive-item`, `.focus-ring`, `.active-scale`, `.disabled-base` at hover / press / disabled / focus sites — replacing ad-hoc transforms and bespoke `cubic-bezier` strings (audit `c-style-consumer.md` found ~20 hand-rolled bezier strings and `--ease-out-expo` reinvented 10×).
   - *Why*: invariant 10 — a styling need met by an `@apply` utility never gets a hand-rolled bezier.
   - *Completion*: `git grep cubic-bezier` and `git grep "transition: all"` in `web/src/` return zero matches; the hand-rolled bezier sites audit C named have all migrated.

3. **AB+1 primitive cohort adoption.**
   - *Goal*: discharge the constellation P12 chronic carry — the AB+1 primitive cohort glass-ui published two tranches back and fourier never adopted.
   - *What*: adopt the published glass-ui primitives at the sites audit `d-style-glassui.md` named:
     - `AnimatedDigit` (`@mkbabb/glass-ui/animated-digit`) — **69** hand-rolled `fira-code` tabular readouts (H2 re-counted from audit C's ~30 by including every readout the consumer rolled, not just the ones marked in audit C).
     - `MetricRow` / `MetricStack` (`/metric-stack`) — coefficient and convergence readout clusters.
     - `MetricCell` / `MetricBadge` (`/metric-cell`) — energy / component pills.
     - `StatusDot` (`/status-dot`) — gallery tier filters.
     - `Skeleton` (root barrel `@mkbabb/glass-ui` — H2 doc-nit correction; the `/skeleton` subpath audit D suggested does not exist) — bespoke loading-shimmer rectangles.
   - *Why*: invariant 4 (substrate lands with its consumer) applied in reverse — the substrate landed in glass-ui; fourier finishes the wiring rather than carrying the gap into a fourth recurrence.
   - *Completion*: each primitive imports and renders at the audited sites; the deleted markup is gone (verified by browser observation per site, recorded in `audit/W3-adoption-ledger.md`).

4. **`@keyframes` de-duplication.**
   - *Goal*: no consumer-side `@keyframes` shadows a glass-ui canonical animation by name.
   - *What*: eliminate the duplicate `@keyframes` shadowing glass-ui's canon. The H2 audit observes **14 total `@keyframes` in fourier `web/src`, of which 7 duplicate a glass-ui canonical animation by name** (audit C's "12 duplicate" conflated name-collision with primitive-shadow).
   - *Why*: a name-shadow defeats the substrate's animation contract silently — the consumer's local keyframes win the cascade and the substrate's tuning is invisible.
   - *Completion*: `git grep` finds no `@keyframes` duplicating a glass-ui canonical animation name; motion routes through glass-ui's `transitions.css` tokens, bracketed by `prefers-reduced-motion`.

5. **D5 fold — `SliderControl.vue` variant prop residual.**
   - *Goal*: the H1-flagged variant prop residual in `SliderControl.vue` resolves explicitly rather than drifts into the next tranche.
   - *What*: inside `SliderControl.vue`, adopt the appropriate glass-ui Slider variant or retire the variant prop with rationale.
   - *Why*: invariant 7 (no silent deferral) — the H1 hardening pass surfaced the residual; W3 owns its disposition.
   - *Completion*: the variant prop either resolves to a glass-ui variant or carries a retire-with-rationale row in `audit/W3-adoption-ledger.md`.

6. **C4-residual fold — `useTouchGate`, `useResizeObserver` composables.**
   - *Goal*: the two composables H1 flagged either adopt a glass-ui replacement, retire with rationale, or carry to fourier tranche C with a named destination — never silent.
   - *What*: verify each has a glass-ui composable replacement and adopt; otherwise retire-with-rationale or carry to fourier tranche C with a named destination.
   - *Why*: invariant 7 again — same flag class as item 5, same explicit-disposition discipline.
   - *Completion*: each composable carries a disposition row in `audit/W3-adoption-ledger.md`.

## File bounds

| File | Access |
|---|---|
| every component with a native `<button>` (89 sites per H2 hardening) | modify |
| readout / metric / loading components | modify |
| `web/src/components/ui/SliderControl.vue` | modify (D5 fold) |
| `web/src/composables/useTouchGate.ts`, `useResizeObserver.ts` | modify or delete (C4-residual fold) |
| `docs/tranches/A/PROGRESS.md` | modify |
| `docs/tranches/A/audit/W3-button-ledger.md`, `W3-adoption-ledger.md` | create |

Do NOT touch: `api/**`; `web/src/lib/**` logic; CRUD stores (W4 — the *Scaling, KISS and correctness pass* wave — owns those).

## Triumvirate dispatch

The triumvirate fires if:
- the file bounds expand beyond consumer components + the named composables (a scope-reveal — the AB+1 cohort revealing a third undocumented primitive would trigger it);
- a glass-ui primitive's adoption requires a substrate-side fix the consumer cannot apply (a hard-gate failure not local-edit-recoverable — escalate via `coordination/CONSTELLATION.md`);
- `<Button as="label">` or the `Collapsible.Trigger asChild` pattern at the H2-named sites fails to render in three sequential local-edit attempts (the diagnostic-loop trigger).

## Agent units

### A.W3.a — Button migration, equation + morph + paper surfaces

- Goal: zero native interactive `<button>` in the equation / morph / paper trees; every site renders via `<Button>` or a justified ledger row.
- Mechanism: replace native `<button>` with `<Button>` / `buttonVariants`; map each ad-hoc style to a variant.
- Files: `web/src/components/{equation,morph,paper}/**`, `web/src/components/layout/**`.
- Sub-gate: zero native interactive `<button>` in scope; browser render parity.

### A.W3.b — Button migration, visualization + gallery surfaces

- Goal: zero native interactive `<button>` in the visualization / gallery trees, and `buttons.css` deleted once a + b both confirm no surviving consumer.
- Mechanism: as in a, for the visualization and gallery component trees; delete `buttons.css` once a and b confirm no consumer.
- Files: `web/src/components/visualization/**`, `web/src/components/ui/**`.
- Sub-gate: zero native interactive `<button>` in scope; `buttons.css` deleted.

### A.W3.c — Metric / readout primitive adoption

- Goal: the AB+1 primitive cohort renders at every site `d-style-glassui.md` named, with the hand-rolled markup it replaced empirically gone.
- Mechanism: adopt `AnimatedDigit`, `MetricRow / Stack`, `MetricCell / Badge`, `StatusDot` at the audited sites; delete the hand-rolled `fira-code` readout markup they replace.
- Files: coefficient / convergence / energy readout components; gallery tier filters.
- Sub-gate: the adoption count matches the `d-style-glassui.md` site list; the deleted markup is proven gone via `git grep`.

### A.W3.d — `Skeleton` + motion-vocabulary cleanup

- Goal: loading states render through `Skeleton`; the duplicate `@keyframes` shadowing glass-ui's canon are gone; motion routes through `transitions.css` tokens.
- Mechanism: adopt `Skeleton` for loading states; delete the duplicate `@keyframes` and bespoke Transition classes; route motion through glass-ui's `transitions.css` tokens, bracketed by `prefers-reduced-motion`.
- Files: loading-state components; component `<style>` blocks.
- Sub-gate: `git grep` finds no `@keyframes` duplicating a glass-ui canon name; no `transition: all`.

## Hard gate (completion criterion, item-by-item)

1. `git grep` for interactive native `<button>` in `web/src/` returns zero, or each remaining site is justified in `audit/W3-button-ledger.md`.
2. `buttons.css` deleted; deletion proof recorded.
3. Each glass-ui primitive in scope item 3 is imported and rendering — browser observation per site, recorded in `audit/W3-adoption-ledger.md`.
4. No `@keyframes` duplicates a glass-ui canonical animation name; no `transition: all` in `web/src/`.
5. `npm run build` and `vue-tsc -b --force` green.

## Verification artefacts

- `docs/tranches/A/audit/W3-button-ledger.md` — migration count plus any justified residue.
- `docs/tranches/A/audit/W3-adoption-ledger.md` — primitive → adopted sites; D5 and C4-residual dispositions.
- Browser screenshots of representative readout / loading states.

## Dependencies

- **Depends on**: W2 (the *Override-stylesheet abrogation* wave).
- **Blocks**: W5 (the *Admin parity and functionality close* wave) — admin parity reuses the migrated button + primitive vocabulary; W6 (close).

## Archaeology

The AB+1 primitive cohort (`AnimatedDigit`, `Metric*`) was published by glass-ui and surfaced as adoptable in two consecutive constellation tranches (P, Q) without fourier adopting it — the C4 chronic item. The reinvented button system (`buttons.css` + 89 native buttons) is the same anti-pattern in CSS form. New guardrail: A invariant 10 (component-over-CSS-class) and the W3 hard gate's grep-zero condition.

## Discharge

W3 closed at commit `8a608e5` on 2026-05-26 (orchestrator close-log at `1376a95`; browser smoke screenshots at `6c7b21d`). Eight sub-agent commits constitute the wave (per `PROGRESS.md §"2026-05-26 — W3 close ceremony"`):

| # | Hard-gate item | Evidence |
|---|---|---|
| 1 | Native `<button>` count → 0 or per-site justified | 68 retired (W3.a 32 + W3.b 36); ~9 justified residue documented at `audit/W3-button-ledger.md` |
| 2 | `buttons.css` deleted | SATISFIED via W2.e (`10e616c`) per the user-directive in-band scope absorption that pulled the recipe migration into W2 |
| 3 | AB+1 P12 primitive cohort adopted | 13 MetricBadge across 8 files at W3.c (`6049995`); 5 primitives + 2 composables retire-with-rationale at `audit/W3-adoption-ledger.md` |
| 4 | `@keyframes` shadow + motion-vocab cleanup | shadow `@keyframes` 6 → 0; `cubic-bezier` 29 → 0; `transition: all` 26 → 0 (W3.d `59f270a` + `8a608e5`) |
| 5 | `npm run build` + `vue-tsc -b --force` green | 8.28 s build at W3 close |

AMEND absorptions inherited from W0-challenge §4: row 4 (`fira-code` count rerune to 82, discharged at W3.c `04cf719` with 13 adopted + 55 kept-as-decorative + ledger row enumeration) and row 9 (`@keyframes` 14 → 16, shadows 7 → 6 — discharged at W3.d with 6 verified shadows; the candidate seventh `tab-slide-in` is a W2.b-folded fourier-local carry, not a glass-ui shadow). Constellation P12 partially discharged. `BouncyToggle.vue` discharged via W3.b's `<Switch>` adoption at ExportModal (the canonical primitive for the binary on/off control the bouncy-toggle approximated).
