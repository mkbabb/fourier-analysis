# A.W2 — Override-stylesheet abrogation

## State

**Name**: W2 — Override-stylesheet abrogation.
**Opens after**: W1 (the *Attribute and land the glass-ui migration cohort* wave — clean tree).
**Agents**: 4 parallel.
**Hard gate**: `web/src/styles/fourier-overrides.css`, `ios-fixes.css`, and `buttons.css` all do not exist (per H2 hardening — `buttons.css` deletes outright; the "slider stub" residual is discharged); no consumer CSS re-declares a glass-ui token; before / after browser screenshots show parity; build green.
**Status**: planned.

## Goal criterion

W2 succeeds when fourier-analysis's `web/src/styles/` directory carries no consumer-authored override layer: the stale fork of `glass-ui/tokens.css` is gone, the iOS-fix file is gone, the reinvented button stylesheet is gone, and every surviving rule has either folded into its owning component idiomatically or filed upstream as a glass-ui addition. The aim is that invariant 10 (token-first, component-over-CSS-class) holds visibly across the consumer tree — a `git grep` for any glass-ui token re-declaration in `web/src/` returns nothing.

## Scope

Each numbered scope item carries Goal + What + Why + Completion.

1. **Delete `fourier-overrides.css` in full.**
   - *Goal*: retire the stale fork of `glass-ui/tokens.css` v1.8.5 — the consumer's drift dressed as configuration.
   - *What*: delete `fourier-overrides.css`. Roughly 70% of its 354 lines re-declare tokens glass-ui already ships (the `--section-color-0..12`, `--viz-*`, `--accent-*`, `--tier-*`, `--success / warning / info`, `--shadow-cartoon*` families, `--type-admin-label / -micro`, and the `@utility cm-serif / fira-code / fourier-f / text-micro / text-admin-label` blocks). The hardened per-rule disposition: **30 delete / 7 fold-to-component / 4 lift-to-glass-ui** (H2 expanded audit C's "~24/7/4" by separating light + dark forks into their own rows).
   - *Why*: invariant 10 — consumer CSS that re-declares a glass-ui token is drift, not configuration; invariant 3 (no legacy code) forbids leaving the fork commented out or feature-flagged.
   - *Completion*: the file does not exist; the W2 disposition ledger (item 7 below) shows every rule discharged as delete / fold / lift.

2. **Delete `ios-fixes.css`.**
   - *Goal*: retire the iOS-fix file by folding its component rule into the owner and filing the second rule upstream.
   - *What*: delete `ios-fixes.css`. Its 2 rules discharge as 1 fold-to-component + 1 glass-ui addition.
   - *Why*: same invariant 10 logic as item 1.
   - *Completion*: the file does not exist; the surviving rule renders in its new home; the lifted rule is filed in `coordination/CONSTELLATION.md`.

3. **Delete `buttons.css` outright.**
   - *Goal*: retire the consumer-side reinvented button system entirely; the actual button migration moves to W3 (the *Interactive-primitive adoption* wave).
   - *What*: delete `buttons.css` outright (the H2 reconciliation). Audit C's "2 glass-ui-addition" candidates collapse to zero, because (a) the `.styled-slider` recipe is discharged by `<Slider variant="glass-scrubber">` (shipped at `glass-ui/src/components/ui/slider/index.ts:21`), and (b) the native-range `:not(.styled-slider)` recipe has zero consumers in the actual tree. The button migration itself is W3's scope; W2 deletes the file and W3 finishes consumer-side adoption.
   - *Why*: invariant 1 (KISS / DRY) — the reinvented button system is the same primitive twice; killing the duplication first lets W3 adopt against a canonical glass-ui target without two competing styles fighting.
   - *Completion*: `git grep buttons.css` returns nothing; `style.css`'s `@import` for the file is gone.

4. **Repair the two regressed forks.**
   - *Goal*: bring the two known-regressed token forks back onto canonical utilities before the fork file disappears.
   - *What*: repair the `fourier-f` fork (which dropped the canonical italic + `--font-display` + variation-settings — restored by using glass-ui's utility, not the fork) and the neutral ladder (the palette never adopted `--neutral-0..5` — re-rooted on the canonical perceptual ladder).
   - *Why*: invariant 8 (numerical / visual correctness precedes UI polish) bound to invariant 10 — a regressed fork in flight is worse than its glass-ui canon.
   - *Completion*: both repairs render in the screenshot pairs; the rules they replace are the rows audit `c-style-consumer.md` named.

5. **Remove the two dead glass tokens.**
   - *Goal*: retire the two tokens that reference glass-ui's already-retired v0.8.0 4-tier glass ladder.
   - *What*: remove `--glass-opacity-subtle` and `--glass-blur-default` from the override file (along with the file itself) — both reference a retired glass-ui ladder.
   - *Why*: invariant 5 (no overfitting) — a token with no live consumer in the current glass-ui ladder is dead surface.
   - *Completion*: `git grep` for either token returns nothing.

6. **Trim `style.css` to its `@import` lines only.**
   - *Goal*: `style.css` carries only what it must — `@import` plumbing — and loses every import of the deleted files.
   - *What*: `style.css` keeps its `@import` lines only, and drops the imports of the deleted files.
   - *Why*: invariant 10 — the entry stylesheet should not double as a token-override sink.
   - *Completion*: the file's diff in W2 is purely import-line removal; build green.

7. **Discharge the lift candidates against the upstream.**
   - *Goal*: the rules that legitimately need glass-ui (rather than the consumer side) reach the constellation as named carries before W2 closes.
   - *What*: reconcile the ~4 + 1 + 2 lift candidates — a styled progress-fill range slider, a native `input[type=range]` recipe, `--font-size-root`, a canonical `::selection`, and `--ease-overshoot` — at the W0 challenge step against audit `d-style-glassui.md` (which found GlassScrubber already shipped). Confirmed gaps are filed to the constellation per `coordination/CONSTELLATION.md`; until upstream lands them, a minimal named local carry, never a re-fork.
   - *Why*: the substrate-with-consumer discipline applied in reverse — fourier files the carry rather than forking the substrate.
   - *Completion*: each lift candidate is named in the W2 disposition ledger with a destination (`coordination/CONSTELLATION.md` row, or a named local carry).

8. **D4-residual fold.**
   - *Goal*: dispatch the two H1-flagged silent-deferral risks ("Card migration" and "CVA decision") explicitly, with their disposition written down.
   - *What*: name each in the W2 disposition ledger with its disposition — fold to component, file to glass-ui as a variant, or retire-with-rationale.
   - *Why*: invariant 7 (no silent deferral) — an unnamed deferral in a styling pass is the same anti-pattern as an unnamed cross-tranche debt.
   - *Completion*: both items appear in the ledger with one of the three named dispositions.

## File bounds

| File | Access |
|---|---|
| `web/src/styles/fourier-overrides.css` | delete |
| `web/src/styles/ios-fixes.css` | delete |
| `web/src/styles/buttons.css` | delete |
| `docs/tranches/A/PROGRESS.md` | modify |
| `docs/tranches/A/audit/W2-disposition-ledger.md` | create |
| `web/src/style.css` | modify-carve (drop dead imports) |
| component `<style>` blocks receiving folded rules | modify |

Do NOT touch: `api/**`; component logic / template (W2 touches `<style>` blocks and class attributes only); `web/src/lib/**`.

## Triumvirate dispatch

The triumvirate fires if:
- the file bounds expand beyond the styles directory + the named folding components (a scope-reveal);
- a token deletion breaks a glass-ui rendering surface that cannot be recovered by a single re-import (a hard-gate failure not local-edit-recoverable);
- the visual-regression pass (agent unit d) flags drift the a–c agents cannot reconcile in three iterations (the diagnostic-loop trigger).

## Agent units

### A.W2.a — Token de-fork

- Goal: every glass-ui-token re-declaration in `fourier-overrides.css` is gone, and every consumer site references the glass-ui token directly.
- Mechanism: delete every `fourier-overrides.css` rule that re-declares a glass-ui token (~24); confirm by `git grep` that the consumer references the glass-ui token directly. Restore `fourier-f` and the neutral ladder onto canonical utilities.
- Files: `fourier-overrides.css` (toward deletion), `style.css`.
- Sub-gate: `git grep` finds zero consumer re-declarations of a glass-ui token; visual parity on the de-forked palette.

### A.W2.b — Fold-to-component (overrides)

- Goal: each fold-to-component rule renders identically in its new component home, no orphaned selectors.
- Mechanism: relocate the ~7 fold-to-component rules from `fourier-overrides.css` into the `<style>` block of the owning component, idiomatically (`@apply` glass-ui utilities where one exists).
- Files: the ~7 owning components named in `c-style-consumer.md`.
- Sub-gate: each folded rule renders identically; no orphaned selector.

### A.W2.c — `ios-fixes` + `buttons.css` delete

- Goal: `ios-fixes.css` and `buttons.css` both gone; the dead glass tokens gone with them; build green.
- Mechanism: fold the `ios-fixes.css` component rule into its owner; delete `buttons.css` outright (per H2 — no slider stub survives); drop the dead glass tokens.
- Files: `ios-fixes.css` (delete), `buttons.css` (delete), owning components.
- Sub-gate: both files' import lines are gone from `style.css`; `git grep buttons.css` returns nothing; build green.

### A.W2.d — Visual regression evidence

- Goal: every route touched by a–c is captured before and after, and any drift triages back to an a–c fix before W2 closes.
- Mechanism: capture before / after browser screenshots of every route touched by a–c; diff for unintended drift.
- Files: none (read-only browser).
- Sub-gate: screenshot pairs saved; any drift triaged to an a–c fix before close.

## Hard gate (completion criterion, item-by-item)

1. `fourier-overrides.css`, `ios-fixes.css`, and `buttons.css` do not exist; `git grep "fourier-overrides\|ios-fixes\|buttons.css"` returns nothing.
2. The per-rule disposition table in `c-style-consumer.md` is fully discharged — every rule deleted, folded, or filed upstream; no rule unaccounted.
3. No consumer CSS re-declares a glass-ui token (`git grep` of `--section-color`, `--viz-`, `--shadow-cartoon`, etc. in `web/src/` finds only glass-ui-sourced references).
4. Before / after screenshots saved; visual parity confirmed.
5. **D4-residual fold** — the H1-flagged "Card migration" and "CVA decision" silent-deferral risks are named explicitly in `audit/W2-disposition-ledger.md` with their disposition (fold to component, file to glass-ui as a variant, or retire-with-rationale) — no implicit deferral survives.
6. `npm run build` and `vue-tsc -b --force` green.

## Verification artefacts

- `docs/tranches/A/audit/W2-disposition-ledger.md` — the discharged per-rule table.
- Before / after screenshots under `docs/tranches/A/audit/W2-screenshots/`.
- Build output recorded in `PROGRESS.md`.

## Dependencies

- **Depends on**: W1 (the *Attribute and land the glass-ui migration cohort* wave).
- **Blocks**: W3 (the *Interactive-primitive adoption* wave) — the override layer is gone before adoption begins, so adoption targets are canonical.

## Archaeology

`fourier-overrides.css` forked `glass-ui/tokens.css` at some pre-v1.8.5 point and drifted; two forks regressed against the canon. Prior failure mode: a consumer override file treated as configuration rather than drift. New guardrail: A invariant 10 — consumer CSS that re-declares a glass-ui token is drift; W2 deletes the file rather than re-syncing the fork.

## Discharge

W2 closed at commit `5fdf6ff` on 2026-05-26 (orchestrator close-log at `cd019b4`). Sixteen sub-agent commits (W2.a / W2.b / W2.c / W2.d planned; W2.e / W2.f / W2.g / W2.h in-band scope absorption) plus 2 orchestrator commits constitute the wave (per `PROGRESS.md §"2026-05-26 — W2 close ceremony"`):

| # | Hard-gate item | Evidence |
|---|---|---|
| 1 | `fourier-overrides.css` / `ios-fixes.css` / `buttons.css` do not exist | `web/src/styles/` directory `rmdir`'d at W2.e (`10e616c`); `git grep "fourier-overrides|ios-fixes|buttons\.css"` returns only provenance comments |
| 2 | Per-rule disposition discharged | `audit/W2-disposition-ledger.md` carries 86 rows across §W2.a (35) / §W2.b (9) / §W2.c (14) / §W2.d (1) / §W2.e (12) / §W2.f (4) / §W2.g (11) |
| 3 | No consumer CSS re-declares a glass-ui token | `git grep` post-W2.a returns zero |
| 4 | Before / after screenshots | 10 PNGs under `audit/W2-screenshots/` (5 pre-contract-v2 + 5 post-contract-v2 + buttons-abrogation + with-backend gallery) |
| 5 | D4-residual fold (Card migration, CVA decision) | Card filed as constellation carry; CVA retire-with-rationale (zero in-tree `class-variance-authority` imports) |
| 6 | `npm run build` + `vue-tsc -b --force` green | 8.28 s build at W2 close |

AMEND absorptions inherited from W0-challenge §4: row 5 (`ios-fixes.css` "2 rules" → 2 concerns / 3 selector blocks, discharged at W2.c `ae84509`) and row 6 (`buttons.css` outright-delete with 5 live `.styled-slider` + `.btn-*` + `.basis-pill` consumers, discharged at W2.e `10e616c` via the user-directive in-band full abrogation that pulled the W3 button-recipe migration into W2). Three cross-repo concerns discharged in-band: glass-ui font-asset hygiene (`e123dc1`, W2.f), contract-v2 dev-resolution adoption (`a7d1904`, orchestrator scope-reveal), backend Docker validation RATIFY (`5fdf6ff`, W2.h).
