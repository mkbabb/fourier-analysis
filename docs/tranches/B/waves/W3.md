# B.W3 — fourier `visualization` entity + migration + `api/lib/crud/` utility module landing

**Name**: W3 — fourier `visualization` entity + migration + `api/lib/crud/` utility module landing.
**Opens after**: the W1 — CRUD-contract ratification wave's close (the contract is ratified; per H4 §4 the W3 wave implements against the §1–§9 must-have fields; the U1 revision of invariant 16 admits per-language utility modules at the per-language LOC ceiling — Python 525 LOC under the R3 refinement assay §3.1; TypeScript is value.js-side and outside W3's scope).
**Agents**: 3 parallel (partitioned by file per H4 §4.W3's recommendation — agent A owns router carve + utility-helper consumption; agent B owns models + indexes + dependencies + the `api/lib/crud/` landing per U3; agent C owns the migration script + the verification artefact).
**Hard gate**: one `visualizations` collection live; `pytest api/tests/test_visualization_*.py` green; `git grep -E "snapshot_hash|snapshots_collection" api/routers/` returns zero on identity paths; the migration artefact `docs/tranches/B/audit/migration-counts.md` proves no loss (invariant 17); **`api/lib/crud/` landed per `coordination/CRUD-LIB-PY.md` (the U3 spec); `visualizations.py` USES the named helpers (`slug_with_retry`, `cursors.paginate`, `errors.problem`, `etag.require_if_match`, `idempotency.idempotent`, `softdelete.soft_delete` / `restore`, `pinned_cron.cron_prune`); per-language LOC ≤ 525** (the R3 refinement assay §3.1 raised the ceiling from the U1 500-LOC initial bound with the `errors.py` `functools.partial` compression rationale named in §11 of CRUD-LIB-PY).
**Status**: provisional (hardened from B.md §3's row "W3 — fourier visualization entity + migration"; the Wχ — challenge wave's P2 migration-preserves-data probe confirms or narrows the migration plan; U1 admits the utility-module landing alongside the entity).

## Goal criterion

This wave succeeds if fourier's identity model collapses from the five-scheme state (human slug, content hash, uuid4 token, ObjectId, IndexedDB key-derived string) to one `visualizations` collection with one human-readable slug, a required non-null owner, the 3-state `visibility` enum, soft-delete by `deleted_at`, and the supporting indexes — and if the per-language utility module `api/lib/crud/` lands alongside as the substrate the W4 — fourier convergence wiring wave consumes. The wave closes only when the migration is verified (counts + spot-check + post-condition assertions) and the legacy `snapshots` and `gallery` collections remain available as rollback substrate until the W5 — close ceremony retires them.

## Scope

1. Create `api/routers/visualizations.py` implementing CRUD + soft-delete + restore against the `visualizations` collection per `CRUD-CONTRACT.md §1–§5`: `POST /visualizations`, `GET /visualizations/{slug}`, `GET /visualizations` (list with `visibility` + `owner` filters), `PATCH /visualizations/{slug}`, `DELETE /visualizations/{slug}` (soft), `POST /visualizations/{slug}/restore`.
2. Carve-merge `api/routers/snapshots.py` (78 lines) and the gallery identity scheme out of `api/routers/gallery.py` (368 lines): the user-facing gallery becomes a thin filter (`visibility="public"`) over `visualizations`; admin moderation endpoints (`flag` / `dismiss` / `suspend`) move into `api/routers/visualizations.py` or remain in `admin.py` per `CRUD-CONTRACT.md §7`. Retire `snapshots.py` entirely; `gallery.py` becomes a presentational alias or deletes.
3. **Structural restructure of `api/services/image_storage.py`** (the H4-named carry from the fourier-A.W4 — image storage cleanup wave: A.W4 fixed only the contour-hash bug, leaving the file's slug-issuance + content-addressing scaffolding intact): partition into (a) blob storage + content hashing only (`sha256`, `contour_hash` — both *non-identity* per `CRUD-CONTRACT.md §1`), and (b) slug issuance lifted out of `image_storage.py:76-77` (the race-prone check-then-insert) into `api/slugs.py` using the contract's `DuplicateKeyError`-on-unique-index pattern.
4. Update `api/models/{gallery,session,shared}.py`: add `models/visualization.py` (a Pydantic model with `slug, owner_slug, visibility, deleted_at, contour_settings, animation_settings, content_hash, created_at, updated_at`); retire `gallery.py` model's identity-bearing fields; `session.py` carries `last_seen_at` + suspension per `CRUD-CONTRACT.md §6`.
5. Update `api/services/database.py` (110 lines): add the `visualizations` collection with the union of today's gallery indexes (7 compound indexes per H4 §2.R1) plus `owner_slug` + `visibility` + `deleted_at` indexes; the `pinned: bool` retrofit (per the Wχ — challenge wave's P3 probe + H4 §6 P3.4) lands here.
6. Update `api/dependencies.py` (208 lines): tighten `SLUG_PATTERN` to `^[a-z]+(-[a-z]+){3}$` (matches `CRUD-CONTRACT.md §2`); `resolve_session()` returning `None` on a publish-path raises 401 — retires the `user_slug: None` orphan at `api/routers/gallery.py:206` (the Wχ — challenge wave's P2 adversarial case).
7. Update `api/services/janitor.py` (207 lines): retire the unbounded `$nin` query at `:60-65`; convert to the per-document `pinned` flag pattern per `CRUD-CONTRACT.md §8`; retire the `storage_budget_gb` band-aid at `:84-119` (the image-blob honesty resolution per the Wχ — challenge wave's P3 probe).
8. Author `api/scripts/migrate_visualization.py` mirroring the value.js migration idiom (`~/Programming/value.js/api/src/migrate-slugs.ts:25-67`): idempotent, per-document `$set`, progress counts, spot-check sample, a post-condition `countDocuments` of stragglers that fails loudly. Migration runs in-script; the brittleness window per `B.md §8` is within-wave-only. Per the R3 refinement assay §5: the script must consume `api.lib.crud.slugs.slug_with_retry` (not a private TOCTOU re-check loop); the script signature carries a `--dry-run` flag; the spot-check sampling uses `random.seed(42); random.sample(..., 10)` to match `CONFORMANCE-MATRIX.md` C11.3; the file-level docstring documents the rollback substrate ("source collections `snapshots` and `gallery` retained until B.W5 — close ceremony close; revert to old reads by restoring the pre-W3 commit").
9. Author `docs/tranches/B/audit/migration-counts.md`: pre-counts (`snapshots`, `gallery`, the union), post-counts (`visualizations`), a spot-check of 10 random `snapshot_hash` rows (seed=42) showing the union resolution (orphan-snapshot rule + the Wχ — challenge wave's P2 `user_slug: None` resolution rule, named).
10. Author the pytest specs: `api/tests/test_visualization_crud.py` (CRUD), `test_visualization_ownership.py` (anonymous publish → 401; owner-only PATCH / DELETE), `test_visualization_soft_delete.py` (delete → restore → grace-window hard-delete), `test_janitor_bounded_query.py` (asserts no `$nin` over an unbounded set; greps the source file as a runtime check).
11. **Land `api/lib/crud/` per `coordination/CRUD-LIB-PY.md` (the U3 spec)** alongside the `visualization` entity (per U1's revised invariant 16). The module is bounded — ≤ 525 LOC total (per the R3 refinement assay §3.1 raised ceiling) across `__init__.py`, `slugs.py` (the `slug_with_retry` helper with slug-words loading rolled into module-init per the R3 assay §4.2 Path A), `cursors.py` (`paginate`), `errors.py` (`problem` per RFC 9457; the `functools.partial` compression lands per R3 §3.1), `etag.py` (`require_if_match`), `idempotency.py` (the `idempotent` decorator), `softdelete.py` (`soft_delete`, `restore`, `not_deleted_filter`, `with_not_deleted`; the `hard_delete_past_grace` helper is NOT exported — the cron's prune path covers it via `pinned_cron.cron_prune` per the R3 assay §1.4), `pinned_cron.py` (`cron_prune` — the bounded-query helper that replaces `janitor.py:60-65`'s `$nin`). The `visualizations` router **USES** these helpers; the utility's own pytest tests at `api/tests/test_crud_lib_*.py` close on the helper contracts independently of entity behaviour. Slug-words data sourced from `docs/precepts/data/slug-words.json` per `coordination/SLUG-WORDS.md` (the U2 spec) — `slugs.py` imports the JSON via the precepts location at module-init.
12. Author the pytest specs for the utility itself: `api/tests/test_crud_lib_slugs.py` (the `DuplicateKeyError` retry; word-list membership; the slug-words loader from the precepts JSON), `test_crud_lib_cursors.py` (base64url round-trip; stable ordering), `test_crud_lib_errors.py` (RFC 9457 problem+json shape), `test_crud_lib_etag.py` (`If-Match` 428 / 412 semantics; the strong-validator full sha256-hex shape per the R3 assay §1.1), `test_crud_lib_idempotency.py` (replay returns the cached response), `test_crud_lib_softdelete.py` (the state machine), `test_crud_lib_pinned_cron.py` (the bounded query — no `$nin` over an unbounded set).
13. Append a `## Single-replica constraint` block to `api/README.md` (or an equivalent `api/ARCHITECTURE.md` block) per the R3 refinement assay §6 / R-auth-spec §6 — the in-process rate-limiter and the `_suspended_cache` carry process-local state; the production posture is `replicas: 1` (the fourier-A.W4 close note). Without the README block the §12 "open items" disposition for "Rate-limiter single-replica documentation" stays unresolved.

14. **Lift the levels-derivation drift to a single seam in `ComputeBasesRequest`** (added 2026-05-26 — Wave-1 audit synthesis L2 §6.2 + L6 §4 FLAG-GAP; routed from A.FINAL.md §5 + A.W3.5.d). The Wave-1 audit confirms the drift between `web/src/stores/workspace.ts:runComputeBases` (the frontend `Array.from({length: min(N,50)}, …)` construction) and `api/services/computation.py:compute_bases` (the backend equivalent) has no B-side absorption row in W1 / W3 / W4 prior to this revision. The lift: a `ComputeBasesRequest` Pydantic model in `api/models/compute.py` (or `api/models/shared.py`) carrying the `levels` field (with the canonical derivation rule named in the model docstring); `workspace.ts:runComputeBases` consumes the model's frontend-typed mirror; `computation.compute_bases` consumes the backend Pydantic model. The derivation rule lives in one place — the model — and the two sides resolve it identically by construction. Sub-gate: `git grep -E "Array\.from.*length.*min.*50" web/src/stores/workspace.ts` returns zero post-W3 (the frontend consumes the model, not the inline construction).

15. **Auto-recompute discipline binding** (added 2026-05-26 — Wave-1 audit synthesis L3 §3.4 D1; Invariant 19 from B.md §2). After `workspace.saveContourPoints` mutates `store.contour`, the auto-compute watcher must fire — the current `ContourSettings.vue:138-147` watcher keys on `[strategy, blur, ..., nHarmonics, nPoints]` and never re-fires on `store.contour` identity change. The fix: extend the auto-compute watcher to include `store.contour` (or — preferably — fire `Promise.allSettled([computeEpicycles, computeBases])` directly from `saveContourPoints` after `markRaw(result)`). The fix is single-line per L3 §7 D1 (FIX-IN-A-RESIDUAL classification at the audit; the W3 model authoring binds the discipline at the seam where `store.contour` identity is mutated). Sub-gate: a Playwright spec or browser-smoke entry asserts that after a saveContourPoints call, the canvas re-renders within one rAF frame (no perturbation of a control required).

16. **Orphan-snapshot detection mechanism** (added 2026-05-26 — Wave-2 audit synthesis C5 §7 G1 / W3.16). The migration's orphan-snapshot detection uses a chunked `$lookup` aggregation `{$lookup: {from: "gallery", localField: "snapshot_hash", foreignField: "snapshot_hash", as: "gallery"}, $match: {gallery: {$size: 0}}}` (or an equivalent chunked-by-`_id`-range scan). The unbounded `$nin` over `gallery.distinct("snapshot_hash")` form is **rejected** — the BSON 16 MB ceiling would gate at ~250 k orphans; the bounded aggregation has no such ceiling and mirrors the janitor `$nin` retirement of W4.a. Sub-gate: `grep -E '\\\$nin.*list\\(' api/scripts/migrate_visualization.py` returns zero.

17. **`anon-migrated-NNN` minting function** (added 2026-05-26 — Wave-2 audit synthesis C5 §7 G2 / W3.17). The migration script's orphan-pass emits owner slugs via a dedicated `mint_anon_migrated_slug(n: int) -> str` returning `f"anon-migrated-{n:05d}"` against an unindexed monotonic counter. The function is **separate from `slug_with_retry`** — `slug_with_retry` enforces the contract's `^[a-z]+(-[a-z]+){3}$` pattern, which `anon-migrated-NNN` deliberately violates per audit-E §4 / R-identity-spec §9 #3. The C-slug-4 conformance row is scoped to admit `^anon-migrated-\\d+$` as a migration-artefact exception. Sub-gate: `grep -E 'mint_anon_migrated_slug' api/scripts/migrate_visualization.py` returns ≥ 1; the pattern-validation grep (`scripts/conformance/grep-slug-pattern.sh`) admits the exception.

18. **Dangling-contour-hash post-condition** (added 2026-05-26 — Wave-2 audit synthesis C5 §7 G3 / W3.18). The migration script's existing post-condition straggler check (scope item 8) is **extended** with an FK-resolution assertion: `db.visualizations.aggregate([{$lookup: {from: "contours", localField: "contour_hash", foreignField: "contour_hash", as: "c"}}, {$match: {c: {$size: 0}}}])` returns zero documents. If any visualization's `contour_hash` does not resolve in `contours`, the migration **aborts** with the unresolved slug list reported for manual reconciliation. C-mig-3 conformance row pins the assertion.

19. **`migrated_from` marker for idempotency** (added 2026-05-26 — Wave-2 audit synthesis C5 §7 G4 / W3.19). Every migrated visualization carries a `migrated_from: {coll: "gallery" | "snapshots", _id: ObjectId}` marker field. The script's first action is `db.visualizations.find({migrated_from: {$exists: false}})` — re-running the script is a no-op if every visualization carries the marker. The marker also disambiguates the orphan-snapshot pass: re-running the orphan pass does not mint multiple `anon-migrated-NNN` slugs for the same orphan. Sub-gate: `python -m api.scripts.migrate_visualization` twice; second invocation reports zero writes (the C11.1 conformance row already pins this; the marker is the implementation).

20. **Epicycle-animation denormalisation canonicalisation** (added 2026-05-26 — Wave-2 audit synthesis C5 §7 G5 / W3.20). The migration's transformation **canonicalises on the snapshot's `animation_settings`** (the parent) — the gallery rows carry `active_bases` and `n_harmonics` denormalised fields (per `gallery.py:181-194`) that may diverge from the parent snapshot's settings. The parent is the source of truth; the gallery denormalisation resets from the parent during transformation. The W3 spot-check (10 random rows; seed=42) verifies parity.

21. **Image-blob FK survival statement** (added 2026-05-26 — Wave-2 audit synthesis C5 §6 + C5 §7 G6 / W3.21). The migration treats `image_slug` as a **stable FK**: image blobs are NOT migrated; the visualization's `image_slug` field resolves wherever the blob lives (today: Mongo-inline at `images.blob`; future fourier-C: external storage). The `storage_budget_gb` band-aid retirement (scope item 7) removes the band-aid without capping growth; W3's brittleness window therefore implicitly bounds the storage clock until fourier-C opens. C5 §6 ranks this as **Option B** (defer entirely; FK-stable); Options A (migrate-now) and C (excise-during-W3) are REJECTED for coupling-risk reasons named in C5 §6. The W3 cutover validation verifies `images.{image_slug}` resolves for every visualization.

22. **IndexedDB `WorkspaceDraft` lift deferral (W3 → W4)** (added 2026-05-26 — Wave-2 audit synthesis C5 §7 G7 / W3.22 cross-reference). The IndexedDB `WorkspaceDraft` lift is **not server-migrated** at W3 — drafts are ephemeral, the `imageSlug` keyPath remains resolvable post-migration (the `image_slug` server-side slug is unchanged per R-identity-spec §7a), and the `WorkspaceDraft → visualization(draft)` lift happens on the user's next save via `POST /visualizations`. The lift is owned by W4 (scope item 4 — `draftStorage.ts` re-point), not W3. This scope item records the cross-reference so the W3 close-state explicitly names the deferral (no silent omission).

23. **Migration test specs** (added 2026-05-26 — Wave-2 audit synthesis C5 §7 G8 / W3.23). The W3 pytest set (scope item 10) is extended with two new specs: `api/tests/test_migrate_transform.py` (unit-level — gallery + parent-snapshot → visualization transformation; orphan-snapshot → visualization with `anon-migrated-NNN` + `visibility=draft` + `pinned=False`) and `api/tests/test_migrate_integration.py` (seeded-DB end-to-end — assert `count(visualizations) == count(gallery) + count(orphans)`; second-run zero writes; dangling contour-hash aborts; dry-run mutates nothing). Plus `api/tests/test_visualization_ownership.py` adds the `test_migrate_anon_migrated_slug_pattern_scoped` case (C-slug-4 admits `^anon-migrated-\\d+$`).

24. **`--reload` interference docstring** (added 2026-05-26 — Wave-2 audit synthesis C5 §7 G9 / W3.24). The `api/scripts/migrate_visualization.py` file-level docstring carries an explicit constraint: the script must be invoked as a standalone `python -m api.scripts.migrate_visualization` against a non-`--reload` backend (or with the backend down entirely). Embedding inside `uvicorn --reload` triggers the L6 chronic-residual #5 — the migration interrupts mid-pass on every dev-server reload. The constraint is documentation-only; no code change. The hard-gate item 9 (legacy collections survive to W5) is the rollback substrate if a partial migration occurs.

## File Bounds

| File | Access |
|---|---|
| `api/routers/visualizations.py` | create |
| `api/routers/snapshots.py` | delete |
| `api/routers/gallery.py` | modify-carve (reduce to a public-visibility filter or delete) |
| `api/services/image_storage.py` | modify-carve (split blob / hash from slug issuance) |
| `api/models/visualization.py` | create |
| `api/models/gallery.py` | modify-carve |
| `api/models/session.py` | modify |
| `api/models/shared.py` | modify |
| `api/services/database.py` | modify (add the collection, indexes, `pinned` flag) |
| `api/services/janitor.py` | modify (retire `$nin`; retire `storage_budget_gb`) |
| `api/slugs.py` | modify (absorb issuance + the duplicate-key retry from `image_storage.py:76-77`) |
| `api/dependencies.py` | modify (tighten `SLUG_PATTERN`; 401 on anonymous publish) |
| `api/main.py` | modify (mount the `visualizations` router; unmount the `snapshots` router) |
| `api/scripts/migrate_visualization.py` | create |
| `api/tests/test_visualization_crud.py` | create |
| `api/tests/test_visualization_ownership.py` | create |
| `api/tests/test_visualization_soft_delete.py` | create |
| `api/tests/test_janitor_bounded_query.py` | create |
| `api/lib/crud/__init__.py` | create (per the U3 spec) |
| `api/lib/crud/slugs.py` | create (per the U3 spec; slug-words loader rolled in per R3 §4.2 Path A) |
| `api/lib/crud/cursors.py` | create (per the U3 spec) |
| `api/lib/crud/errors.py` | create (per the U3 spec; `functools.partial` compression per R3 §3.1) |
| `api/lib/crud/etag.py` | create (per the U3 spec; full sha256-hex strong validator per R3 §1.1) |
| `api/lib/crud/idempotency.py` | create (per the U3 spec) |
| `api/lib/crud/softdelete.py` | create (per the U3 spec; `hard_delete_past_grace` NOT exported per R3 §1.4) |
| `api/lib/crud/pinned_cron.py` | create (per the U3 spec) |
| `api/tests/test_crud_lib_slugs.py` | create |
| `api/tests/test_crud_lib_cursors.py` | create |
| `api/tests/test_crud_lib_errors.py` | create |
| `api/tests/test_crud_lib_etag.py` | create |
| `api/tests/test_crud_lib_idempotency.py` | create |
| `api/tests/test_crud_lib_softdelete.py` | create |
| `api/tests/test_crud_lib_pinned_cron.py` | create |
| `api/README.md` | modify (append the single-replica block per R3 §6) |
| `docs/tranches/B/audit/migration-counts.md` | create |

Do NOT touch: `web/**` (W4 owns the consumers); `coordination/CRUD-CONTRACT.md` (W1 owns); `~/Programming/value.js/**` (cross-repo, value.js-C owns — held DEFERRED under the orphan verdict). The utility module is **bounded** at ≤ 525 LOC total (`wc -l api/lib/crud/*.py`); exceeding the bound trips U1's "framework-in-disguise" guard and the wave fails its close.

## Disjointness

Agent units A (router carve + helper consumption), B (models + indexes + dependencies tighten + `api/lib/crud/` landing), and C (migration + verification) share no `modify` or `modify-carve` paths. Where the router-carve (agent A) and the utility-module landing (agent B) both touch `api/main.py`'s router registration, the work sequences across sub-agent commits — A registers `visualizations.py`; B mounts none of the utility (the utility is consumed by A's router); the integration commit reconciles.

## Agent Units

### B.W3.a Router carve + visualizations endpoint (consumes the utility helpers)

- Goal: `api/routers/visualizations.py` exists with the six CRUD-plus-soft-delete endpoints, consumes the W3-landed utility helpers, and replaces the snapshots / gallery identity paths.
- Mechanism: create `api/routers/visualizations.py` with the six endpoints in scope §1; delete `api/routers/snapshots.py`; reduce `gallery.py` to a public-visibility list alias or delete it; rewire `api/main.py`. Implement ownership-bound mutations per `CRUD-CONTRACT.md §3` (every PATCH / DELETE requires session + `doc.owner_slug == user_slug`). **The router consumes the U3 utility helpers**: `slug_with_retry` on POST; `cursors.paginate` on list; `errors.problem` on every non-2xx; `etag.require_if_match` on PATCH / DELETE; `idempotency.idempotent` decorator on POST; `softdelete.soft_delete` / `restore` on the DELETE and restore endpoints.
- Files: `api/routers/visualizations.py` (create); `api/routers/snapshots.py` (delete); `api/routers/gallery.py` (modify-carve); `api/main.py` (modify).
- Sub-gate: `pytest api/tests/test_visualization_crud.py` green; `git grep -E "snapshots_collection|snapshot_hash" api/routers/visualizations.py api/main.py` returns zero; `curl -s -o /dev/null -w "%{http_code}" -X POST localhost:8000/visualizations` without a session returns `401`; **`grep -E "from api.lib.crud" api/routers/visualizations.py` returns ≥ 6 imports (the named helpers are actually consumed, not merely available); `grep -cE "errors\.(problem|.*)" api/routers/visualizations.py` returns ≥ 5 (the R3 refinement assay §4.4 site-count); `grep -cE "etag\.require_if_match" api/routers/visualizations.py` returns ≥ 2**.

### B.W3.b Models + indexes + dependencies tighten + `api/lib/crud/` landing

- Goal: the Pydantic model, the collection + indexes, the tightened `SLUG_PATTERN`, the `janitor.py` `$nin` retirement, the `image_storage.py` partition, and the `api/lib/crud/` utility module land together as the substrate W3.a consumes.
- Mechanism: author `api/models/visualization.py` (the Pydantic model); update `database.py` with the collection + the union indexes + the `pinned` retrofit; tighten `SLUG_PATTERN` in `dependencies.py`; retire `$nin` in `janitor.py` (delegate to `pinned_cron.cron_prune`); retire the `storage_budget_gb` band-aid; split `image_storage.py` into blob / hash + lift slug-issuance into `slugs.py` with `DuplicateKeyError` retry (retires the race at `image_storage.py:76-77` — the retry primitive itself lives in the utility's `slug_with_retry`). **Land `api/lib/crud/` per the U3 spec**: 8 module files plus 7 named pytest specs; slug-words JSON imported from the precepts location per the U2 spec (`coordination/SLUG-WORDS.md`). The slug-words loader rolls into `slugs.py` module-init per the R3 refinement assay §4.2 Path A (no separate `slug_words.py` sub-module); the `errors.py` helpers collapse via `functools.partial` per the R3 assay §3.1; the `softdelete.py` exports do NOT include `hard_delete_past_grace` per the R3 assay §1.4 (the cron prune path covers it); the `etag.py` shape pins the strong-validator full sha256-hex form per the R3 assay §1.1.
- Files: `api/models/visualization.py` (create); `api/models/{gallery,session,shared}.py` (modify); `api/services/database.py` (modify); `api/services/janitor.py` (modify); `api/services/image_storage.py` (modify-carve); `api/slugs.py` (modify — now a thin wrapper around `api/lib/crud/slugs.py`); `api/dependencies.py` (modify); `api/lib/crud/**` (create — 8 files per the U3 spec); `api/tests/test_crud_lib_*.py` (create — 7 specs); `api/README.md` (modify — the single-replica block per R3 §6).
- Sub-gate: `pytest api/tests/test_janitor_bounded_query.py api/tests/test_crud_lib_*.py` green (the utility helper specs close on contract behaviour); `pytest api/tests/test_visualization_ownership.py` green; `grep -E "storage_budget_gb|\$nin.*list\(" api/services/janitor.py` returns zero; **`wc -l api/lib/crud/*.py | tail -1` reports ≤ 525 lines total (the R3 §3.1 raised ceiling; framework-in-disguise guard)**; `grep -n 'single-replica' api/README.md` returns ≥ 1 (per R3 §6).

### B.W3.c Migration script + verification artefact

- Goal: the migration runs idempotently from `snapshots` + `gallery` into `visualizations` with verified counts, a seed=42 spot-check, and a documented rollback substrate.
- Mechanism: author `api/scripts/migrate_visualization.py` (mirrors `~/Programming/value.js/api/src/migrate-slugs.ts`'s idiom — idempotent, per-document `$set`, progress counts, a post-condition straggler check); consume `api.lib.crud.slugs.slug_with_retry` (no private TOCTOU re-check loop — per the R3 refinement assay §5.1 Gap 1); add a `--dry-run` flag (per R3 §5.1 Gap 2); seed the spot-check sample at `random.seed(42); random.sample(..., 10)` (per R3 §5.3); document the rollback substrate in the file-level docstring (per R3 §5.1 Gap 3 — the source collections `snapshots` and `gallery` are retained until the W5 — close ceremony close); resolve the Wχ — challenge wave's P2 adversarial cases (orphan snapshots without an owner; `user_slug: None` gallery rows) per the rules named in the P2 disposition; run the dry-run pass; author `audit/migration-counts.md` with pre / post counts + the 10-row seed=42 spot-check.
- Files: `api/scripts/migrate_visualization.py` (create); `docs/tranches/B/audit/migration-counts.md` (create).
- Sub-gate: `python -m api.scripts.migrate_visualization --dry-run` exits 0 with the reported counts; `audit/migration-counts.md` shows `len(visualizations_after) == len(gallery_before) + len(snapshots_with_no_gallery_row_before)` ± the explicit orphan-handling delta; the seed=42 spot-check diff is present for 10 rows.

## Triumvirate Dispatch

The triumvirate fires when the wave's file bounds, hard gate, or substrate-with-consumer wiring no longer describes the work required to close. For W3 the trigger surfaces are:

- the Wχ — challenge wave's P2 migration-preserves-data probe disposes `rejected` (the orphan-snapshot or `user_slug: None` resolution cannot be made honest within the within-wave brittleness window) — the plan re-scopes and the brittleness window's restoration discipline is renegotiated;
- the `api/lib/crud/` utility-module LOC budget overshoots the R3 §3.1 raised ceiling of 525 LOC despite the named compressions (the `errors.py` `functools.partial` collapse, the slug-words roll-in to `slugs.py`) — the framework-in-disguise guard fires; the triumvirate's redress lane re-scopes the utility surface before W3 closes;
- the migration's post-condition assertions (zero null owners, valid visibility, count parity, slug uniqueness) fail at the dry-run stage with no local-edit recovery — the diagnostic loop halts at iteration 3.

## Hard Gate

1. `pytest api/tests/test_visualization_crud.py api/tests/test_visualization_ownership.py api/tests/test_visualization_soft_delete.py api/tests/test_janitor_bounded_query.py` all green (run command: `uv run pytest api/tests/test_visualization_*.py api/tests/test_janitor_bounded_query.py -v`).
2. `git grep -E "snapshot_hash" api/routers/ api/main.py` returns zero references on user-facing identity paths (the hash survives only as a dedup key in `image_storage.py`'s blob / hash partition; `git grep -E "/v/.*snapshot_hash|/g/.*snapshot_hash" web/` returns zero — checked in W4 too).
3. `git log --diff-filter=D --name-only` shows `api/routers/snapshots.py` deleted.
4. `python -m api.scripts.migrate_visualization` runs to completion with exit 0; `audit/migration-counts.md` exists with pre / post counts + a 10-row seed=42 spot-check.
5. `grep -E "\\\$nin.*list\\(" api/services/janitor.py` returns zero (bounded queries only, per `CRUD-CONTRACT.md §8`).
6. `grep -E "storage_budget_gb" api/services/janitor.py` returns zero (the band-aid retired per the Wχ — challenge wave's P3 probe).
7. `python -c "import re; assert re.fullmatch(r'\^\[a-z\]\+\(-\[a-z\]\+\)\{3\}\$', open('api/dependencies.py').read().split('SLUG_PATTERN')[1].split('\\\"')[1])"` confirms the tightened pattern (or an equivalent regex check).
8. `curl -X POST localhost:8000/visualizations` (no `X-Session-Token`) returns `401` (the anonymous-publish rejection — retires the `gallery.py:206` orphan path).
9. `PROGRESS.md` row "W3 — fourier visualization entity + migration close" cites the close commit + the migration commit + the `audit/migration-counts.md` path. The hard gate additionally asserts (per the R3 refinement assay §5.4) that the legacy `snapshots` and `gallery` collections survive the migration as rollback substrate: `mongosh --eval "db.getCollectionNames()"` returns `snapshots` AND `gallery` AND `visualizations` (all three present). At the W5 — close ceremony the gate inverts: the legacy two rename to `_snapshots_legacy` / `_gallery_legacy` per `CRUD-CONTRACT §11` and the W5 gate asserts the renamed form.
10. **Utility module landed (per U1's invariant 16 / the U3 spec)**: `ls api/lib/crud/` lists all 8 files (`__init__.py`, `slugs.py`, `cursors.py`, `errors.py`, `etag.py`, `idempotency.py`, `softdelete.py`, `pinned_cron.py`); `uv run pytest api/tests/test_crud_lib_*.py -v` reports all 7 specs green; `wc -l api/lib/crud/*.py | tail -1` total ≤ 525 (the R3 refinement assay §3.1 raised ceiling — the framework-in-disguise guard with the named one-time slack rationale).
11. **Helper consumption proof** (substrate-with-consumer per cohort invariant 4): `grep -E "from api.lib.crud" api/routers/visualizations.py` returns ≥ 6 imports covering the named helpers; `grep -E "cron_prune" api/services/janitor.py` returns ≥ 1 (the janitor delegates to the utility); `grep -E "slug_with_retry" api/slugs.py api/scripts/migrate_visualization.py` returns ≥ 2 (slug issuance lifted through the helper; the migration script consumes the helper rather than re-checking via TOCTOU per the R3 assay §5.1). The R3 assay §4.4 extension: `grep -cE "errors\.(problem|.*)" api/routers/visualizations.py` returns ≥ 5; `grep -cE "etag\.require_if_match" api/routers/visualizations.py` returns ≥ 2.
12. **Slug-words data consumption** (per the U2 spec): `grep -E "slug-words.json" api/lib/crud/slugs.py` returns ≥ 1; the precepts JSON resolves at import time. (The R3 refinement assay §4.2 Path A: the loader is in `slugs.py` module-init, not a separate `slug_words.py` sub-module.)

## Format and Lint Cadence

W3 is a broad implementation wave; `uv run ruff check api/`, `uv run ruff format --check api/`, and `uv run pytest` run after each integration batch (after agent A's router carve; after agent B's utility-module landing; after agent C's migration verification) and before the wave close commit. The format pass on `api/lib/crud/**` runs against the same ruff profile as the rest of `api/`.

## Verification Artefacts

- `api/tests/test_visualization_*.py` (created, green).
- `api/tests/test_janitor_bounded_query.py` (created, green).
- `api/tests/test_crud_lib_*.py` (7 specs, created, green).
- `docs/tranches/B/audit/migration-counts.md` (created with pre / post counts + a 10-row seed=42 spot-check).
- Deletion proof: `git log --diff-filter=D --summary` includes `api/routers/snapshots.py`.
- Pre / post `mongosh` count outputs captured in the `audit/migration-counts.md` appendix.
- `wc -l api/lib/crud/*.py | tail -1` LOC total recorded in `audit/migration-counts.md` (the framework-in-disguise guard's evidence).

## Commit Plan

- Agent-owned implementation commits per sub-unit (three streams).
- Orchestrator integration commits at each batch boundary (router carve; utility-module landing; migration verification).
- The migration commit (a separate scope `tranche-B/W3/migration`) carries its own commit body naming the count parity and the spot-check seed.
- A documentation commit at the wave close updating `PROGRESS.md` and `docs/tranches/B/audit/migration-counts.md`.

## Dependencies

- **Depends on**: the W1 — CRUD-contract ratification wave's close (`coordination/CRUD-CONTRACT.md` ratified — supplies the §1–§9 must-have fields and the §11 migration-disposition template).
- **Blocks**: the W4 — fourier convergence wiring wave (consumer wiring requires the entity); the W5 — close ceremony.

## Archaeology

The fourier-A.W4 — image storage cleanup wave corrected the contour-hash bug in `api/services/image_storage.py` and consolidated `gallery.py`, but explicitly left the file's slug-issuance + content-addressing scaffolding intact — the **structural restructure of `image_storage.py` belongs here**, not in A. The race at `image_storage.py:76-77` (check-then-insert slug uniqueness) is the H4 §3.2 candidate for the unique-index + `DuplicateKeyError` retire; the W3 — fourier visualization entity wave retires it. The Wχ — challenge wave's P2 "does the migration actually preserve data" probe (per H4 §6 P2) is the adversarial test for the orphan-snapshot and `user_slug: None` cases — `accepted` or `narrowed` is the open-gate.

**R3 refinement assay (2026-05-19, recorded with the orphan verdict 2026-05-26):** the assay surfaced 20 binding refinements over the post-U6 corpus. W3 absorbs the following: the strong sha256-hex ETag shape (§1.1); the `api/src/crud/` path on the value.js side (§1.2 — informational for W3 since value.js-side citations are DEFERRED under the orphan verdict); the `slugs.py` plural module name (§1.3); the `hard_delete_past_grace` drop from the softdelete exports (§1.4); the LOC ceiling raised to 525 with the `functools.partial` compression rationale (§3.1); the `slug_words.py` roll-in to `slugs.py` module-init (§4.2 Path A); the `errors.problem` ≥ 5 sites / `etag.require_if_match` ≥ 2 sites W3-hard-gate addition (§4.4); the migration-script `slug_with_retry` consumption / `--dry-run` flag / seed=42 spot-check / rollback-substrate docstring (§5.1, §5.3); the legacy-collections-survive gate (§5.4); the single-replica README block per §12 open item (§6).
