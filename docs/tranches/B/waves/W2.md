# B.W2 — UX coherence (dock idiom + a11y + Configurator adoption)

**Name**: W2 — UX coherence (dock idiom + a11y + Configurator adoption).
**Opens after**: the Wχ — challenge wave's close (per `B.md §3`; Wχ.P4 — invariant 18-20 binding-test — confirms the new invariants are testable). W2 is **independent of W1** at file bounds (W2 touches only `web/src/components/visualization/**`, `web/src/components/equation/EqCoefficientsPanel.vue`, and shared component extraction; W1 is docs-only) and may dispatch in parallel with W3 — fourier visualization entity wave subject to agent budget.
**Agents**: 5 parallel (A — Configurator adoption sweep across D4+D5+D6+D7; B — `ExportModal` Dialog substitution + the four MEDIUM a11y discharges; C — Coefficients dedup extraction; D — EditorTools rationalisation + ContourSettings `data-state` adoption; E — useViewTransform memoization + DC term suppression + HoverPopover import collapse + z-token ladder + dock naming convention).
**Hard gate**: the visualization-route left-panel stack consumes `Configurator` + `ConfiguratorLayer` + `ConfiguratorRow`; `ExportModal` uses glass-ui `Dialog` (`role="dialog"` + `aria-modal` + focus trap + Esc — Invariant 18 binding); the L5 §5 A1 HIGH a11y gap discharged with axe-core sanity; `shared/CoefficientsSpectrum.vue` extracted and consumed by both visualization (D7) and equation (D11) routes; `useViewTransform` memoizes on `epicycleData` / `basesData` identity (Invariant 20 binding); the `z-[15]` literal in `EquationPanel` routes through `--z-*` tokens; `vue-tsc -b --force` green; `npm run build` exit 0.
**Status**: provisional (authored 2026-05-26 from the Wave-1 audit synthesis at `docs/audits/runs/2026-05-26-B-audit-wave-1/SYNTHESIS.md §4`; reactivates the W2 slot which was previously the orphaned value.js-C tracking row — that row is preserved at B.md §3 as `W2-tracking` sub-section).

## Goal criterion

This wave succeeds if the visualization-route surface — the 14 dock-shaped components catalogued at `docs/audits/runs/2026-05-26-B-audit-wave-1/L5-docks.md §2` — adopts the glass-ui substrate primitives the L4 audit found unconsumed (`Configurator`, `Dialog`, the `data-state` channel on `CollapsibleContent`), discharges the eight a11y gaps catalogued at L5 §5 (1 HIGH on `ExportModal`; 3 MEDIUM on `FullscreenViewer`, `EquationPanel`, `MobileFloatingToc`; 4 LOW), unifies the dock-shaped naming idiom (Dock / Panel / Modal — Invariant 18 binding), and lands the two visualization-pipeline performance + correctness fixes (the `useViewTransform` memoization per Invariant 20; the DC-term static-disc suppression). The wave's substrate-with-consumer discipline binds: every primitive adoption has a current consumer at the audited file:line.

## Scope

1. **Configurator adoption sweep** (per L5 §6 G6 + §8 B-wave-item-α). Migrate the visualization-route left-panel stack — `ContourSettings.vue` (D5, 464 LOC) + `BasisSelector.vue` (D6, 320 LOC) + `CoefficientsPanel.vue` (D7, 167 LOC) + the `EditorToolsPanel.vue` (D4, 121 LOC) decision (see scope item 4 below) — to glass-ui's `Configurator` + `ConfiguratorLayer` + `ConfiguratorRow` chassis (`/Users/mkbabb/Programming/glass-ui/src/components/custom/configurator/`). Lift `BasisCanvas` into the `Configurator`'s stage slot. The migration is by-component: each panel becomes a `ConfiguratorLayer` with the existing controls slotted into `ConfiguratorRow` cells; the panels' `cartoon-card` background retires in favour of the substrate's layered chassis.

2. **`ExportModal` → glass-ui `Dialog` migration** (per L4 §5 AUG-2 + L5 §6 G5 + §5 A1 HIGH). Replace `ExportModal.vue`'s hand-rolled `<Teleport>` + `.modal-card` (147 LOC) with the substrate's `Dialog` cohort: `role="dialog"`, `aria-modal="true"`, focus trap, Esc handler, autofocus on the first focusable. The L5 §5 A1 HIGH a11y gap discharges in this single substitution.

3. **Coefficients dedup extraction** (per L5 §6 G10 + §8 B-wave-item-β). Extract `web/src/components/shared/CoefficientsSpectrum.vue` consumed by both the visualization route (D7 — `CoefficientsPanel.vue`) and the equation route (D11 — `EqCoefficientsPanel.vue`); the latter's only structural divergence from the former is the absence of the `FrequencyGraph` import. Concurrently wire `AnimatedDigit` + `MetricRow` from `@mkbabb/glass-ui/animated-digit` + `/metric-stack` to substantially reduce the bespoke amplitude-bar `TransitionGroup` shimmer (per L4 §3 — AnimatedDigit currently has 0 fourier sites).

4. **EditorTools rationalisation** (per L5 §6 G + §8 B-wave-item-γ). Decide canonical home for Smooth / Simplify / Magnet — keep `EditorControlsDock.vue` (D1) as the floating dock; **retire `EditorToolsPanel.vue` (D4) outright** OR fold its content into `ContourSettings`'s advanced sub-section. The L5 audit's Heuristic: D1 is the canonical surface (it carries the magnet-radius slider with `HoverPopover` + `keep-dock-open`; D4 is the static panel duplicate). The decision: retire D4; the panel is dead-code-suspect post-W3.5.d.

5. **`ContourSettings` `data-state` adoption** (per L5 §6 G3 + §8 B-wave-item-δ). Replace the hand-rolled `adv-open` / `adv-close` keyframes at `ContourSettings.vue` (the "Advanced" sub-section keyframes that duplicate `CollapsibleContent`'s already-shipped `--reka-collapsible-content-height` channel) with the substrate's `data-state` animation. The `Collapsible` primitive is already imported at the file's outer block; the Advanced inner block extends the same primitive.

6. **`AnimationControls` cleanup** (per L5 §6 G1 + §8 B-wave-item-ε). Substitute `DockDropdownTrigger` + `DropdownMenuContent` for the hand-rolled `.menu-popup` + `onClickOutside` + `Transition name="popup"` at `AnimationControls.vue:221`. Convert the cross-component `--animation-dock-max-width` CSS-var contract (with `FullscreenViewer.vue:171`) to a typed prop on `AnimationControls`. Keep the `play-btn` rainbow gradient as bespoke (L5 F15 — opulent fourier register; not reuse-shaped).

7. **`useViewTransform` memoization** (per L3 §3.6 D5 + Invariant 20 binding). Refactor `useViewTransform.ts:21-24` so that `Math.min(...xs)` / `Math.max(...xs)` are computed once on `epicycleData` / `basesData` identity change (a `computed` keyed on the path-reference); the per-rAF `drawFrame` consumes the memoized bbox. The current variadic-spread allocates an arguments array of length `n_points` four times per frame; at default n=1024 that's ~4 100 ops + 4 allocs per frame; at n=10 000 the spread approaches V8's ~64 k argument-count limit. The memoization is one `computed` ref.

8. **DC-term static-disc suppression** (per L3 §3.5 D8). `EpicycleChain.__init__` sorts components by descending amplitude; the DC term (`frequency=0`) lands first and renders in `drawEpicycleCircles` (`epicycles.ts:153-200`) as a stationary disc of radius `|c_0|` — potentially the size of the figure, visually dominating. Graceful suppression: skip drawing the circle when `nVis === 0` or `frequency === 0`, OR render the DC component as a centre-marker rather than a circle.

9. **z-index ladder discipline** (per L5 §4 + Invariant 18). The single literal `z-[15]` at `EquationPanel.vue:114` routes through the `--z-*` token ladder — likely `var(--z-controls)` or `var(--z-overlay)`. No literal `z-[N]` survives in dock / overlay / modal surfaces post-wave.

10. **`HoverPopover` import collapse** (per L4 §6 H1 / §7 H1). Three sites import `HoverPopover` from `@mkbabb/glass-ui/hover-popover` whilst the root barrel exports it at `dist/index.d.ts:4665`. Cosmetic 3-line change: collapse the subpath imports to the root barrel for ergonomic parity with the rest of the cohort.

11. **`FullscreenViewer` focus trap** (per L5 §5 A2 MED + §6 G11). The Esc handler is wired; the focus trap is absent — tab leaks to background after open. Land an inline focus-trap (the simplest: a one-line `focus-trap-vue` import or a small `useFocusTrap` composable scoped to the Teleport target). The L5 §6 G11 disposition admits this discharge at the consumer; no glass-ui carry warranted.

12. **`EquationPanel` Esc + `aria-label`** (per L5 §5 A3 MED). The overlay panel has no Esc handler; the close button has no `aria-label`. Two-line discharge.

13. **`MobileFloatingToc` Esc + focus-return** (per L5 §5 A4 MED). The mobile dropdown has no Esc handler; focus does not return to the trigger after dismiss. The `scrollContainer.style.overflow` mutation side-effect is preserved (works but documented as implicit).

14. **Sub-LOW a11y discharges** (per L5 §5 A5 / A6 / A7 / A8). Sidebar `aria-label="Table of contents"` (one-line); `AnimationControls` menu `role="menu"` / `role="menuitem"` (handled incidentally by scope item 6's `DropdownMenu` substitution); `CoefficientsPanel` CSS-`:hover` tooltip lifted to glass-ui `Tooltip` primitive (handled incidentally by scope item 3's shared component extraction with `Tooltip` wiring).

15. **`partial_sums` `as any` cast removal** (per L3 §3.6 D7 + B.W1 augment 8). Drop the `(sumsForBasis as any)?.[level] ?? (sumsForBasis as any)?.[String(level)]` cast at `BasisCanvas.vue:271-274` once `coordination/SCHEMA.md` lands the typed `Record<string, {x,y}>` shape (B.W1 binding). The cast retires; the lookup becomes a typed bracket access.

16. **`@axe-core/playwright` integration — folded forward from W4.d** (added 2026-05-26 — Wave-2 audit synthesis C6 §2 W2-HIGH-risk). The L6 §4 + C6 §2 risk reports the same gap: invariant-18 a11y gates measure-bind only when axe-core actually runs at the same wave the invariant lands. The current W4.d scope item 10 lands axe-core *after* W2 closes — leaving a gap window in which W2's a11y discharges are claim-only. Resolution: fold the axe-core integration forward into W2's hard gate so the a11y discharges measure-bind at the same wave they land. Concretely: `e2e/visualization-ux.spec.ts` (already in scope item 10 / Agent E) wires `import { injectAxe, checkA11y } from '@axe-core/playwright'`; at the four keystone states (workspace default; ContourSettings Configurator-open; ExportModal Dialog-open; AnimationControls dropdown-open) the spec asserts zero serious / critical violations. W4.d's existing axe-core wiring then carries the keystone-state coverage forward into the visualization-crud lifecycle spec without duplication. **Discharges the C6 §2 W2-HIGH-risk; binds Invariant 18 with measurement.**

17. **Auto-recompute regression-guard assertion** (added 2026-05-26 — Wave-2 audit synthesis C5 + C6 invariant-19 testing seam). The W2 hard gate's existing "save-then-render" assertion (per scope item 11 of the W2 hard-gate block) is augmented with an explicit cross-wave Invariant 19 binding-assertion: after a contour-edit `saveContourPoints` call, the canvas re-renders within one rAF frame (~16 ms) without manual perturbation of any control. W3 owns the seam (the `ComputeBasesRequest` model + the auto-compute watcher binding); W2 owns the test that **fails** if W3's seam regresses. The assertion lives in `e2e/visualization-ux.spec.ts::save_contour_then_recompute` (already in `CONFORMANCE-MATRIX.md §F` row F19.1; this scope item formally folds the assertion into W2's authoring scope).

## File Bounds

| File | Access |
|---|---|
| `web/src/components/visualization/ContourSettings.vue` | modify (Configurator adoption; `data-state` keyframe retirement) |
| `web/src/components/visualization/BasisSelector.vue` | modify (Configurator adoption) |
| `web/src/components/visualization/CoefficientsPanel.vue` | modify (consume shared/CoefficientsSpectrum.vue) |
| `web/src/components/equation/EqCoefficientsPanel.vue` | modify (consume shared/CoefficientsSpectrum.vue) |
| `web/src/components/shared/CoefficientsSpectrum.vue` | create |
| `web/src/components/visualization/EditorToolsPanel.vue` | delete (retire per scope item 4) |
| `web/src/components/visualization/EditorControlsDock.vue` | modify (absorb any retained D4 affordances) |
| `web/src/components/visualization/ExportModal.vue` | modify-carve (Dialog substitution) |
| `web/src/components/visualization/FullscreenViewer.vue` | modify (focus trap) |
| `web/src/components/visualization/EquationPanel.vue` | modify (Esc + aria-label + z-token) |
| `web/src/components/visualization/AnimationControls.vue` | modify (DropdownMenu substitution + typed prop) |
| `web/src/components/visualization/CanvasControlsDock.vue` | modify (remove the out-of-band `defineExpose` coupling) |
| `web/src/components/visualization/BasisCanvas.vue` | modify (drop `as any` cast on partial_sums; DC-term suppression handoff to epicycles.ts) |
| `web/src/components/visualization/composables/useViewTransform.ts` | modify (memoize on identity) |
| `web/src/components/visualization/composables/epicycles.ts` | modify (DC-term suppression) |
| `web/src/components/paper/MobileFloatingToc.vue` | modify (Esc + focus-return) |
| `web/src/components/paper/PaperSidebar.vue` | modify (aria-label) |
| `web/src/components/visualization/VisualizationView.vue` | modify (remove `dock-centered` defineExpose coupling) |
| `e2e/visualization-ux.spec.ts` | create (axe-core keystone-state checks for the new Invariant 18 binding) |

Do NOT touch: `api/**` (W3 owns the entity work); `coordination/**` (W1 owns the contract); `~/Programming/value.js/**`. The `useViewTransform` memoization is a *render-path* refactor; the L3 D5 finding is the only file change in `useViewTransform.ts`.

## Disjointness

Agent units A (Configurator adoption — D4/D5/D6/D7), B (Dialog substitution + 3 MEDIUM a11y discharges), C (Coefficients dedup), D (EditorTools retire + ContourSettings keyframe retire), and E (useViewTransform memoize + DC-term + HoverPopover collapse + z-token + naming) share no `modify` paths under exclusive ownership. Where agent A touches `ContourSettings.vue` (Configurator chassis) and agent D touches `ContourSettings.vue` (keyframe retire — the inner Advanced collapse), the work sequences across sub-agent commits: A lands the outer Configurator chassis; D lands the inner `data-state` adoption; the integration commit reconciles.

## Agent Units

### B.W2.a Configurator adoption sweep

- Goal: the visualization-route left-panel stack (D5 + D6 + D7) consumes `Configurator` + `ConfiguratorLayer` + `ConfiguratorRow`; `BasisCanvas` lifts into the stage slot; the bespoke `cartoon-card` background retires for the substrate's layered chassis.
- Mechanism: read the glass-ui Configurator surface (`/Users/mkbabb/Programming/glass-ui/src/components/custom/configurator/`); migrate panel-by-panel with the existing controls slotted into `ConfiguratorRow` cells; the `useConfiguratorState` composable manages section-expand-state (parallel to W3.5.c's `useSidebarState<T>` precedent).
- Files: `web/src/components/visualization/ContourSettings.vue` (modify); `BasisSelector.vue` (modify); `CoefficientsPanel.vue` (modify — but coordinate with agent C's shared extraction); `VisualizationView.vue` (modify — host the Configurator + lift BasisCanvas into stage slot).
- Sub-gate: `git grep -n 'cartoon-card' web/src/components/visualization/ContourSettings.vue web/src/components/visualization/BasisSelector.vue web/src/components/visualization/CoefficientsPanel.vue` returns zero (the Configurator chassis replaced it); `git grep -n 'from .@mkbabb/glass-ui/configurator.' web/src/components/visualization/` returns ≥ 3 sites.

### B.W2.b Dialog substitution + MEDIUM a11y discharges

- Goal: `ExportModal` migrates to glass-ui `Dialog` (closes L5 §5 A1 HIGH); `FullscreenViewer` gains a focus trap (A2 MED); `EquationPanel` gains Esc + close-button `aria-label` (A3 MED); `MobileFloatingToc` gains Esc + focus-return (A4 MED).
- Mechanism: read the glass-ui `Dialog` cohort (root barrel); replace `ExportModal.vue`'s hand-rolled `<Teleport>` + `.modal-card` with `<Dialog>` + `<DialogContent>` + `<DialogHeader>` + `<DialogFooter>`; wire `role="dialog"` + `aria-modal="true"` + focus-trap + Esc-handler + autofocus via the primitive's API. For the FullscreenViewer focus trap, scope a `useFocusTrap` composable to the `<Teleport>` target. For the EquationPanel and MobileFloatingToc Esc handlers, add `@keydown.esc` to the panel root; the focus-return uses `useTemplateRef` to track the trigger element.
- Files: `web/src/components/visualization/ExportModal.vue` (modify-carve); `FullscreenViewer.vue` (modify); `EquationPanel.vue` (modify); `web/src/components/paper/MobileFloatingToc.vue` (modify).
- Sub-gate: `grep -E '<Dialog\b|<DialogContent\b' web/src/components/visualization/ExportModal.vue` returns ≥ 2; `grep -E 'role=\"dialog\"|aria-modal' web/src/components/visualization/ExportModal.vue` returns ≥ 1 (or the primitive supplies it); `npx playwright test e2e/visualization-ux.spec.ts` (created by Agent E) is green on the ExportModal keystone state.

### B.W2.c Coefficients dedup extraction

- Goal: `web/src/components/shared/CoefficientsSpectrum.vue` extracted; consumed by D7 (`CoefficientsPanel.vue`) and D11 (`EqCoefficientsPanel.vue`); AnimatedDigit + MetricRow wired from `@mkbabb/glass-ui/animated-digit` + `/metric-stack`.
- Mechanism: read both D7 and D11 line-by-line; identify the 95% shared portion (amplitude-bar TransitionGroup + hover tooltip lift + readout block); extract to `shared/CoefficientsSpectrum.vue` with props for the divergent FrequencyGraph slot; D7 passes the FrequencyGraph child; D11 passes nothing. Wire `AnimatedDigit` for the per-component amplitude readouts; the bespoke `:hover` CSS tooltip on the amplitude bars lifts to a glass-ui `Tooltip`.
- Files: `web/src/components/shared/CoefficientsSpectrum.vue` (create); `web/src/components/visualization/CoefficientsPanel.vue` (modify — consume the shared component); `web/src/components/equation/EqCoefficientsPanel.vue` (modify — consume the shared component).
- Sub-gate: `wc -l web/src/components/shared/CoefficientsSpectrum.vue web/src/components/visualization/CoefficientsPanel.vue web/src/components/equation/EqCoefficientsPanel.vue` reports the consumer files shrunk by ≥ 60 LOC each; `git grep 'AnimatedDigit\|from \"@mkbabb/glass-ui/animated-digit\"' web/src/components/shared/CoefficientsSpectrum.vue` returns ≥ 1.

### B.W2.d EditorTools retire + ContourSettings keyframe retire

- Goal: `EditorToolsPanel.vue` (D4) retires outright (Smooth/Simplify/Magnet canonical home is D1 `EditorControlsDock`); `ContourSettings.vue`'s hand-rolled `adv-open`/`adv-close` keyframes retire in favour of `CollapsibleContent`'s `data-state` channel.
- Mechanism: delete `EditorToolsPanel.vue`; remove its mount sites in `VisualizationView.vue`; verify the floating dock D1 carries the full Smooth/Simplify/Magnet surface (the L5 audit confirms it does — magnet popover + reset + tool buttons). For ContourSettings, the outer block already imports `Collapsible` + `CollapsibleContent`; the inner Advanced section's hand-rolled keyframes (`adv-open` / `adv-close`) lift to the same `data-state="open"` / `data-state="closed"` channel.
- Files: `web/src/components/visualization/EditorToolsPanel.vue` (delete); `web/src/components/visualization/VisualizationView.vue` (modify — drop the mount); `web/src/components/visualization/ContourSettings.vue` (modify — keyframe retire).
- Sub-gate: `ls web/src/components/visualization/EditorToolsPanel.vue` does not exist; `grep -E '@keyframes adv-open|@keyframes adv-close' web/src/components/visualization/ContourSettings.vue` returns zero; the Advanced sub-section animates via the substrate's `data-state` channel (browser-smoke evidence in `audit/W2-screenshots/`).

### B.W2.e Render-path memoize + DC-term + HoverPopover + z-token + naming

- Goal: `useViewTransform` memoizes bbox on `epicycleData` / `basesData` identity (Invariant 20 binding); DC-term suppression lands in `drawEpicycleCircles`; `HoverPopover` subpath imports collapse to root barrel (3 sites); `EquationPanel`'s `z-[15]` literal routes through `--z-*` tokens; the dock-naming drift codifies (Dock for floating, Panel for in-flow, Modal for blocking); `AnimationControls` hand-rolled menu substitutes `DockDropdownTrigger` + `DropdownMenuContent`; the `--animation-dock-max-width` CSS-var contract converts to a typed prop; `CanvasControlsDock`'s out-of-band `defineExpose(expanded)` reads from `VisualizationView` extract to `useDockState` composable or refactor.
- Mechanism: refactor `useViewTransform.ts:21-24` so `min`/`max` compute once on identity change in a `computed` (the spread retires); refactor `epicycles.ts:153-200` to skip the DC component conditionally; sweep the three `HoverPopover` subpath imports to root-barrel; replace `z-[15]` with `var(--z-controls)`; substitute `DockDropdownTrigger` at `AnimationControls.vue:221`; convert `--animation-dock-max-width` to a typed Vue prop; refactor the `dockRef.expanded` coupling at `VisualizationView.vue:233` to a typed event or a parent-owned `ref`. Author `e2e/visualization-ux.spec.ts` (the axe-core keystone-state spec used by Agent B's sub-gate and by Wχ.P4 — keystone states: workspace default, ContourSettings open, ExportModal open, AnimationControls dropdown open).
- Files: `web/src/components/visualization/composables/useViewTransform.ts` (modify); `web/src/components/visualization/composables/epicycles.ts` (modify — DC-term); `web/src/components/visualization/EditorControlsDock.vue` + `CanvasControlsDock.vue` (modify — HoverPopover root-barrel); `web/src/components/visualization/EquationPanel.vue` (modify — z-token); `web/src/components/visualization/AnimationControls.vue` (modify — DropdownMenu + typed prop); `web/src/components/visualization/CanvasControlsDock.vue` + `VisualizationView.vue` (modify — defineExpose coupling); `web/src/components/visualization/BasisCanvas.vue` (modify — drop `partial_sums as any` cast); `web/src/components/paper/PaperSidebar.vue` (modify — aria-label); `e2e/visualization-ux.spec.ts` (create).
- Sub-gate: `grep -n 'Math\.min(\.\.\.' web/src/components/visualization/composables/useViewTransform.ts` returns zero (or the call is inside a `computed` block bounded by `getViewTransform`'s identity change); `grep -n 'z-\[15\]' web/src/components/visualization/` returns zero; `grep -nE 'from .@mkbabb/glass-ui/hover-popover.' web/src/components/visualization/` returns zero; `grep -E 'as any' web/src/components/visualization/BasisCanvas.vue` returns zero; `npx playwright test e2e/visualization-ux.spec.ts` green with axe-core injection at the 4 keystone states.

## Triumvirate Dispatch

The triumvirate fires when the wave's file bounds, hard gate, or substrate-with-consumer wiring no longer describes the work required to close. For W2 the trigger surfaces are:

- the Configurator adoption surfaces a substrate gap (the `ConfiguratorLayer` API does not support a slot the consumer needs) — the plan-augment lane re-binds the panel-by-panel migration shape or files a glass-ui carry;
- the Dialog substitution surfaces a substrate gap (the `Dialog` primitive lacks a `keep-dock-open`-style affordance the `ExportModal`-from-canvas-context needs) — the redress lane reconciles by inline-focus-trap as a transitional idiom;
- the `useViewTransform` memoization surfaces a Vue reactivity edge case where the `computed` re-evaluates more than once per identity change — the diagnostic loop halts at iteration 3.

## Hard Gate

1. `vue-tsc -b --force` green from `web/`; `npm run build` exit 0; the bundle envelope holds within the W3 close baseline ± 5%.
2. `git grep -n 'cartoon-card' web/src/components/visualization/{ContourSettings,BasisSelector,CoefficientsPanel}.vue` returns zero (Configurator chassis replaces the bespoke cards); `git grep -nE 'from .@mkbabb/glass-ui/configurator.' web/src/components/visualization/` returns ≥ 3 sites.
3. `git grep -nE '<Dialog\b|<DialogContent\b' web/src/components/visualization/ExportModal.vue` returns ≥ 2; the Dialog primitive supplies `role="dialog"` + `aria-modal="true"` + focus-trap + Esc.
4. `ls web/src/components/visualization/EditorToolsPanel.vue` returns "No such file" (D4 retired); `ls web/src/components/shared/CoefficientsSpectrum.vue` exists (extracted shared component).
5. `git grep -nE '@keyframes adv-open|@keyframes adv-close' web/src/components/visualization/ContourSettings.vue` returns zero (substrate `data-state` channel adopted).
6. `git grep -nE 'Math\.(min|max)\(\.\.\.' web/src/components/visualization/composables/useViewTransform.ts` returns zero on the per-frame path (the call inside a `computed` keyed on path identity is admissible — the grep scopes the per-frame `drawFrame` consumer).
7. `git grep -nE 'z-\[1[0-9]\]|z-\[2[0-9]\]' web/src/components/visualization/` returns zero (no literal `z-[N]` survives in dock / overlay / modal surfaces; the Invariant 18 binding).
8. `git grep -nE 'from .@mkbabb/glass-ui/hover-popover.' web/src/components/visualization/` returns zero (the 3 subpath imports collapsed to root barrel — Invariant 18 sub-binding for ergonomic parity).
9. `git grep -nE 'as any' web/src/components/visualization/BasisCanvas.vue` returns zero (the `partial_sums` cast retires once B.W1's SCHEMA.md tightening lands).
10. `npx playwright test e2e/visualization-ux.spec.ts --project=chromium` green; axe-core reports zero serious / critical violations at the 4 keystone states (workspace default; ContourSettings Configurator-open; ExportModal Dialog-open; AnimationControls dropdown-open).
11. `PROGRESS.md` row "W2 — UX coherence close" cites the close commit + the screenshot path (`audit/W2-screenshots/`); the row records the Invariant 18 / 19 / 20 binding.
12. **Invariant 19 binding** (auto-recompute discipline — coordinates with W3): the W2 close does not itself land the `saveContourPoints` auto-recompute (W3 owns the seam at the entity model), but the Playwright spec includes a save-then-render assertion that fails if W3's seam regresses; the cross-wave Invariant 19 closure is at W3 + W4 jointly.

## Format and Lint Cadence

W2 is a broad implementation wave. `cd web && npm run lint`, `cd web && npx vue-tsc -b --force`, and `npx playwright test e2e/visualization-ux.spec.ts` run after each integration batch (after Agent A's Configurator sweep; after Agent B's Dialog migration; after Agent C's shared extraction; after Agent D's EditorTools retire; after Agent E's render-path + naming + z-token sweep) and before the wave close commit. The format pass on the touched `.vue` / `.ts` files runs against the same ESLint + Prettier profile as the rest of `web/`.

## Verification Artefacts

- `e2e/visualization-ux.spec.ts` (created, green, 4 axe-core keystone states).
- `audit/W2-screenshots/` (before/after for the 5 panel migrations + ExportModal Dialog substitution + DC-term suppression).
- `vue-tsc -b --force` clean output.
- `npm run build` bundle-size delta recorded in PROGRESS.md (the Configurator + Dialog adoptions should reduce bespoke LOC by ≥ 200 net).
- `git diff --stat` showing the LOC retirements: `EditorToolsPanel.vue` (-121 LOC); `ExportModal.vue` (-≈80 LOC after Dialog substitution); `CoefficientsPanel.vue` + `EqCoefficientsPanel.vue` (-≈100 LOC each net of the shared extraction).
- The axe-core JSON report at `e2e/playwright-report/axe-w2.json` (4 keystone-state assertions; zero serious / critical violations).

## Commit Plan

- Agent-owned implementation commits per sub-unit (5 streams).
- Orchestrator integration commits at each batch boundary (Configurator sweep; Dialog migration; Coefficients dedup; EditorTools retire; render-path + naming).
- A documentation commit at the wave close updating `PROGRESS.md` + the `audit/W2-screenshots/` index.

## Dependencies

- **Depends on**: the Wχ — challenge wave's close (`audit/challenge.md` with the P4 Wave-1 invariant binding-test disposition); the W1 — CRUD-contract ratification wave's close for the `partial_sums` `Record<string, {x,y}>` SCHEMA.md row (the scope item 15 cast removal depends on the typed contract — but the cast removal can sequence after W1 ratifies).
- **Independent of**: the W3 — fourier visualization entity wave at file bounds; W2 touches only `web/src/components/visualization/**` + `web/src/components/equation/EqCoefficientsPanel.vue` + `web/src/components/shared/CoefficientsSpectrum.vue` + `web/src/components/paper/{MobileFloatingToc,PaperSidebar}.vue`; W3 owns `api/**` exclusively.
- **Blocks**: the W4 — fourier convergence wiring wave's `@axe-core/playwright` integration (W4.d's keystone-state coverage extends the W2 spec); the W5 — close ceremony.

## Archaeology

The Wave-1 audit synthesis (`docs/audits/runs/2026-05-26-B-audit-wave-1/SYNTHESIS.md`) authored this wave. Six concurrent audit lanes (L1 prompt/precept; L2 plan-reality; L3 visualization-stack; L4 glass-ui-usage; L5 docks; L6 chronic-deferral) converged on a single load-bearing observation: the visualization-route surface — 14 dock-shaped components under four naming labels with three background idioms and zero Configurator adoption — is the next-tier coherence work the constellation has been deferring whilst the override-layer / cohort-attribution / admin-parity passes ran (the A.W2 / W3 / W5 waves). With A's surface drift discharged, W2 is the natural next floor — one wave under one architectural intent (the `Configurator` + `Dialog` substitution as the central idiom), discharging the L5 §5 1 HIGH a11y gap + 3 MEDIUM gaps + 4 LOW gaps, the L4 §3 G1 + G3 + G4 + G5 + H1 cosmetic gaps, the L3 §3.5 D8 + §3.6 D5 + D7 visualization-pipeline residue, and the L5 §4 + §6 dock-idiom drift.

The wave does **not** reactivate the cross-repo value.js-C tracking row originally at this position. That tracking row is preserved at B.md §3 under the relabeled `W2-tracking` sub-section ("tracked-as-orphaned, awaits future value.js re-engagement" per the orphan verdict). The W2 slot's name and number are reused for the UX-coherence wave; the orphan tracking is a non-wave sub-section. No load-bearing semantics conflict.
