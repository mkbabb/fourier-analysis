# B.W5 — close

**Name**: W5 — close.
**Opens after**: the W4 — fourier convergence wiring wave's close (entity + consumers live; helper-adoption thresholds satisfied per W4 hard-gate item 11).
**Agents**: 1 (the orchestrator close ceremony; a single reconciliation agent may assist with the conformance-matrix re-grep).
**Hard gate**: `PROGRESS.md` reconciled (every wave row CLOSED with a citing commit); `docs/tranches/B/FINAL.md` authored (paired criteria → reflection, citing every B commit); both coordination docs updated; the brittleness window restored (legacy `snapshots`/`gallery` renamed to `_snapshots_legacy`/`_gallery_legacy` per `CRUD-CONTRACT §11`); orphan-verdict bookkeeping discharged; `uv run pytest` green; `cd web && npx vue-tsc -b --force` green.
**Status**: provisional (authored at the Wχ close 2026-05-26; the close criteria are firm but the citing commits resolve only at W5 dispatch).

## Goal criterion

This wave succeeds when tranche B is closed honestly: the §1 thesis is reconciled against what landed (the converged `visualization` entity; the `api/lib/crud/` utility substrate; the fourier-mandatory contract; the orphan-verdict residuals named with destinations), the brittleness window is restored, and the close artefact (`FINAL.md`) lets the next constellation audit read B's outcome without re-discovering it.

## Scope

1. **Reconcile `PROGRESS.md`.** Every status-board row reads CLOSED (or, for W2-tracking, "orphaned — terminal") with the citing commit hash. The log carries a close entry per wave (W0–W5) and the final reconciliation entry.
2. **Author `docs/tranches/B/FINAL.md`.** Structure mirrors `docs/tranches/A/FINAL.md`: §0 paired goal/completion criteria → §N reflection. Cite every B commit (W0 `b0a85d8`; Wα `193ad57`; the Wχ close; the W1/W2/W3/W4 close commits). Record: the five-scheme → one-entity collapse; the utility-module LOC total; the migration count-parity evidence (`audit/migration-counts.md`); the 9/9 KISS rejections HOLD; the invariant-16 P1 certification; the orphan-verdict residuals (`colors.ts`/`easings.ts` held; destination `fourier-tranche-C-or-successor`); the conformance-matrix close state (94 fourier rows PASS / 88 value.js DEFERRED).
3. **Restore the brittleness window.** Rename the legacy `snapshots` and `gallery` collections to `_snapshots_legacy` / `_gallery_legacy` per `CRUD-CONTRACT §11` (the W3 gate kept all three present; W5 inverts it). Assert `mongosh --eval "db.getCollectionNames()"` shows the renamed forms and `visualizations`.
4. **Update both coordination docs.** `coordination/CRUD-CONSTELLATION.md` — flip the W2-tracking orphan row to its terminal disposition; record the latent-affordance hand-off (the W1 contract is consumable by a future value.js re-engagement); reconcile the §timing version staleness (v0.9.0 → v0.10.0 per Wα R6). `coordination/CONFORMANCE-MATRIX.md` — finalise the 94-PASS / 88-DEFERRED state per its §10 close-rule.
5. **Discharge orphan-verdict bookkeeping.** Every value.js-side conformance row reads DEFERRED with the named-successor entry; every W4 named B-residual (`colors.ts` gut; `easings.ts` sampler retire; value.js dep bump) carries its `fourier-tranche-C-or-successor` destination in `PROGRESS.md` and `FINAL.md`.
6. **Re-run the W4 helper-adoption carry-forward greps** (W4 hard-gate item 11) at the W5 boundary commit: `grep -rE "from api.lib.crud" api/routers/admin.py` ≥ 3; `grep -rE "errors.problem|cursors.paginate|etag.require_if_match|idempotency.idempotent|softdelete\." api/routers/` ≥ 10. The thresholds must still HOLD — prevents framework-in-disguise resurgence at the adoption boundary.
7. **Final green sweep.** `uv run pytest` (full suite — the 97 inherited + the new W3 entity/utility specs) green; `cd web && npx vue-tsc -b --force` green; `cd web && npm run build` exit 0.

## File Bounds

| File | Access |
|---|---|
| `docs/tranches/B/PROGRESS.md` | modify (reconcile all rows + close log) |
| `docs/tranches/B/FINAL.md` | create |
| `docs/tranches/B/coordination/CRUD-CONSTELLATION.md` | modify (terminal orphan disposition + timing reconcile) |
| `docs/tranches/B/coordination/CONFORMANCE-MATRIX.md` | modify (finalise PASS/DEFERRED close state) |
| `~/Programming/value.js/docs/tranches/C/coordination/CRUD-CONSTELLATION.md` | modify (mirror — held; value.js-C is RETIRED, so the mirror records the latent-affordance hand-off) |

The collection rename (scope item 3) is a `mongosh` operation against the live DB, not a file edit.

Do NOT touch: `api/**`, `web/**` (W3/W4 own them — W5 changes no source). W5 is a docs + DB-rename + verification wave.

## Hard Gate

1. `grep -c "CLOSED" docs/tranches/B/PROGRESS.md` covers every wave row; no row reads `provisional` or `planned`.
2. `docs/tranches/B/FINAL.md` exists with §0 paired criteria; `grep -E "b0a85d8|193ad57" docs/tranches/B/FINAL.md` resolves (the W0 + Wα commits cited) plus the later wave commits.
3. `mongosh --eval "db.getCollectionNames()"` returns `_snapshots_legacy`, `_gallery_legacy`, `visualizations` (the brittleness window restored).
4. `uv run pytest` green; `cd web && npx vue-tsc -b --force` green; `cd web && npm run build` exit 0.
5. The W4 helper-adoption greps (scope item 6) still meet their thresholds at the W5 boundary commit.
6. `grep -E "fourier-tranche-C-or-successor" docs/tranches/B/PROGRESS.md docs/tranches/B/FINAL.md` resolves (the named residuals carry destinations).
7. `git grep -E "TODO|TBD|XXX" docs/tranches/B/FINAL.md` returns zero (no narrative deferral in the close artefact; DEFERRED-as-conformance-status is distinct).

## Verification Artefacts

- `docs/tranches/B/FINAL.md` (created).
- `docs/tranches/B/PROGRESS.md` (all rows CLOSED).
- The `mongosh` collection-rename output captured in the close log.
- The full `uv run pytest` + `vue-tsc` + `npm run build` green output.
- The W4-helper-adoption re-grep output at the W5 boundary.

## Commit Plan

- A single close-ceremony commit (scope `tranche-B/W5`) carrying `FINAL.md` + the reconciled `PROGRESS.md` + the coordination updates.
- The collection rename runs before the commit; its output is captured in the commit body.

## Dependencies

- **Depends on**: the W4 — fourier convergence wiring wave's close (entity + consumers live; helper-adoption thresholds satisfied).
- **Blocks**: nothing — W5 is the terminal wave. The latent value.js affordance (the ratified W1 contract) is handed to a future value.js re-engagement, not to a B wave.

## Archaeology

W5 closes the tranche that A deliberately declined to attempt — the architectural transposition of two repos' (and one repo's internal) duplicated CRUD facility into one converged identity model. Under the orphan verdict the close is `complete_with_misses` against the original *cohort* aim (the value.js half never landed) and clean against the *fourier* aim (the entity, the utility substrate, the contract all land fourier-internally). The named-successor discipline means every miss carries a destination; nothing closes silent. The Wχ challenge (`audit/challenge.md`) certified the plan shape COHERENT before any implementation dispatched; W5's FINAL.md records whether the landed reality matched that certification.
