# B.W4 — fourier convergence wiring (orphan-verdict fallback as primary)

**Name**: W4 — fourier convergence wiring (orphan-verdict fallback primary).
**Opens after**: the W3 — fourier visualization entity wave's close (entity + migration live). The cohort-active open-gate also required **value.js-C.W1 published with the `@latest` tag** — the single hard cross-repo dependency per `B.md:97`, `CRUD-CONSTELLATION.md:104`, and H5 §6.4 R-T-2. **Under the orphan verdict (recorded 2026-05-26)** value.js-C never opened; the W4 fallback contract (H4 §4.W4) — originally the contingency path — becomes the primary path. See *Fallback* below.
**Agents**: 4 parallel (partitioned by surface — A stores; B `colors.ts` gut (fallback-default); C admin data-layer + slug-routed URLs; D `draftStorage` + animation).
**Hard gate**: stores point at `visualization` slug-routed URLs; admin components consume the `visualizations` endpoints; `vue-tsc -b --force` green; the Playwright spec `e2e/visualization-crud.spec.ts` green at 3 viewports. Under the orphan-verdict-primary path: `web/src/lib/colors.ts` and `web/src/lib/easings.ts` are byte-identical to the W3 close state, and `PROGRESS.md` records the named B-residual (destination `fourier-tranche-C-or-successor`).
**Status**: provisional (hardened from B.md §3's row "W4 — fourier convergence wiring"; the cross-repo dependency now reads as orphaned and the named fallback is the primary path).

## Goal criterion

This wave succeeds if fourier's frontend consumers — the gallery store, the workspace store, the animation store, `draftStorage.ts`, the fourier-A.W5-lifted admin components, the router URL shape — all re-point at the converged `visualizations` endpoint and consume the W3-landed `api/lib/crud/` helpers, with full CRUD + soft-delete + restore round-tripping at 3 viewports under Playwright. The wave succeeds *despite* the cross-repo dependency's structural orphan: the `colors.ts` gut and the `easings.ts` sampler retirement become a named B-residual with the destination `fourier-tranche-C-or-successor`, recorded explicitly in `PROGRESS.md`. The wave fails if the residual is silent or if any consumer surface still routes by the legacy `snapshot_hash` / `imageSlug` identity.

## Scope

1. Re-point `web/src/stores/gallery.ts` (242 lines): drop the `snapshot_hash`-keyed identity; the store now lists / loads / saves / deletes against `GET | POST | PATCH | DELETE /visualizations`; soft-delete + restore flows added; the `visibility` filter exposed in the public-gallery view.
2. Re-point `web/src/stores/workspace.ts` (369 lines): the workspace's "save" / "publish" actions write to `/visualizations` with the visibility transition (`draft → unlisted | public`); the `imageSlug` / `snapshotHash` URL parameters become `visualizationSlug` (one slug per noun, per `CRUD-CONTRACT.md §1`).
3. Re-point `web/src/stores/animation.ts` (132 lines) where it reads / writes snapshot identity; replace with `visualizationSlug` lookups against the entity store.
4. Re-point `web/src/lib/draftStorage.ts` (71 lines): IndexedDB draft entries are keyed by `visualizationSlug` (drafts are `visibility="draft"` rows on `visualizations` per `CRUD-CONTRACT.md §4`; the IndexedDB layer becomes a local-first cache, not a parallel identity scheme — this collapses one of the "five divergent identity schemes" from audit E).
5. **`web/src/lib/colors.ts` gut** (117 lines) — held under the orphan-verdict fallback: every colour-conversion + palette helper retires onto value.js's `colorScale`, `Palette`, and existing `parseCSSColor` / `mixColors` / `color2`. Under the orphan verdict, value.js-C.W1 never published these library lifts; `colors.ts` therefore remains byte-identical to its W3 close state and the residual carry records as the named B-residual in `PROGRESS.md` (destination `fourier-tranche-C-or-successor`).
6. Re-point `web/src/lib/easings.ts` (127 lines) hand-rolled SVG-path sampler onto value.js's `sampleToSVGPath` (at `~/Programming/value.js/src/math.ts` per H5 §3.3). Held under the same orphan-verdict fallback as scope item 5 — `easings.ts` remains as-is; the hand-rolled sampler retires as a named B-residual; the file keeps its easing-curve presets.
7. Re-point `web/src/lib/api.ts` (598 lines) endpoint table: `snapshots` / `gallery` routes deleted; `visualizations` routes added with the full CRUD shape; TypeScript shapes (`Visualization`, `VisualizationCreate`, `VisualizationPatch`) added.
8. Re-point the fourier-A.W5-lifted admin components (`web/src/components/visualization/gallery/AdminFlaggedPanel.vue`, `GalleryAdminBanner.vue`, `AdminUserList.vue`): consume the `/visualizations` admin endpoints per `CRUD-CONTRACT.md §7`; flag / dismiss / suspend actions resolve against the converged entity.
9. Update `web/src/router/index.ts`: route `/v/:visualizationSlug` for the entity; retire `/w/:imageSlug?/:snapshotHash?` or alias it to the new shape. Slug-routed URLs per `CRUD-CONTRACT.md §1`.
10. Author the Playwright spec `e2e/visualization-crud.spec.ts` exercising create → publish → unlisted → delete → restore on the live UI; runs at 3 viewports (375 × 667, 1280 × 800, 1440 × 900) per fourier-A invariant 3's lineage. **Wire `@axe-core/playwright` in the same spec** (added 2026-05-26 — Wave-1 audit synthesis L2 §6.5 + L6 §4 FLAG-GAP; routed from A.W5-a11y.md:22,103,110 + A.FINAL.md §5 row 12). The axe integration takes the form `import { injectAxe, checkA11y } from '@axe-core/playwright'`; at each viewport's keystone state (create-form open; published view; admin moderation panel open; ExportModal Dialog open per B.W2 — UX coherence), the spec asserts zero serious / critical violations. The harness *measures* the discharge of Invariant 18 — the UI surface conventions — that B.W2 lands; without the harness, the W5.a-filed carry chronic-defers to its third gate per the L6 §3 chronic-deferral analysis.
11. Bump fourier's `package.json` value.js dependency to the `@latest` tag — held under the orphan verdict (value.js-C.W1 never published; the value.js dependency stays at its pre-W4 version and the bump carries forward as a named B-residual).
12. **The migrated callers consume the `api/lib/crud/` utility helpers landed in W3** alongside the (held) `colors.ts` gut. The admin router (`api/routers/admin.py`), the gallery-store-backed visualization endpoints, and the draft-storage backend endpoints — the consumers re-pointed at the `visualization` entity in W3 — acquire their helper usage here per U1's invariant 16: `errors.problem` on every non-2xx response, `cursors.paginate` on every list endpoint, `etag.require_if_match` on every PATCH / DELETE, `idempotency.idempotent` on every POST, `softdelete.*` on the moderation / delete paths. No new helpers are authored — W3 owns the utility-module landing; W4 wires the existing helpers into the consumer surface that W3's entity work also touched.

13. **Session TTL 7 → 30 d migration** (added 2026-05-26 — Wave-2 audit synthesis C1 §6 #7). `api/services/database.py` `session_ttl_days` setting bumps from 7 to 30 to align with `CRUD-CONTRACT §6`. The migration extends live `expires_at` on already-active sessions: `db.sessions.updateMany({expires_at: {$gt: now}}, [{$set: {expires_at: {$add: ["$expires_at", 23 * 86400 * 1000]}}}])`. The one-line config bump + the one-shot Mongo update fold into agent A's stores-repoint commit. Sub-gate: `grep -E "session_ttl_days = (30|.*=30)" api/services/database.py` returns ≥ 1; the live-session extension is captured in the W4 PROGRESS row.

14. **RFC 9239 RateLimit-header middleware emission** (added 2026-05-26 — Wave-2 audit synthesis C6 §4 ADMIT 3 / §7 #3; Invariant 24 from B.md §2). `api/services/rate_limiter.py` exposes `RateLimit-Limit`, `RateLimit-Remaining`, `RateLimit-Reset` on every response; 429 responses additionally carry `Retry-After`. The middleware sits on the existing rate-limiter without changing topology — the single-replica posture per invariant 12 remains; the headers make the budget honest at the response boundary. Implementation: ~15 LOC of middleware. The CS4.* matrix rows (per `CONFORMANCE-MATRIX.md §S4`) ratify the assertion. Sub-gate: `curl -I localhost:8000/api/visualizations | grep -iE "ratelimit-"` returns ≥ 3 header lines on every response.

15. **RFC 9110 ETag / If-Match consumer adoption** (added 2026-05-26 — Wave-2 audit synthesis C6 §4 ADMIT 2 / §7 #2; Invariant 23 from B.md §2). `web/src/stores/gallery.ts` and `web/src/stores/workspace.ts` send `If-Match: <etag>` on PATCH and DELETE requests against `/visualizations/{slug}`; the ETag is captured from the prior GET response. The W3 entity already lands `etag.require_if_match` (per W3 hard-gate item 11 — ≥ 2 sites in `visualizations.py`); W4 wires the consumer half. Sub-gate: `grep -E "If-Match" web/src/stores/{gallery,workspace}.ts` returns ≥ 2 sites; the Playwright spec at `e2e/visualization-crud.spec.ts` exercises a 412 path (PATCH with stale ETag) and confirms the response carries `application/problem+json` with `type: urn:contract:etag-mismatch`.

## File Bounds

| File | Access |
|---|---|
| `web/src/stores/gallery.ts` | modify |
| `web/src/stores/workspace.ts` | modify |
| `web/src/stores/animation.ts` | modify |
| `web/src/lib/draftStorage.ts` | modify |
| `web/src/lib/colors.ts` | modify-carve (held under the orphan-verdict fallback — no W4 modification) |
| `web/src/lib/easings.ts` | modify (held under the orphan-verdict fallback — no W4 modification) |
| `web/src/lib/api.ts` | modify (endpoint table swap) |
| `web/src/components/visualization/gallery/AdminFlaggedPanel.vue` | modify |
| `web/src/components/visualization/gallery/GalleryAdminBanner.vue` | modify |
| `web/src/components/visualization/gallery/AdminUserList.vue` | modify |
| `web/src/router/index.ts` | modify |
| `web/package.json` | modify (held under the orphan-verdict fallback — no `@mkbabb/value.js` bump in W4) |
| `web/package-lock.json` | modify (held under the orphan-verdict fallback) |
| `e2e/visualization-crud.spec.ts` | create |
| `PROGRESS.md` | modify (record the named B-residuals: `colors.ts` gut; `easings.ts` sampler retire; value.js dependency bump) |

Do NOT touch: `api/**` (W3 owns the entity); `coordination/**` (W1 owns the contract); `~/Programming/value.js/**` (value.js-C owns — held DEFERRED under the orphan verdict).

## Disjointness

Agent units A (stores), B (`colors.ts` gut — held under the orphan-verdict fallback), C (admin data-layer + slug-routed URLs), and D (`draftStorage` + Playwright) share no `modify` or `modify-carve` paths under the primary (orphan-verdict) path. Agent B's worktree is largely a no-op under the orphan verdict — its work reduces to authoring the PROGRESS named-residual entries and confirming `colors.ts` / `easings.ts` byte-equality.

## Agent Units

### B.W4.a Stores re-point

- Goal: the gallery, workspace, and animation stores list / load / save / delete / restore against `/visualizations` with `visualizationSlug` as the single URL identity.
- Mechanism: rewrite `gallery.ts` + `workspace.ts` + `animation.ts` to consume `/visualizations` endpoints; replace the dual `imageSlug` / `snapshotHash` URL shape with `visualizationSlug`; expose visibility + soft-delete + restore actions on the workspace store.
- Files: `web/src/stores/{gallery,workspace,animation}.ts` (modify); `web/src/router/index.ts` (modify); `web/src/lib/api.ts` (modify endpoint table).
- Sub-gate: `vue-tsc -b --force` green on `web/`; `grep -rE "snapshot_hash|snapshotHash|imageSlug" web/src/stores web/src/router` returns zero on identity paths (occurrences only in legacy-shim comments cite the contract migration).

### B.W4.b `colors.ts` gut onto value.js (held under the orphan-verdict fallback)

- Goal (primary, cohort-active path — pre-orphan-verdict): swap every `colors.ts` colour-conversion + palette function for the value.js `colorScale` / `Palette` / `parseCSSColor` / `mixColors` / `color2` equivalent; reduce `colors.ts` to ≤ 20 lines (a re-export shim) or delete; retire `easings.ts`'s sampler onto value.js's `sampleToSVGPath`; bump `web/package.json` `@mkbabb/value.js` to the value.js-C.W1 published version.
- Goal (orphan-verdict-primary path, 2026-05-26): record the residual honestly. value.js-C never opened; the library `Palette` / `colorScale` / `sampleToSVGPath` lifts never landed; `colors.ts` and `easings.ts` remain unchanged; the `web/package.json` dependency bump is held. The agent's work product is the `PROGRESS.md` named-B-residual entry with destination `fourier-tranche-C-or-successor` and the audit citation to the R1 / R4 refinement-assay artefacts.
- Files: `web/src/lib/colors.ts` (modify-carve or delete) — **untouched under the orphan-verdict primary path**; `web/src/lib/easings.ts` (modify) — **untouched under the orphan-verdict primary path**; `web/package.json` (modify) — **untouched under the orphan-verdict primary path**; `web/package-lock.json` (modify) — **untouched under the orphan-verdict primary path**; `PROGRESS.md` (modify — named B-residual entry).
- Sub-gate (cohort-active alternative — should value.js re-engage mid-wave): `git diff --stat web/src/lib/colors.ts` shows ≥ 80 lines deleted; `grep -E "from .@mkbabb/value\.js." web/src/lib/colors.ts web/src/lib/easings.ts` confirms the re-export shape; `npm ls @mkbabb/value.js` shows the value.js-C.W1 published version.
- Sub-gate (orphan-verdict primary, default): `PROGRESS.md` row records the named-destination deferral with citations to `docs/audits/runs/2026-05-19-refinement-assay/{r1-assay.md,r4-valuejs-C-refinement.md}`; `colors.ts` and `easings.ts` are byte-identical to the W3 close state (`git diff W3-close..W4-close -- web/src/lib/colors.ts web/src/lib/easings.ts` empty); `web/package.json` value.js version is byte-identical to the W3 close.

### B.W4.c Admin data-layer + slug-routed URLs

- Goal: the fourier-A.W5-lifted admin components and the router's URL shape resolve against `/visualizations`; flag / dismiss / suspend actions round-trip against the live API.
- Mechanism: re-point the three A.W5-lifted admin SFCs (`AdminFlaggedPanel.vue`, `GalleryAdminBanner.vue`, `AdminUserList.vue`) at the `/visualizations` admin endpoints; update `router/index.ts` with the canonical `/v/:visualizationSlug` route; retire `/w/:imageSlug?/:snapshotHash?` to an alias or delete.
- Files: `web/src/components/visualization/gallery/Admin*.vue` (modify); `web/src/router/index.ts` (modify); `web/src/lib/api.ts` (modify admin endpoints).
- Sub-gate: `vue-tsc -b --force` green; a manual visit to `/v/<slug>` resolves the entity; admin actions (flag, dismiss) round-trip against the live API with `X-Admin-Token`.

### B.W4.d `draftStorage` + Playwright

- Goal: the IndexedDB draft layer keys by `visualizationSlug` and is a local-first cache (not a parallel identity scheme); a Playwright spec exercises the entity's full CRUD + soft-delete lifecycle at 3 viewports.
- Mechanism: rewrite `draftStorage.ts` with a `visualizationSlug`-keyed IndexedDB shape; collapse the local-draft identity into a `visibility="draft"` row pattern. Author `e2e/visualization-crud.spec.ts` exercising create → publish → unlisted → delete → restore at 3 viewports.
- Files: `web/src/lib/draftStorage.ts` (modify); `e2e/visualization-crud.spec.ts` (create).
- Sub-gate: `npx playwright test e2e/visualization-crud.spec.ts --project=chromium` green; the spec asserts zero console errors and zero non-2xx asset responses per fourier-A invariant 3.

## Triumvirate Dispatch

The triumvirate fires when the wave's file bounds, hard gate, or substrate-with-consumer wiring no longer describes the work required to close. For W4 the trigger surfaces are:

- value.js mid-wave re-engages (an unexpected publish of `Palette` / `colorScale` / `sampleToSVGPath` under a new value.js tranche letter) — the wave's primary path inverts from the orphan-verdict-default back to the cohort-active form; the triumvirate's plan-augment lane re-binds Agent B's gate and the residual records retract;
- a Playwright spec failure that is not local-edit recoverable (the 3-viewport sweep surfaces a layout defect crossing multiple components) — the diagnostic-loop trigger fires at iteration 3;
- a `vue-tsc` failure surfacing a TypeScript shape collision between the `Visualization` / `VisualizationCreate` / `VisualizationPatch` shapes added in W4 and the helper-module shapes landed in W3 — the redress lane reconciles before W5 dispatch.

## Hard Gate

1. `vue-tsc -b --force` green from `web/` (run command: `cd web && npx vue-tsc -b --force 2>&1 | tail -5` shows no errors).
2. `git grep -E "snapshot_hash|snapshotHash" web/src/stores web/src/router web/src/lib/api.ts` returns zero on identity paths.
3. **Cohort-active alternative**: `git diff --stat web/src/lib/colors.ts` shows ≥ 80 lines deleted (the LOC drop materialises per `B.md §6`); `npm ls @mkbabb/value.js --prefix web` shows the value.js-C.W1 published version with the `@latest` tag.
4. **Orphan-verdict primary (the default at 2026-05-26)**: `PROGRESS.md` row "W4 — fourier convergence wiring partial close" cites the named-destination deferral (the `colors.ts` gut + the `easings.ts` sampler retire + the value.js dependency bump, destination `fourier-tranche-C-or-successor`); `web/src/lib/colors.ts` and `web/src/lib/easings.ts` are byte-identical to the W3 close (`git diff W3-close..W4-close -- web/src/lib/colors.ts web/src/lib/easings.ts` empty); `web/package.json` `@mkbabb/value.js` version is byte-identical to the W3 close; the residual entry cites `docs/audits/runs/2026-05-19-refinement-assay/{r1-assay.md,r4-valuejs-C-refinement.md}` as the orphan-verdict provenance.
5. `npx playwright test e2e/visualization-crud.spec.ts` green; the report shows 3 viewports × 0 console errors × 0 non-2xx asset responses. **`@axe-core/playwright` injected per scope item 10**; the report carries 4 `checkA11y` keystone-state assertions with zero serious / critical violations (the harness binding Invariant 18's discharge).
6. `curl -s localhost:8000/visualizations/<slug>` reachable via a `/v/<slug>` browser visit; `vue-tsc`'s route-graph emits the new path.
7. Admin panel: `flag → dismiss → suspend` round-trips against the `/visualizations` admin endpoints with an `admin_audit` row written per request (verified by `mongosh --eval "db.admin_audit.countDocuments()"` delta).
8. `PROGRESS.md` row "W4 — fourier convergence wiring close" cites the close commit and the Playwright report path; under the orphan-verdict primary the row additionally cites the named-residual entries with the `fourier-tranche-C-or-successor` destination.
9. **Utility-helper adoption in the migrated callers** (per U1 invariant 16 / the U3 spec — W4 consumes the W3 landing): `grep -rE "from api.lib.crud" api/routers/admin.py` returns ≥ 3 imports (admin re-points at the utility helpers); `grep -rE "errors.problem|cursors.paginate|etag.require_if_match|idempotency.idempotent|softdelete\." api/routers/` returns ≥ 10 sites across the migrated routers (the W3-landed utility is actually consumed by the W4 wiring, not merely available).
10. **Conformance-matrix rows touching W4 file bounds read PASS** (per the R3 refinement assay §4.5): `scripts/check-w4-matrix-rows.sh` queries `CONFORMANCE-MATRIX.md` for rows whose test path intersects W4's file-bounds list and asserts status=PASS in the fourier column (the value.js-side column holds DEFERRED under the orphan verdict).

11. **Helper-adoption carry-forward to W5** (added 2026-05-26 — Wave-2 audit synthesis C6 §2 W4-MEDIUM-risk + §8 risk-matrix row 5). W4 closes only when the helper-adoption thresholds bound at hard-gate item 9 (≥ 3 `from api.lib.crud` imports in `admin.py`; ≥ 10 helper sites across migrated routers) **hold** AND W4's PROGRESS row records the explicit carry-forward assertion: "the helper-adoption thresholds at item 9 remain satisfied at W5 close; W5's close-ceremony re-runs the grep at the W5 boundary commit". Prevents framework-in-disguise resurgence at the adoption boundary (the C6 §2 risk-matrix row 5 binding) by binding the adoption-shape across two waves rather than one.

12. **RateLimit headers emitted** (added 2026-05-26 — Wave-2 audit synthesis Invariant 24 / scope item 14): `curl -sI localhost:8000/api/visualizations | grep -ciE "^ratelimit-"` returns ≥ 3 (the three header lines `RateLimit-Limit`, `RateLimit-Remaining`, `RateLimit-Reset`); on a 429 response, `Retry-After` is also present. The CS4.* matrix rows ratify; the assertion binds at this gate.

13. **ETag / If-Match consumer adoption** (added 2026-05-26 — Wave-2 audit synthesis Invariant 23 / scope item 15): `grep -E "If-Match" web/src/stores/{gallery,workspace}.ts` returns ≥ 2 sites; the Playwright spec at `e2e/visualization-crud.spec.ts` exercises the 412 mismatch path with the `urn:contract:etag-mismatch` envelope assertion.

14. **Session TTL bumped** (added 2026-05-26 — Wave-2 audit synthesis scope item 13): `grep -E "session_ttl_days = 30" api/services/database.py` returns ≥ 1; the live-session `expires_at` extension is captured in the W4 PROGRESS row with the `updateMany` command output.

## Format and Lint Cadence

W4 is a broad implementation wave; `cd web && npm run lint`, `cd web && npx vue-tsc -b --force`, and `npx playwright test` run after each integration batch (after agent A's stores re-point; after agent C's admin re-point; after agent D's draftStorage + Playwright spec). Agent B's work under the orphan-verdict-primary path runs only `git diff --check` against the PROGRESS additions. The format pass on the touched `.ts` / `.vue` files runs against the same ESLint + Prettier profile as the rest of `web/`.

## Verification Artefacts

- `e2e/visualization-crud.spec.ts` (created, green at 3 viewports).
- The Playwright report at `e2e/playwright-report/` (captured at the W4 close).
- The `vue-tsc -b --force` output (clean).
- The `mongosh` `admin_audit` count delta capture.
- Under the orphan-verdict primary: the `PROGRESS.md` named-B-residual entry with citations to the R1 and R4 refinement-assay artefacts and the `fourier-tranche-C-or-successor` destination.
- Under the cohort-active alternative (should value.js re-engage mid-wave): `web/src/lib/colors.ts` LOC delta (`git diff --stat` saved to `docs/tranches/B/audit/colors-ts-gut.md`); `web/package.json` + `web/package-lock.json` diff showing the `@mkbabb/value.js` bump.

## Commit Plan

- Agent-owned implementation commits per sub-unit (four streams; Agent B's stream under the orphan-verdict primary reduces to a PROGRESS-only commit).
- Orchestrator integration commits at each batch boundary.
- A documentation commit at the wave close updating `PROGRESS.md` (including the named-B-residual entries under the orphan-verdict primary).

## Dependencies

- **Depends on**: the W3 — fourier visualization entity wave's close (entity live); **value.js-C.W1 published with the `@latest` tag updated** (per H5 §6.4 R-T-2) under the cohort-active alternative; the W4 fallback contract per `B.md §7` + H4 §4.W4 applies otherwise (the orphan-verdict primary path).
- **Blocks**: the W5 — close ceremony.

## Archaeology

The single hard cross-repo dependency (per H4 §4.W4 and `CRUD-CONSTELLATION.md:104`) was `fourier-B.W4 → value.js-C.W1 published`. value.js-C cannot open until value.js-B closes (six linear waves per H4 §5) AND B.W1 ratifies the contract — the projected wait window from B.W1 close to value.js-C.W1 publish was the largest fragility in B at authoring time. The **named fallback** (W4 lands everything except the `colors.ts` gut; the residual carries an explicit destination in `B.md §7`) was the H4 §4.W4 hardening that made W4 dispatchable regardless of sibling-tranche timing. The Wχ — challenge wave's P3 probe ("cross-repo timing real; image-blob deferral honest") was the adversarial test that confirmed the fallback exists and is honest before W4 dispatches.

**Orphan-verdict provenance (recorded 2026-05-26):** the R1 cross-repo state-of-the-world assay (`docs/audits/runs/2026-05-19-refinement-assay/r1-assay.md`) and the R4 value.js-C refinement assay (`r4-valuejs-C-refinement.md`) record that value.js never opened C. The repo raced through D → E → F → G → H under different theses; six of thirteen contract surfaces landed incidentally (the D.W2 Lane D `formatPalette ??` excision; the E.W2 Lane A `cron.ts` `$nin` retirement; the D.W2 / E `palette-api` god-module split; the repository layer landing; `withTransaction` coverage; the `as any` corpus driven to zero) while seven structural surfaces did not (the CRUD-CONTRACT ratification, the library `Palette` type, the `colorScale` + `sampleToSVGPath` lifts, the slug word-list extraction, the `api/src/crud/` utility module, the palette-schema migration to required-non-null-owner + 3-state visibility, the value.js-side `CRUD-CONSTELLATION.md` mirror). The W4 fallback contract — originally the contingency — becomes the primary path; agent B's gut work is held as a named B-residual; the `easings.ts` retirement holds alongside; the value.js dependency bump holds. Past gates that passed stay passed: the W1 — CRUD-contract ratification's fourier-unilateral close, the W3 — fourier visualization entity wave's landing, the `api/lib/crud/` utility-module landing — none retract.
