# B.W1 — CRUD-contract ratification

**Name**: W1 — CRUD-contract ratification.
**Opens after**: the Wχ — challenge wave's close (per `B.md §3`; the three core probes P1 / P2 / P3 dispose to `accepted` or `narrowed` in `audit/challenge.md` per the H4 hardening §6).
**Agents**: 2 parallel (A — contract authoring; B — shared-data extraction + cross-repo sign-off propagation).
**Hard gate**: `coordination/CRUD-CONTRACT.md` lands with all 13 sections per H4 §3; every §10 conformance-matrix row cites a real test name in **both** repo columns (the value.js-side column reads `DEFERRED` under the orphan verdict, per the R3 refinement assay §9); the value.js sign-off commits in §0 if value.js has re-engaged (otherwise §0 records the orphan-verdict-DEFERRED state); the ratification commit hash is recorded in both repos' `CRUD-CONSTELLATION.md`.
**Status**: provisional (hardened from the `B.md §3` row "W1 — CRUD-contract ratification"; H4 §3 is the canonical outline substrate; the Wχ — challenge wave may narrow per the P1 framework-in-disguise probe's disposition).

## Goal criterion

This wave succeeds if a single written CRUD specification holds for both backends — slug algorithm, ownership rules, soft-delete semantics, session model, admin moderation shape, cron / TTL behaviour, error envelope, ETag, idempotency-key — to a degree warranted by KISS, and the §10 conformance matrix names a real test artefact for every assertion. The contract is the substrate the W3 — fourier visualization entity wave implements against, not a negotiation surface mid-flight. Under the orphan verdict the contract ratifies fourier-only at W1 close; the value.js half remains a latent affordance held DEFERRED until value.js re-engages.

## Scope

1. Author `coordination/CRUD-CONTRACT.md` with the **13-section structure** ratified by the H4 hardening §3 (`docs/audits/runs/2026-05-18-tranche-harden/h4-fourier-B.md:121-264`): `§0 Status` · `§1 Identity` · `§2 Slug algorithm` · `§3 Ownership` · `§4 Visibility` · `§5 Soft-delete` · `§6 Sessions` · `§7 Admin moderation` · `§8 Cron / TTL` · `§9 Shared-data-vs-code` · `§10 Conformance matrix` · `§11 Migration disposition` · `§12 Open items / change log`.
2. Lift the §1–§9 must-have fields **verbatim** from H4 §3 (do not re-research): identity is one slug per noun; content hashes (`sha256` and `contour_hash` on fourier; `currentHash` on value.js) are non-identity; slug shape `^[a-z]+(-[a-z]+){3}$` (today fourier's `api/dependencies.py:27` `SLUG_PATTERN` accepts up to 80 chars — the contract tightens); ownership is required non-null with anonymous-publish → 401 (retires the `user_slug: None` orphan at `api/routers/gallery.py:206`); visibility is the 3-state `draft | unlisted | public` (supersedes audit E's two-state `private | public`); soft-delete uses `deleted_at` + grace + restore; sessions use opaque UUIDv4 with the `X-Session-Token` header and `last_seen_at` touch; admin is bearer-token with idempotent flag / dismiss / suspend; cron uses **bounded queries only** (no `$nin` over unbounded sets — retires the `api/services/janitor.py:60-65` shape).
3. Populate §10 — the **conformance matrix** — one row per assertion in §1–§9, two columns (fourier evidence path; value.js evidence path), each cell naming a specific future test file + run command. Empty cells are not permitted; an assertion without a planned test deletes from the contract. The H4 §3 example rows: §3 ownership 401 (`test_visualization_ownership.py::test_anonymous_publish_rejected` ⇄ `palette-ownership.test.ts::anonymous_publish_rejected`); §5 soft-delete (`test_visualization_restore_within_grace` ⇄ `palette-restore-within-grace.test.ts`); §8 cron bounded-query (`test_janitor_no_nin_over_unbounded` ⇄ the same name on the value.js side). Under the orphan verdict the value.js-side cell reads `DEFERRED` and cites the R3 refinement-assay §9 contingency.
4. Resolve the R3 disposition table (the joint research wave's R-identity-spec.md §5 deliverable) into **§9** per H4 §3.9 — per target record `spec | shared-data | shared-library | per-repo`: the slug word-list (per the R3 verdict); slug algorithm logic (`spec`); soft-delete semantics (`spec`); cron policy (`spec`); the `Palette` type (`shared-library` at `value.js/src/palette/`); `colorScale` / `sampleToSVGPath` (`shared-library`); the rate-limiter (`per-repo`, process-local single-replica documented per fourier-A invariant 12); the DB driver / framework (`per-repo`, out of scope by invariant 16).
5. If the Wχ — challenge wave's P1 framework-in-disguise probe disposes `narrowed` (the slug word-list shared-data admit predicate per H4 §2.R3 — more than 50 lines, drift would be a correctness bug, language-agnostic), extract the slug word-list to its decided location (candidates: the `@mkbabb/slug-words` npm package, the precepts submodule data directory, or two copies kept in sync). The actual consumer swaps happen at the W3 — fourier visualization entity wave (fourier) and value.js-C.W2 (value.js, latent under the orphan verdict), not in W1. Do **not** create a shared CRUD framework, a codegen step, or a third coordinating service — these three anti-patterns are rejected by name in `B.md §2` invariant 16; the rejection is certified by the Wχ — challenge wave's P1 probe.
6. Carry the value.js sign-off — the H5 hardening §6.3 confirms the sign-off comes from the single human user who owns both repos (no separate ceremony required); the sign-off is a commit signature row in `coordination/CRUD-CONTRACT.md §0` with date and commit hash. The value.js-C.W0 wave (latent under the orphan verdict) cites this commit hash as its open-gate per `~/Programming/value.js/docs/tranches/C/C.md:7` (corrected per H5 §1.4). Under the orphan verdict §0 records "value.js sign-off held DEFERRED — value.js-C.W0 unopened; fourier-side ratification commits unilaterally".
7. Update `coordination/CRUD-CONSTELLATION.md` row "Shared contract artefact" in **both repos** with the ratified contract commit hash (`docs/tranches/B/coordination/CRUD-CONSTELLATION.md:14` on the fourier side; mirror in `~/Programming/value.js/docs/tranches/C/coordination/CRUD-CONSTELLATION.md`). Update `PROGRESS.md` (both repos) row "W1 — CRUD-contract ratification" with the ratification commit hash. Under the orphan verdict the value.js-side mirror update is held pending value.js's re-engagement; the fourier-side PROGRESS row carries the unilateral ratification record.

8. **Tighten the `AnimationData.partial_sums` type contract** (added 2026-05-26 — Wave-1 audit synthesis L3 §3.6 D7). `coordination/SCHEMA.md` augments to specify `partial_sums: Record<string, {x: number; y: number}>` — the int-vs-string key seam at `BasisCanvas.vue:271-274` defeats type safety with `(sumsForBasis as any)?.[level] ?? (sumsForBasis as any)?.[String(level)]`. The contract codifies the stringified-int key form (backend JSON keys are stringified ints; the typed form binds the frontend lookup); the W3 — fourier visualization entity wave's model authoring picks up the tightened type; the W2 — UX coherence wave drops the `as any` cast at the consumer. No new endpoint surfaces; the change is shape-only.

## File Bounds

| File | Access |
|---|---|
| `coordination/CRUD-CONTRACT.md` | create |
| `coordination/CRUD-CONSTELLATION.md` | modify (cite the ratified contract by commit hash) |
| `PROGRESS.md` | modify (record W1 close + the ratification hash) |
| `~/Programming/value.js/docs/tranches/C/coordination/CRUD-CONSTELLATION.md` | modify (mirror citation; deferred under the orphan verdict) |
| slug-words shared location | create (conditional on Wχ.P1 `narrowed` disposition) |

Do NOT touch: `api/**`, `web/**`, `~/Programming/value.js/src/**`, `~/Programming/value.js/api/src/**`, `~/Programming/value.js/demo/**`. W1 writes only ratification artefacts; entity work belongs to the W3 — fourier visualization entity wave; consumer work belongs to the W4 — fourier convergence wiring wave. The actual word-list import swap (if extraction occurs) lands at W3 / value.js-C.W2, not here.

## Agent Units

### B.W1.a Contract authoring

- Goal: a single ratified `coordination/CRUD-CONTRACT.md` carries the 13-section structure with every §10 row resolving to a named test artefact (or to `DEFERRED` with the orphan-verdict rationale on the value.js side).
- Mechanism: read H4 §3 (`docs/audits/runs/2026-05-18-tranche-harden/h4-fourier-B.md:121-264`); transcribe the 13-section outline into `coordination/CRUD-CONTRACT.md`; fill §1–§9 must-have fields verbatim from the H4 §3 bullets (do **not** re-research — H4 §3 is the canonical authored substrate); populate the §10 matrix row by row by naming the test file each assertion will be proven against (the test file may not yet exist — the contract is the spec; the tests land at W3 — fourier visualization entity wave and value.js-C.W2); fill §11 migration disposition by reference to W3's `api/scripts/migrate_visualization.py` and value.js-C.W2's `api/src/migrate-palette-schema.ts`; leave §12 (open items / change log) initially empty.
- Files: `coordination/CRUD-CONTRACT.md` (create).
- Sub-gate: every §10 row carries a non-empty cell in **both** columns (the value.js column may read `DEFERRED` under the orphan verdict per the R3 assay §9); `grep -E "^\| §[1-9]" coordination/CRUD-CONTRACT.md | wc -l` agrees with the section assertion count (one row per §1–§9 assertion); `grep -E "TBD|TODO|XXX|\\?{2,}" coordination/CRUD-CONTRACT.md` returns zero; `grep -c "^## §" coordination/CRUD-CONTRACT.md` returns ≥ 13.

### B.W1.b Shared-data extraction + sign-off propagation

- Goal: the slug-words shared-data extraction (if Wχ.P1 admits) lives at the decided location, the contract's §0 records the ratification (signature + commit hash) honestly under either the cohort-active or the orphan-verdict state, and both repos' coordination docs cite the ratification commit.
- Mechanism: if the Wχ — challenge wave's P1 probe disposed `narrowed`, extract the slug word-list to the decided location and record the extraction commit hash in §9; do **not** modify the in-repo callers (those swaps belong to the W3 — fourier visualization entity wave and value.js-C.W2). Commit the value.js sign-off in `coordination/CRUD-CONTRACT.md §0` (a signature row with date + commit hash); under the orphan verdict the §0 row instead records "value.js sign-off DEFERRED — value.js-C never opened; ratification fourier-unilateral; future value.js re-engagement consumes the contract as latent affordance". Update `coordination/CRUD-CONSTELLATION.md §"Shared contract artefact"` row in **both** repos with the ratification commit (value.js-side mirror held DEFERRED under the orphan verdict). Update both repos' `PROGRESS.md` row "W1 — CRUD-contract ratification" with the close hash. Mirror the citation in `~/Programming/value.js/docs/tranches/C/coordination/CRUD-CONSTELLATION.md` (the value.js-side coordination doc per H5 §1.3); under the orphan verdict the mirror update queues for value.js's re-engagement window.
- Files: `coordination/CRUD-CONSTELLATION.md` (modify); the slug-words location (conditional create); `PROGRESS.md` (modify); `~/Programming/value.js/docs/tranches/C/coordination/CRUD-CONSTELLATION.md` (modify; deferred under the orphan verdict).
- Sub-gate: `git log --oneline coordination/CRUD-CONTRACT.md` shows the §0 sign-off (or the orphan-verdict-DEFERRED) commit; `git grep "CRUD-CONTRACT.md" ~/Programming/value.js/docs/tranches/C/` returns ≥ 1 citation with the new commit hash (under the orphan verdict this clause is held pending re-engagement); if extraction occurred, the new location resolves on both Python (`importlib`) and Node (`node -e "require(...)"`) reads.

## Triumvirate Dispatch

The triumvirate (research + plan augment + redress; the discipline lives at `docs/precepts/instructions/TRANCHE-AND-WAVE-SPEC.md §"Triumvirate"`) fires when the wave's file bounds, hard gate, or substrate-with-consumer wiring no longer describe the work required to close honestly. For W1 the trigger surfaces are:

- the Wχ — challenge wave's P1 framework-in-disguise probe disposes `rejected` (more than 20 % of contract sections require shared code) — the plan re-scopes materially; B.W1 does not dispatch under the prior shape;
- the R3 refinement-assay §1.1 ETag-shape collision discovery (three different ETag shapes ratified across four documents) — if a similar binding-collision class surfaces during contract authoring, the triumvirate re-binds before B.W3 — fourier visualization entity wave dispatches;
- stale `value.js/api/src/...` file:line citations in the contract that no longer resolve under value.js's post-D / E / F / G / H refactor (per the R3 assay §9 cohort-orphan contingency) — the redress lane re-audits citations against the current value.js HEAD before W3 dispatches.

## Hard Gate

1. `coordination/CRUD-CONTRACT.md` exists with all 13 sections (numbered §0–§12); `grep -c "^## §" coordination/CRUD-CONTRACT.md` returns ≥ 13.
2. The §10 conformance matrix has ≥ one row per §1–§9 assertion; every row has a non-empty fourier-test cell **and** a non-empty value.js-test cell (the value.js cell may read `DEFERRED` with the orphan-verdict rationale under the R3 assay §9): `grep -E "^\| §[1-9]" coordination/CRUD-CONTRACT.md | grep -v "^$" | wc -l` ≥ the assertion count.
3. The §9 disposition table assigns `spec | shared-data | shared-library | per-repo` to each of the eight targets enumerated in H4 §3.9.
4. `git show <W1-close-commit> -- coordination/CRUD-CONTRACT.md` carries the value.js sign-off line in §0 with date + commit hash; under the orphan verdict §0 instead carries the "DEFERRED — value.js-C unopened" entry naming the contingency.
5. `git grep -E "shared-crud-framework|codegen-crud|coordinator-service" coordination/` returns zero (invariant 16 deletion-style — the three anti-patterns rejected by name).
6. `git grep "CRUD-CONTRACT.md" ~/Programming/value.js/docs/tranches/C/coordination/CRUD-CONSTELLATION.md` returns ≥ 1 (held pending under the orphan verdict); `git grep "CRUD-CONTRACT.md" docs/tranches/B/` returns ≥ 1 in **both** `B.md` and `PROGRESS.md`.
7. `PROGRESS.md` row "W1 — CRUD-contract ratification close" cites the ratification commit hash; the value.js-side `~/Programming/value.js/docs/tranches/C/PROGRESS.md` row references the same hash as the value.js-C.W0 open-gate satisfaction (deferred under the orphan verdict).
8. `grep -E "TBD|TODO|XXX" coordination/CRUD-CONTRACT.md` returns zero — no narrative deferral inside the ratified document. (`DEFERRED` as a conformance-matrix cell status is distinct: it is the explicit fifth status alongside TBD / WIP / PASS / WAIVED introduced by the R3 assay §9 to record the value.js-side cohort-orphan state; the document itself carries no `TBD` placeholder text.)
9. The §U sub-sections (`U.1` through `U.9`) and the §S* schema-derived rows are present in `coordination/CONFORMANCE-MATRIX.md` before W1 close (per the R3 assay §4.1): `grep -c "^## §U\." coordination/CONFORMANCE-MATRIX.md` returns ≥ 8; `grep -c "^### §S" coordination/CONFORMANCE-MATRIX.md` returns ≥ 7.

## Format and Lint Cadence

W1 is a docs-only wave. At close it runs `git diff --check` and the repository's available document checks (a markdown lint pass over `coordination/CRUD-CONTRACT.md`; a heading-depth scan); no source lint or format runs.

## Verification Artefacts

- `coordination/CRUD-CONTRACT.md` (created — 13 sections).
- `coordination/CRUD-CONSTELLATION.md` (modified — row "Shared contract artefact" carries the ratification hash).
- `PROGRESS.md` (a "W1 — CRUD-contract ratification close" row + the ratification commit hash).
- The ratification commit hash recorded in `coordination/CRUD-CONTRACT.md §0` (or the orphan-verdict-DEFERRED entry).
- The mirror citation in `~/Programming/value.js/docs/tranches/C/coordination/CRUD-CONSTELLATION.md` (deferred under the orphan verdict).
- The slug-words extraction commit (conditional on Wχ.P1 `narrowed`).

## Commit Plan

- Agent-owned implementation commits on the worktree (one per sub-unit).
- Orchestrator integration commit at W1 close (tranche-letter-tagged scope `tranche-B/W1`).
- A documentation commit at the close ceremony updating `PROGRESS.md` and the constellation mirrors.

## Dependencies

- **Depends on**: the Wχ — challenge wave's close (`audit/challenge.md` with P1 / P2 / P3 dispositions per the H4 hardening §6); the H4 hardening's §3 outline substrate; the R3 disposition table (the joint research wave's R-identity-spec.md §5 deliverable).
- **Blocks**: the W3 — fourier visualization entity wave (entity migration implements against the contract §1–§9 must-have fields); **the value.js-C.W0 open-gate** (latent under the orphan verdict — value.js-C cannot acquire artefacts until B.W1 ratifies; per `~/Programming/value.js/docs/tranches/C/C.md:7` and H5 §1.4).

## Archaeology

This is the first attempt at this ratification. The Wχ — challenge wave's P1 framework-in-disguise probe (per H4 §6 P1) is the adversarial test that ensures invariant 16 holds; if P1 disposes `rejected` (more than 20 % of sections requiring shared code), this spec re-scopes before W1 dispatches — the plan shape changes materially. The H4 §3 13-section outline (specifically §10 — the load-bearing conformance matrix) is the substrate that prevents this wave from shipping narrative; without §10's evidence-cell-per-assertion rule, "ratified by contract" reduces to a claim. The H4 §2.R3 hardening of the slug-words decision (more than 50 lines, drift = correctness bug, language-agnostic) is the test the conditional extraction gates on.

**Orphan-verdict provenance (recorded 2026-05-26):** the R3 refinement assay's §9 cohort-orphan contingency surfaced the cohort-peer state — value.js-C never opened; the repo ran D → E → F → G → H under different theses; six of thirteen contract surfaces landed incidentally (the D.W2 Lane D `formatPalette ??` excision, the E.W2 Lane A `cron.ts` `$nin` retirement, the D.W2 / E `palette-api` god-module split, the repository layer landing, `withTransaction` coverage, the `as any` corpus to zero); seven structural surfaces did not (CRUD-CONTRACT ratification, the library `Palette` type, the `colorScale` + `sampleToSVGPath` lifts, the slug word-list extraction, the `api/src/crud/` utility module, the palette-schema migration, the value.js-side CRUD-CONSTELLATION mirror). W1's hard gate gracefully accommodates the verdict: the §0 sign-off row carries the DEFERRED state honestly; the §10 value.js-side cells carry the DEFERRED status (introduced by the R3 assay §9 as the fifth row state alongside TBD / WIP / PASS / WAIVED); the contract ratifies fourier-unilaterally and waits as a latent affordance for value.js re-engagement.
