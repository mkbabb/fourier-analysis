# K-deploy — progress log

Updated at every wave boundary. Reconciled against reality at W8 close.

## Goal of this log

Record what *actually happened* at each wave of fourier-tranche-K-deploy — the deploy-of-record + e2e-gate tranche (the months-class babb.dev API outage + the inv-27 dock-VT blocker) — so the close can reconcile claim against artefact without archaeology. K-deploy is J's infra/cross-repo successor (ordering ν′): J (the visualization REMIX tranche) STAYS OPEN to finish its data-model arc; K-deploy carries the two highest-stakes chronics the J post-impl audit surfaced — **(1)** the host is 29 commits behind (`f2fe447`; G-close/H/I/J never deployed — every push fires the webhook, builds, fails the 60s health-gate, rolls back, and exits non-zero into a log nobody watches; the F "2-month silent webhook" lineage, structurally), and **(2)** the e2e suite has been red since before J on a glass-dock duplicate `view-transition-name` that also silently breaks the dock morph in prod. **Re-grounded 2026-06-04** (the glass-ui-3.2.0 deep audit, `docs/audits/runs/2026-06-04-glassui-reground-deep-audit/`): glass-ui SHIPPED 3.2.0 (the dock-VT `useId()` fix in 3.1.1; `asideSide` + `useTextHighlight` + `inert` in 3.2.0), so chronic (2) is no longer a future-release wait — it collapses to a one-line `^3.1.0`→`^3.2.0` ADOPT-NOW bump that moves to the FRONT of the chain (bump FIRST → green → land), because the original order landed a CI-red SHA that the inv-28 gate it builds would refuse. The re-sequenced CORE is **the bump (W1 — greens CI at source, un-`fixme`s the 2 a11y keystones, UNBLOCKS J.W6's inv-27 close) → the deploy spine (W2 land observed → W3 inv-28 API-arm green-CI gate fails-closed → W4 kill silent-rollback with off-host paging), now landing a GREEN post-bump SHA**. K-deploy's close is bound by inv-27 (every "green" cites a green run id covering every job), inv-28 (the API arm refuses a red-CI SHA, fails-closed without the named operator PAT), inv-16/inv-16′ (the only outstanding cross-repo ask is the dispatch.sh retirement; the value.js-J peers are DONE-in-value.js-L), and inv-15 (every wave names a consumer).

## Completion criterion

Every wave's row carries (a) a status word, (b) a close timestamp, (c) a notes cell naming the binding deliverable. At W8 close every row reconciles against `FINAL.md`'s gate table; `FINAL.md` cites a GREEN fourier CI run id (all jobs) on the post-bump HEAD (W1 — the bump greens CI at source, NOT a blind bump), the observed-green prod capture at the green post-bump SHA (`/api/health`→`"status":"ok"` AND `/`→`404`), the `deploy_run_id` (inv-25/inv-28), a verified rollback-page delivery, and — for the cohort arm — the fourier-local `/diff` + publish envelope parity verdict (the value.js-J peers are DONE-in-value.js-L, CLOSED `66dcd68`; fourier owes only its own Python parity probe). The parity is verified against `design/J-diff-shape.md` at close. The J.W6-unblock is named in both FINAL.md files (J's and K-deploy's — the §0 interleaving, now RESOLVED-ON-BUMP). The 30-day stale-watch re-triggers at W8.

## Status board (authored 2026-06-04 from the J post-impl audit; re-sequenced 2026-06-04 by the glass-ui-3.2.0 deep audit — the BUMP leads, the deploy spine follows; awaits "Begin")

| Wave | Disposition | Status | Closed at | Notes |
|---|---|---|---|---|
| W0 — open + ordering ν′ + chronic ledger | DEV | **authored / re-grounded** | — | write `CANONICAL-ORDERING §20 / ordering ν′` (chain head κ′=H → λ′=I → μ′=J → ν′=K-deploy); **flip** the `glass-ui-dock-vt-name` row in `ADOPTION-ASKS §7` from "OPEN — gated on glass-ui 3.2.0" to **SATISFIED-UPSTREAM (3.1.1/3.2.0), ADOPT in K.W1** (the fix SHIPPED — it was mis-booked as a future wait); name the single human dependency (host-only read-only GitHub PAT for the inv-28 API-arm gate, operator-owned); record the §8 chronic ledger with terminal verdicts (zero perpetual punts; **P5 KILLED-AS-PHANTOM**, never re-booked) |
| W1 — THE BUMP: glass-ui `^3.2.0` ADOPT-NOW (UNBLOCKS J.W6; was K.W4) | SHIP | **planned** | — | `A4-deploy-e2e-reground.md`. the pure fourier-local `web/package.json:14` `^3.1.0`→`^3.2.0` + `npm install` + lock-regen against an ALREADY-PUBLISHED peer (glass-ui HEAD `06b35d9`, npm `latest=3.2.0`); **greens fourier CI at the SOURCE** (dock-VT `useId()` fix SHIPPED 3.1.1, `glass-dock-1` collision structurally impossible; **NO bridge, NO allow-entry, NO console-filter** — the W6 bridge is KILLED-BEFORE-BIRTH); un-`test.fixme` the 2 a11y keystones (`visualization-crud.spec.ts:630`, `visualization-ux.spec.ts:110`; `inert` shipped 3.1.1); set up `asideSide` (DEC-2 controls-LEFT) + wire `useTextHighlight` (from `/dom`, NOT `/motion-core`); **cite a GREEN run id (inv-27)** — not a blind bump (the un-`fixme`'d keystones + restored dock morph may surface latent app faults); single-copy lock check (keyframes 3.0.0 vs glass-ui peer-widen `^2.2.0||^3.0.0`); **unblocks J.W6, DEC-2, the control-pane lens, AND the inv-28 gate's pass-condition** |
| W2 — LAND the 29-commit backlog (observed; lands a GREEN post-bump SHA) | SHIP | **planned** | — | `A3` Fold A (was K.W1). pre-flight host tree clean (`git status --porcelain`) + `docker volume create image_blobs` (the H5 traps); manual **observed** bring-up of the green post-bump SHA capturing `docker compose logs backend` + `curl :8100/api/health` + `curl :8100/`; the idiomatic startup fix — index creation non-blocking-after-serving OR a real container `HEALTHCHECK` + `depends_on: condition: service_healthy` (NOT a blind longer poll); raise `GATE_RETRIES` ONLY paired with the HEALTHCHECK; re-run the automated chain from a real push and confirm convergence → G/H/I/J LIVE on babb.dev |
| W3 — inv-28 API-arm green-CI gate | SHIP | **planned** | — | `A3` Fold B (was K.W2). `deploy-hook.sh` gains `green_ci_gate()` querying `commits/<sha>/status` BEFORE `git reset --hard`; **fails closed** (no PAT → refuse, the correct safe state); post-bump the deploy SHA is GREEN, so the gate PASSES instead of correctly-refusing a red one (a red SHA is still blocked) — inv-27 holds for the *deploy*; the SPA arm already does this (`deploy-pages.yml` `workflow_run`, H.W2), W3 builds the symmetric API half (inv-28 API half BUILT, not booked) |
| W4 — kill the silent-rollback chronic | SHIP | **planned** | — | `A3` Fold C (was K.W3). the three terminal states (DEPLOY OK / ROLLBACK / no-green-ALERT) POST to a constellation-watched sink (ntfy/Discord/Slack webhook, host-only untracked `.env`, mirroring the HMAC-secret discipline); **backport to `deploy/templates/deploy-hook.sh`** so every repo inherits it; a rollback must page, not whisper — the third silent-for-months failure must be impossible; provisional inv-31 (observability-floor) authored here IFF the discipline is load-bearing beyond the wave |
| W5 — operator entrypoint adoption | FOLD | **planned** | — | `A3` Fold D. adopt `scripts/dev.sh` (the conformance rewrite landed in J) + `scripts/deploy.sh` wrapper + the dev-deploy-standard as fourier's canonical operator entrypoint; **reconcile `deploy.sh`'s advisory health-gate to report the same truth the on-host gate enforces**; ship the wrapper WITH the fixed spine it wraps (K.W2–W4), not before (else inv-15 substrate-without-a-closed-consumer) |
| W6 — cohort `/diff` + publish PARITY confirm (was BOOK peers) | SHIP | **planned** | — | collapsed from "BOOK the value.js-J inv-16′ peers" to a **fourier-local parity confirm** — `valuejs-J-atomdiff` + `valuejs-J-publish` are DONE-in-value.js-L (CLOSED `66dcd68`; `atomdiff.ts`, `publish.ts`, the [P0] `crud-list.ts:114` public filter all shipped); fourier owes only its OWN Python `/diff`-envelope + publish-envelope parity probe against `design/J-diff-shape.md`; no peer ask outstanding |
| A-1 / A-2 — control-pane glass-ui asks | BOOK | **planned** | — | genuinely ABSENT at 3.2.0 (NOT in the bump): **A-1** inter-row divider-rule opt-in (`ConfiguratorLayer.vue:100`, today flat `border-b`) + **A-2** `label`/`sub`→ladder hook (`:118`, today literal `text-sm font-semibold`); glass-ui SELF-BOOKED both to its **AT** successor (`AS/FINAL.md:146-155`); **kill-date = the glass-ui AT/3.3.0 release**; fourier holds no lever (inv-16) and rides rhythm + `.chassis-divider` + a fourier-local ladder meanwhile (NO hand-rolled hairline) |
| W7 — dispatch.sh retirement (gated) | BOOK | **planned** | — | gated on ALL FOUR non-fourier repos adopting per-repo `deploy-hook.sh` + `hooks.json` arms (Ask 2/3; value.js/palette-api rsync→git is the critical-path fourth); kill-date = the fourth migration's green acceptance; until then deletion 404s un-migrated arms; fourier holds no lever (inv-16) |
| W8 — close | DEV | **planned** | — | reconcile PROGRESS; `FINAL.md` (gate table, the bump's GREEN run id, observed-green prod capture, `deploy_run_id`, the rollback-page delivery, the fourier-local `/diff` + publish envelope parity verdict + the chronic-resolution block; babb.dev LIVE at the post-bump SHA; inv-28 API arm fails-closed-without-PAT; the dock-VT fix landed → J.W6 unblocked); CANONICAL-ORDERING → ordering ν′ (written §20); 30-day stale-watch re-triggered |

### What K-deploy does NOT do (recorded, not dressed as a refuted wave)

The J post-impl audit's clean scope-split keeps three things out of K-deploy's infra/cross-repo remit:

| Area | Disposition | Rationale |
|---|---|---|
| the J data-model close (W2-fix integrity + W2-transpose elegance-delete + W5/W6 wire-the-UI + the e2e green) | **not K-deploy work — stays in J** | J-local data-model work (`SYNTHESIS §2`); fracturing it into the infra tranche is the exact thing shapes (a)/(c) were rejected for. K.W1 (the bump) unblocks J.W6, but J authors + runs its own evidence wave. |
| the WebMCP feature graduation | **not K-deploy work — L-webmcp** | a feature tranche hard-gated on Chromium 146 stable, NOT an infra chronic; renamed from the stale "K" booking (`J.md §10`) so the infra chronic and the feature graduation stay un-conflated. |
| a blind gate-timeout bump as the deploy fix | **DECLINED (recorded)** | a longer blind poll is a workaround; the idiomatic fix is the health-condition-gated wait (a real `HEALTHCHECK` + `service_healthy`); `GATE_RETRIES` rises ONLY paired with the HEALTHCHECK. |

## Log

### 2026-06-04 — tranche authored (from the J post-impl audit; the SYNTHESIS forward path)

**WHAT.** After J was AUTHORED (`docs/tranches/J/J.md`, ordering μ′) and its W2–W4 implementation landed (`origin/master` HEAD `9d7c387`, the 6-commit J W2–W4 push), a 6-dimension post-impl audit (`docs/audits/runs/2026-06-04-J-postimpl-audit/`, A1 plan/waves · A2 W2 CORE code · A3 deploy chronic · A4 e2e/CI chronic · A5 prompt/precept coverage · A6 chronic/deferred/no-legacy + the SYNTHESIS forward path) re-read the J plan + this session's implementation. **Verdict (`SYNTHESIS §0`): J STAYS OPEN (it cannot honestly close — CI red over `9d7c387`, W5–W8 planned, the CORE has zero frontend consumer, the deploy never landed); the successor is a SEPARATE tranche K-deploy (ordering ν′) carrying ONLY the cross-repo infra chronics; the booked WebMCP-K is RENAMED L-webmcp to keep the chain honest.**

**The two highest-stakes chronics (the K-deploy CORE):**
- **CH-DEPLOY (`A3` entire).** Host stuck at `f2fe447` (verified: `git log f2fe447..9d7c387` = 29 commits). G-close/H/I/J NEVER landed in prod — every "closed" fourier tranche since G closed against a tree the world never saw. The gate (`scripts/deploy-hook.sh:82-94,175,191`; `GATE_RETRIES=30 × GATE_INTERVAL=2 = 60s`) requires `/api/health`→`"status":"ok"` AND `/`→`404`; it fails, rolls back to `f2fe447`, exits non-zero into journalctl. **Root cause `A3`-confirmed**: backend lifespan startup-blocking (`api/main.py` `await connect_db()` inside the FastAPI lifespan; `database.py` raises on timeout then synchronously builds ~30 indexes — incl. the new J fork indexes — BEFORE `/api/health` answers) **compounded by** the 60s gate being too tight for a 29-commit cold `WORKERS=1` start with no container `HEALTHCHECK` + no `depends_on: condition: service_healthy`. **inv-22 `/→404` RULED OUT** (nginx-direct, `fourier.conf:63`); **the J backend RULED OUT** (empty prod DB → no-op migration, AND migrations run after the gate — J is the victim, not the cause). The chronic is invisible because the API arm has no green-CI gate (inv-28 API half BOOKED-not-built) and a rollback reaches no constellation-watched sink (`grep deploy-hook.sh` for any off-host notify = NONE). The F "2-month silent webhook" anti-pattern, structurally — the SECOND silent-for-months failure.
- **glass-dock-1 duplicate VT-name (`A4`).** The e2e suite has been red since before J (prior master run `26831216131` byte-identical) on a glass-dock duplicate `view-transition-name`. **Root cause `A4`-corrected**: a setup-local counter (`dist/dock.js:228 let n=0` inside `setup()`), NOT a module counter — every GlassDock instance independently mints `glass-dock-1`, so two co-mounted docks always collide; the fix is `useId()` (`A4-1`), not "fix the counter." **Not cosmetic** (`A4-2`): during the viz↔workspace `startViewTransition`, the browser drops the duplicate-named snapshot → the dock geometry-morph silently fails in prod while editing. One symptom = two defects (inv-27 red + a real visual regression). The fix lands in the glass-ui-3.2.0 adoption bundle (`A4 §5/§7`); **K.W4 unblocks J.W6** — the load-bearing interleaving (`SYNTHESIS §0/§6.1`).

**The forward shape (transcribed from `SYNTHESIS §3`):** W0 open + ordering ν′ + ledger; W1 land the 29-commit backlog observed (Fold A — `HEALTHCHECK` + `service_healthy`, NOT a blind poll); W2 inv-28 API-arm green-CI gate fails-closed (Fold B); W3 kill silent-rollback with off-host paging + template backport (Fold C); W4 glass-ui-3.2.0 adoption — dock-VT `useId()` + P5 + asideSide + useTextHighlight + a11y inert — UNBLOCKS J.W6; W5 operator entrypoint adoption (dev.sh/deploy.sh — Fold D); W6 value.js-J inv-16′ peers; W7 dispatch.sh retirement gated; W8 close. Every wave names an inv-15 consumer. The single human dependency = the host-only read-only GitHub PAT for the inv-28 API-arm gate (operator-owned; the gate fails closed without it — the correct safe state). The SSH access is `~/.ssh/id_rsa`; the PAT is the new credential.

**Provisional new invariant.** K-deploy may author **inv-31** (observability-floor — a deploy/rollback terminal state MUST page a constellation-watched sink, or the path is not closed) at K.W3, the next clean fresh-integer (G 25/26, H 27/28, I 29/30 → 31). The chronic stayed invisible for months TWICE (F's webhook secret, this session's health-gate) — the discipline gap an invariant codifies. Provisionally reserved at 31; authored IFF K.W3 confirms it is load-bearing beyond the wave (the I-tranche test); else 31 stays unspent and the paging sink rides inv-28's "verified."

**Honest scoping (the directive's boundary).** AUTHORED-only — nothing executed, nothing "fixed." These are booked waves. The deploy fixes, the gate, the paging sink, and the glass-ui adoption are all `planned` until "Begin." J stays OPEN; K-deploy is its infra/cross-repo successor at ordering ν′. The audit run dir `docs/audits/runs/2026-06-04-J-postimpl-audit/` is the binding provenance.

### 2026-06-04 — re-grounded against the glass-ui-3.2.0 deep audit (bump-first re-sequence; docs-only)

**WHAT.** A subsequent 6-dimension deep audit (`docs/audits/runs/2026-06-04-glassui-reground-deep-audit/` — A1 glass-ui-3.2.0 state · A2 value.js J/K/L cohort · A3 fourier plan + this-session changes · A4 deploy+e2e re-ground · A5 prompt/precept · A6 chronic/no-legacy + the SYNTHESIS forward path) re-verified K-deploy's premises against the SIBLINGS' ACTUAL HEADs and found the central "K.W4 adoption gated on a future glass-ui 3.2.0 release" scaffolding FALSE. **glass-ui SHIPPED 3.2.0** (HEAD `06b35d9`, npm `latest=3.2.0`): the dock-VT `useId()` fix shipped 3.1.1; `asideSide` + `useTextHighlight` (on `/dom`, NOT `/motion-core`) + a11y `inert` shipped 3.2.0. So K.W4 collapses to a one-line `^3.1.0`→`^3.2.0` ADOPT-NOW bump (inv-16 irrelevant — the upstream lever was already pulled and released). K.md + this PROGRESS were re-grounded + re-sequenced (docs-only; inv-16: K-tranche docs only; no code, no implementation).

**THE RE-SEQUENCE (bump-first).** The original order was internally contradictory — W1 landed a CI-red SHA `9d7c387` that the W2 inv-28 `green_ci_gate` built in the same tranche would refuse. The bump is the hard PREDECESSOR of CI-green, and CI-green is the deploy gate's pass-condition, so it moves to the FRONT:

| New wave | Was | What |
|---|---|---|
| **W1 — THE BUMP** | K.W4 | `^3.1.0`→`^3.2.0` ADOPT-NOW; greens CI at source; un-`fixme`s the 2 a11y keystones; sets `asideSide`/`useTextHighlight`; KILLS the console-bridge before birth; cite a GREEN run id (inv-27). UNBLOCKS J.W6, DEC-2, the control-pane lens, AND the inv-28 gate's pass-condition. |
| **W2 — LAND the backlog** | K.W1 | the deploy spine, UNCHANGED — the REAL load-bearing chronic — now lands a GREEN post-bump SHA behind a real `HEALTHCHECK` + `depends_on: condition: service_healthy`. |
| **W3 — inv-28 API-arm gate** | K.W2 | BUILD the fail-closed `green_ci_gate()`; the green post-bump SHA is the first it lets PASS. |
| **W4 — page-on-rollback** | K.W3 | the three terminal states POST to a constellation-watched sink; backport to `deploy/templates/`. |
| **W5 — operator** | K.W5 | unchanged; shipped WITH the spine (K.W2–W4). |
| **W6 — cohort PARITY confirm** | K.W6 | collapsed from "BOOK inv-16′ peers" to a fourier-local `/diff` + publish envelope parity probe — `valuejs-J-atomdiff` + `valuejs-J-publish` are DONE-in-value.js-L (CLOSED `66dcd68`; `crud-list.ts:114` public filter shipped). |
| **A-1 / A-2** | (was in the K.W4 bundle) | SPLIT OUT — genuinely ABSENT at 3.2.0; glass-ui SELF-BOOKED both to its AT successor (`AS/FINAL.md:146-155`); BOOK-with-kill-date = the glass-ui AT/3.3.0 release. |
| **W7 — dispatch.sh retirement** | K.W7 | unchanged, gated. |

**THE DISSOLVED GATES + KILLS.** The `glass-ui-dock-vt-name` row flips OPEN→SATISFIED-UPSTREAM (W0); the J↔K "circular #1 risk" (§0) is RESOLVED-ON-BUMP with zero external dependency (the fix is already published — not a maintainer wait); the W6 console-filter bridge is KILLED-BEFORE-BIRTH (born-legacy; the bump greens the dock at source, so the bridge would mask a non-existent error — NO-LEGACY); the value.js-J peers flip OPEN→DONE-in-sibling. **P5 inner-rounding is STRUCK as KILLED-AS-PHANTOM** (was K.md:51/60/78) — the container owns the radius BY DESIGN (`Configurator.vue:130` `rounded-panel … overflow-hidden`; `779fed7` reverted the per-section radius; glass-ui's `AS/FINAL.md:113-118` calls fourier's ledger a "misdiagnosis"); the "satisfaction test, not satisfied until the inner sections round" acceptance shape is DELETED; never re-booked. **A-1 + A-2 survive** as genuinely-ABSENT BOOK-with-kill-date asks (glass-ui AT/3.3.0). The deploy spine (W2–W4) remains the REAL load-bearing chronic and is glass-ui-independent — the bump only removes the CI-red co-blocker; the cold-start `HEALTHCHECK` structural fix is still mandatory.

**Honest scoping.** AUTHORED + re-grounded only — nothing executed. The bump is the FIRST executable move; it is NOT yet done (`web/node_modules/@mkbabb/glass-ui` is still `3.1.0`; the working tree has not been reinstalled). The re-ground edited only the two K-tranche docs (`K.md`, `PROGRESS.md`) per inv-16. The audit run dir `docs/audits/runs/2026-06-04-glassui-reground-deep-audit/` is the binding provenance.
