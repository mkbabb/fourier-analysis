# K-deploy — the deploy-of-record + e2e-gate tranche (the months-class babb.dev API outage + the inv-27 dock-VT blocker)

**Tranche letter**: K-deploy — fourier-analysis's successor to J (the visualization REMIX tranche), authored as the infra/cross-repo discharge the directive's "fold ALL chronic items into a NEW tranche" demands. Where J asks *does the viz-server EARN its fork/version/provenance substrate?* (and stays OPEN to finish that data-model arc), K-deploy asks the question J's close cannot answer for itself: **the host is 29 commits behind (G-close/H/I/J never deployed), every push since `f2fe447` fires the webhook, builds, fails the 60s health-gate, rolls back to `f2fe447`, and exits non-zero into a journalctl log nobody watches — what idiomatic, non-workaround change LANDS the backlog with an observed-green gate, refuses a red-CI SHA, makes a rollback PAGE instead of whisper, and clears the glass-ui-dock e2e blocker that holds J's own inv-27 close hostage?**
**Predecessor**: J — `docs/tranches/J/J.md` (AUTHORED 2026-06-02, ordering μ′; **STAYS OPEN**, it does NOT close into K-deploy). J is the data-model tranche; K-deploy is the infra/cross-repo tranche; the two run **interleaved, not strictly sequential** (the load-bearing interleaving named explicitly in §0 below). A–I are CLOSED (ordering λ′, `docs/tranches/CANONICAL-ORDERING.md §18`); J is AUTHORED-open at μ′ (§19). K-deploy is the successor that carries the two highest-stakes chronics out of the J post-impl audit so the data-model close is not held hostage to the deploy chain.
**Authored**: 2026-06-04 — from the J post-impl audit `docs/audits/runs/2026-06-04-J-postimpl-audit/` (6 dimension auditors A1 plan/waves · A2 W2 CORE code · A3 deploy chronic · A4 e2e/CI chronic · A5 prompt/precept coverage · A6 chronic/deferred/no-legacy + the SYNTHESIS forward path). **Re-grounded 2026-06-04** against `docs/audits/runs/2026-06-04-glassui-reground-deep-audit/` (the glass-ui-3.2.0 deep audit — see the dated re-ground note at the foot of this charter): glass-ui SHIPPED 3.2.0, so the "K.W4 adoption gated on a future glass-ui release" scaffolding is FALSE and the adoption collapses to a one-line `^3.1.0`→`^3.2.0` ADOPT-NOW bump that must move to the FRONT of the executable chain (bump FIRST → green → land). The binding substrate: `SYNTHESIS.md §0` (the shape verdict — J stays open, K-deploy is the successor at ordering ν′, the WebMCP-K booking renamed L-webmcp), `§3` (the K-deploy W0–W8 wave skeleton this charter transcribes), `§4` (the terminal chronic ledger), `§6` (the top risks), `§7` (the executive answer); the deploy root-cause `A3-deploy-chronic.md` (lifespan index-blocking + the 60s-too-tight cold WORKERS=1 start + no container HEALTHCHECK; inv-22 RULED OUT, J backend RULED OUT, no off-host observability); the e2e root-cause `A4-e2e-ci-chronic.md` + the re-ground `A4-deploy-e2e-reground.md` (the glass-dock `useId()` fix SHIPPED in glass-ui 3.1.1; the `^3.2.0` bump greens the e2e suite at the source — no bridge, no allow-entry — and lets the inv-28 gate PASS instead of correctly-refusing a red SHA).
**Mode**: **direct** for the fourier-local + host-coupled waves (the 29-commit landing, the `deploy-hook.sh` green-CI gate + paging sink, the glass-ui-3.2.0 adoption, the operator-entrypoint adoption — fourier's own clean tree + the `mkbabb/deploy` template + the host); **authored-now / executed-on-clean-checkout** under **inv-16′** for the cross-repo arms (the value.js-J inv-16′ peers; the dispatch.sh retirement gated on the four non-fourier migrations — the constellation may be mid-flight, so each is a NAMED, ledgered, per-repo-green-CI-gated ask, NOT a silent sweep).
**Open**: NOT YET — K-deploy is AUTHORED only and awaits the user's "Begin," exactly as H, I, and J did. Phase 0 (W0 open + ordering ν′ + the chronic ledger) opens on authorization.

---

## §0 — The shape verdict and the J↔K-deploy interleaving (the load-bearing insight)

The J post-impl audit (`SYNTHESIS.md §0`) asked three shapes — (a) J closes now + a successor carries W5–W8 + the chronics; (b) J stays open + a SEPARATE tranche carries ONLY the cross-repo infra chronics; (c) fold J entirely into the successor — and adopted (b), refined. The decision, stated once so no future session reads "J open, K-deploy open" as a contradiction:

- **J STAYS OPEN.** It cannot honestly close (CI red over `9d7c387`, no covering green run; W5–W8 `planned`; the CORE has zero frontend consumer — a binary inv-15 breach; the deploy-of-record never landed). Closing J now would be the exact inv-25 honesty shape H was opened to kill (a "verified" claim against a red surface). J completes its own data-model arc — W2-fix integrity + W2-transpose elegance-delete + W5/W6 wire-the-UI + the e2e green.
- **K-deploy (ordering ν′)** is the new successor. Its CORE is the two highest-stakes chronics — **(1)** the months-class babb.dev API deploy outage (host 29 commits behind; the F "2-month silent webhook" lineage, structurally), and **(2)** the e2e/glass-dock inv-27 gate. A deploy chronic dropped into a data-model close is the anti-pattern the directive forbids; these earn their own named tranche with acceptance shapes, NOT a quiet J residual.
- **L-webmcp (further-out)** keeps the booked WebMCP feature graduation (Chromium-146-stable-gated), renamed from the stale "K" booking in `J.md §10` so the infra chronic and the feature graduation are never conflated. The chain head ν′ is K-deploy; the WebMCP successor is the next letter after K, hence L.

**The interleaving (named explicitly, per `SYNTHESIS.md §0/§1.3/§6.1) — RE-GROUNDED 2026-06-04:** K-deploy's **glass-ui-3.2.0 adoption (the FRONT wave, was K.W4 — the dock-VT `useId()` fix) is a HARD PREDECESSOR of J's W6 inv-27 close gate.** Although J is the "older" tranche, **K-deploy executes the e2e unblock that J's inv-27 close gate consumes.** What the original charter named the "circular-looking-but-real #1 forward risk" is now **RESOLVED-ON-BUMP, with ZERO external dependency**: the dock-VT fix SHIPPED upstream in glass-ui 3.1.1 (`GlassDock.vue:137 glass-dock-${useId()}`, `DockLayerGroup.vue:69`), is present in the published 3.2.0, and is npm-`latest`. The unblock is therefore a one-line `^3.1.0`→`^3.2.0` caret bump + `npm install` against an ALREADY-PUBLISHED peer — NOT a wait on a glass-ui maintainer, and NOT an inv-16 cross-repo ask (the upstream lever was pulled and released; fourier owes only the adoption). So the chain-honesty is: ordering ν′ = K-deploy is the successor by letter, but J's close is *downstream* of the bump, which K-deploy now runs FIRST. The two tranches are interleaved, not strictly sequential; the dependency is real but no longer circular-or-external. Both FINAL.md files (J's and K-deploy's) must name the resolved-on-bump unblock.

---

## §1 — Thesis

J built a correct, idiomatic data-model PATTERN and shipped it onto a deploy chain that has not delivered in 29 commits and an e2e gate red since before J began. The post-impl audit found the deploy chronic is a **verified close-blocker, not a booked footnote** (`A3` entire). The host is stuck at `f2fe447` (verified: `git log f2fe447..9d7c387` = 29 commits). G-close, H, I, and J have therefore NEVER landed in prod — every "closed" fourier tranche since G has closed against a tree the world never saw. The failure is structurally invisible by two mechanisms: (a) the API arm has **no green-CI gate** (the API half of inv-28 is BOOKED-not-built — named at H, carried to J), so a red SHA still fires the webhook and attempts a deploy; and (b) a rollback **reaches no constellation-watched sink** (`grep` of `deploy-hook.sh` for any off-host notify = NONE; output is stdout → journalctl only). This is the F "2-month silent webhook" anti-pattern, structurally — the SECOND silent-for-months failure, after F's missing webhook HMAC secret.

The audit root-caused the gate failure precisely, and ruled out the session's own competing hypotheses. The gate (`scripts/deploy-hook.sh:82-94,175,191`) requires BOTH, polled 30×2s ≈ 60s: `/api/health` → `"status":"ok"` (nginx `:8100` → `backend:8000`) AND `/` → HTTP `404` (inv-22; served directly by nginx, `nginx/fourier.conf:63`). The **inv-22 `/→404` half is RULED OUT** (served by nginx, not proxied; the prod-overlay hardening was already present at `f2fe447`). The **J backend is RULED OUT** (empty prod DB → the additive fork-migration is a no-op, AND migrations run AFTER the gate so they never execute on a gate failure — J is the victim riding the broken chain, not the cause). The actual cause is **backend lifespan startup-blocking** (`api/main.py` `await connect_db()` inside the FastAPI lifespan; `database.py` raises on `ServerSelectionTimeoutError` then synchronously builds ~30 indexes — incl. the new J fork indexes — BEFORE `/api/health` answers) **compounded by the 60s gate being too tight** for a 29-commit cold `WORKERS=1` start (down from `WORKERS=4` at `f2fe447`, H.β) with **no container `HEALTHCHECK`** and no `depends_on: condition: service_healthy` — so nginx can be "up" and proxying to a backend still building indexes. The session's "connect_db OK in 0.1s" diagnostic is insufficient: it was a connect-only probe, NOT the full lifespan-with-index-creation under the prod hardening.

K-deploy is the **infra/cross-repo discharge** tranche, governed by one spine — **observed-correctness over a blind gate**:

> The non-workaround landing is NOT "bump the gate timeout." It is to make the cold-start *observably correct* before trusting the automated gate, then make the automated chain *fail-closed and loud* so the third silent-for-months failure is impossible-to-miss.

Concretely — **re-sequenced 2026-06-04: the bump goes FIRST**, because the original order was internally contradictory (W1 landed a CI-red SHA `9d7c387` that the W2 inv-28 `green_ci_gate` built in the same tranche would refuse). **K.W1** is THE BUMP: the pure fourier-local `^3.1.0`→`^3.2.0` + `npm install` + lock-regen against an already-published peer — it greens fourier CI at the SOURCE (the dock-VT `glass-dock-1` collision is structurally impossible in `>=3.1.1` via app-scoped `useId()` + the upstream `proof:vt-names` static gate; NO bridge, NO allow-entry, NO console-filter), un-`test.fixme`s the two a11y keystones (the restored dock geometry-morph now actually asserts), enables DEC-2 controls-LEFT via `aside-side="left"`, wires `useTextHighlight` (from `/dom`, NOT the doc-drifted `/motion-core`), and makes the inv-28 gate able to PASS instead of correctly-refusing a red SHA. It cites a green run id (inv-27 — not a blind bump; the un-`fixme`'d keystones + restored morph may surface latent app faults), single-copy-lock-checks keyframes 3.0.0 against glass-ui's peer-widen (`^2.2.0||^3.0.0`). **K.W2–W4** are the deploy spine, UNCHANGED but now landing a GREEN SHA (post-bump): **K.W2** lands the 29-commit backlog via a manual, observed bring-up (the H2/H3 diagnostic promoted to a first-class deploy step), with the idiomatic structural fix — a real container `HEALTHCHECK` + `depends_on: condition: service_healthy` so the gate waits on *health*, not a blind longer sleep (a longer poll is a workaround; a health-condition-gated wait is idiomatic); **K.W3** builds the inv-28 API-arm green-CI gate: `deploy-hook.sh` gains a `green_ci_gate()` querying `commits/<sha>/status` BEFORE `git reset --hard`, **fails closed** (no PAT → refuse, never deploy-anyway); **K.W4** kills the silent-rollback chronic: the three terminal states (DEPLOY OK / ROLLBACK / no-green-ALERT) POST to a constellation-watched sink, backported to `deploy/templates/deploy-hook.sh` so every repo inherits it — a rollback must PAGE, not whisper. The dock-VT fix landed at the bump is what unblocks J.W6's inv-27 close.

K-deploy refuses the anti-patterns it was born to avoid. It is not a blind-retry: the landing is *observed* (the `docker compose logs backend` capture is a deploy step, not a throwaway probe). It is not a workaround: the gate waits on a health condition, not a sleep; the green-CI gate fails-closed, not deploy-anyway; the dock fix is `useId()` at the glass-ui root (SHIPPED in 3.1.1; adopted, never masked), not a fourier `!important` over a broken morph, and NOT a born-legacy console-filter bridge masking an error that no longer exists (the bridge is KILLED-BEFORE-BIRTH — building it after the upstream shipped would be the exact NO-LEGACY violation the directive forbids; §8). It is not a silent sweep: the dispatch.sh retirement is a named, ledgered, kill-dated inv-16′ ask (inv-16 held — fourier holds no lever over a sibling's tree); the value.js-J peers are DONE-in-value.js-L (CLOSED `66dcd68`), so K.W6 collapses to a fourier-local `/diff` + publish envelope PARITY confirm rather than a cross-repo sweep. And it carries no perpetual punt: every chronic the audit surfaced exits this tranche with a SHIP-wave, a KILL (P5-as-phantom; the console-bridge-before-birth), a BOOK-with-kill-date, or a recorded DECLINE (§8).

## §2 — Binding question

> The host is 29 commits behind; every push since `f2fe447` fires the webhook, builds, fails the 60s health-gate, rolls back, and exits non-zero into a log nobody watches; the API arm has no green-CI gate (a red SHA still attempts a deploy) and a rollback reaches no watched sink; and the e2e suite has been red since before J began on a glass-dock duplicate `view-transition-name` that also silently breaks the dock morph in prod. **What idiomatic, non-workaround change LANDS the 29-commit backlog with an observed-green health gate, REFUSES a red-CI SHA (fail-closed without a named operator PAT), makes a rollback PAGE a constellation-watched sink, and clears the glass-ui-dock e2e blocker that holds J's own inv-27 close hostage — without a blind-retry, a longer-blind-poll, or a fourier-local `!important` mask?**

The answer is the close, re-sequenced bump-first: the glass-ui `^3.2.0` ADOPT-NOW bump greens fourier CI at the source (K.W1 — the dock-VT `useId()` fix SHIPPED in 3.1.1, no bridge), which both clears J.W6's inv-27 close hostage AND makes the inv-28 gate able to PASS a green SHA; the 29-commit backlog then lands observed-green at `9d7c387+` (now a GREEN SHA) behind a real container `HEALTHCHECK` + `service_healthy` gate (K.W2); the API arm refuses a red-CI SHA via a fail-closed `green_ci_gate()` reading a host-only read-only PAT (K.W3 — the inv-28 API half BUILT, not booked); a rollback POSTs to a watched sink, backported to the deploy template so every repo inherits it (K.W4). The single named human dependency — the host-only read-only PAT for the inv-28 API-arm gate — is operator-owned; until it is provisioned, K.W3's gate **fails closed** (refuse-to-deploy), which is itself the correct safe state, not a punt.

## §3 — Goal criterion and completion criterion (paired)

**Goal criterion.** Land the two highest-stakes chronics out of the J post-impl audit as the CORE — the e2e/glass-dock inv-27 gate (NOW a one-line ADOPT-NOW bump, moved to the FRONT) and the deploy backlog/health-gate (the spine, landing a GREEN post-bump SHA) — plus the operator-entrypoint adoption and the booked cross-repo arms, sequenced by stakes × dependency:

- **CORE-bump (W1, was K.W4)** — the glass-ui-3.2.0 ADOPT-NOW bump (`A4-deploy-e2e-reground.md`): `web/package.json:14` `^3.1.0`→`^3.2.0` + `npm install` + lock-regen against an already-published peer (glass-ui HEAD `06b35d9`, npm `latest=3.2.0`). It greens fourier CI at the SOURCE (the dock-VT `useId()` fix SHIPPED 3.1.1 — `glass-dock-1` collision structurally impossible; NO bridge, NO allow-entry, NO console-filter), un-`test.fixme`s the two a11y keystones (`visualization-crud.spec.ts:630`, `visualization-ux.spec.ts:110`; `inert` shipped 3.1.1), enables DEC-2 controls-LEFT (`aside-side="left"`, deleting the 3 grid overrides at `VisualizationView.vue:322/326/329`; `asideSide` shipped 3.2.0), wires `useTextHighlight` (from `/dom`, NOT `/motion-core`; shipped 3.2.0). Cite a GREEN run id (inv-27 — the un-`fixme`'d keystones + restored dock morph may surface latent app faults; not a blind bump). Single-copy lock check (keyframes 3.0.0 vs glass-ui peer-widen `^2.2.0||^3.0.0`; value.js aurora externalization). **UNBLOCKS J.W6, DEC-2, the control-pane lens, AND the inv-28 gate's pass-condition.**
- **CORE-deploy (W2 → W3 → W4)** — the three folds of `A3`, UNCHANGED but now landing a GREEN post-bump SHA:
  - *land observed* (W2, `A3` Fold A): pre-flight the host tree clean + the `image_blobs` external volume exists (the H5 latent traps); a manual **observed** bring-up capturing `docker compose logs backend` + `curl :8100/api/health` + `curl :8100/`; the idiomatic startup fix (move index creation non-blocking-after-serving OR add a real container `HEALTHCHECK` + `depends_on: condition: service_healthy`) — NOT a blind longer poll; raise `GATE_RETRIES` ONLY paired with the HEALTHCHECK.
  - *gate green-CI* (W3, `A3` Fold B): `deploy-hook.sh` gains a fail-closed `green_ci_gate()` querying `commits/<sha>/status` BEFORE `git reset --hard` (post-bump the deploy SHA is GREEN, so the gate PASSES — inv-27 holds for the *deploy*, not just the merge; a red SHA is still refused).
  - *page on rollback* (W4, `A3` Fold C): the three terminal states POST to a constellation-watched sink (ntfy/Discord/Slack webhook, host-only untracked `.env`, mirroring the HMAC-secret discipline); backported to `deploy/templates/deploy-hook.sh`.
- **OPERATOR (W5)**: adopt `scripts/dev.sh` (the conformance rewrite landed in J) + `scripts/deploy.sh` wrapper + the dev-deploy-standard as fourier's canonical operator entrypoint — reconcile `deploy.sh`'s advisory health-gate to report the same truth the on-host gate enforces. Ship the wrapper WITH the fixed spine it wraps (K.W2–W4), not before.
- **CROSS-REPO (W6)**: collapsed from "BOOK the value.js-J inv-16′ peers" to a **fourier-local `/diff` + publish envelope PARITY confirm** — the `valuejs-J-atomdiff` + `valuejs-J-publish` peers are DONE-in-value.js-L (CLOSED `66dcd68`; `atomdiff.ts`, `publish.ts`, and the [P0] `crud-list.ts:114` public filter all shipped). fourier owes only its OWN Python parity probe against `J-diff-shape.md`.
- **GATED-RETIREMENT (W7)**: the dispatch.sh retirement, gated on ALL FOUR non-fourier repos adopting per-repo `deploy-hook.sh` + `hooks.json` arms (Ask 2/3; value.js/palette-api rsync→git is the critical-path fourth).

**Completion criterion (the evidence).** The close holds when:
- **CORE-bump (e2e green at source)**: the glass-ui `^3.2.0` bump lands (`web/package.json:14` `^3.1.0`→`^3.2.0`, `package-lock.json` regenerated, single keyframes copy verified) — the dock-VT `useId()` fix (SHIPPED upstream 3.1.1) is verified by two `<GlassDock>` instances emitting two distinct `view-transition-name`s with no duplicate-VT console warning under a route VT capture, AND the dock geometry-morph surviving; the two a11y keystones are un-`test.fixme`'d and assert green (`inert` consumed); `asideSide` + `useTextHighlight` (from `/dom`) consumed; fourier CI green (run id, inv-27 — a verified run, not a blind bump); **J.W6's inv-27 close is unblocked** (named in both FINAL.md files), AND the inv-28 gate can now PASS a green SHA. (P5 is STRUCK — KILLED-AS-PHANTOM; the container owns the radius by design; no satisfaction test, never re-booked.)
- **CORE-deploy (land)**: the green post-bump SHA is observed-green in prod — `curl :8100/api/health` → `"status":"ok"` AND `curl :8100/` → `404` (inv-22) on the host, with the `docker compose logs backend` capture showing "Application startup complete" within the gate window; the cold-start is gated on a real container `HEALTHCHECK` + `depends_on: condition: service_healthy` (not a blind longer poll); the automated chain re-runs from a real push and converges; G/H/I/J are finally LIVE on babb.dev.
- **CORE-deploy (gate)**: the API arm refuses a red-CI SHA — `deploy-hook.sh`'s `green_ci_gate()` queries `commits/<sha>/status` BEFORE `git reset --hard` and **fails closed** without the named operator PAT (refuse-to-deploy is the correct safe state); the green post-bump SHA is the FIRST it lets PASS (a red SHA is still demonstrably blocked); inv-28's API half is BUILT (not a third book).
- **CORE-deploy (page)**: a rollback POSTs to a constellation-watched sink (a verified delivery, the inv-27 evidence discipline applied to the page); the three terminal states (DEPLOY OK / ROLLBACK / no-green-ALERT) each page; the sink is backported to `deploy/templates/deploy-hook.sh` so every repo inherits it; the third silent-for-months failure is structurally impossible.
- **OPERATOR**: `scripts/dev.sh` + `scripts/deploy.sh` adopted as fourier's canonical operator entrypoint; `deploy.sh`'s advisory gate reconciled to report the same health truth the on-host K.W2 gate enforces; shipped WITH the fixed spine (K.W2–W4), never before.
- **CROSS-REPO**: the fourier-local `/diff` + publish envelope PARITY confirm verified against `J-diff-shape.md` (the value.js-J peers are DONE-in-value.js-L, CLOSED `66dcd68` — fourier owes only its OWN Python parity probe, not a peer sweep).
- **GATED-RETIREMENT**: dispatch.sh retirement either executed (kill-date = the fourth migration's green acceptance) or held-gated (until then deletion 404s un-migrated arms — booked, not deleted); fourier holds no lever (inv-16).
- `PROGRESS.md` matches reality; `FINAL.md` cites every commit + gate + the green CI run ids + the deploy `deploy_run_id` (inv-25/inv-28) + the observed-green prod capture; CANONICAL-ORDERING → ordering ν′ (§20).

The §7 hard-gate list is the binding ledger.

## §4 — Wave sequence (CORE-deploy + CORE-e2e + operator + cross-repo + close)

K-deploy is an **infra/cross-repo tranche**, not a data-model tranche and not a modern-web modernization tranche — so it is honestly presented as: **the glass-ui-3.2.0 ADOPT-NOW bump leads** (W1, was K.W4 — re-sequenced 2026-06-04 to the FRONT because it is the hard predecessor of CI-green, which the inv-28 deploy gate requires); **the deploy spine follows** (W2–W4, now landing a GREEN post-bump SHA); the operator-entrypoint adoption follows (W5, shipped WITH the spine it wraps); the cross-repo parity confirm + gated-retirement arms (W6/W7); the close reconciles (W8). The W0 open + W8 close are **DEV** (planning + reconciliation); W1–W7 are **SHIP/BOOK** (the host-coupled + cross-repo execution). **Each wave names its inv-15 consumer** (the SYNTHESIS §3 discipline — every K-deploy wave names a consumer).

| Wave | Disposition | Contents | Consumer (inv-15) |
|---|---|---|---|
| **W0** — open + ordering ν′ + ledger | DEV | Write `CANONICAL-ORDERING §20 / ordering ν′` (the chain head; κ′=H → λ′=I → μ′=J → **ν′=K-deploy**). Flip the `glass-ui-dock-vt-name` row in `ADOPTION-ASKS §7` from "OPEN — gated on glass-ui 3.2.0" to **SATISFIED-UPSTREAM (3.1.1/3.2.0), ADOPT in K.W1** (it was mis-booked as a future wait; the fix SHIPPED). Name the **single human dependency**: the host-only read-only GitHub PAT for the inv-28 API-arm gate (operator-owned; `~/.ssh/id_rsa` is the SSH access, the PAT is the new credential). Record the §8 chronic ledger with terminal verdicts (the audit's `SYNTHESIS §4` — zero perpetual punts; P5 KILLED-AS-PHANTOM). | the chronic ledger |
| **W1** — THE BUMP: glass-ui `^3.2.0` ADOPT-NOW (UNBLOCKS J.W6; was K.W4) | SHIP | `A4-deploy-e2e-reground.md`. The pure fourier-local bump against an ALREADY-PUBLISHED peer (glass-ui HEAD `06b35d9`, npm `latest=3.2.0`): `web/package.json:14` `^3.1.0`→`^3.2.0` + `npm install` + `package-lock.json` regen. This **greens fourier CI at the SOURCE** — the dock-VT `useId()` fix SHIPPED 3.1.1 (`GlassDock.vue:137 glass-dock-${useId()}`, `DockLayerGroup.vue:69`; the `glass-dock-1` collision is structurally impossible via app-scoped `useId()` + the upstream `proof:vt-names` static gate); **NO bridge, NO allow-entry, NO console-filter** (the W6 bridge is KILLED-BEFORE-BIRTH — building it masks a non-existent error). Un-`test.fixme` the 2 a11y keystones (`visualization-crud.spec.ts:630`, `visualization-ux.spec.ts:110`; `inert` shipped 3.1.1) — they flip to live asserts and the restored dock geometry-morph now actually runs. Set up `asideSide` (DEC-2 controls-LEFT; shipped 3.2.0), wire `useTextHighlight` (from `/dom`, NOT `/motion-core`; shipped 3.2.0). **Cite a GREEN run id (inv-27)** — not a blind bump (the un-`fixme`'d keystones + restored morph may surface latent app faults; a new app-owned fault is a fourier-local fix in THIS wave, not a new glass-ui ask). Single-copy lock check (keyframes 3.0.0 vs glass-ui peer-widen `^2.2.0||^3.0.0`; value.js aurora externalization). **Unblocks J.W6, DEC-2, the control-pane lens, AND the inv-28 gate's pass-condition.** | J.W6 e2e green; CI-green-for-the-deploy-gate; the control-pane lens (J.W5) |
| **W2** — LAND the 29-commit backlog (observed, not blind; lands a GREEN SHA) | SHIP | `A3` Fold A. Pre-flight: assert `/var/www/fourier-analysis` clean (`git status --porcelain`, the H5 dirty-tree guard) + `docker volume create image_blobs` exists (the H5 external-volume trap). Manual **observed** bring-up of the green post-bump SHA capturing `docker compose logs backend` + `curl :8100/api/health` + `curl :8100/`. If lifespan-index-blocking (H2): move index creation **non-blocking-after-serving** OR add a real container `HEALTHCHECK` + `depends_on: condition: service_healthy` (the idiomatic fix, NOT a blind longer poll). If still too tight (H3), raise `GATE_RETRIES` ONLY paired with the HEALTHCHECK. Then let the automated chain re-run from a real push and confirm convergence. | babb.dev (G/H/I/J finally LIVE) |
| **W3** — build the inv-28 API-arm green-CI gate | SHIP | `A3` Fold B. `deploy-hook.sh` gains `green_ci_gate()` querying `commits/<sha>/status` BEFORE `git reset --hard`; **fails closed** (no PAT → refuse, the correct safe state). Post-bump the deploy SHA is GREEN, so the gate PASSES instead of correctly-refusing a red one — inv-27 holds for the *deploy*, not just the merge; a red SHA is still blocked. The SPA arm already does this (`deploy-pages.yml` `on: workflow_run [CI] completed`, H.W2); W3 builds the symmetric API half. | the deploy-of-record (inv-28 API half BUILT) |
| **W4** — kill the silent-rollback chronic | SHIP | `A3` Fold C. The three terminal states (DEPLOY OK / ROLLBACK / no-green-ALERT at `deploy-hook.sh:211`) POST to a constellation-watched sink (ntfy/Discord/Slack webhook, host-only untracked `.env`, mirroring the HMAC-secret discipline). **Backport to `deploy/templates/deploy-hook.sh`** so every repo inherits it. A rollback must page, not whisper — the third silent-for-months failure must be impossible. Pairs with W3 in the same `deploy-hook.sh` edit + template backport. | the operator / the constellation |
| **W5** — operator entrypoint adoption | FOLD | Adopt `scripts/dev.sh` (the conformance rewrite landed in J) + `scripts/deploy.sh` wrapper + the dev-deploy-standard as fourier's canonical operator entrypoint — **reconcile `deploy.sh`'s advisory health-gate to report the same truth the on-host gate enforces** (`A3` Fold D). Ship the wrapper WITH the fixed spine it wraps (K.W2–W4), not before — until then it is inv-15 substrate-without-a-closed-consumer. | the operator |
| **W6** — cohort `/diff` + publish PARITY confirm (was BOOK peers) | SHIP | Collapsed from "BOOK the value.js-J inv-16′ peers" to a **fourier-local parity confirm**: the `valuejs-J-atomdiff` + `valuejs-J-publish` peers are DONE-in-value.js-L (CLOSED `66dcd68`; `atomdiff.ts`, `publish.ts`, the [P0] `crud-list.ts:114` public filter all shipped). fourier owes only its OWN Python `/diff`-envelope + publish-envelope parity probe against `J-diff-shape.md` — no peer sweep, no inv-16′ ask outstanding for these two. | the J↔value.js cohort parity |
| **A-1 / A-2** — control-pane glass-ui asks | BOOK-with-kill-date | Genuinely ABSENT at 3.2.0 (NOT in the bump): **A-1** (`ConfiguratorLayer`/`ConfiguratorRow` inter-row **divider-rule** opt-in — the twin-line groove, today `.instrument-rail`-only, `ConfiguratorLayer.vue:100`) **+ A-2** (`ConfiguratorLayer` `label`/`sub` bound to the `text-title`/`text-heading` ladder, today the magic literal `text-sm font-semibold` at `:118`). glass-ui SELF-BOOKED both to its **AT** successor (`AS/FINAL.md:146-155`). **Kill-date = the glass-ui AT/3.3.0 release.** fourier holds no lever (inv-16); meanwhile it rides rhythm + an explicit `.chassis-divider` + a fourier-local ladder on its own headers (NO hand-rolled hairline — NO-LEGACY). | the control-pane hierarchy (J.W5) |
| **W7** — dispatch.sh retirement (gated) | BOOK | Gated on ALL FOUR non-fourier repos adopting per-repo `deploy-hook.sh` + `hooks.json` arms (Ask 2/3; value.js/palette-api rsync→git is the critical-path fourth). Kill-date = the fourth migration's green acceptance; until then deletion 404s un-migrated arms. fourier holds no lever (inv-16). | the constellation deploy spine |
| **W8** — close | DEV | `FINAL.md`: the bump landed green (CI run id, inv-27); babb.dev observed-green at the post-bump SHA; inv-28 API arm fails-closed-without-PAT; a rollback pages a watched sink; the dock-VT fix landed → J.W6 unblocked; the `/diff` + publish parity verdict. CANONICAL-ORDERING → ordering ν′ (written §20). 30-day stale-watch re-triggered. | the close reconciliation |

### §4.1 — What K-deploy does NOT do (recorded, not dressed as a refuted wave)

K-deploy deliberately does not author three things, because they are out of its infra/cross-repo remit (the J-audit's clean scope-split):

- **The J data-model close (W2-fix integrity + W2-transpose elegance-delete + W5/W6 wire-the-UI + the e2e green).** That is J-local data-model work — it STAYS in J (`SYNTHESIS §2`). K-deploy carries ONLY the infra/cross-repo chronics; fracturing the data-model close into the infra tranche is the exact thing shape (a)/(c) were rejected for. K.W1 (the bump) unblocks J.W6, but J authors and runs its own evidence wave.
- **The WebMCP feature graduation.** That is L-webmcp (the renamed booking), hard-gated on Chromium 146 stable — a feature tranche, not an infra chronic. K-deploy keeps the infra chronic and the feature graduation strictly un-conflated (the rename's whole purpose).
- **A blind gate-timeout bump as the deploy fix.** Recorded as DECLINED (§8): a longer blind poll is a workaround; the idiomatic fix is the health-condition-gated wait (a real container `HEALTHCHECK` + `service_healthy`). `GATE_RETRIES` rises ONLY paired with the HEALTHCHECK.

## §5 — Inherited invariants

K-deploy inherits all prior invariants (`docs/tranches/INVARIANTS.md`, inv-1…30 + the named C/F/G/H/I additions, incl. inv-16′) unchanged. The load-bearing ones for K-deploy:

- **inv-15 — domain-model-in-library, persistence-in-app + substrate-without-consumer-is-binary.** Every K-deploy wave names a consumer (§4): the bump → J.W6 + CI-green-for-the-deploy-gate; the landing → babb.dev; the green-CI gate → the deploy-of-record; the paging sink → the operator/constellation; the operator entrypoint → the operator. The `deploy.sh` wrapper (W5) is held until the spine it wraps lands (substrate-without-a-closed-consumer until K.W2–W4) — the inv-15 discipline applied to the wrapper's own ordering.
- **inv-16 — shared-by-contract; per-language utility modules admitted, frameworks rejected; no SILENT cross-repo mutation.** fourier's write surface for the cross-repo arms is `fourier-analysis/docs/**` only. The dock-VT fix lives in glass-ui's source — but it is ALREADY SHIPPED (3.1.1, published in 3.2.0); fourier's W1 is the *adoption* of a published peer (a one-line caret bump), NOT a cross-repo ask, so inv-16 is irrelevant to the bump (the upstream lever was pulled and released; fourier never needed to write glass-ui's tree, and a fourier `!important` was never the path). The deploy-template backport (W4) writes `mkbabb/deploy` (a fourier-authorized write surface per H.ε); the dispatch.sh retirement (W7) writes neither — it is an inv-16′ ask. The value.js-J peers are DONE-in-value.js-L, so they carry no outstanding inv-16′ fourier ask either.
- **inv-16′ — authorized-cross-repo-sweep.** The only outstanding inv-16′ ask in K-deploy is the dispatch.sh retirement (W7) — a NAMED, ledgered ask in `docs/constellation/ADOPTION-ASKS.md §7`, its own commit, gated on the target repos' own green CI (inv-27), authored-now / executed-on-clean-checkout (the H.ε BOOK-ALL posture) — NOT a silent sweep against a mid-flight tree. The A-1/A-2 control-pane asks are glass-ui-owned, BOOKED-to-AT (inv-16; fourier holds no lever). The value.js-J atom-diff + publish peers are NO LONGER outstanding asks — they are DONE-in-value.js-L (CLOSED `66dcd68`); K.W6 is fourier's own parity probe, not a peer sweep.
- **inv-26 — single-contract-source (hand-typed-canonical, no codegen).** K-deploy authors no api↔web contract (it is infra) — but it does NOT revive codegen and it keeps the `green_ci_gate()` reading the GitHub status API directly (no generated client). The W6 `/diff`-envelope parity asserts fourier's impl against the hand-typed `J-diff-shape.md` doc, not against the peer (the inv-26 spirit).
- **inv-27 — green-means-green.** Every "green" in the close cites a CI run id GREEN on EVERY job. K.W3 makes inv-27 hold for the *deploy* (a red SHA is refused), not just the merge. The K.W1 bump is what lets fourier CI actually go green over a workspace-editing e2e flow (the dock-VT collision is gone at the source) — the inv-27 claim J could not make. The bump itself must cite a GREEN run id (not a blind bump — the un-`fixme`'d keystones + restored morph may surface latent app faults).
- **inv-28 — verified-deploy-of-record.** The SPA arm landed at H.W2 (`deploy-pages.yml` `workflow_run` gate). K.W3 BUILDS the API half (the booked-not-built piece) — the webhook→deploy-hook path gains a fail-closed `commits/<sha>/status` precondition. The re-sequence is load-bearing HERE: the bump (W1) is the predecessor that makes the deploy SHA green, so the gate can PASS instead of correctly-refusing a red SHA. Composes with inv-25 (automated AND verified).

K-deploy may add **one** new invariant — an **observability-floor** discipline (provisional **inv-31**, the next clean fresh-integer; G modeled 25/26, H 27/28, I 29/30, so 31 collides with nothing). The audit found the chronic stayed invisible for months because a rollback reached no watched sink — TWICE now (F's webhook secret, this session's health-gate). An observability-floor invariant would codify: **a deploy/rollback terminal state MUST page a constellation-watched sink, or the deploy path is not closed** — the structural fix for "broken for months and nobody noticed." This is genuinely a new discipline (not a restatement of inv-25/inv-27/inv-28, which govern automated/green/verified, not *observed-by-a-human-when-it-fails*), so the integer is **provisionally reserved at 31** and authored at K.W4 (the paging-sink wave) IFF the wave confirms the discipline is load-bearing beyond the single wave (the I-tranche test: a fresh invariant earns its keep by a cross-wave/cross-repo reach, not a one-wave convenience). If K.W4 finds the paging sink is adequately governed by inv-28's "verified" without a fresh discipline, no new invariant is authored and 31 stays unspent. NAMESPACE note: the integer 31 also names a glass-ui **precept** (per `INVARIANTS.md §3`'s 28–33 band) — `K-Inv 31` would be fourier-tranche-local and namespace-partitioned exactly as `H-Inv 28` / `I-Inv 29`/`30` are.

## §6 — Cross-repo perimeter (inv-16′)

K-deploy's executable spine is **fourier-local + host-coupled + deploy-template**: the glass-ui-3.2.0 ADOPT-NOW bump (W1 — fourier bumps + consumes against an already-published peer), the 29-commit landing (W2, host), the `deploy-hook.sh` green-CI gate + paging sink (W3/W4, fourier-owned `scripts/` + the `mkbabb/deploy` template), the operator entrypoint (W5, fourier `scripts/`), the `/diff` + publish parity probe (W6, fourier-local Python) — all against fourier's own clean tree / the host / the fourier-authorized deploy repo, gated by inv-27 green CI + inv-28 verified deploy.

What is NOT a fourier cross-repo ask anymore (re-grounded 2026-06-04 against the siblings' actual HEADs):
- **`glass-ui-dock-vt-name`** — SATISFIED-UPSTREAM (3.1.1, published in 3.2.0); the dock-VT `useId()` fix is SHIPPED. fourier's W1 is the *adoption* of a published peer (a caret bump), NOT a cross-repo ask gated on a future release. The `ADOPTION-ASKS §7` row flips from "OPEN — gated on glass-ui 3.2.0" to "SATISFIED-UPSTREAM, ADOPT in K.W1" (W0).
- **`valuejs-J-atomdiff`** + **`valuejs-J-publish`** — DONE-in-value.js-L (CLOSED `66dcd68`): `atomdiff.ts` (the atom-diff PATTERN + `GET /:slug/diff?from=`), `publish.ts` (`POST /:slug/{publish,unpublish}` idempotent in-place flip), and the named [P0] `crud-list.ts:114` public-visibility filter (the private-palette leak) ALL shipped. fourier owes only its OWN W6 parity probe (`/diff` + publish envelope, against `J-diff-shape.md`) — not a peer ask.

The genuinely-outstanding cross-repo items are:
- **A-1 / A-2** (control-pane glass-ui asks) — genuinely ABSENT at 3.2.0; glass-ui SELF-BOOKED both to its **AT** successor (`AS/FINAL.md:146-155`). BOOK-with-kill-date = the glass-ui AT/3.3.0 release; fourier holds no lever (inv-16) and rides rhythm + `.chassis-divider` + a fourier-local ladder meanwhile.
- **dispatch.sh retirement** (W7) — gated on the four non-fourier migrations (Ask 2/3); fourier holds no lever.

Each remaining ask is its own commit, gated on the target repo's own green CI (inv-27), authored-now / executed-on-clean-checkout (the H.ε BOOK-ALL posture) — NOT a silent sweep against a mid-flight tree (inv-16 held). The fourier-local `/diff` envelope parity (against `J-diff-shape.md`) + the publish-response-envelope parity are verified at the K-deploy close.

## §7 — Hard gates (completion criterion)

- **CORE-bump (e2e green at source)**: the glass-ui `^3.2.0` bump landed (`web/package.json:14` `^3.1.0`→`^3.2.0`, lock regenerated, single keyframes copy verified); the dock-VT `useId()` fix verified (two distinct `view-transition-name`s, no duplicate-VT warning under a route VT capture, the dock morph surviving); the 2 a11y keystones un-`test.fixme`'d and asserting (`inert` consumed); `asideSide` + `useTextHighlight` (from `/dom`) consumed; fourier CI GREEN (run id, inv-27 — a verified run, not a blind bump); **J.W6 inv-27 close unblocked** (named in both FINAL.md files) AND the inv-28 gate can now PASS a green SHA. (P5 STRUCK — KILLED-AS-PHANTOM; no satisfaction test; never re-booked.)
- **inv-27 green-means-green**: every "green" cites a CI run id GREEN on EVERY job (fourier; the value.js-J arm is DONE-in-value.js-L, CLOSED `66dcd68`, its own green CI already cited there).
- **inv-28 verified-deploy-of-record**: the API arm refuses a red-CI SHA (`green_ci_gate()` fails closed without the named operator PAT); the SPA arm's `workflow_run` gate (H) stands; the deploy-of-record citations are green-CI-gated; cited with a `deploy_run_id` (inv-25). The bump (W1) is the predecessor that lets the gate PASS a green SHA rather than refuse a red one.
- **CORE-deploy (land)**: the green post-bump SHA observed-green in prod (`/api/health`→`"status":"ok"` AND `/`→`404`); the cold-start gated on a real container `HEALTHCHECK` + `depends_on: condition: service_healthy` (NOT a blind longer poll); the host tree pre-flighted clean + `image_blobs` volume present (the H5 traps); the automated chain re-runs from a real push and converges; G/H/I/J LIVE on babb.dev.
- **CORE-deploy (gate)**: `deploy-hook.sh`'s `green_ci_gate()` queries `commits/<sha>/status` BEFORE `git reset --hard`, fails closed without the PAT; the green post-bump SHA is the first it lets PASS (a red SHA is still demonstrably blocked); inv-28's API half BUILT (not a third book).
- **CORE-deploy (page)**: a rollback POSTs to a constellation-watched sink (a verified delivery cited); the three terminal states each page; backported to `deploy/templates/deploy-hook.sh`.
- **OPERATOR**: `scripts/dev.sh` + `scripts/deploy.sh` adopted as the canonical operator entrypoint; `deploy.sh`'s advisory gate reconciled to the on-host K.W2 truth; shipped WITH the spine (K.W2–W4), never before.
- **CROSS-REPO**: the fourier-local `/diff` + publish envelope PARITY confirm verified against `J-diff-shape.md` (the value.js-J peers are DONE-in-value.js-L; fourier owes only its own Python parity probe — no peer ask to book or execute).
- **A-1 / A-2 (glass-ui asks)**: BOOK-with-kill-date = the glass-ui AT/3.3.0 release (glass-ui SELF-BOOKED, `AS/FINAL.md:146-155`); fourier holds no lever (inv-16) and rides rhythm + `.chassis-divider` + a fourier-local ladder meanwhile (no hand-rolled hairline).
- **GATED-RETIREMENT**: dispatch.sh retirement executed (kill-date = the fourth migration's green acceptance) or held-gated (deletion 404s un-migrated arms until then); fourier holds no lever (inv-16).
- **single human dependency**: the host-only read-only PAT is named (W0/W3), operator-owned; the gate fails closed without it (the correct safe state, not a punt).
- **chronic-resolution gate**: every chronic carried into K-deploy exits with a terminal verdict or a NAMED hard external gate (§8 — zero perpetual punts; P5 KILLED-AS-PHANTOM, never re-booked).
- **observability-floor (provisional inv-31)**: IF authored at K.W4, a deploy/rollback terminal state pages a watched sink or the path is not closed; ELSE 31 stays unspent and the paging sink rides inv-28.

## §8 — Cross-tranche debt + explicit deferrals (folded)

**Folded into K-deploy (SHIP-as-wave — the audit's terminal verdicts, `SYNTHESIS §4`; re-grounded 2026-06-04):**
- **glass-ui-3.2.0 ADOPT-NOW bump** — the dock-VT `useId()` fix + `inert` + `asideSide` + `useTextHighlight`, ALL SHIPPED upstream (3.1.1/3.2.0). **SHIP-as-wave at W1 (the FRONT wave).** Was framed as "K.W4, gated on a future glass-ui 3.2.0 release"; that frame is FALSE (glass-ui HEAD `06b35d9`, npm `latest=3.2.0`). It is a one-line `^3.1.0`→`^3.2.0` caret bump + `npm install`; it greens fourier CI at the source (NO bridge), un-`fixme`s the 2 a11y keystones, and is the predecessor the deploy gate needs. Kill-date = the bump commit (cite a GREEN run id, inv-27).
- **glass-dock-1 duplicate VT-name** — inv-27 e2e red (~13 specs) + a real prod regression (silent dock-morph failure during the viz↔workspace `startViewTransition`). **ADOPT-NOW at W1 (the bump).** Root cause `A4`-corrected: a setup-local counter; the fix is `useId()` — SHIPPED upstream in 3.1.1 (`GlassDock.vue:137 glass-dock-${useId()}`, the `proof:vt-names` gate makes the class structurally impossible). The W6-side console-filter bridge (the 5-filter consolidation + a kill-dated allow-entry + a self-deleting satisfaction probe) is **KILLED-BEFORE-BIRTH**: the bump greens the dock at the source, so the bridge would mask a non-existent error (NO-LEGACY). Only a plain green e2e assertion remains; the `_probes.ts` DRY consolidation (if kept) is a pure helper with NO dock-VT allow-entry.
- **CH-DEPLOY** — the months-class babb.dev API deploy outage (host `f2fe447`, 29 commits G/H/I/J never deployed; the F "2-month webhook" lineage). **BOOK-with-kill-date as W2+W3+W4 (NOT a punt).** The REAL load-bearing chronic; glass-ui-independent. CLOSED when: (1) the green post-bump SHA observed-green in prod (W2); (2) the API arm refuses a red-CI SHA, fails-closed-without-PAT (W3); (3) a rollback pages a watched sink (W4). Root cause `A3`-confirmed = lifespan index-blocking + 60s-too-tight cold start; J backend RULED OUT; inv-22 RULED OUT. Re-sequenced AFTER the bump so it lands a GREEN SHA.
- **inv-28 API-arm green-CI gate** "BOOKED-not-built" (named at H, carried to J). **BUILD in W3** (not a third book). Fails-closed without the named PAT.
- **glass-ui-a11y `inert`** (H.W1) — SHIPPED upstream 3.1.1. **ADOPT-NOW at W1** (un-`fixme` the 2 keystones on the bump). Terminal.
- **dispatch.sh retirement + the constellation adoption asks** (D/E/F→G→H→J). **BOOK-GATED with triage in W7.** Deletion gated on all-four-migrate (mechanical, not a punt); the value.js/palette-api rsync→git is the critical-path fourth. Maintainer-owned (inv-16).
- **valuejs-J-atomdiff + valuejs-J-publish** inv-16′ peers (J deep-audit fold). **DONE-in-value.js-L** (CLOSED `66dcd68`; `atomdiff.ts`, `publish.ts`, the [P0] `crud-list.ts:114` public filter all shipped). The OPEN rows flip to DONE-in-sibling; the residual is a fourier-local `/diff` + publish envelope PARITY confirm at **W6** (no peer ask outstanding). inv-16 held.

**Named-forward (BOOKED, not built):**
- **L-webmcp** — the WebMCP tool surface (`registerTool("remix-visualization")` + `("diff-visualizations",{readOnlyHint:true})` + `("publish-visualization")`), Early-Preview, hard external gate Chromium 146 stable. **Renamed from the stale "K" booking in `J.md §10`** so the infra chronic (this tranche) and the feature graduation (L) are never conflated. NOT a K-deploy wave — the named successor (§10).
- **A-1 inter-row divider-rule + A-2 label/sub→ladder hook** (control-pane glass-ui asks) — genuinely ABSENT at 3.2.0 (NOT in the bump); glass-ui SELF-BOOKED both to its **AT** successor (`AS/FINAL.md:146-155`). **BOOK-with-kill-date = the glass-ui AT/3.3.0 release.** fourier holds no lever (inv-16); rides rhythm + an explicit `.chassis-divider` + a fourier-local ladder meanwhile (NO hand-rolled hairline).

**KILLED (terminal — never re-booked):**
- **P5 inner-ConfiguratorLayer-rounding** (the carried "literal user defect") — **KILLED-AS-PHANTOM.** Twice over: user-REJECTED ("the rounding is controlled by the container") AND already container-owned upstream BY DESIGN — `Configurator.vue:130` `rounded-panel … overflow-hidden` owns the radius + clip; commit `779fed7` deliberately REVERTED the per-section radius as geometrically inert + divider-deforming; glass-ui's own `AS/FINAL.md:113-118` calls fourier's ledger a "misdiagnosis." STRUCK from K.md (was K.md:51/60/78) + all booking sites; the "visual-evidence satisfaction test, NOT satisfied until the inner sections round" acceptance shape is DELETED. Never re-book — the 3-tranche I→J→K inertia is the lesson (a book is not a fact until re-verified against the sibling's actual close).
- **the W6 console-filter bridge** (the dock-VT allow-entry + self-deleting satisfaction probe) — **KILLED-BEFORE-BIRTH** (born-legacy; the bump greens the dock at the source, so the bridge masks an error that no longer exists — the exact NO-LEGACY violation the directive forbids).

**Disclaimed (J-local or value.js-local — NOT a K-deploy residual):**
- the J data-model integrity + elegance folds (W2-fix palette-PATCH force-resolve, W2-transpose phantom-chain delete, the §11 prose amend, the inv-15 consumer wire-up) — these are J-local data-model work (`SYNTHESIS §2`); K-deploy carries the infra chronics only.
- **VAL-9** (`spring()→LinearStop[]` emitter) — **KILL** at value.js-J.W0 (keyframes owns the emitter; lifting to value.js inverts a dependency for zero de-dup; the ≥2-consumer gate is structurally unmeetable). fourier disclaims.
- **VAL-1** (OKLab aurora-LUT) — **BOOK-with-hard-kill-date** at value.js-J.W0 (ship IFF glass-ui `deriveAurora()` + a 2nd consumer live, else KILL at close). fourier disclaims.
- **CH-6** (value.js 6-tranche glass-ui-primitive carry) — **FORCE-TERMINAL at value.js-J.W0** (KILL-as-moot / RE-EXPRESS-as-inv-16′ / SHIP; no 7th carry). fourier names-to-flag only.
- **C1 / CH-3 colour-lift** — **KILLED** (recorded, J.md §8; `easings.ts` self-sufficient, no fourier consumer; the deadlock dissolves). Terminal.

**Declined (recorded, not deferred):**
- a blind `GATE_RETRIES`/timeout bump as the deploy fix — a longer blind poll is a workaround; the idiomatic fix is the health-condition-gated wait (a real `HEALTHCHECK` + `service_healthy`); `GATE_RETRIES` rises ONLY paired with the HEALTHCHECK (`A3` Fold A);
- a fourier-local `!important` or wrapper-VT-name mask for the dock collision — it cannot rename a `view-transition-name` glass-ui sets via an inline style binding, and it would *hide the real prod regression* (the silent dock-morph failure); the only correct fix is `useId()` at the glass-ui root (`A4 §5/§7`, inv-16);
- a "deploy-anyway on missing PAT" fallback for the green-CI gate — the gate fails CLOSED (refuse-to-deploy is the correct safe state); a deploy-anyway is the exact silent-ship the chronic exists to kill;
- a Mongo replica-set infra wave to make the lifespan startup transactional — out of scope; the startup fix is the non-blocking-index / `HEALTHCHECK` split, not new infra.

## §9 — Brittleness window (provisional)

K-deploy plans a **real, bounded brittleness window at K.W1** — it is a genuine prod bring-up (the host moves from `f2fe447` to `9d7c387+`, a 29-commit jump that rebuilds both images and cold-starts a `WORKERS=1` backend). The rollback substrate is named: **the deploy-hook's existing rollback-to-`f2fe447` path is the floor** — if the observed bring-up fails the gate, the host returns to the last-known-good `f2fe447` exactly as it does today (the chronic's own mechanism, here used as the safety net rather than the silent failure). The window is bounded by the observed-bring-up step (the `docker compose logs backend` capture is read BEFORE trusting the automated chain), and the `image_blobs` external volume + the clean-tree pre-flight (the H5 traps) are asserted as step 0 so the window does not open on a latent precondition failure. Once `9d7c387+` is observed-green, the window closes and the automated chain re-runs from a real push to confirm convergence.

The other waves are non-brittle: W2/W3 are additive `deploy-hook.sh` edits (a `green_ci_gate()` that fails-closed + a paging POST) + a template backport — revertible, and the green-CI gate's failure mode is refuse-to-deploy (the working chain is never made worse, only stricter). W4 is a glass-ui dependency bump + consumption behind the inv-16′/inv-29 floor (absent ⇒ the prior path). W5 is the operator wrapper (chrome-only `scripts/`, shipped after the spine it wraps). W6/W7 are cross-repo asks, each its own commit, per-repo-green-CI-gated, revertible independently (inv-16′). The single host-disruptive op is K.W1, and it carries the rollback floor by construction.

## §10 — Successor

K-deploy's successor is **L-webmcp** (the renamed WebMCP booking, chain letter L after K):
- if the WebMCP tool surface graduates from Early-Preview (Chromium 146 ships stable), **L-webmcp** wraps the agent-legible remix/diff/publish endpoints (which J authored agent-legibly for exactly this) in `registerTool` calls — a thin wave, hard-gated on the browser release. Renamed from the stale "K" booking in `J.md §10` so the infra chronic (K-deploy) and the feature graduation (L-webmcp) are never conflated (the rename's whole purpose — chain honesty);
- J's own close is downstream of K-deploy's K.W1 bump (the dock-VT unblock; the §0 interleaving, RESOLVED-ON-BUMP since the fix is already shipped) — so J reaches its green-run close *after* K.W1 lands, not as a K-deploy successor but as the interleaved peer;
- the observability-floor discipline (if K.W4 authors inv-31) carries forward as a constellation-wide deploy-template precept (the §5 backport makes it inheritable by every repo's hook).

The 30-day stale-watch re-triggers at K-deploy close (the I/J-tranche discipline; `ADOPTION-ASKS §3/§4` is the canonical owner of the inherited coordination chronics).

---

## Re-ground (2026-06-04 — the glass-ui-3.2.0 deep audit)

This charter was authored 2026-06-04 from the J post-impl audit on the premise that **glass-ui 3.2.0 was unreleased** and that K.W4 was an adoption wave "gated on a future glass-ui ship." A subsequent 6-dimension deep audit (`docs/audits/runs/2026-06-04-glassui-reground-deep-audit/` — A1 glass-ui-3.2.0 state · A2 value.js J/K/L cohort · A3 fourier plan + this-session changes · A4 deploy+e2e re-ground · A5 prompt/precept · A6 chronic/no-legacy + the SYNTHESIS forward path) re-verified every claim against the SIBLINGS' ACTUAL HEADs and found that premise FALSE. This note records the re-sequence; the wave bodies above are re-grounded to match.

**The load-bearing correction.** glass-ui SHIPPED 3.2.0 (HEAD `06b35d9`, npm `latest=3.2.0`, installable today). The dock-VT `useId()` fix shipped **3.1.1**; `asideSide` + `useTextHighlight` (on `/dom`, NOT `/motion-core`) + a11y `inert` shipped **3.2.0**. So the "K.W4 adoption gated on a future release" framing collapses to a one-line `^3.1.0`→`^3.2.0` ADOPT-NOW bump against an already-published peer (inv-16 irrelevant — the upstream lever was already pulled and released). The bump greens fourier CI at the SOURCE (no bridge, no allow-entry, no console-filter), un-`test.fixme`s the 2 a11y keystones, enables DEC-2 controls-LEFT (`asideSide`), and wires `useTextHighlight`.

**The re-sequence (bump-first).** The original K.md order was internally contradictory: **W1 landed a CI-red SHA `9d7c387` that the W2 inv-28 `green_ci_gate` built in the same tranche would refuse.** The bump is the hard PREDECESSOR of CI-green, and CI-green is the deploy gate's pass-condition — so the bump moves to the FRONT. The re-sequenced executable order:

| New wave | Was | What |
|---|---|---|
| **W1 — THE BUMP** | K.W4 | `^3.1.0`→`^3.2.0` ADOPT-NOW; greens CI at source; un-`fixme`s the a11y keystones; sets `asideSide`/`useTextHighlight`; KILLS the console-bridge before birth; cite a GREEN run id (inv-27). UNBLOCKS J.W6, DEC-2, the control-pane lens, AND the inv-28 gate's pass-condition. |
| **W2 — LAND the backlog** | K.W1 | The deploy spine, UNCHANGED — the REAL load-bearing chronic — but now lands a GREEN post-bump SHA behind a real `HEALTHCHECK` + `depends_on: condition: service_healthy` (NOT a blind poll). |
| **W3 — inv-28 API-arm gate** | K.W2 | BUILD the fail-closed `green_ci_gate()`; the green post-bump SHA is the first it lets PASS. |
| **W4 — page-on-rollback** | K.W3 | The three terminal states POST to a constellation-watched sink; backport to `deploy/templates/`. |
| **W5 — operator** | K.W5 | Unchanged; shipped WITH the spine (K.W2–W4). |
| **W6 — cohort PARITY confirm** | K.W6 | Collapsed from "BOOK inv-16′ peers" to a fourier-local `/diff` + publish envelope parity probe — the `valuejs-J-atomdiff` + `valuejs-J-publish` peers are DONE-in-value.js-L (CLOSED `66dcd68`; `crud-list.ts:114` public filter shipped). |
| **A-1 / A-2** | (was in the K.W4 bundle) | BOOK-with-kill-date = the glass-ui AT/3.3.0 release (glass-ui SELF-BOOKED, `AS/FINAL.md:146-155`). Genuinely ABSENT at 3.2.0; SPLIT OUT of the bump. |
| **W7 — dispatch.sh retirement** | K.W7 | Unchanged, gated. |

**The dissolved gates.** The dock-VT row in `ADOPTION-ASKS §7` flips OPEN→SATISFIED-UPSTREAM; the J↔K "circular #1 risk" (§0) is RESOLVED-ON-BUMP with zero external dependency; the W6 console-filter bridge is KILLED-BEFORE-BIRTH; the value.js-J peers flip OPEN→DONE-in-sibling. **P5 inner-rounding is STRUCK as KILLED-AS-PHANTOM** (was K.md:51/60/78) — the container owns the radius BY DESIGN (`779fed7` reverted the per-section radius; glass-ui's `AS/FINAL.md:113-118` calls fourier's ledger a "misdiagnosis"); never re-booked. **A-1 + A-2 survive as genuinely-ABSENT BOOK-with-kill-date asks** (glass-ui AT/3.3.0). The deploy spine (W2–W4) is the REAL load-bearing chronic and is glass-ui-independent — the bump only removes the CI-red co-blocker; the cold-start `HEALTHCHECK` structural fix is still mandatory.

**Honest scoping.** AUTHORED + re-grounded only — nothing executed. The bump is the FIRST executable move; it is NOT yet done (`web/node_modules/@mkbabb/glass-ui` is still `3.1.0`; the working tree has not been reinstalled). The audit run dir is the binding provenance.

End of K.md.
