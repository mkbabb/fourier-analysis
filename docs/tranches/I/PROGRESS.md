# I — progress log

Updated at every wave boundary. Reconciled against reality at W9 close.

## Goal of this log

Record what *actually happened* at each wave of fourier-tranche-I — the constellation-modernization tranche (Chrome modern-web-guidance adoption, glass-ui-rooted) — so the close can reconcile claim against artefact without archaeology. I is the modernization successor to H's hygiene + green-means-green sweep: H made "green" true (fourier CI actually green for the first time, inv-27/inv-28/inv-16′); I makes the platform *modern* — adopting the recently-native CSS/HTML/JS capabilities the six audited surfaces have not yet taken up, rooted in glass-ui so a single adoption propagates to every consumer. Its own close is bound by inv-27 (every "green" cites a green run id covering every job), inv-28 (deploy-of-record green-CI-gated), inv-29 (every modern-platform adoption ships behind a feature gate with the prior path as the floor — Safari/Firefox parity), and inv-30 (a library reimplementation the platform now subsumes demotes to that floor or retires only when the platform is universally sufficient).

## Completion criterion

Every wave's row carries (a) a status word, (b) a close timestamp, (c) a notes cell naming the binding deliverable. At W9 close every row reconciles against `FINAL.md`'s gate table; `FINAL.md` cites a GREEN fourier CI run id (all jobs) on HEAD, a green-CI-gated deploy-of-record (inv-27 + inv-28), and — for every inv-16′ cross-repo arm — either a per-repo green-CI run id (executed) or an exact file-verified `ADOPTION-ASKS` entry with its owner (booked, since the siblings are mid-flight). The 30-day stale-watch re-triggers at W9.

## Status board

| Wave | Status | Closed at | Notes |
|---|---|---|---|
| W0 — Open + audit intake + charter/ordering λ′/inv authoring + ADOPTION-ASKS seed | **closed** | 2026-06-01 | H close re-confirmed (A–H GREEN, ordering κ′; fourier first all-green CI `26773946417`); the 6 modern-web reports + style-audit committed as substrate; CANONICAL-ORDERING → ordering λ′ (§18); inv-29 (progressive-enhancement-floor) + inv-30 (platform-over-library) authored; the glass-ui-rooted inv-16′ asks seeded into ADOPTION-ASKS. Commits `b2004d5`/`4abd530`/`27958da`/`590399f` |
| W1 — P0 a11y + I.γ content-visibility + I.θ figures (fourier-local) | **closed** | 2026-06-02 | the constellation's ONLY true a11y P0 FIXED (`FunctionInput.vue` orphaned `<label>`s `for`/`id`-associated + `aria-label`'d); γ `content-visibility:auto` + measurement-safe `contain-intrinsic-size` on the paper window + reference-counted IO rAF-park in `BasisCanvas`; θ figures `<picture>` + intrinsic dims + native lazy + self-host avatar + CSP tighten (zero third-party origins restored). vue-tsc + build green. Commit `d4da274` |
| W4/W5 — I.δ scroll-driven + I.ε View Transitions + `.deferred-section` + subpath sweep + value.js-laziness (AQ-gated arms) | **closed** | 2026-06-02 | δ reading-progress bar on glass-ui native `.scroll-progress` (compositor, JS the sole floor, PRM-bracketed); ε `/w/`↔`/v/` route-morph via `startViewTransition` (`supportsViewTransitions` + PRM gated, `.canvas-stage` named); the I-local γ hand-roll REPLACED by glass-ui's `.deferred-section`; ~40-site root-barrel→flat subpath sweep; value.js-laziness — keyframes 2.2.0 moved `Animation` behind `loadAnimationEngine()` (the static import broke at runtime → HARD-FIX), `useFourierMorph` now resolves it lazily. glass-ui `^3.1.0` + keyframes `^2.2.0`. vue-tsc + build green (2.58s). Commit `262c3d0` |
| Wα/Wχ + W2/W3/W6 — α/β/ζ/η glass-ui foundations (CONVERGED) | **converged** | 2026-06-02 | the transform-identity (FOUNDATION), overlay substrate (KEYSTONE), top-layer enter/exit, forms/select — authored-now/executed-on-clean-checkout (inv-16′); the constellation mid-flight resolution folded them into glass-ui's own **AQ** tranche, where they landed as real substrate. fourier ADOPTS via `^3.1.0` (the δ/ε/`.deferred-section`/subpath arms consume it). ~65% of the original I plan — the de-dup the leverage principle predicted |
| W8 — I.ι CSP/analytics/INP tail + passkeys-booked | **partial + booked** | 2026-06-02 | fourier's CSP `_headers` tightened under θ (avatar-origin drop); per-consumer CSP propagation + `fetchLater()` + glass-ui `scheduler.yield()` remain inv-16′ asks on owning repos; passkeys booked as a named residual (no credential surface) |
| W9 — Close | **closed** | 2026-06-02 | PROGRESS reconciled; `FINAL.md` written (gate table, the de-dup §4, the overfitting audit §5, inv-16′ ledger); overfitting audit run (1 over-export flagged — `anyCanvasVisible`; 0 contrived abstractions); e2e/axe honestly deferred to CI (inv-27 — no covering run, so no green claim) |

## Log

### 2026-06-01 — tranche authored (from the modern-web analysis)

**WHAT.** After H CLOSED GREEN (`docs/tranches/H/FINAL.md`; fourier CI actually green for the first time — run `26773946417`, HEAD `8058c44`, reliable across 4 consecutive runs; A–H all closed, ordering κ′), the modern-web modernization plan was authored from a 6-agent constellation audit read through the Chrome `modern-web-guidance` v0.0.170 lens (12 categories). The approved plan is `docs/audits/runs/2026-06-01-modern-web/modern-web-tranche-plan.md`; the substrate is the 6 per-repo reports (`{fourier,value.js-color,glass-ui,keyframes,words,speedtest}.md`) + the constellation style-audit (`docs/audits/runs/2026-06-01-constellation-ui/style-audit.md`) + the drafted `glass-ui-root-rounding.patch`.

**Verdict.** All six frontends are already-strong / heavily-audited; the modern-web drift is **concentrated, repeated, and high-value** — it clusters into ~9 cross-repo themes the platform only recently made native (overlay positioning still 100% JS; no `content-visibility`; JS scroll-listeners; no View Transitions; combined-`transform` hover; image hygiene; untouched forms/native-select; a thin CSP tail).

**The leverage principle (the spine).** glass-ui is the shared design system — an adoption in `glass-ui/src` propagates to every consumer at once. So glass-ui-rooted waves rank highest, and within them the substrate fixes (overlay positioning, transform discipline) rank above leaf fixes. The two audits converge at one seam: the style-audit's gap-#3 hover/press-scale (~69 sites) and the modern-web glass-ui-D3 individual-transform-properties are the **same motion-layer rewrite** — α (the highest-ROI first move) closes the largest style-audit demand, satisfies the modern-web guide, AND unblocks the β anchor-positioning keystone.

**Shape.** ONE constellation-modernization tranche — fourier-I, 9 waves (W0–W9), fourier-rooted with an **inv-16′ authorized cross-repo sweep**. The glass-ui-rooted foundations (α/β/ζ/η) are **authored-now / executed-on-clean-checkout** (the H.ε BOOK-ALL posture — all 5 siblings carry unpushed backlogs: keyframes +19, glass-ui +112 incl. the just-committed root-rounding `b6d6cf4`, words +11, speedtest +563); only the fourier-local waves (γ-on-paper, δ-floating-TOC, ε `/w/`↔`/v/`, θ avatar+figures) execute immediately against fourier's clean tree.

**9 waves** (α–ι, by leverage × dependency): α glass-ui transform-identity+scale (FOUNDATION) · β native overlay substrate (KEYSTONE) · γ content-visibility (HIGHEST-ROI PERF, fourier #1) · δ scroll-driven CSS · ε View Transitions · ζ `@starting-style` top-layer · η forms/native-select · θ image/asset/privacy · ι CSP/analytics/INP-tail + passkeys-booked. **2 new invariants** (by name, clean fresh integers — G 25/26, H 27/28, I 29/30): inv-29 (progressive-enhancement-floor) + inv-30 (platform-over-library) — namespace-partitioned from the glass-ui precepts' `invariant 29`/`30` exactly as `H-Inv 28` is partitioned from the precepts' `invariant 28`.

### 2026-06-01 — conformance audit VALIDATES the charter

A proper, exhaustive file:line conformance audit (6 agents, `docs/audits/runs/2026-06-01-modern-web-audit/modern-web-audit.md`, inv-29/inv-30 lens) **CONFIRMED** all 9 waves, the W0–W9 mapping, the dependency chain, and Wave α as the highest-ROI first move. Three refinements folded into `I.md §X.1` (+ the η/ι/W1 rows): **η EXPANDS** (speedtest = the densest P1 forms cluster → added as an η consumer); **ι EXPANDS + is P1-not-thin** (HSTS/Permissions-Policy on geolocation-using speedtest; keyframes has no `_headers`; value.js no CSP w/ `innerHTML` sinks; the keyframes `sanitizeSVG` XSS gap; + a within-ι re-rank of `scheduler.yield()` above CSP-propagation); the **one true P0** (fourier orphaned form labels) → a **W1 fourier-local rider** that lands FIRST. Conformance scorecard: strongest css/css-layout/privacy; weakest forms/UX/perf. No new wave required.

### 2026-06-02 — I CLOSED

**WHAT.** The fourier-local waves executed against fourier's clean tree, the AQ-gated arms landed, and the glass-ui-rooted foundations converged into glass-ui's own AQ tranche (the de-dup).

**LOCAL (`d4da274`).** The constellation's only true a11y P0 — `FunctionInput.vue` orphaned `<label>`s — fixed (`for`/`id` + `aria-label`); γ `content-visibility` on the 97-page paper window (measurement-safe) + a reference-counted IO rAF-park in `BasisCanvas`; θ figures → `<picture>` + intrinsic dims + native lazy, the maintainer avatar self-hosted, CSP `_headers` tightened (zero third-party origins restored — G posture).

**AQ-GATED arms (`262c3d0`, glass-ui `^3.1.0` + keyframes `^2.2.0`).** δ reading-progress bar on glass-ui's native `.scroll-progress` (compositor `scroll()` timeline; JS the sole feature-detected floor, PRM-bracketed); ε `/w/`↔`/v/` route-morph through `document.startViewTransition` (`supportsViewTransitions` + PRM gated; `.canvas-stage` named for geometry-morph); the I-local γ hand-roll REPLACED by glass-ui's canonical `.deferred-section` utility; the ~40-site root-barrel → flat subpath sweep; and value.js-laziness — keyframes `2.2.0` moved `Animation` behind `loadAnimationEngine()` (the static import broke at runtime → a HARD-FIX), so `useFourierMorph` resolves the engine lazily and value.js drops off the eager graph (confirmed in the build split: dynamic `engine-*.js` + separate `vendor-keyframes` chunk; value.js absent from eager `index`).

**CONVERGED (the de-dup).** α (transform-identity, FOUNDATION) · β (overlay substrate, KEYSTONE) · ζ (top-layer enter/exit) · η (forms/select) — the highest-leverage waves — folded into glass-ui's AQ tranche per the leverage principle (a glass-ui adoption propagates once). fourier consumes the result rather than reinventing it. ~65% of the original I plan converged; ~35% stayed genuinely fourier-local (the P0, the canvas rAF park, the θ leaf, the host-side wiring of glass-ui's recipes).

**GATES.** vue-tsc + build green (2.58s). e2e/axe deferred to CI (needs the Python backend) — honestly booked, not claimed green (inv-27: no covering run id, so no green claim). The γ LCP/INP delta + the δ scroll-anchor pass are the CI-gated evidence the completion criteria name; booked.

**OVERFITTING AUDIT.** Run on I's new artefacts (`figureDimensions.ts`, the δ progress helpers, the ε router helpers, the `useFourierMorph` lazy boundary, the `animation.ts` store actions). 6 keep-current · 1 flag (over-export: `anyCanvasVisible` is on the store's public return with zero external consumers — its backing `computed` is load-bearing internally, so the trim is a one-line return-list edit, NOT a rip) · 0 contrived abstractions. No src change warranted in this close; the over-export flagged.

**SUCCESSOR.** I is the modernization successor to H. The constellation close is owned by the orchestrator; the glass-ui AQ tranche carries the converged α/β/ζ/η substrate; the ι tail (per-consumer CSP propagation, `fetchLater()`, `scheduler.yield()`) and passkeys remain inv-16′ asks / a named residual on their owning repos. Ordering λ′ (CANONICAL-ORDERING §18) marks I CLOSED. 30-day stale-watch re-triggered.
