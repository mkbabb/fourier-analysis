# C.W6 — close (scaffold)

**Wave**: W6 (close; 1 agent — team-lead). **Status**: scaffold — authored at Wχ harden, *filled at the actual close* once W1–W5 land. This file is the close-ceremony checklist; the narrative close record is `FINAL.md` (authored at close, mirroring `A/FINAL.md` + `B/FINAL.md` §0→§9 structure).

## Goal criterion

C closes honestly: every wave's hard gates are met or carry a named successor; `PROGRESS.md` reconciles claim against artefact without archaeology; `FINAL.md` cites every commit + gate; the conditional δ disposition and the inverted cross-repo edge are recorded.

## Completion criterion (the close checklist — each line is a gate)

### Reconcile
- [ ] `PROGRESS.md` status board: every W0–W5 row carries a status word + close timestamp + binding-deliverable note, reconciled against the tree (not against claims).
- [ ] `FINAL.md` authored — §0 paired criteria → §9 reflection (mirror `B/FINAL.md`); every gate in `C.md §6` cited with the commit that discharged it.

### Thread α (infra) — gates discharged or residual-named
- [ ] **W1**: `scripts/deploy.sh` does not exist (`test ! -f`); `git grep -nE '[:/]8091' -- ':!docs/*' ':!*.lock' ':!**/*.json'` → zero; `scripts/deploy-hook.sh` tracked + reviewed; `docs/precepts/infra/` deploy note present. **Host residual** (the shared `/opt/deploy/` dispatcher wiring) recorded honestly — "repo-local landed, host-activation pending" if not performed; the deploy-chain + bad-commit-revert transcripts cited iff host wiring done.
- [ ] **W2**: `docker-compose.prod.yml` source has zero `tlsAllowInvalid*` + zero `--tlsAllowConnectionsWithoutCertificates` (all 3 sites); `.env.example:21` reconciled (4th site + host spelling); `docs/precepts/infra/tls.md` records the issuer (`CN=fourier-internal-ca`); the live `ping`-with-CA verification cited; `database.py` unchanged (`tz_aware=True` preserved). Dev-parity residual (dev-local plaintext-on-bridge) cited as accepted.
- [ ] **W3**: 11 janitor destructive ops each emit an `admin_audit` row (`janitor:<sweep>`); `test_janitor_audit.py` green (5 tests); the `--reload-dir src` drop landed (`dev.sh:76` + `api/Dockerfile:16`); prod confirmed untouched; the background-queue deferral to fourier-D recorded.

### Thread β (storage) — W5 gates
- [ ] **W5**: `grep 'Binary(content)' api/services/image_storage.py` → zero (deletion proof); no inline-`Binary` thumbnail write-back (C9); the janitor unlinks relocated files (C1, delete-then-assert-file-gone test green); volume ownership established (C2); `_resolve` path-confinement guard (C3); backup/DR note + `external:true` volume (C4); migration count-parity artefact + 10-row byte-identity spot-check recorded; `test_dedup_hit_on_migrated_doc` green; the §8 window confirmed struck (no suspended gate).

### Thread γ (residual discharge) — W4 gates
- [ ] **W4**: the strengthened gate green — G1 (`snapshot_hash`/`snapshotHash` → zero on identity paths), G2 (`as unknown as` → zero), G3 (no alias), G4 (no `as any`/`@ts-expect-error` swap), G5 (`FlaggedListResponse` → zero — dead-duplicates `listFlaggedEntries` + `dismissFlags` deleted), G6 (cursor-envelope shape), T1 (`vue-tsc -b --force` green WITH cast removed — keystone), T2 (`npm run build`); all 14 conformance skeletons FILLED (zero `test_placeholder` bodies); `CONFORMANCE-MATRIX.md` cites only methods + files that exist (the method-mismatch + the 6 phantom files struck); the e2e axe-keystone settle-wait landed.

### Thread δ (conditional) — disposition recorded
- [ ] The `sampleToSVGPath` consume disposition recorded: **named residual** (value.js v0.10.0 does not export it — verified at W4 harden) unless value.js published during C's window. `coordination/COLOUR-LIFT.md` reconciled.

### Coordination + tests
- [ ] `coordination/` updated: the conditional δ disposition; the inverted cross-repo edge; the W1 shared-dispatcher host residual (`coordination/` deploy-reconcile note).
- [ ] `uv run pytest` green; `vue-tsc -b --force` green; `npm run build` succeeds (the tranche-level close gates, `C.md §6`).
- [ ] `CANONICAL-ORDERING.md` reconciled to post-C-close (ordering δ): fourier-A/B/C closed; value.js-H closed, I-open.
- [ ] Brittleness window §8 — confirmed struck (it was removed at Wχ; W6 records the final disposition, no restoration needed).

### Residuals ledger (each carries a named destination — P-Inv 28)
- [ ] The W1 host-dispatcher wiring (host-ops, possibly constellation-level).
- [ ] The `--reload` background-queue → fourier-D (trigger: compute outliving a request).
- [ ] The full `Palette`/`colorScale` colour-domain model → a value.js tranche + a fourier successor consumer (held latent).
- [ ] Multi-replica fourier deployment → a fourier-D if ever needed (invariant 19).
- [ ] Any δ-consume not fired → a fourier successor.

## Out of scope at close
The close ceremony cannot run while any brittleness window is open — §8's window is struck, so this is satisfied at entry. No new feature work; W6 is reconciliation + record only.
