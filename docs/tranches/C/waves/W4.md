# C.W4 — slug-identity completeness + B-residual discharge (thread γ + conditional δ)

**Name**: W4 — slug-identity completeness + B-residual discharge (thread γ; + conditional thread δ colour-consume).
**Thread**: γ (B-residual discharge — NO legacy code) + conditional δ (the `sampleToSVGPath` consume).
**Opens after**: Wχ close (the strengthened invariant-20 gate G1–G6 + T1 + the C-gate is bound; `audit/challenge-P4.md` ACCEPTED-WITH-STRENGTHENING; `C.md §3`/§6 reconciled to 14 conformance files). **W4 is INDEPENDENT of W1/W2/W3/W5 at file bounds** (`web/src/**` + `e2e/**` + `api/tests/conformance/**` vs `api/services/**` + `api/routers/**` + `docker-compose*.yml`) — it may run fully parallel with the infra/storage waves subject to the 10-agent ceiling.
**Agents**: 2–3 parallel (A — frontend rename + FlaggedListResponse reconcile + dead-duplicate deletion; B — conformance fill + matrix reconcile; C [optional, foldable into A] — e2e axe settle-wait). The δ-consume is a sub-task of A but reduces to a PROGRESS named-residual entry under the verified residual branch (§7).
**Status**: provisional (hardened from `C.md §3` W4 row + invariant 20 carrying G1–G6 + T1; `audit/challenge-P4.md`; `audit/challenge.md §4`).

---

## Goal criterion (paired)

This wave **succeeds** if the slug-identity convergence the B landing left stranded at the frontend DTO band is completed **at the type root** — the legacy `snapshot_hash` name (and its `snapshotHash` camel route-param variant) is gone end-to-end, renamed to `slug`/`owner_slug` matching the wire the backend already emits; the `as unknown as` masking cast at `AdminFlaggedPanel.vue:56` is removed; the stale `FlaggedListResponse` is reshaped to the cursor envelope `{items, next_cursor, has_more}` (or deleted) and its dead-duplicate keep-alive (`listFlaggedEntries`, `api.ts:691`) is deleted so the symbol vanishes from `web/src`; **all 14** `api/tests/conformance/*.py` skeletons are FILLED (zero retire) and `CONFORMANCE-MATRIX.md` cites only test methods *and files* that exist; the e2e axe-keystone settle-wait is replaced by a deterministic state-wait; and the strengthened binding gate (G1–G6 + T1 + T2 + the C-gate, §6) is green.

This wave **completion criterion** is the §6 hard-gate ledger reading green in full — most decisively **T1** (`vue-tsc -b --force` green *with the cast removed* — the keystone that proves the reshape is truthful, not masked) and **G5** (`git grep -n FlaggedListResponse web/src` → zero). The wave **fails** if the rename happens behind a new cast / type-alias (the three cheats P4 catalogued at §3), if any conformance skeleton is un-skipped with a still-empty `test_placeholder()` body, if the matrix cites a non-collecting node-id, or if the δ-consume is silently dropped rather than recorded as a named residual.

---

## §1 — The ROOT (verified from the response-producing code, not model declarations)

The rename target is the backend's *actual emitted field name*, confirmed at Wχ-P4 against the response-producing code:

- **Gallery / visualization band** — `visualizations.py:215-263` (`list_visualizations`) + `gallery.py:43-85` (`list_public_gallery`) build the body from `_public_doc(doc)`; the persisted `Visualization` entity (`api/models/visualization.py:102-103`) has `slug` + `owner_slug` and **no `snapshot_hash` field** (`content_hash` is an explicit non-identity dedup substrate). Envelope: `{items, next_cursor, has_more}`. The frontend already names this correctly at the `api.ts` boundary (`Visualization { slug; owner_slug; … }`); the lie begins one layer up in `gallery.ts:37` (`snapshot_hash: v.slug`).
- **Flagged band** — `admin.py:508` declares **no `response_model`**; it hand-builds `{slug, owner_slug, content_hash, image_slug, tier, created_at, flag_count, flags}` in a `{items, next_cursor, has_more}` envelope. The wire is already correct; only the *declared frontend types* lie.

**Root names: `slug` (identity) + `owner_slug` (flagged-item owner). No backend change is required by this wave** — invariant 20 is `web/src`-scoped; the runtime payloads are already truthful (`challenge-P4.md §1.3`, `§5`). The dead backend Pydantic `FlaggedListResponse`/`FlaggedEntryInfo` (`admin.py:69-84`) bind to no `response_model` and MAY be deleted for hygiene but are NOT required by this wave's bounds.

---

## §2 — The rename spine (`snapshot_hash` → `slug`; `snapshotHash` → retired)

Live census (run at HEAD `fc5b3b0`; matches W0-baseline §2.1; broader than CA1's 4-site sample): `git grep -nE "snapshot_hash|snapshotHash" web/src` → **44 hits / 14 files**. Decomposed by disposition with the rename sequenced so `vue-tsc` stays green at each step (incremental is viable — see the per-step proof):

### Step R1 — the type roots first (`web/src/lib/types.ts`)

The three legacy names live at the type roots — these are the rename anchors. Renaming the field at the root makes every consumer that reads `.snapshot_hash` a type error, which is the *intended* signal that pins the consumer-edit blast radius:

| Site | Current | Rename to |
|---|---|---|
| `types.ts:89` (`interface Snapshot`) | `snapshot_hash: string` | `slug: string` |
| `types.ts:116` (`interface GalleryEntry`) | `snapshot_hash: string` | `slug: string` |
| `types.ts:192` (`interface FlaggedEntryInfo`) | `snapshot_hash: string` | (folded into the reconcile — §3; `FlaggedEntryInfo`'s `user_slug:196` → `owner_slug` to match the wire) |

After R1, `vue-tsc` reports errors at every `.snapshot_hash` read — the §2 component-consumer list is exactly the error set; resolve them in R2/R3 in the same agent-A batch.

### Step R2 — the store mirror sites (`web/src/stores/{gallery,workspace}.ts`)

The mirror is where the slug is shoved into the legacy slot. **Delete the mirror — the slug flows under its own name**:

- `gallery.ts:37` `snapshot_hash: v.slug` → **delete the mirror line**; `toGalleryEntry` projects `slug: v.slug` (the field is now named `slug` on `GalleryEntry`).
- `gallery.ts:29` `return e.snapshot_hash` (the read getter) → `return e.slug`.
- `workspace.ts:33` `snapshot_hash: string` (the `SavedVisualizationRef`-shaped interface field) → `slug: string`.
- `workspace.ts:364` `return { slug: data.slug, snapshot_hash: data.slug }` → `return { slug: data.slug }` (the dual-name return collapses to one).

### Step R3 — the ~20 component consumers (read `.snapshot_hash` as key/id)

These are the read sites the R1 root rename turns into type errors; flip each `.snapshot_hash` → `.slug` (the binding is identical; the value was always the slug):

`GalleryView.vue:73,117,118,119,139,373`; `VisualizationView.vue:112` (`snapshot.snapshot_hash` → `snapshot.slug`); `GalleryCard.vue:32,82,129,150,159,168`; `GalleryCardModal.vue:124,173,183`; `GalleryFeaturedCarousel.vue:29`; `GalleryGrid.vue:37`; `GalleryInfiniteGrid.vue:32,36`; `GalleryMarquee.vue:37,53`.

### Step R4 — the camel route-param variant (`useWorkspaceLoader.ts`)

The router **already** retired the `:snapshotHash` route segment (`router/index.ts:35` is `path: "/w/:imageSlug?"` — no `:snapshotHash`). But `useWorkspaceLoader.ts:24-37` still *reads* `route.params.snapshotHash` (`:24`, `:25`, `:26`, `:37`) — a **dead read against a route param that no longer exists** (always `undefined`). Retire the dead read:

- `useWorkspaceLoader.ts:24` `const snapshotHash = route.params.snapshotHash …` → delete (the param is gone from the route).
- `:25-26` the `if (imageSlug && snapshotHash) { … store.loadSnapshot(imageSlug, snapshotHash) }` branch → collapse to the single-slug load path (the store's `loadSnapshot(_imageSlug, vizSlug)` at `workspace.ts:240` already takes the viz slug as its load key; the camel param was a no-op feeding it).
- `:37` the `() => [route.params.imageSlug, route.params.snapshotHash]` watch dependency → drop `route.params.snapshotHash`.

### Step R5 — tidy the benign comment mentions (so G1/G3 are clean)

These are retirement-history comments, not identity-bearing — but G3 (the strongest form) wants ZERO `snapshot_hash` in `web/src`, so tidy them: `api.ts:24,405`; `gallery.ts:10,17,162,240`; `workspace.ts:26,27,239,346`; `router/index.ts:26`. Rewrite each to reference the converged `slug` identity without the legacy token (or strike the now-obsolete migration-narration line).

**Incremental viability**: R1 → R2 → R3 in one agent-A batch keeps the type-check honest (the type errors *are* the consumer worklist); R4 + R5 are independent tidies. If a partial-rename intermediate is awkward, the whole spine is small enough (44 hits / 14 files) for a single big-bang reshape commit — either is acceptable; the gate measures the end state, not the path.

---

## §3 — The FlaggedListResponse reconciliation (frontend-only)

**The stale type is genuinely wrong-shaped, not merely mis-named** (`challenge-P4.md §5`):
- `types.ts:201-206` `FlaggedListResponse { items: FlaggedEntryInfo[]; total; page; pages }` — the **retired offset shape**; `FlaggedEntryInfo` (`types.ts:191-199`) carries `snapshot_hash:192` + `user_slug:196` (pre-converged names).
- The runtime payload (`admin.py:574-587`) is the **cursor envelope** `{items, next_cursor, has_more}` with items `{slug, owner_slug, …}`. The declared type and the payload disagree on BOTH the envelope (offset vs cursor) AND the item identity field. This is what the `as unknown as` cast at `AdminFlaggedPanel.vue:56` masks.

### The reconcile (correct end-state)

1. **Reshape `FlaggedListResponse`** (`types.ts:201-206`) to the cursor envelope:
   ```ts
   export interface FlaggedListResponse {
       items: FlaggedVisualization[];
       next_cursor: string | null;
       has_more: boolean;
   }
   ```
   where `FlaggedVisualization` is the canonical lift of `AdminFlaggedPanel.vue:30-38`'s local interface into `types.ts`:
   ```ts
   export interface FlaggedVisualization {
       slug: string;
       flag_count: number;
       flags: FlagInfo[];
       image_slug: string | null;
       owner_slug: string | null;
       tier: string | null;
       created_at: string | null;
   }
   ```
   This subsumes the old `FlaggedEntryInfo` (`types.ts:191-199`) — delete `FlaggedEntryInfo` (its `snapshot_hash`/`user_slug` names are the §2 R1 anchor for the flagged band; it does not survive the rename, it is *replaced*).

2. **Remove the cast** at `AdminFlaggedPanel.vue:56` — the `(await api.listFlaggedVisualizations(...)) as unknown as { items; next_cursor; has_more }` collapses to a direct assignment once `listFlaggedVisualizations`'s declared return type (`api.ts:577`, `:582`) is the reshaped `FlaggedListResponse` that now matches the payload. The local `FlaggedVisualization` interface (`AdminFlaggedPanel.vue:30-38`) is deleted in favour of the imported canonical one; the comment block at `:18-29` narrating the cast workaround is struck.

3. **DELETE the dead-duplicate `listFlaggedEntries`** (`api.ts:691-702`) — confirmed live: `git grep -n "listFlaggedEntries" web/src` → declared at `api.ts:691`, **consumed nowhere** (the live caller `AdminFlaggedPanel.vue:53` invokes only `listFlaggedVisualizations`). It is a dead re-export that keeps the stale `FlaggedListResponse` symbol alive past the reconcile (P4 §3 cheat #3). **This deletion is what makes G5 pass.**

4. **Foreclose the sibling dead-duplicate** (a W4-harden finding beyond P4): `dismissFlags` (`api.ts:704-712`) is ALSO consumed nowhere (`git grep -n "dismissFlags" web/src` → declared only; the live caller `AdminFlaggedPanel.vue:124` invokes `dismissVisualizationFlags`, `api.ts:589`). It is a sibling dead duplicate to `listFlaggedEntries` in the same `// ── Admin: flagged content ──` block (`api.ts:683-712`). **Delete it in the same sweep** — it does not touch `FlaggedListResponse` so it is not gate-binding for G5, but leaving it is exactly the kind of dead-duplicate residue this wave exists to discharge.

5. **Drop the now-unused import** — once the cast is gone and `listFlaggedEntries` deleted, the `FlaggedListResponse` import at `api.ts:13` either stays (re-pointed at the reshaped type used by `listFlaggedVisualizations`) or, if the symbol is deleted entirely, the import drops. G5 requires zero `FlaggedListResponse` *occurrences* in `web/src`; the simplest compliant end-state is to **rename the symbol** (e.g. to `FlaggedListResponse` → reuse the generic cursor-envelope already present, or inline the shape) — but the harden mandate's recommended form is: reshape the symbol, keep one live wrapper (`listFlaggedVisualizations`) returning it, and delete the dead duplicate. **G5's literal requirement (`FlaggedListResponse` → zero in `web/src`) means the symbol name itself must go** — so the end-state is: `listFlaggedVisualizations` returns the cursor envelope typed inline as `{ items: FlaggedVisualization[]; next_cursor: string | null; has_more: boolean }` OR a renamed `FlaggedCursorResponse`, and the `FlaggedListResponse` identifier appears nowhere. Either spelling passes G5; the inline/renamed form is the cleaner kill.

**Backend change required? — NO.** Confirmed at `challenge-P4.md §1.2`/§5: `admin.py:508` already emits the cursor envelope with `slug`/`owner_slug`; this is a frontend-only reconcile.

---

## §4 — The 14 conformance skeletons — all FILL, zero retire

`api/tests/conformance/` holds **14** files (count corrected 15 → 14 at Wχ; `C.md §3`/§6 reconciled). `grep -rl "pytest.mark.skip" api/tests/conformance/` → **14/14**; `grep -rn "def test_placeholder" api/tests/conformance/` → **14**. Each is `pytestmark = pytest.mark.skip(reason="conformance skeleton — implemented at B.W3")` + a lone `test_placeholder()` — the skip reason is a **false claim** (the implementation did not occur). The backing utilities exist (`ls api/lib/crud/` → `cursors.py`, `errors.py`, `etag.py`, `idempotency.py`, `pinned_cron.py`, `slugs.py`, `softdelete.py`, `slug_words.json`); the proxy suites pass (re-run at this harden: `uv run pytest api/tests/test_crud_lib_* api/tests/test_visualization_* -q` → **94 passed, 33 skipped** — the skips are `@requires_mongo`-gated, honest). **There is no abandoned subject — "retire" is dishonest for any of the 14.**

| # | Skeleton | Subject | Backing utility / entity | Green proxy suite | Disposition |
|---|---|---|---|---|---|
| 1 | `test_etag.py` | ETag / If-Match (inv 23) | `api/lib/crud/etag.py` | `test_crud_lib_etag.py` ✓ | **FILL** (proxy green) |
| 2 | `test_idempotency.py` | Idempotency-Key | `api/lib/crud/idempotency.py` | `test_crud_lib_idempotency.py` ✓ | **FILL** (proxy green) |
| 3 | `test_slug_format.py` | slug format/algorithm | `api/lib/crud/slugs.py` + `slug_words.json` | `test_crud_lib_slugs.py` ✓ | **FILL** (proxy green) |
| 4 | `test_pagination.py` | cursor pagination | `api/lib/crud/cursors.py` | `test_crud_lib_cursors.py` ✓ | **FILL** (proxy green) |
| 5 | `test_problem.py` | problem+json (RFC 9457, inv 22) | `api/lib/crud/errors.py` | `test_crud_lib_errors.py` ✓ | **FILL** (proxy green) |
| 6 | `test_soft_delete.py` | soft-delete state machine | `api/lib/crud/softdelete.py` | `test_crud_lib_softdelete.py` + `test_visualization_soft_delete.py` ✓ | **FILL** (proxy green) |
| 7 | `test_identity.py` | single-slug identity | `visualization` entity | `test_visualization_crud.py` ✓ | **FILL** (proxy green) |
| 8 | `test_ownership.py` | owner-bound mutation | entity `owner_slug` | `test_visualization_ownership.py` ✓ | **FILL** (proxy green) |
| 9 | `test_janitor.py` | cron / bounded-prune | `api/lib/crud/pinned_cron.py` | `test_crud_lib_pinned_cron.py` ✓ | **FILL** (proxy green) |
| 10 | `test_visibility.py` | 3-state visibility | entity `visibility` enum | `test_visualization_crud.py` (partial) | **FILL** (live-server seed — note) |
| 11 | `test_admin.py` | admin moderation + audit | `api/routers/admin.py` | none for audit-row | **FILL** (audit-row overlaps W3 janitor-audit — coordinate at the matrix level only; no file overlap) |
| 12 | `test_sessions.py` | sessions register/login/logout | `api/routers/sessions.py` | none | **FILL** (live-server; the C6.3 timing test is heavy — note) |
| 13 | `test_rate_limit.py` | RateLimit headers (RFC 9239, inv 24) | rate-limiter middleware | none | **FILL** |
| 14 | `test_url_shape.py` | URL no-secrets / grep guards | `scripts/conformance/*.sh` | none | **FILL** (subprocess-grep form; cheap) |

**Split: 9 of 14 already have a green proxy suite (#1–9); 5 (#10–14) need implementation** (#10/#12 need a live-server + Mongo seed; #13/#14 are middleware/grep assertions; #11's audit-row assertion overlaps W3's janitor-audit at the *matrix* level — no file overlap, W4 owns the conformance file).

**Two honest FILL sub-paths** (per skeleton; both acceptable):
- **(a) lift the proxy assertions** into the named conformance path (preferred for #1–9 — `test_etag.py` imports + exercises the same `etag.py` cases the proxy proves), so the matrix-cited node-id runs real assertions;
- **(b) re-point the matrix `Run` cell** at the proxy suite that already proves it AND delete the empty skeleton — acceptable ONLY if the matrix cell is rewritten to cite a path that actually collects, never leaving the skeleton cited.

### The `CONFORMANCE-MATRIX.md` honesty reconcile (two distinct lies — both fixed)

`docs/tranches/B/coordination/CONFORMANCE-MATRIX.md` carries **two** distinct citation lies, both bound by the C-gate:

1. **Method-mismatch** (P4 found): rows cite methods that do not exist — e.g. `test_identity.py::test_no_hash_in_url`, `test_identity.py::test_slug_read_shape`, `test_slug_format.py::test_slug_shape`, `test_etag.py::test_concurrency`. The 14 actual files contain **only** `test_placeholder`. Every cited `::test_X` must resolve to a collected node-id post-fill.

2. **Phantom-file citations** (a W4-harden finding beyond P4): the matrix cites **6 conformance files that do not exist in the tree** — `comm -23` of cited-vs-actual yields `test_database.py`, `test_migration.py`, `test_no_shared_framework.py`, `test_rate_limiter.py`, `test_shared_data.py`, `test_slug_collision.py` (matrix rows C2.2, C3.4, C8.3, C9.1, C9.2, C9.3, C11.1, C11.3). These cite a `Run` cell that **can never collect**. Each such row must EITHER (a) gain a real file under the 14-skeleton fill, (b) re-point at an existing proxy/integration suite that proves it, or (c) be struck honestly with its subject's true disposition recorded — never left citing a phantom path.

---

## §5 — The e2e axe-keystone settle-wait (timing-tuning; harness exists)

The harness exists at `web/e2e/visualization-ux.spec.ts` (the path is `web/e2e/`, NOT repo-root `e2e/`): `@axe-core/playwright` is a devDep (`web/package.json:27`, `^4.10.2`); the spec imports `AxeBuilder` and wraps it in a local `checkA11y(page, label)` (`:27-43`, WCAG 2a/2aa/21a/21aa tags, asserts zero `serious`/`critical`); 4 keystone-state assertions fire (`:63` workspace default; `:80` ContourSettings Configurator-open; `:99` ExportModal Dialog-open; `:115` AnimationControls dropdown-open). **The harness is complete — this is timing-tuning only**, the B-residual `C.md §7` named (`e2e axe-keystone settle-wait — timing-tuning only; the harness exists`).

The defect: `openWorkspace` (`:46-55`) settles the keystone state with a **fixed `await page.waitForTimeout(2_000)`** (`:54`, "Let the auto-compute settle so the dock + panels are fully mounted"). A fixed timeout is a flake source — too short on a slow CI box (axe runs against a half-mounted DOM → spurious violations), wasteful on a fast one. **Replace the blind `waitForTimeout(2_000)` with a deterministic state-wait** keyed on the actual settle condition the comment describes (dock + panels mounted): a `page.waitForFunction`/`expect(locator).toBeVisible` on the keystone DOM landmark (e.g. the dock root and a mounted panel control), or `waitForLoadState('networkidle')` after the compute round-trip resolves. The second fixed wait at `:150` (`waitForTimeout(64)` — a single rAF-frame nudge) is benign and may stay.

**Sub-gate**: `npx playwright test e2e/visualization-ux.spec.ts --project=chromium` green at the 3 viewports with the blind 2 s settle removed (replaced by a state-wait); the 4 `checkA11y` keystone assertions hold with zero serious/critical violations and no fixed-timeout settle in `openWorkspace`.

---

## §6 — The binding failure test (verbatim the strengthened gate)

PASS iff ALL of {G1, G2, G3, G4, G5, G6, T1, T2, C-gate} hold. Each is a runnable command. (Source: `audit/challenge.md §4`, `audit/challenge-P4.md §3`, invariant 20.)

```
# G1 — no legacy identity name on web identity paths (necessary, insufficient alone)
git grep -nE "snapshot_hash|snapshotHash" web/src -- ':!*.md' ':(exclude)web/src/**/*.test.ts' \
  | grep -vE '//|/\*|\*'
  → MUST be empty

# G2 — no `as unknown as` masking cast on the converged surface (necessary, insufficient alone)
git grep -n "as unknown as" web/src/components/visualization web/src/lib/api.ts web/src/lib/types.ts
  → MUST be empty

# G3 — anti-type-alias: the legacy concept name is gone from ALL of web/src
git grep -niE "snapshot_hash|snapshotHash|snapshot.hash" web/src
  → strongest form: ZERO (R5 tidies the benign comment mentions); FAIL if any
    new `type`/`interface`/`as` aliases the old slot under a renamed name

# G4 — anti-new-cast: NO cast of ANY spelling masks a type-truth gap on the converged surface
git grep -nE "as any|as unknown|@ts-expect-error|@ts-ignore" \
  web/src/components/visualization web/src/lib/api.ts web/src/lib/types.ts web/src/stores
  → MUST be empty of any cast/suppression on the flagged/gallery/visualization DTO
    boundary. (Pre-existing benign narrowings elsewhere — e.g. the equation-surface
    casts at web/src/lib/equation/api.ts:36,53 — are out of scope; the discriminating
    question per hit: "does this cast bridge a declared API type to a runtime payload
    of a different shape?" Only such a cast is a violation.)

# G5 (DECISIVE) — anti-dead-duplicate: the stale type symbol is gone, killing every re-export
git grep -n "FlaggedListResponse" web/src
  → MUST be ZERO. Forces deleting the dead-duplicate listFlaggedEntries (api.ts:691)
    AND reshaping/renaming the symbol off the offset shape.

# G6 — positive shape assertion: the reconciled flagged type carries the cursor envelope
git grep -nE "total|page|pages" web/src/lib/types.ts | grep -i flag
  → MUST be empty; the flagged-list type exposes {items, next_cursor, has_more} with
    item identity field `slug` (not snapshot_hash) and owner field `owner_slug`.

# T1 (KEYSTONE) — vue-tsc compiles with the cast removed
cd web && npx vue-tsc -b --force        → exit 0, zero errors
  → the load-bearing proof the rename is truthful: the local FlaggedVisualization shape
    cannot assign to the stale {items,total,page,pages} type WITHOUT a cast, so
    green-without-cast forces the FlaggedListResponse reshape, not just cast deletion.

# T2 — build green
cd web && npm run build                 → exit 0

# C-gate — zero placeholder bodies; zero phantom/dangling matrix citations
grep -rl "pytest.mark.skip" api/tests/conformance/   → empty (or only honest @requires_mongo)
grep -rn "def test_placeholder" api/tests/conformance/ → empty
uv run pytest api/tests/conformance/ --collect-only -q
  → every collected node-id matches a CONFORMANCE-MATRIX Run cell; no matrix Run cell
    points at a non-collected node-id (covers BOTH the method-mismatch AND the 6
    phantom-file citations from §4)
uv run pytest api/tests/conformance/ -q
  → green (with live Mongo provisioned; else the @requires_mongo subset skips honestly,
    never the whole file via a blanket skip; no @requires_mongo on a body that would
    assert nothing anyway)
```

**Why G1+G2 alone are insufficient** (three cheats that pass them, foreclosed by G3/G4/G5 + T1): (1) the type-alias cheat — rename the token but alias the legacy concept under a new name (G3 forecloses); (2) the new-cast cheat — swap `as unknown as` for `as any`/`@ts-expect-error`/`satisfies`-misuse (G4 forecloses); (3) the dead-duplicate cheat — fix the panel but leave `listFlaggedEntries` keeping `FlaggedListResponse` alive (G5 forecloses). **T1 binds them together**: you cannot delete the cast AND keep the stale type AND have `vue-tsc` pass — removing the cast *forces* the reshape.

---

## §7 — Conditional thread δ — the `sampleToSVGPath` consume (VERIFIED named-residual branch)

**Disposition: the named-residual branch fires. value.js has NOT published `sampleToSVGPath`** — verified at this harden:
- value.js HEAD is `16129e0` ("Tranche H close (v0.10.0)"); `~/Programming/value.js/package.json:3` → `"version": "0.10.0"`.
- `grep -rn "sampleToSVGPath" ~/Programming/value.js/src/` → **no match** (grep rc=1). Only `cubicBezierToSVG` exists (`~/Programming/value.js/src/math.ts:69`) — the function `COLOUR-LIFT.md §2` asks value.js to *generalise into* `sampleToSVGPath`, not yet done.
- fourier pins `@mkbabb/value.js` at `file:../../value.js` (`web/package.json:18`) — a local-path pin frozen at H-close; no published `@latest` tag to bump to, no mid-wave re-engagement anticipated (value.js-C is RETIRED; `B/waves/W4.md` H-W4-2 "orphan verdict SETTLED").
- fourier's sampler is unchanged and intact: `web/src/lib/easings.ts:89` `generateCurveSVGPath(fn, n=32)`, consumed at `:105`; `easings.ts` imports only `timingFunctions` (`:9`) from value.js, NOT a sampler.

**Therefore W4 lands thread γ ONLY.** The `sampleToSVGPath` consume holds as a **named residual** — recorded in `PROGRESS.md` with destination `fourier-tranche-C-or-successor`, citing `coordination/COLOUR-LIFT.md §4-§5` (the inverted-edge ask: value.js authors `sampleToSVGPath` in `src/math.ts`, fourier-C.W4-δ consumes on the next bump). `easings.ts` is **untouched** (its hand-rolled sampler stays; the file keeps its fourier-specific easing presets per invariant 15). The richer `Palette`/`colorScale` model is NOT built under any branch (CA4 KISS guard; the "library nobody calls" anti-pattern). **The residual is named, never silent** — that is the δ completion criterion under this branch.

*(If — contrary to this verification — value.js publishes `sampleToSVGPath` before W4 dispatch, the δ-consume sub-task of agent A fires instead: `easings.ts:89`'s `generateCurveSVGPath` body swaps onto the imported `sampleToSVGPath`, the internal dup collapses, and the bump records in `web/package.json`. The verification above makes this branch dormant.)*

---

## §8 — File bounds, disjointness, agent units

W4 is **INDEPENDENT of W1/W2/W3/W5 at file bounds** — `web/src/**` + `web/e2e/**` + `api/tests/conformance/**` + `docs/tranches/B/coordination/CONFORMANCE-MATRIX.md` vs the infra/storage waves' `api/services/**` + `api/routers/**` + `docker-compose*.yml`. The single matrix-level adjacency (the `test_admin.py` audit-row, #11, overlaps W3's janitor-audit *subject*) is no file overlap — W4 owns the conformance file; W3 owns `janitor.py`. Runs fully parallel.

| File | Access | Owning unit |
|---|---|---|
| `web/src/lib/types.ts` | modify (R1 type roots; §3 reshape) | A |
| `web/src/stores/gallery.ts` | modify (R2 mirror delete + getter; R5 comments) | A |
| `web/src/stores/workspace.ts` | modify (R2 field + return; R5 comments) | A |
| `web/src/lib/api.ts` | modify (§3 reshape return; DELETE `listFlaggedEntries`:691 + `dismissFlags`:704; R5 comments) | A |
| `web/src/components/visualization/gallery/AdminFlaggedPanel.vue` | modify (remove cast :56; delete local iface :30-38; strike :18-29 comment) | A |
| `web/src/components/visualization/GalleryView.vue`, `VisualizationView.vue`, `gallery/GalleryCard.vue`, `GalleryCardModal.vue`, `GalleryFeaturedCarousel.vue`, `GalleryGrid.vue`, `GalleryInfiniteGrid.vue`, `GalleryMarquee.vue` | modify (R3 `.snapshot_hash` → `.slug`) | A |
| `web/src/components/visualization/composables/useWorkspaceLoader.ts` | modify (R4 dead camel-param read) | A |
| `web/src/router/index.ts` | modify (R5 comment only — the route already retired `:snapshotHash`) | A |
| `api/tests/conformance/*.py` (14 files) | modify (FILL all 14) | B |
| `docs/tranches/B/coordination/CONFORMANCE-MATRIX.md` | modify (method-mismatch + phantom-file reconcile) | B |
| `web/e2e/visualization-ux.spec.ts` | modify (settle-wait state-wait) | C (foldable into A) |
| `PROGRESS.md` | modify (the δ named residual; the close row) | orchestrator |

**Do NOT touch**: `api/services/**`, `api/routers/**` (W1/W3/W5 own; §1 confirms no backend change is required for γ), `web/src/lib/easings.ts` (δ residual branch — untouched), `web/package.json` value.js pin (δ residual branch — no bump), `docker-compose*.yml`.

### Agent units

- **W4.a — frontend rename + FlaggedListResponse reconcile + dead-duplicate kill.** R1–R5 rename spine; the §3 reconcile (reshape/rename `FlaggedListResponse`, remove the cast, delete `listFlaggedEntries` + `dismissFlags`). Sub-gate: G1–G6 + T1 + T2 green; `git grep -n "listFlaggedEntries\|dismissFlags" web/src` → zero.
- **W4.b — conformance fill + matrix reconcile.** FILL all 14 skeletons (lift proxy assertions for #1–9; implement #10–14); reconcile `CONFORMANCE-MATRIX.md` (method-mismatch + 6 phantom-file citations). Sub-gate: the C-gate (zero `test_placeholder`; every collected node-id ↔ a matrix Run cell; no Run cell at a non-collecting path).
- **W4.c — e2e settle-wait** (foldable into A). Replace the blind `waitForTimeout(2_000)` (`visualization-ux.spec.ts:54`) with a deterministic state-wait. Sub-gate: §5.

---

## §9 — Hard-gate ledger (completion criterion)

1. **G1** — `git grep -nE "snapshot_hash|snapshotHash" web/src` (identity-path-filtered, §6) → empty.
2. **G2** — `git grep -n "as unknown as" web/src/components/visualization web/src/lib/api.ts web/src/lib/types.ts` → empty.
3. **G3** — `git grep -niE "snapshot_hash|snapshotHash|snapshot.hash" web/src` → zero (strongest form, R5 tidied); no new alias.
4. **G4** — `git grep -nE "as any|as unknown|@ts-expect-error|@ts-ignore" web/src/components/visualization web/src/lib/{api,types}.ts web/src/stores` → empty on the converged surface.
5. **G5 (decisive)** — `git grep -n "FlaggedListResponse" web/src` → ZERO (`listFlaggedEntries` deleted).
6. **G6** — flagged type carries `{items, next_cursor, has_more}` with item `{slug, owner_slug, …}`; no `total`/`page`/`pages` on a flagged type.
7. **T1 (keystone)** — `cd web && npx vue-tsc -b --force` → exit 0 (WITH the cast removed).
8. **T2** — `cd web && npm run build` → exit 0.
9. **C-gate** — zero `test_placeholder` bodies; zero `pytest.mark.skip` (bar honest `@requires_mongo`); every collected conformance node-id ↔ a matrix `Run` cell; no matrix `Run` cell at a non-collecting path (method-mismatch + the 6 phantom files); `uv run pytest api/tests/conformance/ -q` green (Mongo-provisioned).
10. **e2e settle-wait** — `npx playwright test e2e/visualization-ux.spec.ts --project=chromium` green at 3 viewports with the blind 2 s settle replaced by a state-wait; 4 `checkA11y` keystones zero serious/critical.
11. **δ residual recorded** — `PROGRESS.md` carries the `sampleToSVGPath` named residual (destination `fourier-tranche-C-or-successor`; citing `COLOUR-LIFT.md §4-§5`); `easings.ts` + `web/package.json` value.js pin byte-identical to HEAD.
12. **PROGRESS reconciled** — the W4 row cites the close commit, the conformance fill, the matrix reconcile, and the e2e settle-wait; the δ disposition recorded.

**Invalid gates (rejected — `C.md §6`)**: "`snapshot_hash` removed" by renaming behind a new cast or alias rather than at the type root (G3/G4/T1 foreclose); a conformance test un-skipped with a still-empty `test_placeholder()` body (C-gate forecloses); a matrix row left citing a non-collecting node-id or a phantom file (C-gate forecloses); the δ-consume silently dropped rather than named (gate 11 forecloses).

---

## §10 — Out of scope

- **Any `api/services/**` / `api/routers/**` change** — §1 confirms the wire is already correct (`slug`/`owner_slug`, cursor envelope); invariant 20 is `web/src`-scoped; γ is frontend-only. (Deleting the dead backend Pydantic `FlaggedListResponse`/`FlaggedEntryInfo` at `admin.py:69-84` is OPTIONAL hygiene, NOT required, and would cross into W1/W3/W5's api bounds — defer it or route it to a backend wave.)
- **`web/src/lib/easings.ts` and the `web/package.json` value.js bump** — held under the δ residual branch (§7); untouched.
- **The `Palette`/`colorScale` colour-domain model** — held latent (CA4; invariant 15; "library nobody calls"); not built under any branch.
- **The infra/storage threads (α/β)** — W1 (CI/CD), W2 (TLS), W3 (janitor/`--reload`), W5 (image-blob migration) own their surfaces; W4 shares no files and runs parallel.
- **The equation-surface casts** (`web/src/lib/equation/api.ts:36,53`) — not on the visualization/gallery converged surface; out of invariant-20 scope.

---

## §11 — Summary

The **rename spine** runs root-first: rename `snapshot_hash` → `slug` at the three `types.ts` roots (`:89` `Snapshot`, `:116` `GalleryEntry`, `:192` `FlaggedEntryInfo`), which turns every `.snapshot_hash` read into a type error pinning the consumer blast radius; then delete the store mirror (`gallery.ts:37` `snapshot_hash: v.slug` — the slug flows under its own name — and `gallery.ts:29`, `workspace.ts:33,364`); then flip the ~20 component consumers `.snapshot_hash` → `.slug`; then retire the dead `snapshotHash` camel route-param read in `useWorkspaceLoader.ts:24-37` (the route already dropped the segment); then tidy the ~10 benign comment mentions so G3 is clean. The **FlaggedListResponse + dead-duplicate disposition**: reshape the stale `{items,total,page,pages}` offset type to the cursor envelope `{items, next_cursor, has_more}` with `FlaggedVisualization {slug, owner_slug, …}` items (lifting the panel's local interface to canonical), remove the `as unknown as` cast at `AdminFlaggedPanel.vue:56`, DELETE the dead-duplicate `listFlaggedEntries` (`api.ts:691`) — and its sibling dead duplicate `dismissFlags` (`api.ts:704`, a W4-harden finding) — so `git grep FlaggedListResponse web/src` → zero (G5); frontend-only, no backend change. The **14-fill split**: all 14 conformance skeletons FILL (zero retire — every subject has a landed `api/lib/crud/` utility or the `visualization` entity), 9 of 14 already proven by a green proxy suite (re-verified 94 passed / 33 honest-skip), 5 need implementation; the matrix reconcile fixes BOTH the method-mismatch P4 found AND 6 phantom-file citations a W4-harden grep surfaced. The **δ branch**: VERIFIED named-residual — value.js v0.10.0 (`16129e0`) does not export `sampleToSVGPath` (only `cubicBezierToSVG`, `math.ts:69`), fourier pins `file:../../value.js` frozen at H-close, so the consume holds as a `PROGRESS.md` named residual (destination `fourier-tranche-C-or-successor`) and `easings.ts` stays untouched. The keystone gate is **T1** (`vue-tsc -b --force` green with the cast removed) bound with **G5** (the symbol-kill) — together they make the three P4 cheats mutually exclusive with passing.
