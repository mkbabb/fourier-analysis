# G.W0 — Open + audit intake + cheapest γ excision

**Status**: CLOSED
**Thread**: — (open)
**Agents**: 0 (direct — befitting one-line excision; audit substrate pre-committed)
**Predecessor**: F closed `d34d21b`; G authored `a2e05ea`.

## Intent

Open tranche G. Re-confirm the F baseline survives falsification (already done — GA3 re-derived all 13 §6 gates; recorded in `docs/audits/runs/2026-05-29-G-audit/`). Commit the 6-lane G-audit as the binding baseline (already at `a2e05ea`). Land the cheapest, **dependency-free** γ excision.

## What landed

1. **`compute_cache.py:105` — naive `datetime.utcnow()` → `datetime.now(tz=UTC)`** (+ `from datetime import UTC, datetime`). The `compute_cache` TTL index runs under a tz-aware Mongo client; a naive UTC stamp is the GA5 T7 legacy item. Pure, independent, no downstream entanglement. **DONE.**

## Team-lead schedule refinement (recorded; honest)

The charter's provisional W0 also listed *"the misleading `rate_limiter.py:143` comment"* as cheapest excision. On inspection that comment (`:139–152`) is the **rationale block for `read_limiter=1200`** — the exact global-headroom workaround that **β.2 (W3) retires wholesale** when it converges the middleware onto `get_client_ip` + nginx `real_ip`. The middleware at `:227` uses raw `request.client.host` + `hash_ip`; the comment names `get_client_ip` (which the code never calls) — so the comment is misleading *because* the convergence hasn't happened yet.

Correcting the comment at W0 and then rewriting the whole block at W3 is churn — a "quick solution" the mandate forbids. **Decision: the misleading-comment correction is folded into W3 (β.2)**, where the block is rewritten honestly as a side-effect of the real convergence. This is the gestalt sequencing; W0 lands only the truly-independent `datetime` item. Recorded in `PROGRESS.md`.

## Gate

- `compute_cache.py` parses; `from datetime import UTC` valid (py3.11+). ✓
- No behavioural change beyond tz-awareness of the stored `created_at`. ✓
- Baseline audit substrate committed (`a2e05ea`). ✓

## Next

Wα — two READ-ONLY research lanes (frontend-deploy path; nginx `real_ip` chain) governing α + β.
