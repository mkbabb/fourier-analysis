# G.W4 — NO-legacy excision (thread γ, inv-20/inv-15)

**Status**: CLOSED
**Thread**: γ
**Agents**: 1 (implementation agent; team-lead verified + committed)

## Excised (each grep-proven zero-consumer before removal)
1. **Dead `like_limiter` + `"/like"` arm** (`api/services/rate_limiter.py`) — the like route was removed at B-convergence. Removed the limiter def, the `if "/like" in path` branch, the doc-comment refs, and the `conftest.py` limiter-reset entry. `grep -rn like_limiter api/` → empty.
2. **Six dead `web/src/lib/types.ts` exports** (all 0 importers): `Snapshot`, `ContourData`, `CursorInfo`, `GalleryCursorResponse`, and the **duplicate** `NotationMode` + `EquationTier` (canonical kept in `web/src/lib/equation/types.ts` — every importer reads `@/lib/equation/types`, confirming the `types.ts` copies were dead).
3. **`GalleryEntry` / `toGalleryEntry` vestige** (`web/src/stores/gallery.ts` + `web/src/lib/types.ts`) — the lossy `owner_slug`→`user_slug` projection from the pre-CRUD-CONTRACT separate-gallery entity. **Key finding**: no consumer ever read `.user_slug` off a `GalleryEntry` (every gallery card field — `slug/image_slug/contour_hash/created_at/views/likes/tier/active_bases/n_harmonics` — exists on `Visualization` identically), so the repoint is field-for-field clean. ~8 components + the store repointed `GalleryEntry`→`Visualization`; the dead rename vanished with the mapper. `grep GalleryEntry|toGalleryEntry web/src/` → empty.
4. **Naive `datetime.utcnow()`** (`api/scripts/run_pending_migrations.py`, folded from W3) — found at **4** sites (69/92/108/119), not just :69; all → `datetime.now(tz=UTC)` (+ `from datetime import UTC`). Same γ class as the W0 `compute_cache` fix; this is the one that emitted the DeprecationWarning in the live deploy log.

## Gates (all pass)
- `grep` proofs: `like_limiter`, `GalleryEntry`/`toGalleryEntry`, `datetime.utcnow` → all empty. ✓
- `npx vue-tsc -b --force` → GREEN (0). ✓
- `npm run build` → ✓.
- `uv run pytest api/tests/` → **132 passed**/83 skipped (rate-limit conformance stayed green after `like_limiter` removal). ✓

## Not removed (still consumed — correctly left)
`AdminUserInfo.user_slug` (admin user-management entity), `SessionResponse`/`UserInfo.user_slug` (session/auth identity), `GalleryTier` (still imported) — all distinct from the gallery vestige, retained.
