# G.W2 — one contract source (thread β.1, T1, inv-26)

**Status**: CLOSED
**Thread**: β.1 (inv-11/inv-26 — the TOP elegance transposition)
**Agents**: 1 (implementation agent; team-lead reviewed + committed)

## The collapse
The api↔web type boundary had THREE sources of truth → now ONE (`web/src/lib/types.ts`, hand-written). Per Wχ P1 (ratified (B) hand-types-canonical): the generated codegen is dead AND structurally incapable (routes return raw `Response`, no `response_model=` → it cannot even emit `Visualization`).

**Deleted (3):**
- `web/src/lib/api-schema.d.ts` (65 KB generated; **zero importers** — the inv-15 "library nobody calls").
- `web/scripts/gen-types.sh` (the generator; `web/scripts/` now empty).
- `api/openapi.json` (committed snapshot; only consumers were the two above — the `nginx/fourier.conf` `location = /openapi.json` proxies the *live* FastAPI route, unrelated).

**Edited (4):**
- `web/package.json` — removed the `gen-types` script + the `openapi-typescript` devDep.
- `web/package-lock.json` — pruned (`npm install --package-lock-only`; 0 vuln).
- `web/src/lib/types.ts` — the 6 contract types now live here canonically (`Visibility`, `Visualization`, `VisualizationCreate`, `VisualizationPatch`, `VisualizationListResponse`, `WithETag<T>`), with their doc-comments intact; interdependencies (`ContourSettings`/`AnimationSettings`/`GalleryTier`) already in-file.
- `web/src/lib/api.ts` — imports the 6 from `./types` (it uses them in its own signatures) + a single `export type { … }` re-export block, so the ~14 `@/lib/api` consumers resolve UNCHANGED.

## Gates (all pass)
- `grep -rn "api-schema" web/src/` → empty.
- `grep -rn "openapi-typescript\|gen-types" web/ --include=*.json --include=*.sh` → empty.
- No shadow inline decls in `api.ts`; all 6 present in `types.ts` (inv-26 proof).
- `npx vue-tsc -b --force` → **GREEN (0 errors)**.
- `npm run build` → **`✓ built`**.

## Honest residual (booked, out of inv-26's named scope)
A 4th hand-type island remains: `web/src/lib/equation/types.ts` (`FourierTermDTO`, `ComputeEquationResponse`, …; 10 importers). inv-26 targeted the 3 named api↔web sources; this parallel equation-domain hand-source is noted for the close (not folded — it's a distinct contract surface, not a duplicate of the collapsed one).

## inv-25 note (W1 follow-through)
The W1 deploy-id-capture hardening is validated: deploy-pages run `26694375343` surfaced the clean citation — CF deployment `135ab532`. This is the canonical inv-25 `deploy_run_id` format the close cites. (W2's own web/** change re-ships the SPA via the same automated path.)
