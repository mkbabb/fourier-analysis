# G.W7 — deploy-hook convergence + compose hardening + host hygiene (thread ε.2)

**Status**: CLOSED — verified LIVE
**Thread**: ε.2 (deploy-spine completion)
**Agents**: 0 (team-lead direct — host-coupled + prod-deploy-sensitive)

## T3 — deploy-hook nginx-recreate (the W3-found gap)
`scripts/deploy-hook.sh`: when the pulled diff touches `nginx/`, the bring-up now **force-recreates nginx** (`NGINX_CFG_CHANGED` → `up -d --force-recreate nginx`). Closes the live gap W3 exposed: the single-file bind-mount (`nginx/fourier.conf:ro`) keeps the pre-pull inode after git's atomic-rename, and `up -d` won't recreate an unchanged service def → config-only changes were silently NOT applied. Runs on forward AND rollback bring-up; the health gate then validates the new config (a broken nginx config trips rollback like any bad bring-up).

**Convergence (backported to the spine)**: `deploy/templates/deploy-hook.sh` gained the parameterized form — `RECREATE_GLOB` (git-diff path prefix) + `RECREATE_SERVICE` (default `nginx`); empty glob = no-op. Every constellation repo with a bind-mounted config inherits it (deploy-repo `a7b58ab`, shellcheck clean). fourier's hook is the concrete instance; its divergence from the template (hardcoded inv-22 gate + migrations vs the template's parameterized `HEALTH_PATH`/`ROOT_EXPECT`/`MIGRATE_*`) is the documented richer specialization, not drift.

## Compose hardening floor (`docker-compose.prod.yml`)
- **backend — FULL floor**, verified applied LIVE: `ReadonlyRootfs=true`, `CapDrop=[ALL]`, `SecurityOpt=[no-new-privileges:true]`, `Tmpfs=/tmp` (UV_CACHE_DIR; `/data/blobs` is the rw volume, `.venv` read-only). Health-gated, so a bad bring-up auto-rolls-back — it didn't: health gate GREEN, real API paths (`/api/visualizations`, `/openapi.json`) → 200 under hardening.
- **frontend / mongo / nginx — `no-new-privileges`** (universally safe; nginx verified `SecurityOpt=[no-new-privileges:true]` live). `read_only`/`cap_drop` **BOOKED as residuals** — mongo is stateful (writes `/data/db`, can't read_only); nginx/frontend bind privileged ports + write cache/run dirs, needing per-image tmpfs/cap tuning that requires a staging test, not a blind prod gamble. **Honestly scoped, not claimed** — the floor is full where verifiable (backend), `no-new-privileges` elsewhere with the remainder named.

## Host hygiene (inv-21 — receipt `receipts/host-hygiene-W7.txt`)
Pruned 2 stale backups after a metadata receipt (sha256 + size + mtime; hooks backup content NOT shown — it carried secrets): `/opt/deploy/hooks.json.f-w3b.bak.*` (849 B, pre-per-repo-split; the live 4259 B `hooks.json` is the source of truth, untouched) and `/var/www/fourier-analysis/docker-compose.prod.yml.W1-pre` (2184 B untracked cruft). **Host tree now clean.**

## Gates (verified LIVE)
- Hardened backend deployed GREEN via the webhook (host HEAD `e9faab6`; health gate passed; migrations OK). ✓
- `docker inspect`: backend full floor applied; nginx no-new-privileges applied. ✓
- Functional smoke under hardening: `/api/visualizations`→200, `/openapi.json`→200. ✓
- deploy-hook + template `bash -n` + shellcheck clean. ✓
- Host backups pruned; tree clean; live `hooks.json` intact. ✓
