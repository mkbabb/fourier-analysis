# G.W1 — deploy-of-record + δ ships for real (thread α)

**Status**: in-progress → CLOSED on live-verify
**Thread**: α (correctness — top priority)
**Agents**: 0 (team-lead direct — deploy wiring is judgment-bounded, not parallelizable; Wα ratified the architecture)

## The gap (why α exists)
F's δ frontend (`9bd80b3`: meta-description, pinned CM-font SHA `333f55e…`, F's 103 B robots.txt, a11y aria-labels) **never reached prod**. The constellation auto-deploy (webhook → host `deploy-hook.sh`) drives ONLY the API; the CF-Pages SPA was shipped by a MANUAL `wrangler` that nobody ran after `9bd80b3`. Live prod served `cm-web-fonts@latest`, no meta-description, Cloudflare's 4497 B auto-robots. δ was PASS-in-source, not live.

## What W1 builds (the standing automated path — inv-25)
- **`scripts/pages-deploy.sh`** (fourier-tracked) — the spine recipe (`mkbabb/deploy` cf/pages-deploy.sh) adopted per its own "copy to the app's scripts/" instruction, fourier-tuned: project preflight, rollback-target capture, U+2014 commit-msg sanitisation, build in `web/`, deploy `web/dist` → CF Pages, **+ a NEW-deployment-ID capture** surfaced to `$GITHUB_OUTPUT`/`$GITHUB_STEP_SUMMARY` as the inv-25 `deploy_run_id`.
- **`.github/workflows/deploy-pages.yml`** — on push to master touching `web/**` (or the script/workflow itself), type-check gate → `bash scripts/pages-deploy.sh`. `paths` filter so backend-only waves (β.2, ε) don't pointlessly redeploy the SPA. Gated `if: push to master` (public-repo: fork PRs never get the secret). CF creds via `${{ secrets.* }}`, never inlined.
- **inv-25 + inv-26 authored** in `docs/tranches/INVARIANTS.md` (G-Inv 25 / 26 — clean fresh integers, the first post-C9 numbering hygiene; §2 point 6).

## Live preflight corrections (caught before CI)
- **CF project NAME is `fourier`**, NOT `fourier-682` (the latter is only the `.pages.dev` subdomain). Lane A inferred the slug from audit docs; a read-only `wrangler pages project list` corrected it. `scripts/pages-deploy.sh` `PAGES_PROJECT` fixed to `fourier`. (This is exactly the slug-trap the recipe's preflight guards.)
- CF token verified working (Pages:Read succeeded); project confirmed **direct-upload** (Git Provider: No) — consistent with Wα.

## Secret provisioning (user-authorized)
Per the user's explicit choice ("Set from .env"), `CLOUDFLARE_API_TOKEN` (Pages:Edit, len 53) + `CLOUDFLARE_ACCOUNT_ID` (len 32) set as GH Actions repo secrets via `gh secret set` from repo-root `.env` (values piped via stdin, never echoed). `gh secret list` confirms both by name.

## Gate (close criteria) — RESULTS
- [x] **`deploy-pages` GH Actions run SUCCEEDED** (run `26694289830`, 55s) → CF deployment `52f90604` (`https://52f90604.fourier-682.pages.dev`), alias `fourier.babb.dev`. The standing automated path is LIVE and proven on a real push.
- [x] **Pinned CM-font SHA live**: `cm-web-fonts@333f55ec…` (was `@latest`). ✓
- [x] **`<meta name="description">` live**. ✓
- [x] **a11y aria-labels**: shipped with the same deployment (in the JS bundle; source-verified — the deploy ships HEAD, and the font/meta verification proves the current source tree shipped). ✓
- [~] **robots.txt — honest reconciliation**: the deploy ships F's 103 B file VERBATIM (confirmed at the raw Pages URL `52f90604.fourier-682.pages.dev/robots.txt` → 103 B). The **custom domain** serves Cloudflare's **zone-managed** robots.txt (1841 B AI content-signals): `Content-Signal: search=yes,ai-train=no`, allows search indexing, **blocks GPTBot/ClaudeBot/CCBot/Bytespider/Google-Extended/Amazonbot/meta-externalagent**, AND appends F's file verbatim. This is a CF-dashboard zone feature the deploy cannot override — and it is **strictly superior** to F's bare `Allow: /` (disabling it would REGRESS the AI-crawler policy). δ's robots intent is met-and-exceeded; **accepted, no operator action wanted**. Booked as the honest δ finding.

## Chronic discharged in-wave (the headline beyond α)
The deploy-pages failure surfaced a **CI-red chronic**: `actions/checkout` with `submodules: recursive` could not clone the private docs-only `docs/precepts` submodule (inaccessible to the Actions token) — failing checkout on EVERY CI run for ~months. Removed `submodules: recursive` from `deploy-pages.yml` + all 3 `ci.yml` jobs (precepts is read by no test/build; `api/slugs.py` only name-checks it in a docstring). Two further deploy-blockers fixed: `scripts/pages-deploy.sh` was caught by the `scripts/*` blanket-gitignore (added the `!` negation per convention); the jq-based deployment-ID capture failed (no jq on the runner) → reworked to parse wrangler's stdout deployment URL.

## inv-25 citation (for FINAL)
- **SPA deploy-of-record**: GH Actions workflow `deploy-pages`, run id `26694289830` → CF deployment `52f90604`. Automated, push-triggered, re-runnable. NOT a manual one-off. ✓
- **API deploy-of-record**: the webhook → host `deploy-hook.sh` chain (unchanged; cited at close).

(δ self-host at W5 SUPERSEDES the jsdelivr CM pin → same-origin; W1 ships the current pinned-SHA source as the immediate correctness fix; the final prod δ state is re-verified post-W5 via the prod Lighthouse network trace showing zero third-party font origins.)
