# G.W6 — secret-model reconciliation + deploy-repo self-CI (thread ε.1)

**Status**: CLOSED
**Thread**: ε.1 (deploy-spine completion — `mkbabb/deploy`, inv-16 in-bounds)
**Agents**: 1 (implementation agent in the deploy repo; team-lead verified + committed)
**Deploy-repo commit**: `26e9160` (was `7c4e96b`)

## The lie (GA2 §1) — reconciled
Spine docs + the HMAC rotation runbook claimed webhook secrets were `${HMAC_<REPO>}`-interpolated via `EnvironmentFile`. **Host reality** (read-only confirm): `/opt/deploy/hooks.json` INLINES literal 64-hex secrets because `adnanh/webhook` **v2.8.2** reads hooks.json verbatim with NO env-expansion (no `-template` flag; the unit's `EnvironmentFile=/opt/deploy/.env` only sets the process env, which the receiver never consults — and `.env` held a single stale pre-multiplex `WEBHOOK_SECRET`, inert). A `${HMAC_<REPO>}` left in the live file would be HMAC'd as the literal string → silently broken verification. **The runbook was non-executable.**

**Reconciliation (option (a) — render wrapper, removes the foot-gun):**
- `host/render-hooks.sh` — `envsubst` render of the redacted template + host-only `secrets.env` → `/opt/deploy/hooks.json` (literals inlined, atomic 0600). **Fails LOUD** on a missing/empty secret, an unknown `${...}` placeholder (a typo would otherwise render an empty accept-everything secret), invalid JSON, or any surviving placeholder. `--dry-run` redacts 64-hex. `${HMAC_<REPO>}` is now an honest RENDER-time placeholder, not a runtime env-ref.
- `host/secrets.env.example` (placeholders only) + `.gitignore` (excludes `secrets.env` + `hooks.json`).
- Corrected the false headers in `hooks.json.template` / `webhook.service` / `deploy-dir-layout.md`; rewrote `security/hmac-rotation.md` to drive `secrets.env` + `render-hooks.sh` — **now executable verbatim, cannot leave a `${...}` literal as the HMAC**.

## Deploy-repo self-CI (dog-food — GA2 §2)
- `.github/workflows/ci.yml` (deploy repo) — `shellcheck` + `bash -n` over every tracked `*.sh`, + `yamllint`/parse over the template YAMLs. One job, push/PR to master.
- **Template fix**: SC2034 (unused loop counter `i`) in `host/dispatch.sh` → `for _ in …`. The only shellcheck finding across the vended templates (`cf/*.sh`, `templates/deploy-hook.sh` were already clean).

## Gates
- `render-hooks.sh` proven executable (5/5 substituted, JSON valid, 0 surviving placeholders; all 3 fail-loud guards fire; `--dry-run` redacts). ✓
- `grep -rnE '[0-9a-f]{64}'` over the deploy repo → CLEAN (no committed literal secret). ✓
- `secrets.env` gitignored (verified). ✓
- Host mutated NOTHING (inv-21). The host `hooks.json` render is an operator step (the secrets live only on the host); documented in the runbook. ✓

## Residual (operator-owned, booked)
Rendering the live host `hooks.json` from the new wrapper (so the host stops carrying a hand-inlined file) is an operator step requiring the host `secrets.env` — coordinated via the runbook, not done here (inv-21: no host mutation without the single-window + the secrets aren't in fourier's tree).
