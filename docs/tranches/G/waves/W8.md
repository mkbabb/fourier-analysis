# G.W8 — invariant honesty + chronic re-affirmation + coordination (thread ζ)

**Status**: CLOSED
**Thread**: ζ (ASK-only / doc — no host mutation, no cross-repo source edits; inv-16)
**Agents**: 0 (team-lead direct — doc reconciliation)

## inv-22 honest reconciliation (the third F overstatement)
Live-checked 2026-05-30:
- **`api.fourier`** (the enforced surface): `/`→404 problem+json, `/health`/`/docs`/`/openapi.json`→200. Correct, and the deploy-hook health gate **co-enforces** `/`→404 every deploy.
- **`api.color`** (value.js-owned): `/`→200 only; `/health`/`/docs`/`/openapi.json`→**404**.

F's `F-Inv 22*` "vhost-correctness-**symmetric**" overstated reach. Reconciled honestly in `INVARIANTS.md §2.7`: the invariant binds as **fourier-vhost-correctness (enforced + gate-co-enforced) + a cross-repo aspiration (coordination, maintainer-owned)** — NOT a constellation-wide guarantee. The color gap is a value.js item (inv-16-out-of-bounds), booked in `ADOPTION-ASKS.md §4` (new `inv-22-color` row), not a fourier residual.

## C1 / C5 / C6 — 6-gate STAYS-OUT re-affirmed (NOT silently re-deferred a 7th time)
Each is structurally cross-repo; inv-16 binds it out of fourier's write surface. The **binding gating predicate** (stated, not just asserted): *an item opens only when its OWNING repo drives it; fourier holds no source lever.*
- **C1 — colour-lift**: value.js-owned (publish-bound — needs a value.js release). Predicate: opens on a value.js publish cycle. **STAYS OUT.**
- **C5 — glass-ui substrate carries**: glass-ui-owned. Predicate: opens when glass-ui advances its substrate tranche. **STAYS OUT.**
- **C6 — glass-ui cold-boot race**: glass-ui-owned. Predicate: opens when glass-ui addresses the cold-boot ordering. **STAYS OUT.**
All three are re-affirmed OPEN-but-out with their owners + predicates — explicitly held, not silently rolled forward.

## E2 — cross-env Playwright: STAYS OUT with rationale (the honest KISS call)
fourier's `ci.yml` already runs an `e2e-tests` job (Playwright **chromium** against the live stack). E2 proposed a cross-env (multi-browser) matrix as "one CI-config delta." **Decision: stays out.** Rationale: the app is a standard Vue 3 / Canvas 2D / KaTeX SPA — chromium coverage is representative; a `[chromium, firefox, webkit]` matrix triples e2e CI minutes for marginal cross-browser coverage on a single-author educational tool. The charter permits "one CI-config delta OR stays out with rationale" (`G.md §6 ζ`); the cost/value here favors out. Booked as available-on-demand if a real cross-browser defect ever surfaces.

## Coordination — 7-ask + dispatcher stale-watch re-triggered (G.W8)
`ADOPTION-ASKS.md §4` re-stamped. Material change: **Ask 4's fourier portion LANDED** (G.W7 — backend full hardening floor live; the one ask with a fourier-source lever, now discharged). The other 6 asks + the dispatcher-retirement gate (still gated on all 4 non-fourier repos adopting the per-repo hook) remain maintainer-owned, OPEN, re-affirmed. New `inv-22-color` coordination row added (value.js).

## inv-16 attestation
This wave edited only `fourier-analysis/**` docs (`INVARIANTS.md`, `ADOPTION-ASKS.md`, this receipt). No cross-repo source edit, no host mutation.
