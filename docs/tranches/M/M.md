# M — the convergence-and-suffusion tranche (the EXECUTION discharge of J + K-deploy on a re-grounded 4.0 world, the design-language suffusion, and the unified mathematical motion architecture)

**Tranche letter**: M — fourier-analysis's successor to K-deploy (ν′) and the convergence vehicle for the open J (μ′). Where J asks *does the viz-server EARN its fork/version/provenance substrate?* and K-deploy asks *what idiomatic change LANDS the deploy backlog and clears the e2e gate?*, M asks the question both their closes have deferred for ~6 recurrences of the recap-and-fold meta-directive: **now that the siblings have shipped a whole major past every prior fourier audit's premise (glass-ui 4.0.0, keyframes.js 4.2.0, value.js 0.12.0), what EXECUTES — not re-plans — the twice-authored chronics (the deploy-of-record that never landed; the inv-15 consumer gap; the phantom data-model; the e2e red gate) AND suffuses the long-authored-never-shipped design language (the φ-display ladder, Fraunces, the audacious icon-rainbow pops, the math/grid chrome) AND collapses the eight hand-rolled rAF loops into ONE mathematical-motion architecture — all no-legacy, gestalt, proportion-governed, with a PHYSICAL close-gate?**

**Predecessors**: **J** — `docs/tranches/J/J.md` (AUTHORED 2026-06-02, ordering μ′; W2–W4 EXECUTED but OPEN — CI-red e2e, the binary inv-15 consumer breach, the deploy unlanded) and **K-deploy** — `docs/tranches/K/K.md` (AUTHORED 2026-06-04, ordering ν′; the deploy-spine + e2e tranche, never executed). A–I are CLOSED (ordering λ′, `CANONICAL-ORDERING.md §18`). **M is the convergence tranche**: it ABSORBS J's open remainder (W2-fix integrity + W2-transpose elegance-delete + the W5/W6 wire-the-UI + the e2e green) and K-deploy's fourier-side spine (the HEALTHCHECK + the inv-28 API-arm gate + the page-on-rollback sink), so **J + K-deploy + M close together at M's close** — the "fold ALL chronic items into THIS new tranche" the directive demands, executed rather than re-booked. L-webmcp (the Chromium-146-stable WebMCP graduation) stays the further-out successor, external-gated, re-affirmed at M close.

**Authored**: 2026-06-16 — from the 32-agent deep audit `docs/audits/runs/2026-06-16-M-deep-audit/` (8 plan/recap + 7 per-pane design + 7 design-language + 4 glass-ui + 6 animation auditors, batched three-at-a-time on Opus/Sonnet to spread the rate pools; two Opus synthesis digests — `plan-synth.json`, `design-synth.json` — merged by the core model). The full finding corpus (303 findings, 32 critical, 82 chronic-flagged, 59 glass-ui asks) is `raw-findings.json`; the scannable verdict index is `findings-index.txt`. The three session-limit/socket re-runs (A8 no-legacy sweep, B1 home, B6 paper) are `{A8-no-legacy-sweep,B1-home-design,B6-paper-design}.md`.

**Mode**: **direct** for the fourier-local + host-coupled waves (the multi-major bump, the deploy spine on fourier's own clean tree + the `mkbabb/deploy` template + the host, the design-language + motion + consumer-wire waves); **authored-now / executed-on-clean-checkout** under **inv-16′** for the residual cross-repo arms (the dispatch.sh retirement; the constellation adoption asks 1–7 — each a NAMED, ledgered, per-repo-green-CI-gated ask against a sibling fourier holds no lever over, NOT a silent sweep). The glass-ui ADOPTION-ASKS (§10) are booked-not-written — fourier consumes glass-ui, it never writes it (inv-16).

**Open**: NOT YET — M is AUTHORED only and awaits the user's "Begin," exactly as H, I, J, and K-deploy did. Phase 0 (M.W0 — charter, the re-ground sweep, the ledger/numbering hygiene) opens on authorization. This authoring is **tranche development only** — no implementation ran; the working tree's in-flight `glass-subtle→glass-quiet` rename is left untouched (M.W1 corrects and commits it; see §4 and the §7 NO-LEGACY fold).

> **⚠ AMENDMENT 2026-06-16 — `M-AMENDMENT-latest-and-BB.md` (3 user directives, folded post-authoring; supersedes the version prose in §0/§4/§5 below).** (1) **The landing IS the paper** — §4.1 resolved to the in-place PaperView hero (no `/` route). (2) **All packages to latest** — M.W1 widens from the 3 siblings to the WHOLE tree: keyframes **4.3.0** (not 4.2.0), value.js **0.13.0** (not 0.12.0; `sampleColorRamp` SHIPPED), + five framework majors (**vite 8, vue-tsc 3, vue-router 5, pinia 3, lucide 1.0**), katex 0.17. (3) **glass-ui's tranche BB is the active upstream** — fourier's glass-ui target is **`^4.1.0` (BB's fold-all close cut), GATED on glass-ui BB publishing** (the first hard upstream edge M carries); M's asks route to BB's `W-CROSSREPO-ASKS`; M CONSUMES BB primitives (`W-LIQUIDHOVER` specular auto-arm → STRIKES the `useSpecular` ask; `W-PAPER-GRID-TEXTURE`, `W-BORDER-PROGRESS`, `W-ON-GLASS-FG`); glass-ui BB executes under the glass-ui arm (inv-16 — fourier consumes, never writes it).

---

## §0 — The shape verdict (the deepest chronic, named once)

The 32-agent recap (A1) found the deepest pattern in the whole fourier corpus, and it is not a missing feature — it is a **process chronic**: the recurring directive *"DEEPLY audit / devise an idiomatic gestalt path forward / fold ALL chronics / recap ALL prompts / NO legacy / NOT an implementation phase, tranche development only"* has been honoured at the **planning layer ~6 times** (each tranche produced a plan not code, each recapped the prompts, each booked its chronics with terminal verbs) **while the two PHYSICAL chronics it keeps re-planning have never shipped**:

1. **The deploy-of-record never landed.** The host (EC2 `34.197.214.67`, `/var/www/fourier-analysis`) is **44 commits behind** at `f2fe447` (re-verified live this run — worse than the 29 `K.md` recorded). G-close, H, I, and J have NEVER reached prod; every "closed" fourier tranche since G closed against a tree the world never saw. The webhook fires, the deploy-hook builds, the 60s health-gate fails, the chain silently rolls back to `f2fe447`, and exits non-zero into a journalctl log nobody watches — the F "2-month silent webhook" anti-pattern, structurally, now the SECOND silent-for-months failure.
2. **The inv-15 consumer gap is a binary breach at HEAD.** J's seven CORE endpoints (`remix`, `/forks`, `/provenance`, `/diff`, `/versions`, `publish`, `unpublish`) have **ZERO `web/src` callers** (re-verified: `grep` of `api.ts` for any of the seven = 0 hits), and `gallery.ts` still routes publish through the generic `PATCH {visibility}` instead of the idempotent `POST /:slug/publish` the verb exists to serve. The entire 266-green-test J backend is substrate-without-a-consumer — the exact thing inv-15 (and its modern analog inv-30) forbid.

M **resolves this by construction**. M is not a seventh re-plan; it is the EXECUTION tranche whose entire reason to exist is to discharge what J and K-deploy authored. M.W0 states it in one line — *"this tranche discharges what J/K planned; it does not re-plan it"* — and M's close-gate is PHYSICAL (not a status claim): **observed-green prod with the host caught up to HEAD + a live UI consumer for every J endpoint + a single covering green inv-27 CI run + the design language demonstrably shipped.** The recap discipline stays (the §7 coverage fold *is* the recap); the close criterion is artefacts, not prose.

**The re-grounding correction that dissolves the prior premise.** Every prior fourier audit — and the 11-day-stale `docs/constellation/tri-tranche-run/RUN-BOARD.md`, whose "BLOCKED root hinge E1 = glass-ui 3.3.0 on npm" long since cleared — assumed a glass-ui **3.2.0** world. Reality, re-verified live (`npm view`): glass-ui **4.0.0** (a MAJOR over fourier's pinned `^3.1.0`), keyframes.js **4.2.0** (TWO majors over `^2.2.0`), value.js **0.12.0** (over `^0.10.0`). The K-deploy "one-line `^3.1.0→^3.2.0` caret bump" is dead at the root: the front move is a **multi-major reconciliation** with a real breaking-change surface. The happy inversion: nearly every primitive the last three audits booked as a *future* glass-ui ask has **already shipped** (dock `useId`, `asideSide`, `useTextHighlight`, `inert`, `cartoon-surface`, the scroll-driven + View-Transition recipes), so the design/motion work collapses from "wait + hand-roll" to **"bump + consume + delete."** That is M's gestalt.

---

## §1 — Thesis

J built a correct, idiomatic data-model PATTERN and shipped it onto a deploy chain that has not delivered in 44 commits and an e2e gate red since before J began. K-deploy correctly diagnosed the deploy chronic and authored the spine — but never executed, and authored it against a stale 3.2.0 premise. Meanwhile the design language the user has asked for across the J WC lenses — *"the design language of glass, grid, math, large and audacious typography, with colorful audacious pops like those found in our icons … more mathematical animation, hero items, scrolling animations"* — was authored in five J `design/WC-*.md` docs and **executed nowhere** (Fraunces is vendored, preloaded, and dead; the φ-display ladder has zero consumers; the most on-brand effect of all — the partial sum visibly converging onto its target — is built at ZERO sites despite the renderers already computing every quantity it needs).

The audit's gestalt, merged from groups B–E, is that **fourier's design language is half-built and quarantined, and its motion is fragmented across eight hand-rolled rAF loops — and BOTH conditions resolve through the same library bump plus a discipline of deletion**:

- **The glass story**: primary content surfaces are paper "stickers" (the `.cartoon-card` shim resurrected from a class glass-ui removed at C.W5, carried since D.W4 across seven tranches, 22 sites), not glass; the GLASS pillar appears only on transient chrome. The fix is a frozen three-band depth ladder (bare math substrate → in-flow instrument panels → lifted/anchored chrome) with the shim DELETED to glass-ui 4.0's native `cartoon-surface`, and the specular catch-light wired over the bright math canvas.
- **The colour story**: the icon-rainbow IS audacious but lives in one chart legend, while four duplicated HSL `spectrumColor` copies, a 118-line bespoke colour parser, and a divergent `STATIC.rainbow` twin hand-mix what value.js 0.12 generates perceptually from the favicon's own six stops. Collapse them to ONE OKLCH ramp and key per-route accents off it.
- **The typography story**: `.cm-serif` is bound and self-hosted (the prior audit's "degrades to Times" claim is corrected — the live token `--font-serif` IS wired; the real risk is the 4.0 bump *shadowing* it), but Fraunces is vendored-but-dead, the φ-display ladder has zero consumers, and gallery + equation have NO `h1` at all. Bind the tokens, adopt the ladder, make KaTeX the hero.
- **The motion story**: two ping-pong rAF clocks are duplicated verbatim (`animation.ts` + `ConvergencePlot.vue`), the manual rAF store is the MEMORY-flagged legacy, and four more rAF lerp/shimmer loops hand-roll what keyframes.js 4.2's light tier does idiomatically. Collapse to ONE `useFourierPlayhead` and build the convergence-reveal as THE signature.

M is the **convergence-and-suffusion** tranche, governed by one spine — **execute the planned chronics on the re-grounded world, and suffuse the design language as a first-class wave, not a defer-if-tight tail; every transposition DELETES rather than wraps**:

> The non-workaround discharge is NOT "bump the caret and re-plan the design." It is to land the multi-major bump as ONE atomic, breaking-change-audited keystone that greens the e2e at source and unlocks all five mandate axes at once; then run two parallel arms — the deploy spine (bump-independent) and the design-language-plus-motion-plus-consumer suffusion (bump-gated) — joining at a single physical evidence wave whose close-gate is observed-green prod, a live consumer per endpoint, and the design shipped.

M refuses the anti-patterns it was born to avoid. The bump is breaking-change-audited (a full MIGRATION sweep of the eleven used glass-ui subpaths BEFORE it lands, cited green run — not a blind caret bump). The deploy gate waits on a real container `HEALTHCHECK` + `depends_on: condition: service_healthy`, not a longer blind poll; the green-CI gate fails-closed, not deploy-anyway; the rollback PAGES a watched sink, not whispers. Every legacy item EXITS (the `.cartoon-card` shim DELETED, the phantom `glass-scrubber` variant dropped, the four `spectrumColor` copies and the 118-line parser collapsed, the eight rAF loops retired, the phantom within-viz version chain excised) — net code DECREASES. Where a primitive belongs IN glass-ui, fourier BOOKS the ask and consumes once shipped (§10), never reaches inside the component. And it carries no perpetual punt: every chronic the audit surfaced exits M with a SHIP-wave, a DELETE, a KILL, or a BOOK-with-hard-kill-date (§7).

## §2 — Binding question

> The siblings shipped a major past every prior fourier audit's premise (glass-ui 4.0.0 / keyframes 4.2.0 / value.js 0.12.0) while fourier's pins sit at `^3.1.0`/`^2.2.0`/`^0.10.0`; the deploy-of-record is 44 commits behind and silently rolling back for months; J's seven CORE endpoints have zero UI consumer and `gallery.publish` still PATCHes; the design language is authored across five WC docs and shipped nowhere; and the motion is fragmented across eight hand-rolled rAF loops with the most on-brand effect built at zero sites. **What idiomatic, no-legacy, proportion-governed transposition EXECUTES (not re-plans) the multi-major bump as the keystone that greens the e2e at source, LANDS the deploy backlog behind an observed-green health gate that pages on rollback, WIRES the inv-15 consumer gap onto the idempotent verbs, TRANSPOSES the phantom data-model, SUFFUSES the glass/grid/math/audacious-typography/colour-pop design language with proper hierarchy, and COLLAPSES the eight rAF loops into ONE mathematical-motion architecture — closing J, K-deploy, and M together against a PHYSICAL gate (observed-green prod + a live consumer per endpoint + a covering green CI run + the design shipped)?**

The answer is the close. The multi-major bump (M.W1) is the single hard predecessor of all five mandate axes: it greens the `glass-dock-1` dup-VT e2e blocker at source (the `useId()` fix is present at 4.0), un-`fixme`s the two a11y keystones, unblocks DEC-2 `asideSide` + `useTextHighlight`, unblocks the keyframes 4.2 light motion tier, unblocks the value.js 0.12 colour leverage, and clears the stale RUN-BOARD hinge. The deploy spine (M.W2–W4, bump-independent) lands the 44-commit backlog behind a real HEALTHCHECK, refuses a red SHA fail-closed, and pages a watched sink. The suffusion arm (M.W5–W9, bump-gated) ships the glass depth ladder, the audacious typography, the OKLCH colour pops, the control-pane hierarchy, and the unified motion CORE. The two arms join at M.W10 (wire the consumer gap + transpose the data-model + the scroll heroes) and M.W11 (the single covering green evidence run). The one named human dependency — the host-only read-only GitHub PAT for the inv-28 API-arm gate (M.W3) — fails-closed without provisioning (the correct safe state, not a punt; M.W2 may land first with the gate in warn-only for exactly one deploy until the PAT exists).

## §3 — Goal criterion and completion criterion (paired)

**Goal criterion.** Discharge the twice-planned physical chronics AND suffuse the long-authored design language AND build the motion CORE, on the re-grounded 4.0 world, as ONE convergence tranche that closes J + K-deploy + M together — sequenced by dependency into two parallel arms joining at a single physical evidence wave. No axis is a defer-if-tight tail; the design language and the motion architecture are first-class waves.

**Completion criterion (the evidence — PHYSICAL, per §0).** The close holds when ALL of:

- **The bump (keystone, e2e green at source)**: `web/package.json` pins `@mkbabb/glass-ui ^4.0.0` + `@mkbabb/keyframes.js ^4.2.0` (now a direct dep) + `@mkbabb/value.js ^0.12.0`; the lock is regenerated single-copy; the enumerated breaking surface is discharged (the SegmentedTabs/MetricBadge/glass-floating/glass-scrubber/import-path migrations of §4); the `glass-subtle→glass-wash` correction (NOT `glass-quiet`) is committed atop the resolved dep with the four committed silent-no-op `glass-subtle` sites fixed; Node-20's Actions cutover (checkout/setup-node → v5) lands in the same commit; fourier CI is green over the bump (cited run id, inv-27 — not a blind bump). The dock-VT e2e blocker is gone at source and the two a11y keystones assert green — **J.W6's inv-27 close is unblocked**.
- **The deploy (land + gate + page)**: the host is observed-green at HEAD — `curl :8100/api/health → "status":"ok"` AND `curl :8100/ → 404` (inv-22), with `docker compose logs backend` showing "Application startup complete" inside the gate window; the cold start is gated on a real container `HEALTHCHECK` + `depends_on: condition: service_healthy` (index creation moved off the lifespan serving path), NOT a blind longer poll; the API arm refuses a red-CI SHA via a fail-closed `green_ci_gate()` (inv-28's API half BUILT, not a third book); a rollback PAGES a constellation-watched sink (a verified delivery; inv-31 authored), backported to `deploy/templates`; **G/H/I/J/M are finally LIVE on babb.dev**.
- **The consumer wire-up (the binding inv-15 gate)**: all seven J client methods exist in `api.ts`; the `/v/:slug` detail host renders the diff-viewer (the AtomDiff consumer over CSS Custom Highlight + `useTextHighlight`, supported-flag PE floor), the provenance breadcrumb, the forks drill-down, and the remix action; `gallery.publish()`/`publishDraft()` are re-pointed OFF `PATCH {visibility}` onto `POST /:slug/{publish,unpublish}` (the idempotent in-place verb finally exercised). "No `api.ts` caller for an endpoint" is a RED gate.
- **The data-model transpose**: the phantom within-viz version chain is DELETED (`parent_hash`/`depth`/`root_hash`/`version_count`/`GET /versions` excised; the per-viz atom snapshot `/diff` + the cross-viz `fork_of` lineage kept); `palette_slug` is remix-only (the set_hash-staleness hole closed); `/diff`'s `to` param is real; the migration recomputes `atom_diff` for forks; `types.ts` swept in lockstep.
- **The design language (shipped, demonstrable)**: the three-band glass depth ladder is frozen and every surface mapped (the `.cartoon-card` shim DELETED); the φ-display ladder + Fraunces display voice + KaTeX-as-hero land on the paper hero + all seven page heads (gallery + equation gain an `h1`); the icon-rainbow OKLCH ramp is the ONE colour source (the four `spectrumColor` copies + the 118-line parser + the `STATIC.rainbow` twin DELETED), per-route `--route-accent` keyed off it; the control panes are ONE Configurator instrument (`.eq-grid` + `.config-grid` retired) with `asideSide=left` (DEC-2) and `:dividers`; the math/grid chrome (the ℱ signature, the EpicycleSpinner, the math-literate grid) is consistent.
- **The motion CORE**: zero `requestAnimationFrame` animation call-sites remain (layout-sync measure loops exempt + doc-marked); `stores/animation.ts` is a pure STATE atom over the unified architecture; ONE `useFourierPlayhead` drives the epicycle canvas + ConvergencePlot (the duplicate clock + the off-screen loop gone); the convergence-reveal signature fires at every canvas first-paint; scroll-driven section reveals + View-Transitions adopt the glass-ui 4.0 recipes; the three PRM gaps closed.
- **The evidence**: a single covering green inv-27 CI run (all jobs) over the migrated + wired tree — remix-flow + publish-flow-no-duplicate e2e, the two un-`fixme`'d a11y keystones, axe on gallery + diff-viewer, the measured CWV/INP deltas for the shipped perf waves, no live row with `set_hash==''`, and NO console-filter bridge (the dock error no longer exists at 4.0 — a bridge would mask a non-existent error, the exact NO-LEGACY violation forbidden).
- `PROGRESS.md` matches reality; `FINAL.md` cites every commit + gate + the green CI run ids + the deploy `deploy_run_id` (inv-25/28) + the observed-green prod capture + the J and K-deploy close ledgers (M discharges both); `CANONICAL-ORDERING.md → ordering ξ′` (§21).

The §12 hard-gate list is the binding ledger.

## §4 — Wave sequence (two arms, joining at evidence)

M is a **convergence-and-suffusion tranche** — so it is honestly presented as two parallel arms joining at one physical evidence wave. **Arm A (infra, bump-independent)**: the deploy spine, W2→W3→W4. **Arm B (product, bump-gated)**: the bump keystone W1, then the suffusion fan-out W5/W6/W7/W9 (all gate only on W1, parallel), W8 (gates on W5's glass ladder), W10 (gates on W9 + W8). The arms join at W11 (evidence). The critical path is **W0 → W1 → W9 → W10 → W11 → W12 → W13**, with **W2 → W3 → W4** in parallel. W0 + W13 are DEV (planning + reconciliation); W1–W12 are SHIP. Each wave names its inv-15 consumer (the per-wave-consumer discipline).

| Wave | Arm | Title | Goal (compressed) | Dep |
|---|---|---|---|---|
| **M.W0** | — | Charter + re-ground sweep + ledger/numbering hygiene | Declare M the EXECUTION tranche (close-gate = physical artefacts). Re-verify live facts at source (`npm view` → 4.0.0/4.2.0/0.12.0; host backlog = 44; the `glass-wash`-vs-`glass-quiet` correction against glass-ui `ladder.css`; A-1/A-2/A-3 against glass-ui 4.0 SOURCE — STRIKE if shipped). Author inv-31/32/33/34 + the inv-29 scope amendment. Reconcile `ADOPTION-ASKS.md` (struck-row the C1 colour-lift kill; flip the value.js-J peers DONE-in-L; re-verify A-1/A-2/A-3). Re-ground the stale RUN-BOARD 3.2.0/3.3.0 edges to 4.0.0 reality. | none |
| **M.W1** | B | The multi-major bump (KEYSTONE) | One atomic changeset: glass-ui `^4.0.0` + keyframes `^4.2.0` (direct dep) + value.js `^0.12.0`; regen lock single-copy; a full MIGRATION sweep of the 11 used glass-ui subpaths FIRST; discharge the breaking surface — `UnderlineTabs→<SegmentedTabs variant="underline">` (3), `MetricBadge :amount→:value` (≈11), `glass-elevated→glass-floating` (2), drop the phantom `variant="glass-scrubber"` (8 → `standard`), the import-path moves (`useClipboard→/dom`, `Collapsible→/collapsible`, `useTextHighlight→/motion-core` — source-verified; the prior `/dom` and `/motion` doc-drift is exactly the inv-32 book-rot); CORRECT + commit the `glass-subtle→glass-wash` rename (NOT `glass-quiet`; ~0.30α, the dirty-tree rename over-weights every overlay a full rung) atop the resolved dep, fix the 4 committed silent-no-op `glass-subtle` sites, commit the correct `MobileFloatingToc glass-medium→glass-resting/floating`; Node-20 Actions checkout/setup-node→v5 (deadline today). Gate on a CITED green CI run (inv-27). Consumer: the whole product surface. | W0 |
| **M.W2** | A | Deploy spine I — readiness ≠ liveness | Real container `HEALTHCHECK` (GET `/api/health`, `start_period 60s`) on backend + nginx `depends_on: condition: service_healthy`; move the ~35 serial `create_index` calls OFF the lifespan serving path (`asyncio.create_task` after `yield`, idempotent) so `/api/health` answers immediately and a cold WORKERS=1 start is never a race against the 60s poll. The structural fix for why G/H/I/J never landed — NOT a longer blind poll. Consumer: the deploy-hook gate. | W0 |
| **M.W3** | A | Deploy spine II — fail-closed inv-28 API-arm gate | `green_ci_gate()` in `deploy-hook.sh` querying `commits/<sha>/status` (host-only read-only PAT, untracked `.env`) BEFORE `git reset --hard`; no PAT or non-success ⇒ refuse (non-zero), never deploy-anyway; backport to `deploy/templates`; reconcile `deploy.sh`'s advisory gate to the same two conditions (`/api/health` token AND `/`→404) from a shared constant. Consumer: every constellation deploy. | W2 |
| **M.W4** | A | Deploy spine III — kill silent-rollback + author inv-31 | `page()` POSTing all three terminal states (DEPLOY OK / ROLLBACK / no-green ALERT) to a constellation-watched sink (host-only untracked `.env`, HMAC discipline); backport to `deploy/templates` as `SINK_URL` (no-op when empty); a verified real delivery (inv-27 applied to the page). Author inv-31 (observability-floor) firmly. Land a known-green SHA through the now-fixed spine → **host catches up to HEAD**. Consumer: the on-call human. | W3 |
| **M.W5** | B | Glass depth hierarchy + incongruence sweep | Freeze the 3-band glass depth ladder (BAND 0 bare math substrate · BAND 1 in-flow instrument = `glass-wash` ambient / `glass-resting` focused · BAND 2 lifted-anchored chrome = `glass-floating`; text-plates compose `veil-surface`) and map every surface in one pass; DELETE the `.cartoon-card` @utility shim → `<Card surface="cartoon">`/`cartoon-surface` (22 sites); strip hand-rolled box-shadow/blur/color-mix glass recipes; wire the specular catch-light on BAND 1 over the bright canvas; sweep the C2 cross-page incongruences (1.5px border token, spinner→Pulse, rgba shadows→`--shadow-*`, raw radii→`--radius-*`, `text-red`→`--destructive`). Consumer: every panel + card. | W1 |
| **M.W6** | B | Audacious typography — φ-ladder + Fraunces + KaTeX hero | Verify the 4.0 bump does not shadow `--font-serif` (the B6 correction — CM is bound + self-hosted, not degrading); activate the vendored-dead Fraunces AS `--font-display` (override glass-ui's Plus-Jakarta default; book the `--font-display-serif` intent, §10); adopt the φ-display ladder across the paper hero + the 7 page heads (gallery + equation gain an `h1`); make KaTeX a first-class ladder member (`.katex-display→--type-title`); fix the ℱ-glyph sans-clash (it renders in the display sans inside a serif headline); reconcile canvas-drawn font literals; ONE PRM-gated char-stagger entrance on the paper hero. Consumer: the paper + every page head. | W1 |
| **M.W7** | B | Colour pops + math/grid chrome from ONE OKLCH source | Promote the icon-rainbow favicon palette to ONE canonical OKLCH ramp via value.js 0.12 (`mixColorsN`/`safeAccentColor`; book `sampleColorRamp` for 0.13.0 with kill-date); DELETE the divergent `STATIC.rainbow` twin + the 4 `spectrumColor` copies + the 118-line bespoke parser; per-route `--route-accent` keyed off it; source notation/tier/energy hues from value.js; contrast-safe accents. Math chrome: consume the 5 orphaned per-page Fourier-path assets as ambient epicycle signatures; ONE `EpicycleSpinner` replacing 8 generic spinners; a math-literate grid (π-ticks, mode-aware tokens, dot-lattice at significant positions); consolidate the split-brain grid modules; unify the grid-pitch token; apply the ℱ signature consistently. Consumer: every accent, swatch, spinner, grid. | W1 |
| **M.W8** | B | Control-pane hierarchy + per-pane composition | Migrate `.eq-grid` (equation) + `.config-grid` (morph) onto `Configurator` + `ConfiguratorLayer` (they never consume the chassis today); retire `CollapsibleSection` + the `ImageUpload <h3>` mimic + the morph `config-card-title` onto the canonical section-label; adopt `asideSide="left"` (DEC-2 authoring-rail) + delete the 3 `grid-template-columns` overrides + the mobile `:deep` stage patch; pass `:dividers` for inter-row grooves; density as the ONE rhythm lever; enforce the two-tier altitude + one-amber-per-pane rule; fix the `BasisSelector` value-row void; per-pane composition (workspace compose-the-void dropzone hero, gallery card 3-tier hierarchy + empty-state CTA, equation Compute-button glass + legend de-occlusion + f(x) coherence, morph timeline + spring-native easing + glass reskin); adopt `inert` a11y + `useTextHighlight` + native-popover overlay primitives. Consumer: equation/morph/workspace/gallery panes. | W5 |
| **M.W9** | B | Unified mathematical motion architecture (CORE) | Collapse the 8 hand-rolled rAF loops + 2 duplicated ping-pong clocks into ONE three-layer architecture (CSS-first → glass-ui motion composables → keyframes.js 4.2 light primitives); make `stores/animation.ts` a pure STATE atom over `RAFPlayback` + `useIntersectionPause` (the MEMORY-flagged legacy DELETED); extract ONE `useFourierPlayhead` for the epicycle canvas + `ConvergencePlot` (kills the duplicate clock + the off-screen loop); transpose `useFourierMorph`→`NumericAnimation+Sequence`, `useCurveTransition`→`SmoothProgress`, `useCanvasHover`→`SpringProgress` + the shared shimmer; build THE signature convergence-reveal at every canvas first-paint + harmonic-accumulation staggers + draw-on; adopt `useScrollProgress`/`.scroll-progress` + `[data-scroll-reveal]`, `useViewTransition`/`view-transition.css`; fix the `ConvergencePlot` per-frame resize; close the 3 PRM gaps; unify the easing catalog on keyframes `resolveEasing` + spring twins; move play-btn rainbow-drift to a compositor filter. Consumer: every canvas + the paper scroller. | W1 |
| **M.W10** | A+B | Wire the inv-15 consumer gap + data-model transpose + scroll heroes (close J's product half) | Add the 7 client methods to `api.ts`; build the `/v/:slug` detail host with the diff-viewer (`useTextHighlight` from `/motion-core` + CSS Custom Highlight + supported-flag PE floor + the booked AtomDiff primitive), provenance breadcrumb, forks drill-down, remix action; re-point `gallery.publish()`/`publishDraft()` OFF `PATCH{visibility}` onto `POST /:slug/{publish,unpublish}`. W2-transpose: DELETE the phantom within-viz version chain; keep the per-viz atom `/diff` + cross-viz `fork_of`; sweep `types.ts` in lockstep. W2-fix: `palette_slug` remix-only, `/diff` `to`-param real, migration `atom_diff` recompute. The home/landing mathematical hero (graft on the PaperView header OR a real `/` landing — **USER DECISION, §4.1**) + scroll-driven section reveals with once-discipline + parallax math-lattice. Consumer: the gallery + the new `/v/:slug` host. | W9 + W8 |
| **M.W11** | join | EVIDENCE — green inv-27 run + measured CWV/INP + axe | Over the migrated + wired + deployed tree, capture ONE covering green CI run (all jobs): remix-flow + publish-flow-no-duplicate e2e (downstream of W10), un-`test.fixme` the 2 a11y keystones into live asserts, axe on gallery + diff-viewer, the I-deferred + W-shipped measured INP/CWV deltas on the live post-deploy SPA (chrome-devtools / lighthouse), assert no live row carries `set_hash==''`. NO console-filter bridge. Tie each perf claim to a number or demote it to "shipped, unmeasured" honestly. Consumer: inv-27. | W4 + W10 |
| **M.W12** | A+B | Tail hygiene + cohort parity + book-with-kill-dates | CSP recipe intact post-J-endpoints + `fetchLater` behind the `sendBeacon` floor; the fourier-local Python `/diff` + publish envelope parity probe against `J-diff-shape.md`; ship `deploy.sh`/`dev.sh` as the canonical operator entrypoint; re-verify each constellation adoption ask (1–7) against the actual sibling repos at HEAD (no evidence-free re-affirmation) with a terminal disposition (ESCALATE/KILL/ACCEPT-chronic; words-spa 404 → P0); `dispatch.sh` retirement BOOKED with a hard kill-date = value.js `rsync→git` green-CI (escalate to the user, no 7th silent roll); re-affirm L-webmcp's external Chromium-146 gate. Consumer: the operator + the constellation. | W11 |
| **M.W13** | — | Close | `FINAL.md` cites observed-green prod at HEAD + the green inv-27 run id + a live consumer per endpoint + the design language shipped + the J and K-deploy close ledgers (M discharges both); `CANONICAL-ORDERING → ordering ξ′` (§21). | W12 |

### §4.1 — The home/landing (RESOLVED 2026-06-16: the landing IS the paper)

The `/` route's redirect to `/paper` is **intentional — the PaperView title page IS the product's front door** (user-directed 2026-06-16; `M-AMENDMENT-latest-and-BB.md §1`). M does NOT add a dedicated `/` route; it grafts the contained epicycle/convergence mathematical hero (Fraunces display title, the OKLCH section palette, the keyframes-spring entrance) onto the **PaperView header in place** — discharging B1-01's "front door is a greyscale document, not a hero" by enriching the paper title page itself. The redirect logic is untouched. The M.W10 "home/landing hero" merges with the M.W6 paper-hero typography + the M.W9 hero entrance — one surface, dressed once, PRM/INP-gated.

## §5 — The re-grounding (the live facts M is built on)

| Fact | Prior premise (stale) | Live (re-verified 2026-06-16) | Source |
|---|---|---|---|
| glass-ui | 3.2.0 (RUN-BOARD E1 "blocked on 3.3.0") | **4.0.0** (a MAJOR over fourier `^3.1.0`) | `npm view @mkbabb/glass-ui version` |
| keyframes.js | 3.0.0 | **4.2.0** (TWO majors over `^2.2.0`) | `npm view @mkbabb/keyframes.js version` |
| value.js | 0.10/0.11 | **0.12.0** (`sampleColorRamp` ratified for 0.13.0, not yet published) | `npm view @mkbabb/value.js version` |
| host deploy backlog | 29 commits (K.md) | **44 commits** behind `f2fe447` | live `git log f2fe447..HEAD` |
| `glass-subtle` successor | `glass-quiet` (dirty-tree rename) | **`glass-wash` (~0.30α)** — `glass-quiet` is ~0.50α, one rung too heavy | glass-ui `ladder.css` (A8-02) |
| `--font-serif` (CM) | "degrades to Times if unbound" | **bound + self-hosted + preloaded** (live); the real risk is the 4.0 bump *shadowing* the token | B6-11 |
| RUN-BOARD E1 hinge | BLOCKED on glass-ui 3.3.0 | **vacuously CLEARED** (4.0.0 is a major past 3.3.0); the board is 11 days stale | `RUN-BOARD.md` vs `npm view` |
| A-1/A-2/A-3 glass-ui asks | BOOK-to-AT (carried I→J→K) | **likely SHIPPED at 4.0** (`dividers` prop, `.configurator-section-label` hook, `asideSide`) — RE-VERIFY at W0, STRIKE if present | design-synth chronic_fold |

The inv-32 (version-currency) discipline M authors exists precisely to stop this book-rot: a pin must not trail a sibling's published major by more than one, and any "gated on a future sibling release" edge must cite a dated `npm view` re-verification within the tranche's W0 — a stale gate-prose is an inv-27-class honesty defect, not a real block.

## §6 — New invariants (clean fresh integers 31–34 + an amendment)

Continuing the clean fresh-integer sequence (G 25/26, H 27/28, I 29/30 — `INVARIANTS.md §1`), M adds 31–34, each binding by integer AND name; the canonical definitions land in `INVARIANTS.md §1` at M.W0. They are **namespace-partitioned** from the glass-ui precept invariants 31/32/33 (component-props-fail-explicit / RETIRED-class grep / dead-code grep) exactly as §3 of that ledger prescribes — a reader resolves by namespace, never the bare integer.

- **inv-31 — observability-floor** (K-RESERVED, authored firmly in M.W4): a deploy/rollback terminal state MUST page a constellation-watched sink, or the deploy path is NOT closed. Orthogonal to inv-25 (automated) / inv-27 (green) / inv-28 (verified) — those govern WHAT ships; inv-31 governs that a HUMAN is notified when it fails. Reaches all repos via the `deploy/templates` backport. Satisfaction evidence = a verified real page delivery. Rationale: the deploy chain went silently-broken-for-months TWICE (F's missing HMAC secret; the health-gate silent rollback).
- **inv-32 — version-currency**: a fourier pin on a constellation sibling MUST NOT trail the sibling's published major by more than one major, AND any cross-tranche "gated on a future sibling release" edge MUST cite a `npm view <pkg> version` re-verification dated within the current tranche's W0. A stale gate-prose is an inv-27-class honesty defect, not a real block. The structural fix for the I→J→K three-tranche book-rot (3.1.0/3.2.0/4.0.0 re-grounds).
- **inv-33 — design-system-single-source** (strengthens inv-10 token-first): the control-pane surface has exactly one platform source per axis — every control pane is a glass-ui `Configurator`/`ConfiguratorLayer` instrument (title on the typography ladder, rows on the density grid, the machined groove between sub-regions), NEVER a hand-rolled `<h3>`+card mimic (`.eq-grid`/`.config-grid`/`cartoon-card`); every glass surface picks its rung by its semantic depth BAND, not local taste, and the rung owns the material (no hand-rolled box-shadow/blur/color-mix glass); all runtime colour derives from value.js over the canonical icon-rainbow OKLCH ramp (no duplicated `spectrumColor`, no bespoke parser, no hardcoded palette literals). Net code DECREASES on conformance. Composes with inv-30 (platform-over-library, hardened — no resurrected dead classes, no phantom variant strings, no parallel re-implementations of shipped primitives).
- **inv-34 — motion-proportion floor**: every animation target (a) honours `prefers-reduced-motion` by collapsing to its static terminal state via the platform/glass-ui/keyframes PRM-aware primitives (NOT a hand-rolled `@media` carve — that carve retires per inv-3), (b) stays INP-safe (no main-thread block on the interaction-driven path; transform/opacity/filter on the compositor; no per-frame `getBoundingClientRect` on hot input paths), and (c) obeys a proportion ceiling (the user's standing "audacious but not gaudy" directive — pops/scales bounded). Fourier source contains ZERO `requestAnimationFrame` animation call-sites (layout-sync measure loops are exempt and doc-marked); all animation flows through the three-layer architecture; the epicycle clock is a pure STATE atom; the playhead is a single shared primitive.
- **inv-29 (progressive-enhancement-floor) — SCOPE AMENDMENT** (not a new integer): inv-29 governs platform-CAPABILITY adoption (a feature-gated behaviour change); it does NOT apply to a library class-vocabulary rename (e.g. `glass-subtle→glass-wash`), which is a hard RETIRED-class swap under inv-3 — retaining the old class as a "floor" would be the dead-class legacy inv-3 forbids.

## §7 — The chronic + deferred fold (terminal verdicts — no perpetual punt)

Every chronic the audit surfaced exits M with a terminal verb. The full corpus is `findings-index.txt`; the load-bearing folds:

| Chronic / deferred item | Origin | Terminal verdict | Wave |
|---|---|---|---|
| Deploy-of-record never landed (host 44 behind since G) | G | **DISCHARGE** — the spine lands + a known-green SHA flows; close-gate = observed-green prod | W2–W4 |
| inv-15 consumer gap (7 endpoints, 0 callers; publish PATCHes) | J | **DISCHARGE** as the binding inv-15 close-gate | W10 |
| Sibling re-ground unconsumed (pins 1–2 majors behind) | I→J→K | **DISCHARGE** front-loaded; inv-32 stops the book-rot | W1 |
| Phantom within-viz version chain (depth always 0) | J | **DELETE** (W2-transpose; net code decrease, inv-3) | W10 |
| Design language authored in J WC, shipped nowhere | J | **EXECUTE** as first-class waves (not a tail); inv-33/34 codify the floors | W5–W9 |
| e2e/inv-27 red on `glass-dock-1` since before J | J | **DISCHARGE** downstream of the W1 bump (useId at 4.0); NO console-bridge | W11 |
| `.cartoon-card` dead-class shim (22 sites) | D.W4 | **DELETE** → glass-ui 4.0 `cartoon-surface` (the no-shims directive finally honoured) | W5 |
| 8 hand-rolled rAF loops + 2 ping-pong clocks | (engine rip-out) | **RETIRE** → ONE `useFourierPlayhead` (inv-34) | W9 |
| 4 `spectrumColor` copies + 118-line colour parser + `STATIC.rainbow` twin | — | **DELETE** → ONE value.js OKLCH ramp (inv-33) | W7 |
| `variant="glass-scrubber"` phantom (8 sites, breaks on bump) | — | **DROP** → `standard` (already the correct semantic) | W1 |
| A-1/A-2/A-3 ConfiguratorLayer asks (BOOK-to-AT) | I→J→K | **RE-VERIFY at 4.0 → STRIKE if shipped** (design-synth reports all three present); become fourier-local adoptions | W0→W5/W8 |
| `--viz-amber` light-mode contrast override (axe AA fail) | D.W4 | **BOOK-UPSTREAM with kill-date** (value.js `safeAccentColor` makes it systematic); local override deletes on adopt | §10 → W7 |
| C1 colour-lift (`sampleToSVGPath`) — killed in prose only | J.W0 | **LEDGER-THE-KILL** (struck `ADOPTION-ASKS` row; locatable without J archaeology); never re-book | W0 |
| `CanvasOverlayButton.vue` — dead component, 0 consumers | — | **DELETE** outright | W1/W5 |
| `dispatch.sh` retirement (stuck since F, gated on 4 migrations) | F | **BOOK with hard kill-date** = value.js `rsync→git` green-CI; escalate to user (no 7th silent roll); fourier holds no lever (inv-16) | W12 |
| Constellation adoption asks 1–7 (open 18+ days) | F/H | **RE-VERIFY-LIVE** at HEAD; terminal disposition per ask (words-spa 404 → P0) | W12 |
| The recap/fold meta-directive satisfied-at-planning-deferred-at-execution (~6×) | — | **RESOLVE-BY-CONSTRUCTION** — M's CORE is execution; close = physical artefacts | W0 (stated) → W13 (proven) |

## §8 — The design language (the suffusion gestalt)

The design-language substance is `design/M-design-language.md` (the glass depth ladder + the φ-typography ladder + the icon-rainbow OKLCH colour system + the math/grid chrome), authored at M.W0/W1. The gestalt: fourier's three brand pillars — GLASS, GRID, MATH — and the user's two amplifiers — large audacious TYPOGRAPHY and colourful icon-derived POPS — are present in token form but quarantined off the surfaces that matter. M does not invent a new language; it **suffuses the authored one onto the surfaces, within proportion**, governed by inv-33 (one source per axis) and inv-34 (proportion). Every move DELETES a bespoke duplicate as it lands the platform idiom — the suffusion and the no-legacy excision are the same act.

## §9 — The motion architecture (the unified CORE)

The motion substance is `design/M-motion-architecture.md` (the three-layer architecture + the `useFourierPlayhead` + the convergence-reveal signature + the scroll-driven/View-Transition adoption), authored at M.W0/W9. The principle: a Fourier project's signature motion is the partial sum visibly converging onto its target — and fourier computes every quantity that effect needs while building it at ZERO sites. M collapses the eight fragmented rAF loops into ONE clock, makes the epicycle store a pure STATE atom, and builds the convergence-reveal as the brand's signature — all on keyframes.js 4.2's light tier (value.js-free, no bundle regression), PRM-honoured and INP-safe by inv-34.

## §10 — The glass-ui ADOPTION-ASKS (booked, never written)

fourier consumes glass-ui; it never writes it (inv-16). The asks below are booked to `docs/constellation/ADOPTION-ASKS.md` at M.W0; the net-new ones generalize beyond fourier (the case is in each row). The A-1/A-2/A-3 carries are RE-VERIFIED at 4.0 SOURCE first (likely STRIKE — design-synth reports them shipped).

| Ask | Kind | What | Why it generalizes |
|---|---|---|---|
| **tonal-accent recipe** (`accent-tone`) | net-new | a contrast-floored 3-channel tonal accent (`--accent-fill`/`--accent-edge`/`--accent-ink`) from ONE `--tone`, reusing value.js mixing + flooring | fourier re-derives this `color-mix` recipe ~57× (pills, toggles, badges, swatches); it IS the colour-pop system; value.js palette chips + speedtest tone badges want the same |
| **AtomDiff / diff-highlight viewer** | net-new | a three-tone (added/removed/changed) keyed-row renderer over the canonical J `atomdiff` shape, reusing the tonal-accent recipe + View-Transitions row enter/leave | J `/diff` has zero consumer; value.js palette diffs + speedtest run-compares want the same grammar; born in glass-ui, fourier's consumer stays thin |
| **ConfiguratorLayer header trailing/actions slot** | net-new | a right-aligned trailing slot on the trigger header inside a `@click.stop` boundary | fourier needs a panel-wide reset aligned to the title; today it drops the reset in the body with negative-margin nudges AND maintains a whole parallel `CollapsibleSection.vue` for its actions slot — this retires that |
| **DockIconButton active/pressed toggle** | net-new | an `active?: boolean` prop stamping `aria-pressed` + `data-active` and a selected visual register | fourier hand-rolls `:class="{is-active}"` at 7 dock sites where the class has NO backing paint (toggled-on renders as idle) |
| **useSpecular composable** | RE-VERIFY→likely STRIKE | a pointer-anchored catch-light (`--mouse-x/-y` from `pointermove`, rAF-throttled, PRM-aware) composing `.glass-specular-track` | glass-ui 4.0 ships `useSpecularTracking` (source-verified by the motion-arch authoring) — confirm it covers the BAND-1-over-canvas need at W0 and STRIKE if so; fourier wants the catch-light on BAND 1 panels over the bright canvas |
| **useCharStagger / SplitChars** | net-new | a per-glyph split with `--char-index` + accessible full-text label — the JS partner to the shipped `.char-stagger` CSS | every hero (paper, landing, speedtest) hand-rolls glyph-splitting + a11y labelling |
| **canvas-anchored-overlay** | net-new | anchor the 4.0 top-layer popover to a canvas-relative point via a synthetic `getBoundingClientRect` | the coefficient + convergence-curve hovers live over `<canvas>`; without a virtual anchor they cannot adopt the 4.0 overlay substrate (bespoke positioned divs, no keyboard path) |
| **data-scroll-reveal once-discipline** | net-new | an opt-in `once` mode on `[data-scroll-reveal]` (a `[data-scroll-reveal=once]` / `data-revealed` latch) | the `view()`-timeline recipe re-fires every re-entry — wrong for fourier's virtual section window (unmount/remount) and the teleport stable-frame loop; the paper body cannot adopt scroll-reveal safely without it |
| **convergence-reveal motion preset** | net-new | a "partial-sum settle" preset (spring + orchestration, PRM-collapse-to-terminal) — the canonical "series settling onto target" grammar | THE signature; fourier viz + equation want the identical accumulation curve; belongs in the shared motion layer |
| **glass-tier-migration staleness gate** | process | a `proof:consumer-staleness` allowlist / PostCSS lint warning on retired tier class names after a major bump | the v0.8.0 ladder rename was a SILENT visual regression — fourier ran four elements with zero glass for the entire 3.1.0 era with no diagnostic |
| **`--viz-amber` upstream rebaseline** | book-with-kill-date | fold the contrast rebaseline (3.54:1→4.6:1) into glass-ui so all consumers inherit; `safeAccentColor` makes it systematic | the local `:root` override is a D.W4 axe-fail patch carried D→I |
| **A-1 / A-2 / A-3** | RE-VERIFY→likely STRIKE | inter-row divider-rule / label-sub ladder hook / `asideSide` | design-synth reports all three SHIPPED at 4.0 (`dividers`, `.configurator-section-label`, `asideSide`); confirm at W0, strike if present |

## §11 — Risks (the load-bearing ones)

1. **The bump is the single point of failure.** glass-ui is a MAJOR and keyframes TWO majors — latent faults can surface beyond the enumerated ~25-site migration. M.W1 runs a full MIGRATION sweep against the 11 used glass-ui subpaths + a class/prop/composable grep BEFORE the bump lands, cited green run (inv-27), not a blind caret bump. The keyframes lazy-import boundary (`loadAnimationEngine`/`Animation`/`addFrame`/`parse`) must be re-verified against 4.x before the morph transposition is treated as low-risk.
2. **The dirty-tree `glass-quiet` rename is semantically wrong** (A8-02, double-confirmed): `glass-subtle`'s 4.0 successor is `glass-wash` (~0.30α), not `glass-quiet` (~0.50α) — committing it standalone, or as `glass-quiet`, pins fourier to a half-migrated 4.0 vocab against a 3.1.0 `node_modules`. The ordering is load-bearing: bump THEN commit the corrected rename, never the reverse.
3. **The host-only inv-28 PAT (M.W3) is the single named human dependency and is UNPROVISIONED.** The gate fails-closed (refuses to deploy) without it — which would block ALL API deploys. M.W2 may need to land FIRST with the gate in warn-only mode for exactly one deploy until the PAT exists, or M.W4's known-green-SHA push is blocked.
4. **The off-host sink for inv-31 (M.W4) is an operator decision** — which sink the constellation already watches (ntfy/Discord/Slack) must precede the `page()` implementation.
5. **W2-transpose may collapse the VersionTimeline to a single node** — after the within-viz chain is deleted, every viz is born depth-0 and remix creates a NEW viz, so the diff-viewer's only real comparison is the cross-viz fork delta. Confirm the within-viz timeline earns its keep before building it, or fold it into the cross-viz forks drill-down.
6. **value.js `sampleColorRamp` (the ideal OKLCH-ramp primitive) is ratified for 0.13.0 but NOT published** — M.W7 uses `mixColorsN` equal-weight as the interim and books `sampleColorRamp` with a hard kill-date = the 0.13.0 publish, verified at adoption.
7. **The motion CORE (M.W9) is the deepest transposition** — collapsing 8 rAF loops + 2 clocks risks regressing the I.γ off-screen-park semantics. Verify `RAFPlayback.loop` + `Sequence.pause` retain exact phase on resume; keep the paper teleport measure-loop explicitly EXEMPT (inv-34 doc-mark) to avoid category-error over-reach.
8. **The cohort tail (M.W12) is genuinely outside fourier's write boundary** (inv-16) — M can re-verify and escalate but cannot DISCHARGE `dispatch.sh` / the adoption asks; booking with a hard kill-date risks a 7th silent roll unless the user is explicitly engaged on the value.js `rsync→git` critical-path item.

## §12 — The hard-gate ledger (binding close criteria)

The close holds when every row is green (the §3 completion criterion, enumerated):

1. Bump landed (3 pins at `^4.0.0`/`^4.2.0`/`^0.12.0`, lock single-copy, breaking surface discharged, `glass-wash` correction committed, Node-20 v5) + a cited green CI run id (inv-27).
2. Host observed-green at HEAD (`/api/health`→ok, `/`→404) behind a real HEALTHCHECK + `service_healthy`; index creation off the serving path; G/H/I/J/M LIVE.
3. inv-28 API-arm `green_ci_gate()` BUILT + fails-closed (a red SHA demonstrably blocked); backported to `deploy/templates`.
4. A rollback PAGES a watched sink (verified delivery); inv-31 authored; the third silent-for-months failure structurally impossible.
5. 7 `api.ts` client methods + the `/v/:slug` host + the diff-viewer/provenance/forks/remix; publish re-pointed onto the verb (no `PATCH{visibility}`); no endpoint without a caller.
6. Phantom version chain DELETED; `palette_slug` remix-only; `/diff` `to`-param real; migration `atom_diff` recompute; `types.ts` swept.
7. Glass depth ladder frozen + mapped; `.cartoon-card` shim DELETED (22 sites migrated); C2 incongruences swept.
8. φ-display ladder + Fraunces + KaTeX-hero on the paper hero + 7 page heads (gallery + equation gain `h1`); ℱ sans-clash fixed.
9. ONE OKLCH colour source; the 4 `spectrumColor` copies + the 118-line parser + the `STATIC.rainbow` twin DELETED; per-route `--route-accent`; math/grid chrome consistent.
10. Control panes ONE Configurator instrument (`.eq-grid`/`.config-grid` retired); `asideSide=left`; `:dividers`; per-pane composition; `inert`/`useTextHighlight`/native-popover adopted.
11. Motion CORE: zero rAF animation call-sites; `useFourierPlayhead` shared; convergence-reveal signature; scroll-driven + VT adopted; 3 PRM gaps closed.
12. ONE covering green inv-27 run (remix/publish e2e, un-`fixme`'d keystones, axe, measured CWV/INP, no `set_hash==''`, no console-bridge).
13. Tails swept (CSP/`fetchLater`, parity probe, operator entrypoint); adoption asks 1–7 + `dispatch.sh` re-verified-live with terminal dispositions; L-webmcp re-affirmed.
14. `PROGRESS.md` matches reality; `FINAL.md` + the J and K-deploy close ledgers cite every artefact; `CANONICAL-ORDERING → ordering ξ′`.

## §13 — What M does NOT do (declined, recorded — not deferred)

- **M does not re-plan.** The recap discipline stays as the §7 fold; the work is execution. A finding that resolves to "author another design doc" instead of shipping is rejected (that is the §0 chronic).
- **M does not write any sibling repo** (inv-16). The glass-ui asks (§10) are booked; the cross-repo tail (§7 `dispatch.sh`, adoption 1–7) is re-verified + escalated, never silently swept.
- **M does not build a console-filter bridge** for the dock VT error — it no longer exists at glass-ui 4.0; a bridge would mask a non-existent error, the exact NO-LEGACY violation the directive forbids.
- **M does not re-book P5 inner-rounding** — KILLED-AS-PHANTOM (the container owns the radius by design; glass-ui's own FINAL calls fourier's prior ledger a misdiagnosis). Struck, never re-booked.
- **M does not subsume L-webmcp** — the Chromium-146-stable WebMCP graduation stays the further-out successor, external-gated, re-affirmed at M close.

---

**The J ↔ K-deploy ↔ M absorption (named once, so no future session reads "J open, K open, M open" as a contradiction):** M is the convergence vehicle. It EXECUTES J's open remainder (the data-model transpose at W10, the consumer wire-up at W10, the e2e green via the W1 bump, the evidence at W11) and K-deploy's fourier-side spine (the HEALTHCHECK + the inv-28 gate + the page-sink at W2–W4). J and K-deploy do NOT execute separately; their FINAL.md files land at M's close citing M's commits. The chain reads μ′=J → ν′=K-deploy → **ξ′=M**, and M's close discharges all three. K-deploy's residual cross-repo arm (the value.js peers — DONE-in-L; `dispatch.sh` — booked-with-kill-date) folds into M.W12. This is the "fold ALL chronic items into THIS new tranche" the directive demands — executed, not re-booked.
