# M — AMENDMENT: critique-hardening (the live-critique 32-agent audit → render-verified design gate)

**Provenance**: the 32-agent deep audit `docs/audits/runs/2026-06-17-M-critique-audit/` (batched 3 on Opus/Sonnet; 316 findings, 30 critical, **29 alpha = glass-ui-owned**; two Opus synthesis digests `plan-synth.json` + `design-synth.json` merged by the core model; `findings-index.txt`), grounded — for the first time — in the user's LIVE critique of the bumped + M.W1-migrated instance (`http://localhost:3000`, branch `m/w1-bump-migration`). Evidence: `CRITIQUE.md` + `user-critique/*.png` (the user's own crops) + `live/*.jpeg` (full-pane desktop @1440). This amendment HARDENS Tranche M against that critique; it does not re-plan it (the two synthesis halves converged: **the M plan is right, the M.W1 bump STANDS, the work is to correct-at-root**). It supersedes the prior prose where it conflicts; the dated records stand.

**The structural lesson (the §0 anti-chronic, applied to design).** The bumped+migrated tree passed every *migration* gate (renames correct, exports resolve, app loads) while compositing **opaque** (not-glassy), **square** (not-rounded + raw focus ring), **underline** (not-glass tabs), **washed** (idle-invisible buttons), and **right-pinned** (controls-RIGHT). Nothing in the invariant ledger bound *rendered* fidelity — inv-33 governs the design SOURCE, inv-34 governs motion PROPORTION, but a surface could declare a glass rung and still paint grey. **The four new invariants (35–38) close that seam**, and inv-38 extends M's physical-close discipline (observed-green prod, live consumer per endpoint) to the design axis: the 8 critiques become *observed-green render captures* at the M close, never a class-grep.

---

## §1 — The 8-critique resolution (each → root class → owner → idiomatic fix → wave)

`α` = glass-ui-4.0-owned (root-caused in source; booked to glass-ui BB `W-CROSSREPO-ASKS`, inv-16 — fourier consumes, NEVER an `!important`). `β` = fourier adoption gap. `γ` = M.W1 bump residue.

| # | Critique | Class | Owner | Idiomatic fix (no-legacy) | Wave |
|---|---|---|---|---|---|
| 1 | surfaces **not glassy** (cartoon-stamp cards) | γ+β | fourier | DELETE the `.cartoon-card` @utility shim (15 live sites confirmed, not 25) → the shipped 5-tier ladder (in-flow → `glass-wash`/`glass-resting`; lifted chrome incl. error/empty/loading cards → `glass-floating`; text-plates → `veil-surface`). The rung OWNS the material — no surface composes a rung AND a hand-rolled border/blur/shadow. The paper **article** sheet stays the ONE deliberate cartoon accent (ledgered); the sidebar → `glass-resting`. Net code DECREASES. | M.W5 |
| 2 | borders **not rounded** | γ | fourier | Container-owns-rounding (P5 stays KILLED — never re-book inner rounding): outer containers round via the `--radius-{card,panel,input,control,pill}` alias scale + `overflow-hidden`; inner rows stay square by design. ADOPT glass-ui 4.0 `squircle.css` (`@supports`, inv-29 floor) on glass shells. DELETE every raw `0.75rem`/`calc(var(--radius)-Npx)` literal. | M.W5 |
| 3 | **no desktop tabs** for the visualizer | γ | fourier | The Configurator chassis already collapses single-column < lg and two-column at lg+, making the parallel `Controls\|Canvas` switcher redundant scaffolding. DELETE the mobile-tab state machine (the SegmentedTabs bar + `mobileView` ref + `panel-inactive` bindings + the `:deep(.configurator-stage)` flex patch + the orphaned `isDesktop`). **FIRST** (M.W0) reproduce the exact @1440 viewport to rule out a Tailwind-v4/vite-8 `lg:hidden` emission regression (built-CSS smoke) — if the utility genuinely fails to compile, that is a separate γ fix at source, never a JS `v-if` over a working media query. | M.W8 (gated on M.W0 disambiguation) |
| 4 | **ALL controls LEFT** (DEC-2) | β | fourier | Pass `aside-side="left"` to `<Configurator>` (shipped at 4.0; pure grid-column placement + border-side swap; DOM/tab-order/a11y unchanged) and DELETE the **4** hand-rolled `grid-template-columns` overrides + the `:deep` stage patch that re-define the template and DEFEAT `asideSide` (the deeper cause). Express the aside band via `:aside-width` / `--configurator-aside-{min,max}`. Apply uniformly: workspace + equation (`.eq-grid`) onto ONE Configurator; morph stays single-column stage-above-controls (per-route discrimination, not mechanical uniformity). | M.W8 |
| 5 | dropdown/glass **darkened** (flat opaque grey) | **α** | glass-ui | ROOT at `dist/styles/glass/ladder.css:200-204`: the overlay band (`.glass-floating`/`.glass-overlay` — every DropdownMenu/Select/Popover) self-darkens **unconditionally** toward warm-ink at `--glass-tint-strength-aa`, while the CONTENT tiers (`:216-219`, the BA `W-DARK-MATERIAL` R9-1 gray-slab fix) gate the full darken behind the `@container style(--glass-backdrop:light)` bright signal. The overlay band was deliberately left ungated ("floats over an unknown surface") → over fourier's substrate it composites flat-grey. **BB ask:** extend the bright-signal gating to the overlay band (floor unconditionally; full AA only under a declared/sampled light backdrop — backdrop-DETECTING, not a blanket revert, so dark siblings keep the lift). **fourier consume (β):** migrate `AppHeader` off the opaque `bg-background/90` stack → `glass-wash` (so the dropdown samples the page, not an opaque plate); drop the hand-rolled `.nav-dropdown-item` for the shipped `.glass-menu-row`; declare `--glass-backdrop` where the darken IS intentional. NO `!important`, NO per-surface `--glass-tint-strength:0%` binary (kills the legibility lift). | M.W0 (book) → M.W5 (consume) |
| 6 | buttons **invisible unhovered** | β | fourier (+α asks) | **RE-DIAGNOSED**: the pills DO paint an idle fill — the defect is WASHED *contrast* (light-on-light on cream), not absent paint. The `outline`/`ghost` variants idle-paint `bg-background` (= the page). FIX: the **contrast-floored** tonal-accent recipe (`--accent-fill ~6-8%` idle / `--accent-edge` / `--accent-ink` from ONE `--tone`, floored ≥3:1 via value.js `safeAccentColor`, keyed to `--route-accent`) so every option paints a calm resting chip and the active deepens one rung. DELETES the ~57 hand-rolled `color-mix` recipes + the 3 forbidden `!important` sites (`FunctionInput .is-auto-active` triple, `EquationResult` coeff-hover). Dock: set glass-ui 4.0's `--dock-active-*` cohort so the 7 hand-rolled `is-active` classes (today NO backing paint) finally render. | M.W7 |
| 7+10 | tabs should be **GLASS** (not underline) | **γ — the headline** | fourier | M.W1 pinned `variant="underline"` at 3 sites as a verbatim port of the retired UnderlineTabs — **migration-faithful ≠ design-correct**. The user reverses it. **DROP the variant prop** (the glass PILL is the 4.0 DEFAULT and IS the glass material — `--glass-bg-quiet` track + the gliding `--glass-bg-floating` indicator) at all 3 sites. Pure prop deletion, zero CSS. Key the active-pill fill off `--route-accent` (M.W7). FOLD the 2 hand-rolled glass-pill mimics (`NotationPills`, `EquationModeToggle`) onto the SAME pill engine (which also discharges #6 at the recipe level). Underline kept ONLY where a document-register affordance is argued in-charter (none on the critiqued surfaces). | M.W8 |
| 8 | search **offset + square** focus ring | **α** | glass-ui | Confirmed: glass-ui 4.0 ships NO Input/SearchField primitive and NO rounded glass focus-ring — only the Tailwind `focus-visible:outline-2/outline-offset-2` utilities (a SQUARE outline pushed OUTSIDE a rounded field; `outline` does not follow `border-radius`). fourier hand-rolled a raw `<input>` (ring on the child, border on the wrapper → structural offset by construction). **BB ask:** a real Input/SearchField primitive + a radius-following glass focus-ring recipe (a `box-shadow` ring via `border-radius:inherit`, brand-tinted, contrast-target = the composited glass fill). **fourier interim (a recipe, NOT an override):** compose a tokenized `:focus-visible` ring on the wrap via `box-shadow` + `border-radius:inherit` (the `style.css:136` conformant pattern), kill-date = the primitive ships; also fix the floating-search `border-radius:0` (let the outer container clip). a11y ring stays visible — never `outline:none` with no replacement. | M.W0 (book) → M.W5 (interim + consume) |

**Net:** 2 α (booked to glass-ui BB), 6 fourier — and the fourier waves are predominantly *subtractive* (delete the shim, the recipes, the `!important`s, the overrides, the underline prop). Net code DECREASES at nearly every site (inv-3).

## §2 — The headline corrections (what this audit changes in the prior plan)

1. **The tab-variant reversal (γ, the sharpest).** `M-bump-migration.md`/`M.md` recorded `UnderlineTabs → SegmentedTabs variant="underline"` as the *correct, final* 1:1 migration. The user reverses it: tabs are GLASS. The plan had no wave owning the reversal. → M.W8 drops the prop (glass pill default); `M-bump-migration.md` is annotated that the W1 underline was a transitional faithful-migration step, deliberately superseded.
2. **Critique #6 re-diagnosis.** From "no idle paint" → "washed idle *contrast*." The fix is the contrast-floored accent-tone idle fill (≥3:1 resting), not a missing fill.
3. **The M.W1 migration is INCOMPLETE (γ residue) — a finish pass is owed at M.W1:**
   - **STEP-2h slider retint** unfinished — the `--slider-scrub-*` → `--slider-range-bg`/`-thumb-*` remap (7 files) was not done, so the sliders silently lost their accent ink.
   - **vue-tsc 3 never gated** — `vue-tsc -b` masks `TS2882` (latex-paper/theme) with exit 0; the bump's type-safety (vue-router 5, pinia 3, TS churn) is UNVERIFIED. M.W1 must run `vue-tsc --noEmit` and resolve TS2882 via a fourier-owned `env.d.ts` ambient `declare module`, citing a green CI run (inv-27). Closing M.W1 on the `-b` exit code re-commits the §0 anti-chronic at the type axis.
   - **The 4 transitive peers (`vaul-vue`/`embla-carousel-vue`/`@lucide/vue`/`perfect-freehand`) are `--no-save`** — non-reproducible; a clean `npm ci` in CI would fail (false-local-green). Persist them per glass-ui's `peerDependenciesMeta`, or uninstall once W1b's 4.1.0 declares them.
   - **Import-path moves** (`useClipboard→/dom`, `Collapsible→/collapsible`, `useTextHighlight→/motion-core`) + **Node-20 Actions** (checkout/setup-node → v5) + **DELETE `CanvasOverlayButton.vue`** (dead) + update the inert `glass-scrubber` comments — all pending.
4. **PROGRESS.md is stale** — M.W1 physically ran on `m/w1-bump-migration` but every wave reads `planned`. → M.W0 reconciles (M.W1 = `executed-partial`; record the live tree runs glass-ui **4.0.0**, NOT the 4.1.0 BB target).
5. **keyframes API-doc drift** — `M-motion-architecture.md` cites keyframes **4.2** + a "light barrel vs heavy `loadAnimationEngine` import-path" premise that is FALSE at **4.3** (one flat barrel; the light/heavy boundary is tree-shaking, not an import path). → M.W0 re-pins 4.2→4.3, rewrites the framing, re-verifies every cited primitive against the 4.3 `.d.ts`, and books a bundle-budget assertion (prove the heavy engine tree-shakes out).
6. **value.js `sampleColorRamp` shipped at 0.13.0** — the M.W7 `mixColorsN` interim + kill-date are STRUCK. (But 0.13 is W1b-gated; see §6 — the OKLCH ramp rides W1b.)

## §3 — New invariants (clean fresh integers 35–38 + an inv-34 clause; `INVARIANTS.md §1`)

The precept band stops at 33; 34 is fourier's motion-proportion floor; 35–38 are fourier-clean (no precept collision). The colour-source axis is already covered by **inv-33** (design-system-single-source: value.js OKLCH) — reaffirmed, no new integer. The motion-clock axis is **inv-34** — extended with a signature clause.

- **inv-35 — glass-fidelity-floor**: every surface that DECLARES a glass rung (`glass-wash/-quiet/-resting/-floating/-overlay`, or `Card surface=…`) MUST, in a captured render against the live page background, composite as translucent glass — a measurable `backdrop-filter` blur AND a fill alpha < 1 with backdrop content discernibly bleeding through. A rung that paints opaque-grey is a defect, root-caused to (α) a glass-ui token regression → booked to glass-ui BB, or (β/γ) a fourier opaque overlay/shim → DELETED; NEVER a fourier `!important` fill (inv-3). Each region declares `--glass-backdrop` by its backplate luminance. The `.cartoon-card` shim is permanently retired; the cartoon register is an itemised ledger of named deliberate accents, never a default. Satisfaction = a render capture (inv-38), never a class-grep.
- **inv-36 — geometry-continuity-floor**: corner radius derives from the container or a `--radius-*` token (no raw per-site magic radii; `squircle.css` adopted on glass shells, `@supports` PE-floor per inv-29); inner sections stay square by design (P5 KILLED-AS-PHANTOM — never re-book). A focus indicator is a ROUNDED ring concentric with its field's radius (`border-radius:inherit` box-shadow ring, or the glass-ui input focus primitive), NEVER a raw `outline … outline-offset` square. A square/offset ring root-caused to glass-ui input → booked to BB; the fourier interim is a recipe with a kill-date, never `!important`; the a11y ring stays visible.
- **inv-37 — control-affordance-floor**: (a) every interactive control paints a discernible IDLE affordance contrast-floored ≥3:1 against its substrate (a tonal-accent fill/edge from the value.js OKLCH source, NEVER hover-only paint — washed-until-hover fails); (b) option/configuration controls occupy the LEFT authoring rail (`asideSide=left`, DEC-2 — a Configurator with controls on the right is a defect); (c) a tabbed instrument defaults to the GLASS variant (glass-track/pill), underline admitted ONLY where a document-register affordance is argued in-charter. Satisfaction = a captured render of the idle state + an `asideSide` assertion.
- **inv-38 — render-verified-design-gate**: the design invariants (33 source, 34 motion, 35 glass-fidelity, 36 geometry, 37 control-affordance) gate on a CAPTURED RENDER at the M close — the 8-critique regression checklist (opaque-glass · square-ring · underline-tab · washed-button · right-controls · square-corners · darkened-menu · desktop-tab-leak) as observed-green captures, **backend-up** (the live evidence was frontend-only, so the migrated data views were never runtime-verified). A code-state assertion is necessary but the render is the binding evidence (composes with inv-6 gates-close-on-evidence + inv-9 surface-appropriate-evidence — for a VISUAL claim, the surface-appropriate evidence is a pixel capture). The consistent application of the §0 anti-chronic to the design axis.
- **inv-34 — AMENDMENT (signature-everywhere clause, not a new integer)**: the ONE `useFourierPlayhead` (keyframes 4.3 light tier; zero `loadAnimationEngine` on the canvas/UI path, proven by a bundle assertion) drives the convergence-reveal signature, PRM-gated and **once-latched by stable visualization id** (no remount replay), present in the **empty/idle/error states** — the brand motion shows at the first encounter (every live capture was an empty/error surface; the signature was invisible exactly when first seen). At most one ambient heartbeat visible, sharing the scene playhead.

## §4 — The refined wave plan (the deltas; the full table is `M.md §4` + `PROGRESS.md`)

The wave SPINE is unchanged; this audit folds the critique fixes and tightens the gating. **The binding sequencing fact (§6): nearly the entire design surface (W5–W9) + value.js 0.13 + the e2e-green close are W1b-gated — they wait on glass-ui BB publishing 4.1.0.** Only W0 + W1a (framework finish) + W2–W4 (deploy spine) proceed now.

- **M.W0** — charter + author inv-35/36/37/38 + the inv-34 clause; reconcile PROGRESS (M.W1=executed-partial, live=4.0.0); re-pin keyframes 4.2→4.3 + rewrite the barrel framing; **book the α asks to glass-ui BB** (§5); disambiguate the @1440 `lg:hidden` leak (built-CSS smoke); confirm the value.js pin reality (§6).
- **M.W1** — bump FINISH. **W1a (now):** the slider `--slider-scrub-*` remap (7 files), `vue-tsc --noEmit` gate + the `env.d.ts` TS2882 fix, persist the 4 peers, import-path moves, Node-20 Actions v5, DELETE `CanvasOverlayButton`, lint; **pin value.js `^0.11`** (the glass-ui-4.0-compatible range — `^0.12`/`^0.13` BREACH the `^0.10‖^0.11` peer, §6). **W1b (BB-gated):** lift to glass-ui `^4.1.0` + value.js `^0.13` when BB's `W-PEER-SPINE` publishes; cite a green CI run.
- **M.W2–W4** — deploy spine (bump-INDEPENDENT, stage NOW): HEALTHCHECK + `service_healthy` + async `create_index`; fail-closed `green_ci_gate()`; `page()` + inv-31 sink.
- **M.W5** — glass ladder + rounding + focus-ring + menu-fidelity + incongruence sweep (critiques 1,2,5,8 fourier-half). Authored against BB HEAD now; executes on W1b.
- **M.W6** — typography (Fraunces φ-ladder + first h1 on gallery/equation + ℱ-clash fix). W1b.
- **M.W7** — colour pops: ONE value.js `sampleColorRamp` OKLCH ramp + the contrast-floored accent-tone idle fill (critique 6); DELETE the 4 spectrumColor copies + 118-line parser + STATIC.rainbow twin + the 3 `!important` + the `--viz-amber` patch. W1b (needs value.js 0.13).
- **M.W8** — controls-LEFT (`asideSide=left`) + glass tabs (drop the underline prop) + desktop/mobile gating + Configurator propagation (`.eq-grid`/`.config-grid`) (critiques 3,4,7). W1b.
- **M.W9** — motion CORE: ONE `useFourierPlayhead` over keyframes 4.3; retire the 8 rAF loops + 2 ping-pong clocks; the convergence-reveal signature in empty/idle/error (inv-34 clause). W1b.
- **M.W10** — inv-15 consumer wire-up + W2-transpose + scroll heroes (the binary J close-gate).
- **M.W11–W13** — render-verified evidence (the inv-38 8-critique capture checklist, backend-up) + adoption-ask re-verify + close (observed-green prod + live consumer per endpoint + green CI + the 8 captures).

## §5 — The glass-ui ADOPTION-ASKS (α — booked to glass-ui BB `W-CROSSREPO-ASKS`, inv-16)

fourier consumes glass-ui, never writes it. Each carries a no-legacy fourier INTERIM with a kill-date = the glass-ui 4.1.0 (BB) ship. Booked at M.W0; full rows in `ADOPTION-ASKS.md §12`.

| Ask | What | fourier interim (kill-dated) |
|---|---|---|
| **overlay-band-bright-signal-gating** (critique 5) | extend the `@container style(--glass-backdrop:light)` gating (shipped on the content tiers, `ladder.css:216-219`) to the overlay band (`:200-204`): floor unconditionally, full AA only under a declared/sampled light backdrop (backdrop-DETECTING) | declare the calm-light `--glass-backdrop` register on the known-light shell + the AppHeader→`glass-wash` migration — NOT `!important`, NOT a `0%` binary |
| **rounded glass focus-ring + Input/SearchField primitive** (critique 8) | a real Input/SearchField consuming `--radius-input` + a radius-following `box-shadow` glass focus-ring (`border-radius:inherit`, contrast-target = the composited glass fill) | a tokenized `:focus-visible` `box-shadow`+`border-radius:inherit` ring on the search wrap |
| **accent-tone / SelectableChip** (critique 6) | a contrast-floored 3-channel tonal accent (`--accent-fill` idle ~6-8% + active band, `-edge`, `-ink` from one `--tone`, floored via `safeAccentColor`) as a `.accent-tone` utility / `SelectableChip` with idle/hover/selected states | compose the recipe locally, consume-and-delete on ship |
| **DockIconButton active prop** (re-affirm/re-scope) | an `active?:boolean` stamping `aria-pressed`/`data-active` + a default visible selected register (re-verify at BB — glass-ui already keys `--dock-active-*`; STRIKE if the prop+default ship) | set the `--dock-active-*` cohort locally |
| **Configurator stage-mobile-height + aside-width prop** | (1) `.configurator-stage` resolves to a nonzero flex height in the mobile column; (2) expose `:aside-min/max-width` / `--configurator-aside-*` | the existing `:deep(.configurator-stage)` patch + grid overrides (the LAST inv-30 breaches) — deleted on ship |

Plus re-affirm the BB consume-contracts that sequence the design waves: **W-ON-GLASS-FG** (muted-foreground contrast-target = the composited glass fill — so M.W5's newly-translucent surfaces don't whisper-collapse below AA), **W-LIQUIDHOVER** (the specular auto-arm — retires per-consumer `useSpecularTracking`), and the booked **convergence-reveal preset / useCharStagger / data-scroll-reveal=once / AtomDiff viewer**.

## §6 — The binding open decisions + risks (surface to the user)

1. **BB 4.1.0 timeline is INDETERMINATE and gates the entire design surface.** glass-ui is published at **4.0.0** (npm `latest`); the 4.1.0 cut (BB `W-CLOSE`) is SPEC. **M.W1b + M.W5–W9 (all the design/critique fixes) + value.js 0.13 + the e2e-green dock-VT unblock all wait on that one publish.** M can advance W0 + W1a (framework finish) + W2–W4 (deploy spine) + AUTHOR the design waves against BB HEAD now — but cannot CLOSE the design axis until BB publishes. **This is the load-bearing open decision: does the user drive glass-ui BB to its 4.1.0 close (its own arm, inv-16), or does M ship the design fixes against glass-ui 4.0 with the α asks as long-lived kill-dated interims?**
2. **The value.js peer pin (precise correction to the synth).** glass-ui 4.0 peers value.js `^0.10.0 ‖ ^0.11.0` — so `^0.12` AND `^0.13` both BREACH. W1a must pin value.js **`^0.11`** (the highest in-range; the session's `0.13.0` install was peer-invalid, papered with `--legacy-peer-deps`). `sampleColorRamp` (0.13) — and thus the M.W7 OKLCH ramp — is therefore **W1b-gated** (rides glass-ui 4.1.0, which admits `^0.13`). Do NOT commit the `--no-save` lock anomaly to `package-lock.json` (it poisons a clean `npm ci` → false CI green).
3. **vue-tsc must run `--noEmit`** — `-b` masks TS2882 (exit 0). M.W1 cannot close on the `-b` code.
4. **The @1440 `lg:hidden` leak (critique 3) is unconfirmed-root**: a genuine Tailwind-v4/vite-8 utility-emission regression vs a sub-1024 capture. M.W0 disambiguates (built-CSS smoke + a backend-up @1440 capture) before M.W8 reasons about layout.
5. **Fraunces as `--font-display`** is an app-wide override that re-points every glass-ui `--font-display` consumer (which may assume a geometric SANS). Confirm at M.W6 it doesn't regress glass-ui surfaces; the cleaner contract is a booked `--font-display-serif` distinct slot (the one typography α — book to BB if needed).
6. **Operator-provisioned (deploy spine):** the inv-28 host-only read-only PAT (M.W3 fails-closed/warn-only without it) and the inv-31 watched sink (ntfy/Discord/Slack, M.W4) are undesignated — surface as blocking decisions before W3/W4.
7. **The α interims need explicit kill-dates** (= the 4.1.0 ship) or they become the next chronic; the overlay dial-down must be verified over the DARK theme (the band's dark arm re-tunes to a luminous-dark LIFT) before shipping.

## §7 — inv-16 attestation

This amendment authored `fourier-analysis/docs/**` only; READ glass-ui 4.0 `dist/styles/glass/ladder.css` + `tabs`/`slider`/`dialog` + `tranche/BB` (read-only) to root-cause the α findings + confirm the version reality; edited NO sibling tree, NO host state, NO `package.json` (the M.W1 finish is execution, not authoring). Every α claim cites glass-ui's own committed source (`ladder.css:200-204`/`:216-219` the overlay-band asymmetry; the absent Input/focus-ring primitive). glass-ui BB executes under the glass-ui arm; fourier books to `W-CROSSREPO-ASKS` and consumes. Provenance: the 2026-06-17 critique-audit run dir + the live bumped-instance critique.
