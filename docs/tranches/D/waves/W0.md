# D.W0 — open · baseline · research dispatch

**Wave**: W0 (the open/baseline/dispatch gate; 1 agent — team-lead authored). **Authored**: 2026-05-27 (hardened at Wχ-close from `D.md §3` W0 row + `PROGRESS.md` row 1 + the 10+6-lane D-development audit `docs/audits/runs/2026-05-27-D-audit/SYNTHESIS.md`). **Predecessor close confirmed**: C closed at `complete_with_host_residuals` (`docs/tranches/C/FINAL.md`, master `1e47115` per `DA4 §0`); the D-development authoring committed thereafter — `D.md` (this tranche's five-thread charter), `PROGRESS.md`, `coordination/{CRUD-COHESION,DOMAIN-NAMING,CONSTELLATION-DEPLOY}.md`, the 6 `DA*` audit lanes, the 4 `design/` lanes, the 6 `normalization/NA*` lanes, the `validation-matrix.md`. **Status on completion**: the strict gate W0 → Wα → Wχ opens once this record + the Wα dispatch land.

This record is the *binding baseline* — every Wα ratification, every Wχ probe, every implementation wave (W1–W12) measures against the facts catalogued here, grounded in `file:line` against the live tree (`1e47115` at audit time) **and** in pasted SSH output against the live shared host (`mbabb@mbabb.fridayinstitute.net:1022`, `8818ae5` at audit time). Where this record and `D.md` disagree on a fact, this record (read fresh from tree and host on 2026-05-27) governs and `D.md` is corrected at Wχ harden via the §X congruence pass.

---

## Goal criterion

W0 succeeds if (a) the pivotal D-development reality — *none of A/B/C is in production; prod serves `8818ae5` (pre-A, 2026-03-28) from a dirty host tree, empty DB, no `image_blobs` volume; three Mongos live-exposed on `0.0.0.0` + UFW `ALLOW`; the backend `snapshot_hash` band, the dead `gallery` stratum, the untyped image-asset shim; the 14-`.cartoon-card`-consumer regression; the live cross-repo `palette_slug` FK into value.js's deployed `palette-api` v2.0.0* — is committed as a fixed-point baseline doc that every successor wave cites; (b) the three new invariants (production parity / code-and-migration-together / token-system-single-source) are recorded by *name* against the live evidence; (c) the §8 brittleness window (the W1 deploy + migration cutover span) is ratified provisional pending Wχ-P2; (d) Wα is dispatched as a **ratification + research-dispatch** wave (not a fresh research wave — the D-development audit already did most of Wα's substantive work; Wα at execution confirms the dev-era findings hold and dispatches at most one narrowed follow-up lane).

## Completion criterion (the paired gate)

W0 closes when:

1. This baseline doc exists at `docs/tranches/D/waves/W0.md`, is reachable from fourier's commit chain, and catalogues every fact below with `file:line` (repo) or pasted SSH output (host).
2. The Wα charter (§5 below) names the ratification lanes + any narrowed follow-up research, with deliverables, KISS-ordered verdict shape, and the W0→Wα→Wχ ordering binding.
3. C is confirmed closed at `complete_with_host_residuals` per `C/FINAL.md`; the C residuals D inherits are catalogued (§3 below).
4. The §8 window is ratified provisional with its restoration wave (W1) and reason (the first real A/B/C deploy on a shared multi-app host, the migration-with-deploy cutover, the live Mongo-exposure closure).
5. The Wχ probe set (P1–P5 per `D.md §3`) is listed with subject + planned PASS criterion (the binding Wχ adversarial-test shape — hardened separately at `waves/Wchi.md`).
6. No source file is touched; no implementation commits; the first source change is W1, post-Wχ.

W0 names an honest successor for anything it cannot land; nothing is claimed proven that is not.

---

## §1 — The pivotal D-development reality (DA4) — none of A/B/C is in production

The single sharpest finding of the 10-lane audit (`SYNTHESIS.md §0 row 1`; `DA4 §0`). Every prior tranche close (A, B, C) recorded `landed` or `complete_with_host_residuals` against the repo boundary; **the live site serves a pre-A build**. The measured host facts (read-only SSH, `DA4 §0`/§2/§3/§4):

| Axis | Live prod reality (SSH, audit-time) | The repo (master `1e47115`) | Gap |
|---|---|---|---|
| Deployed SHA | **`8818ae5`** (`git rev-parse HEAD` in `/var/www/fourier-analysis`) | `1e47115` (post-C close) | prod predates **all of A, B, C** |
| Backend image | built **2026-03-28** (2 months old) | n/a | every A/B/C `api/**` change is absent on prod |
| `MONGO_URI` (live container env) | inline **plaintext password** + `tlsAllowInvalidCertificates=true` | `${MONGO_PASSWORD:?}` + (W2 target) `tlsCAFile` | the C.W2 cutover unshipped; prod compose is hand-edited |
| Mongo CA on host | `CN=mbabb.fridayinstitute.net, O=mbabb, C=US`, 3-year self-signed, FP `44:86:A9:…:B9:70` | `gen-mongo-certs.sh` target `CN=fourier-internal-ca, O=fourier-analysis, OU=infra`, 10-year | the C.W2 cert provenance unshipped + foreign-subject (a `gen-mongo-certs.sh` reuse against existing names is impossible — the script keys on `mongo-ca.pem` subject, and the live CA is a different subject; operator decides `FORCE_CA=1` clobber or accept the live subject) |
| `image_blobs` volume | **does not exist** (`docker volume inspect image_blobs` → error); no `/data/blobs` in the backend container | declared `external: true` in `docker-compose.prod.yml:101-103` | the C.W5 blob backend unshipped; a naive `git push` deploy of current master would **fail** to start the backend (external volume missing) |
| Blob migration | **never run** (and prod `images` collection has **0 documents**) | `api/scripts/migrate_image_blobs.py` exists | nothing to migrate yet at first deploy — the cutover is a no-op against an empty collection, a simplification |
| Deploy-hook wiring | live shared dispatcher's inline fourier arm (the **weak** one — `grep -c flock` → 0, `grep -c porcelain` → 0; `build … \|\| build …` fallback that defeats `set -e`; `up -d` only rollback with no rebuild, no re-gate) | `scripts/deploy-hook.sh` (the four C.W1 improvements live in tree, untouched on host) | the C.W1 hook never wired into the fourier arm; the four improvements absent from prod's deploy path |
| Fourier deploys ever | **zero** (`/opt/deploy/logs/` has only `mkbabb-speedtest-*` + `mkbabb-words-*`) | n/a | the webhook chain has never fired for fourier; G10/G11 (the C-pending chain transcript + rollback proof) remain host-pending, now D.W1's to discharge |
| Host fourier tree | **dirty + stale** — `M docker-compose.prod.yml`, `M docker-compose.yml`, `?? ssl/`, HEAD `8818ae5` (unchanged since C audit) | clean tree on master | a blind `git reset --hard origin/master` (the live dispatcher's first move) would **silently discard the modified tracked compose files** that carry the inline password + the RW source mounts |

**Net**: the live site IS up and healthy (`/api/health` → `{"status":"ok"}` on `:8100`, `/` → `200`, fronted by host Apache `fourier.babb.dev`), but it serves a pre-A build. "Integrated and deployed" for D means, in order: (1) reconcile the dirty/stale host tree; (2) close the live Mongo exposure (§1.2); (3) ship master through a wired deploy-hook + run the migration in the same cutover; (4) provision + cut over verified-TLS; (5) split the domain (`fourier.babb.dev`/`api.fourier.babb.dev`) + promote the staged precepts. All four C host residuals are **confirmed unshipped** — now actionable with prod SSH.

### §1.1 — The shared multi-app host (the blast-radius ledger)

Five compose projects co-tenant the AWS EC2 host (`34.197.214.67`, Ubuntu 22.04, **497 GiB disk, 24% used** — the `project_infra_plan.md` "15 GiB" premise is stale by an order of magnitude, `DA4 §0`/`NA1 §1`):

| Project | working_dir | Mongo bind | Gateway | Owned by |
|---|---|---|---|---|
| **fourier-analysis** | `/var/www/fourier-analysis` | `0.0.0.0:27017` | nginx `127.0.0.1:8100` | this repo |
| **floridify** (= words) | `/home/mbabb/floridify` | `0.0.0.0:27018` | nginx `127.0.0.1:8110` | mbabb sibling |
| **palette-api** (value.js backend) | `/home/mbabb/Programming/palette-api` (standalone repo — **NOT** `value.js/api/`, a provenance discrepancy Wα-R3 reconciles) | `0.0.0.0:27020` | api `127.0.0.1:8130` | value.js / cross-repo |
| **csp-solver** (sudoku) | `/var/www/csp-solver` | — (stateless) | nginx `127.0.0.1:8120` | mbabb sibling |
| **speedtest** | `~/speedtest` (orphaned image fragments seen) | — | `:8140` (dead vhost) | already on CF Pages |

Apache2 (`/etc/apache2/sites-enabled/`) is the single TLS terminator on the box; `babb-dev.conf` carries the shared multi-domain vhost for `fourier/sudoku/words.babb.dev`. Host nginx + caddy are inactive (`DA4 §5`). The blast-radius rule (binding on every D wave): any change to a host-wide artefact (the dispatcher, the Apache config, a host-Mongo, a host port, the host `:80/:443`) touches sibling apps; D's safe move is to **fourier-scope the change** (the fourier arm of the dispatcher, fourier's own Apache vhost, fourier's own Mongo bind, fourier's own CA) and carry shared-infrastructure proposals as named, coordinated, host-ops residuals — never unilaterally imposed (DA6 §1(a) + NA6 §1).

### §1.2 — The live Mongo exposure (the FIRST act of W1, per user direction)

The 6-lane normalization audit's headline (`NA1 §4`/`NA5 §0`, confirmed live by external TCP connect from off-host): **three Mongos publish on `0.0.0.0` and are reachable from the public Internet** at `34.197.214.67`. Confirmed by `ss -tlnp` on the host + `sudo ufw status verbose`:

```
LISTEN 0 4096  0.0.0.0:27017  fourier-analysis-mongo-1
LISTEN 0 4096  0.0.0.0:27018  floridify-mongodb
LISTEN 0 4096  0.0.0.0:27020  palette-api-mongo-1
(+ matching [::]:27017/18/20 IPv6 listeners)

UFW (active, default deny incoming):
  27017/tcp  ALLOW IN  Anywhere    # MongoDB - fourier-analysis
  27018/tcp  ALLOW IN  Anywhere    # MongoDB - floridify
  27019/tcp  ALLOW IN  Anywhere    # MongoDB - speedtest   (stale rule — no listener)
  27020/tcp  ALLOW IN  Anywhere    # MongoDB - palette-api
```

Triple-allowed by (a) the docker `0.0.0.0` publish, (b) the explicit UFW `ALLOW IN Anywhere` per port, (c) the AWS SG permitting the ports. **Only SCRAM `--auth` + `requireTLS` with `tlsAllowInvalidCertificates=true` (unverified) stands between the public Internet and each DB**; the Mongo credentials are **committed plaintext in the compose files** (`palette-api/compose.yaml`, `floridify/docker-compose.prod.yml`) or hand-edited inline on the host (fourier — DA4 §1.2). This is a **live, observable security exposure**, not theoretical (NA1 §4b/§5).

Per user direction (PROGRESS log 2026-05-27, "resolutions folded"), the fix moves to W1, **front-loaded as the FIRST act of the implementation phase**:
- compose `ports:` `0.0.0.0:` → `127.0.0.1:` or no-publish (the backend reaches `mongo:27017` over the compose network, never needing a host bind);
- withdraw the four UFW `ALLOW IN Anywhere` rules (27017–27020, including the stale 27019/speedtest rule);
- across **all three apps** (fourier owns its own bind; floridify + palette-api are cross-app and require coordinated host-ops touches with the sibling/value.js sides).

The fourier-side bind is fourier-D's to land directly; the floridify + palette binds are constellation-flagged (proposed + coordinated, not unilaterally imposed — they touch sibling app stacks). The Wχ-P5 probe interrogates whether the rollout truly closes the exposure across all three without breaking the co-tenants.

### §1.3 — The shared deploy dispatcher (the C5 reconciliation reasserted at D)

The live `/opt/deploy/scripts/dispatch.sh` (read-only, DA4 §2.3) is a **multi-repo `case "$REPO" in …`** dispatcher serving four sibling repos in addition to fourier (`mkbabb/{fourier-analysis,words,speedtest,value.js,csp-solver}`). C.W1 (`C/waves/W1.md §1`/§3) recorded the dispatcher's four absent improvements (no `flock`, blind health gate, no rebuild-on-rollback, no dirty-tree guard, `build … || build …` fallback that defeats `set -e`) and the disposition: **fourier-D wires the fourier arm to `scripts/deploy-hook.sh`** (one `case` arm pointer change, fourier-scoped); the shared-dispatcher rewrite is carried as a named host-ops residual (constellation-flagged, requires coordination across the four sibling repos). D.W1 inherits this disposition unchanged: it touches the fourier arm only.

### §1.4 — The host secret-permission posture (DA4 §2.2 — recorded finding, host-ops)

```
$ ls -la /opt/deploy/hooks.json /opt/deploy/.env
-rw-rw-r-- 1 mbabb mbabb  849  /opt/deploy/hooks.json   (0664, world-readable)
-rw-rw-r-- 1 mbabb mbabb   80  /opt/deploy/.env         (0664, world-readable)
```

The HMAC secret + `WEBHOOK_SECRET` live in two world-readable files on the host (the **same secret value** in both — DA4 §2.2). Any local host user (including a read-only recon SSH like the D-development audit) can read the secret and forge `X-Hub-Signature-256`. The trigger rule is the invariant-19 model (HMAC + ref-match), live and correct in shape; the perms are looser than the C-staged precept recommends. **W1 dispositions** (per `D.md §3` W1 row): `0664 → 0600` hardening on `hooks.json` + `/opt/deploy/.env`; the HMAC secret rotated in lockstep with the perm change (GitHub webhook config + `hooks.json` updated together).

---

## §2 — The design-debt catalog (β surfaces — DA-design A1–A4)

The 4-lane design audit (`docs/audits/runs/2026-05-27-D-audit/design/DA-design-A1..A4.md`) found the shipped UI carries surgical debt — strong identity (serif `ℱ` wordmark, cut-paper cards, the sun↔moon Fourier-morph toggle, a real glass-ui light/dark token system) but a cluster of regressions a glass-ui bump silently introduced.

### §2.1 — `.cartoon-card` is a DEAD class — the headline cross-screen regression (A4 #1)

glass-ui removed the `.cartoon-card` recipe at C.W5 (`@mkbabb/glass-ui dist/styles/cards.css:2`: "recipe classes were removed"); the class is defined NOWHERE — not in glass-ui's built CSS, not locally. Live-confirmed on `/equation`: `getComputedStyle('.cartoon-card')` → `border-width: 0px; background: rgba(0,0,0,0); box-shadow: none`. Yet **14 components still apply `class="cartoon-card"`** for their surface:

`EquationView.vue:229,238,242,250,307` · `FunctionInput.vue:93,171` · `EqCoefficientsPanel.vue` · `InfoCard.vue:18` · `MorphPhaseConfig.vue:2` · `HarmonicLevelGrid.vue:2` · `MorphShapePreview.vue:4` · plus the workspace/gallery cards.

Every one renders flat and borderless — the intended 2px cartoon border + offset-stamp shadow + hover-lift is gone (visible in the light-mode renders captured at `screens/d-equation.png` + `screens/d-paper.png`). **The single biggest design lift in the entire D thread**: one fix (a `@utility cartoon-card { @apply cartoon-surface; … }` shim in `web/src/style.css`, mirroring glass-ui's `cartoon-surface` utility at `cards.css:~40`) restores all 14 surfaces at once. This is the new invariant 3's testable gate at `D.md §2` ("the token system is the single source of surface truth"): `git grep cartoon-card` → zero dead refs (the class lives or every consumer migrated to `<Card surface="cartoon">`). W4's headline.

### §2.2 — The workspace upload IA (A2 #A2-01)

The empty workspace state shows **three upload affordances simultaneously**:
- canvas-center placeholder (`web/src/lib/canvas-drawing/placeholder.ts:24-60` + `VisualizationView.vue:198`, "Drag & drop an image here", hardcoded `rgba(150,150,150,0.6)`);
- right-panel `ImageUpload.vue:88-110` dashed dropzone ("Drop an image or click to upload", token-driven);
- global drag-overlay (`VisualizationView.vue:144-153`, "Drop image anywhere").

Three competing CTAs for one action, with divergent copy, styling, and capability statement. W4 disposition: **one hero dropzone in the canvas** + a **slim source-strip in the panel** (which expands to thumbnail/replace only once an image exists; the global overlay stays — it appears only mid-drag).

### §2.3 — The gallery orphans (A3 #1)

`web/src/components/visualization/gallery/GalleryMarquee.vue` and `gallery/GalleryGrid.vue` are imported by **nothing** (grep across `src/` returns zero references). `GalleryView.vue` wires only `GalleryInfiniteGrid` + `GalleryFeaturedCarousel`. The marquee — the most distinctive motion idea in the surface (dual auto-scroll tracks) — is built, styled, and never mounted. W4 disposition (one of two): either **mount the marquee** as a living empty-state band (transforms the cold "No visualizations yet" dead-end into a preview of what the gallery becomes), or **delete both** orphans. Pair with a real `→ Open the Visualizer` CTA on the empty state (currently inert text, A3 #12).

### §2.4 — The light-mode contrast cluster (A1 #2–3, A2 #A2-02–03, A3 #3, A4 #3–4)

Systematic WCAG-AA failures in light mode (dark mode passes all measured surfaces, AAA on muted-foreground):
- `--viz-amber` light = `hsl(35 70% 42%)` = **3.55:1** on the light page — under the 4.5:1 floor; this is the active nav-trigger label + icon (`AppHeader.vue:187-190,326-329`) on **every route**.
- Hardcoded `#f0b632` text = **1.76:1** in light mode (`EquationView.vue:420-421` `.eq-coeff:hover`, `FunctionInput.vue:247-249` `.is-auto-active`); also a token-bypass.
- Canvas placeholder text `rgba(150,150,150,0.6)` = **3.05:1** (`placeholder.ts:33,41,55-59`); the box stroke at **1.47:1** is the affordance boundary reading nearly invisible.
- `text-muted-foreground/60` and `/70` modifiers on `ImageUpload.vue:47,107` = **3.28:1 / 3.96:1** — the opacity-modifier-on-an-already-muted-token anti-pattern; drop the modifier (the base token is 6.76:1, already AA).
- Card timestamps `text-foreground/35` (`GalleryCard.vue:98`, `GalleryCardModal.vue:108`) = **~1.9:1** in dark mode — the alpha-on-foreground anti-pattern.
- Section-color smalls (`theme.css:476,481,491-499`): light `--section-color-5` amber 3.54:1, `--section-color-4` forest 4.45:1 — both under 4.5:1 for small `section-number`.

W4 disposition: one token sweep — darken light-mode `--viz-amber`/`--section-color-5` toward `hsl(35 76% 35%)` (≈4.6:1) as a glass-ui carry; replace the `#f0b632` text hardcodes with `var(--viz-amber)`; replace the canvas literals with the resolved `--muted-foreground` hex (`lib/colors.ts:90` `resolveVizColors` already exposes the pattern); drop the `/60`/`/70`/`/35` alpha-on-foreground modifiers in favour of the base muted token. One axe light-mode pass on the measured surfaces is the close gate.

### §2.5 — Missing `:focus-visible` rings (A1 #1, A3 #4–5)

The glass-ui `Button` base `variant="ghost"` carries **no `focus-visible:` ring** (verified `dist/glass-ui.js:270`); the paper sidebar TOC links (`PaperSidebar.vue:215-221`) override `background:none;border:none` and add no focus style. A keyboard user tabbing the TOC sees nothing. The gallery `GalleryCard` is a bare `<div @click>` (`GalleryCard.vue:63-67`) — not focusable, no role, no keyboard handler; the whole gallery is mouse-only. The `GalleryCardModal` is a non-dialog div (no `role="dialog"`, no focus trap, no aria-labelledby — A3 #5).

W4 disposition: add `:focus-visible { outline: 2px solid var(--ring); outline-offset: 2px; }` to `.sidebar-link`, `.floating-toc-item`, `.callout-btn`, and make `GalleryCard` a real control (`role="button"`, `tabindex="0"`, `@keydown.enter.space`, `:aria-label`); re-point `GalleryCardModal` onto the glass-ui `<Dialog>` primitive (already used for the batch + flagged confirms — `GalleryView.vue:381`, `AdminFlaggedPanel.vue:264`).

---

## §3 — The backend NO-legacy catalog (γ — DA1 §1.1, §3.1, §3.3; the symmetric debt)

C's NO-legacy discharge was **frontend-only**. The W4 gate `git grep -nE "snapshot_hash|snapshotHash" web/src` was `web/src`-scoped by construction — true for the frontend, silent on the backend. The backend still speaks `snapshot_hash` as a live, written field name across three collections, plus carries dead-stratum substrate and an untyped shim that *caused* the C9/C10 bugs.

### §3.1 — The backend `snapshot_hash` band (the symmetric W4 debt)

- **`flags` collection is LIVE and `snapshot_hash`-keyed.** `api/routers/admin.py:218,357,468,530,606` read/delete/aggregate on `{"snapshot_hash": …}`; `api/services/database.py:125-126` create a **unique `(snapshot_hash, reporter_slug)` index** + a plain `snapshot_hash` index. The value stored is actually the visualization's `content_hash` (`admin.py:218` `db.flags.delete_many({"snapshot_hash": content_hash})`). **This is the exact legacy-name-behind-a-value pattern invariant 20 forbids** — a `content_hash` value wearing the retired `snapshot_hash` name — surviving on the backend after W4 fixed its frontend twin.
- **`snapshots` collection indexes created on a DEAD collection** — `database.py:67-69` create two unique `snapshot_hash` indexes on `_db.snapshots`, but the `snapshots.py` router was deleted at B.W3 and no live code reads/writes `db.snapshots` (only `migrate_visualization.py` + the migration test).
- **Models still carry `snapshot_hash` fields** — `api/models/gallery.py:41,70` (`GalleryEntryResponse`, a flag model), `api/models/admin.py:70`, `api/models/assets.py:51`. `git grep -nc "snapshot_hash" -- api/` → 11 sites in `migrate_visualization.py`, 9 in `admin.py`, 5 each in `database.py`/`models/gallery.py`/conformance.

W3 disposition: rename the backend `snapshot_hash` band → slug-identity (the `flags` field + index `database.py:125-126`, the 9 `admin.py` sites, the model fields); the dead `snapshots` indexes deleted (`database.py:67-69`). Symmetric to C.W4's frontend discharge. Gate: `git grep -nE "snapshot_hash|snapshotHash" api/` → zero on identity paths.

### §3.2 — The dead `gallery` collection stratum (DA1 §3.1)

`api/routers/gallery.py:32-35 _entry_from_doc` is **dead** — its docstring claims `admin.py` consumes it for moderation over the legacy `gallery` collection; `git grep -nE "_entry_from_doc|import gallery" api/routers/admin.py` → not referenced. `admin.py` operates entirely on `db.visualizations` (zero `db.gallery` reads). `_entry_from_doc` is the only consumer of `GalleryEntryResponse` (`models/gallery.py:37`) — both dead. The `gallery` collection has no live writer (`git grep -nE "gallery\.(insert|update|replace)" api/` → only `test_migrate_integration.py`); the only live `db.gallery` touches are `database.py:82-93` creating **9 indexes** (including a unique `snapshot_hash` index and a `user_slug` index — both retired names) on every boot, and the janitor cascade-*deleting* from it (`janitor.py:239,407`).

W3 disposition: drop the `gallery` collection, its 9 `init_db` indexes (`database.py:82-93`), the dead `_entry_from_doc` + `GalleryEntryResponse`, and the janitor's two gallery-cascade branches. Net: less boot-time index churn, one fewer collection, the retired `snapshot_hash`/`user_slug` index names gone, ~40 LOC of dead router/model removed. Pairs naturally with §3.1.

### §3.3 — The untyped image-asset `dict` shim (DA1 §3.3 — the cause of C9/C10)

`image_storage.py:189-199 image_bytes(asset: dict)` resolves `storage_uri` → file correctly (no dual-read, idiomatic — the C9 fix). The accidental complexity is that the *whole* asset model is an untyped Mongo `dict` threaded through `dependencies.py:104`, `routers/images.py:178`, and `image_storage.py:106,207` — every consumer does `asset.get("storage_uri")` / `doc["image_slug"]` against a shapeless dict. The C10 bug (the `{"blob": 1}` projection starving the shim, `dependencies.py:91-98`) was *caused* by exactly this untypedness — a projection no type system would have caught. The validation matrix's headline finding (`validation-matrix.md §2`, "`KeyError: 'storage_uri'`") is the same class: `api/routers/images.py:140,159` subscript `doc["storage_uri"]` rather than the `.get()` shim, so a pre-W5 (unmigrated) doc 500s.

W3 disposition: lift the image asset to a Pydantic model (mirroring the `api/lib/crud` discipline); `images.py:140,159` resolve through the typed shim so the projection→field-presence coupling is a typed contract, not a runtime `KeyError` waiting in a broad `except`. This is the kind of "fix-at-ROOT, idiomatic" architectural transposition the user mandated — it would have prevented C9 and C10 structurally.

### §3.4 — One stale docstring (DA1 §1.2 — fold into γ cleanup)

`api/services/image_storage.py:1` still reads `"""Asset-based image and contour storage (MongoDB documents with Binary blobs)."""` — stale after the C.W5 relocation. A one-line doc-truth gap, not load-bearing. Bundled with the §3.3 typed-asset transposition.

---

## §4 — The cross-repo CRUD cohesion catalog (δ — DA3, CRUD-COHESION.md)

The B thesis ("one identity model across fourier AND value.js") was landed fourier-unilaterally; value.js-C (the cohort peer) was RETIRED. The D-development audit established the cohesion target is **live, not academic**:

- **value.js ships a deployed `palette-api` v2.0.0** — in-repo at `value.js/api/` (Hono + MongoDB + Zod, ~70 `.ts` files, its own `Dockerfile`/`compose.yaml`), **deployed + healthy on prod** (`palette-api-api-1`, HTTP 200, 2 months uptime). Distinct from the npm library `@mkbabb/value.js` v0.10.0 (`value.js/src/`).
- It is **richer** than fourier's CRUD (version history, forks, votes, proposed-names, an 8-route admin tree) but **divergent on ~11 contract clauses** (DA3 §3): a top-level `id` field (`value.js/api/.../format/palette.ts:59` — direct C1.3 violation), client-supplied non-4-word slugs, a single 4-state `status` instead of 3-state `visibility`+`tier`, **hard cascade delete with no soft-delete/grace/restore**, an admin `feature` toggle (not the contract's idempotent setter), and **none** of the SOTA envelopes (no problem+json, no ETag/If-Match, no RateLimit headers, no Idempotency-Key).
- **The link is LIVE**: fourier's `visualization.palette_slug: str | None` (`api/models/visualization.py:119,163,177`; `web/src/lib/api.ts:41,65,73`; ETag field list `lib/crud/etag.py:14`) is a **dangling slug-FK into value.js's palette noun**. A visualization can reference a palette by slug. The "one identity model" is not academic — the two nouns are already linked by slug.
- **The provenance discrepancy** (DOMAIN-NAMING §3, NA4 §0): prod `palette-api` deploys from the **standalone repo `/home/mbabb/Programming/palette-api`** on the host (rsync'd, deploys via the dispatcher's `mkbabb/value.js` arm), **NOT** `value.js/api/` where the source code lives. Wα-R3 reconciles which: a separate repo, a checkout of `value.js/api/`, or a divergent copy. This bears on both the rename and the δ cohesion.

W5 disposition: re-author `CRUD-CONTRACT v2.0.0` with **two KISS relaxations** (CRUD-COHESION §3, DA3 §3 "what cohesion looks like"):
1. **§2 admits user-supplied slugs** — the original §2 over-specified to fourier's `coolname`/4-word accident; value.js's human-named palettes are legitimate. Cohesion binds slug *identity* (one human-readable slug, unique, no hash), not the word-count.
2. **§0 binds behaviour, not module layout** — value.js keeps its service+repository+events+DI architecture (the D.W2 `626b107` parallel-evolution win, DA3 §1 last row); the contract binds the observable CRUD behaviour (identity, visibility, soft-delete, the SOTA envelopes), **not** fourier's `api/lib/crud/` file shape.

fourier-side is light (re-author the contract; flip the ~88 DEFERRED conformance-matrix cells against the live `palette-api`; record the value.js ask). value.js-side is heavy (the I.W1–W4 visibility split + soft-delete + SOTA envelopes + conformance suite per DA3 §4) and **user-re-mandate-gated** — fourier-D does not author value.js's tranche. The `palette_slug` FK contract (what fourier guarantees about the slug it stores, what value.js guarantees about resolving it) is the one binding cross-repo artefact Wα-R1 produces. The colour-lift (`sampleToSVGPath`) is **orthogonal** (DA3 §4 "two distinct value.js surfaces") — it touches `value.js/src/` (the library), not `value.js/api/` (the backend) — and rides as a bounded W5 sub-item, fires iff value.js publishes; else it stays a named residual (v0.10.0 does not export it).

---

## §5 — The constellation deployment baseline (α′ — CONSTELLATION-DEPLOY.md, NA1–NA6)

The 6-lane normalization audit folded a new thread α′ into D, sequenced as W8–W12 after the fourier pilot (W1/W2/W9). The binding plan is `coordination/CONSTELLATION-DEPLOY.md`; the NA lanes are the evidence (~2,047 L). Captured here in summary so subsequent waves cite a single baseline.

### §5.1 — The constellation (NA1 §2, NA4 §0)

| App | = repo | Frontend today | Backend today | Target |
|---|---|---|---|---|
| **fourier** | `fourier-analysis` | mbabb nginx `:8100` behind `fourier.babb.dev` | docker (Mongo) | SPLIT — `fourier.babb.dev` → CF Pages, `api.fourier.babb.dev` → mbabb (the pilot) |
| **color** | `palette-api` (standalone on host) + value.js demo | GH Pages (`color.babb.dev` → `mkbabb.github.io`) | docker Hono `:8130` | `color.babb.dev` → CF Pages, `api.color.babb.dev` → mbabb; rename palette-api → color (user-gated) |
| **sudoku** | `csp-solver` | mbabb nginx `:8120` (`sudoku.babb.dev`) | docker `:8000` (stateless) | SPLIT — frontend → CF Pages, backend → mbabb `api.sudoku.babb.dev` |
| **words** | `floridify` | mbabb nginx `:8110` (`words.babb.dev`) | docker `:8001` (Mongo) + SSE + notification server | **ALL-MBABB** — same-origin wins; split would be net-negative |
| **grammar** | `bbnf-lang` | static Vite via Apache (`grammar.babb.dev`), no container | none (`server/` is `bbnf-lsp`, an editor LSP, not a web API) | static → CF Pages, **DEFERRED** (1009 commits/14d, author-coordinated quiet window) |
| **keyframes.js** | `keyframes.js` | GH Pages (`keyframes.babb.dev`) | none | fully CF Pages |
| **speedtest** | `speedtest` | already CF Pages | CF Workers + EC2 API | already normalized — the reference recipe |

Support services (not apps, NA1 §2): `code-server :8080`, host MySQL `:3306`, the `adnanh/webhook :9000` deploy receiver, Apache. Dangling images (`gaggle`, `server-api`, two `speedtest-*`) + a dead `speedtest :8140` vhost — cleanup carries to W12.

### §5.2 — The `api.<app>` TLS path — RESOLVED (CONSTELLATION-DEPLOY §3.2)

The user's "re-add direct DNS records?" intuition resolved the earlier framing (ACM ~$10/mo vs `<app>-api.babb.dev` rename): **`api.<app>.babb.dev` is DNS-only (grey-cloud) A → origin `34.197.214.67`**, the mbabb Apache serves a Let's Encrypt cert (certbot is already on prod with a live `/etc/letsencrypt/live/sudoku.babb.dev/fullchain.pem` covering fourier/sudoku/words as SANs; `certbot --expand` with DNS-01 via the CF token adds the api.* SANs). LE has no subdomain-depth limit (the CF Universal-SSL single-level constraint applies only to orange-cloud edge TLS, not LE). **Free, exact naming, no ACM, no rename.** Frontends remain CF-Pages proxied (single-level `<app>.babb.dev`, free Universal SSL covers). Tradeoff: lose CF CDN/WAF on the api subdomains — acceptable (APIs don't need a CDN; the origin IP is already public).

### §5.3 — The CF token discipline (NA6 §2, CONSTELLATION-DEPLOY §6) — binding

The CF API token is **NEVER committed or written to any tracked file**. It IS saved (per user direction, 2026-05-27) in **gitignored `.env`s** at `fourier-analysis/.env` and `value.js/.env` (`0600`, verified gitignored via `git check-ignore`), plus the CI provider's secret store (GitHub Actions) when CI publishes Pages. Referenced by name (`CLOUDFLARE_API_TOKEN`) — never echoed in shell history, logs, commits, or chat. **The user's provided perm-set is sufficient** (broader than the minimal `Zone:DNS:Edit` + `Pages:Edit` + `Zone:Read` the plan needs; SSL/Certificates perms are NOT needed under grey-cloud + LE). **Do NOT rotate** (per user direction); rotate only on suspicion. W12 close records this disposition as a precept (under `docs/precepts/infra/`).

### §5.4 — Pilot-then-rollout ordering (binding, NA6 §3)

**fourier is the pilot.** W1/W2/W9 prove the full pattern end-to-end on one app on the shared multi-tenant host (the backend deploy + verified TLS + the domain split + the CF-Pages frontend), with blast radius = one app. Verified via the ε prod matrix at W6, **then** the proven recipe rolls to the co-tenants bounded-parallel (W10 + W11 — never a big-bang). Grammar **explicitly deferred** (active dev, author-coordinated). The 4-agents/wave ceiling holds (NA6 §3 + DA6 §5 — reject one-agent-per-app fan-out).

---

## §6 — The three new invariants (production parity, code-and-migration-together, token-system-single-source)

`D.md §2` records three new invariants by name (the numbering inherits the C-era 18/19/20 known doc inconsistency — a γ sub-item reconciles at execution, binding by name not number meanwhile). Recorded here against the live evidence:

### §6.1 — Production parity (the DA4 lesson)

A tranche does not close as "landed" while production serves a pre-tranche build. The deployed prod HEAD equals the closed-tranche HEAD (or the gap is a named, dated residual with a deploy date). **Live evidence**: prod `8818ae5` (2026-03-28, pre-A); the repo at `1e47115` (post-C close). A/B/C all "closed" while prod ran a build predating all three. **Testable gate** (`D.md §6`): the recorded commit-to-deploy chain shows prod at D-HEAD; `curl` of the live `/api/health` + a build-stamp confirms it; a rollback proof (intentional bad commit) is captured.

### §6.2 — Code and migration cut over together (the validation-matrix lesson)

A storage/schema migration and the code that assumes its post-condition deploy atomically, in **every** environment (local, dev, prod); no environment runs code ahead of its migration. **Live evidence**: the local e2e ran red (`validation-matrix.md §2`) because the W5 serving code subscripts `doc["storage_uri"]` (`api/routers/images.py:140,159`) on a pre-migration doc — the local DB carried inline `blob` docs, the migration had run nowhere. The W5 deletion-proof removed dual-read by design (the NO-legacy precept) — the code is correct only against a migrated DB. **Testable gate**: the deploy chain runs the migration in the same cutover that ships the code; a post-deploy probe finds zero unmigrated docs. Pairs with `D.md §8` (the brittleness window is the W1 deploy-chain span only — no dual-read; rollback restores the prior build).

### §6.3 — The token system is the single source of surface truth (the design lesson)

No dead style class is referenced (`.cartoon-card`), no hardcoded colour stands where a token exists (`#f0b632`, `rgba(150,150,150,…)`, raw `red-400/red-500` Tailwind palette), and every surface resolves through the glass-ui light/dark token system with measured AA contrast in **both** themes. **Live evidence**: §2 above (14 dead-class consumers; the contrast cluster; the `/60`–`/70` alpha-on-foreground anti-pattern). **Testable gate**: `git grep cartoon-card` → zero dead refs; an axe light-mode pass clean on the measured surfaces.

---

## §7 — The §8 brittleness window — RATIFIED PROVISIONAL (Wχ-P2-gated)

`D.md §8` declares a possible W1 brittleness window — the span of the first deploy (build → health-gate → migration → cut). The filesystem cutover is **atomic per-document** (proven at `C/audit/challenge-P3.md` "C.Wχ-P3"; the migration script writes the file, then sets `storage_uri`, then deletes the inline `blob` in one document update — `blob` XOR `storage_uri` holds per-document at every instant). So there is **no dual-read** required; the window governs only the deploy-chain span (build, health-gate, migration-cutover, cut), and rollback (the deploy-hook's `reset --hard $PREV` + rebuild + re-gate, per C.W1 `scripts/deploy-hook.sh`) restores the prior (pre-A) build if the gate fails.

```yaml
breaking_changes_during_wave: maybe (W1)
suspended_gates:
  - the live site during the first-deploy build+migration cut
restoration_wave: W1 (the deploy completes within it; rollback restores prior build)
reason: the first real A/B/C deploy replaces a pre-A build on a shared host; the
        window opens for the build+health-gate+migration cut and closes on a green
        gate. Wα-R2 + Wχ-P1/P2 determine whether the shared-host co-tenants
        (floridify, palette-api) are touched (they should not be — fourier's
        containers + volume + CA are isolated; §1.1, §1.2) and whether the
        migration-with-deploy is atomic + rollback-safe on real data. No
        dual-read (the filesystem cutover is atomic per-doc, C.Wχ-P3).
```

W2–W12 close green with no window (the α′ DNS / Pages / ingress cutovers are individually reversible — a DNS record flip back, a Pages rollback, a vhost revert — and bounded to one app at a time after the fourier pilot). W1 owns its own restoration; the close ceremony cannot run while the window is open. Wχ-P2 measures whether the W1 deploy-chain on real prod data is truly atomic + rollback-safe; the window stays declared until Wχ closes.

---

## §8 — C residuals D inherits (the actionable-with-SSH list)

Per `D.md §7` + `C/FINAL.md §6`. With prod SSH now available, the residuals C closed `complete_with_host_residuals` become D deliverables:

| Inherited from | What | Lands at |
|---|---|---|
| C.W1 | Wire fourier arm to `scripts/deploy-hook.sh`; hook secret hardening `0664 → 0600`; dirty-tree reconcile; first commit-to-deploy chain transcript (G10) + intentional-bad-commit rollback proof (G11) | **W1** |
| C.W2 | `gen-mongo-certs.sh` host run; `infra/tls.md §9` 3-site diff (`docker-compose.prod.yml:8,53,58`); live verified-cert ping (`tlsCAFile`, no invalid-cert flag) | **W2** |
| C.W5 | `docker volume create image_blobs` precondition (`docker-compose.prod.yml:101-103` `external: true`); run `migrate_image_blobs.py` against prod data (no-op against the empty DB at first deploy, but the script must ride the cutover) | **W1** (volume + migration ride the deploy cutover) |
| C.W2/W6 | Promote staged `docs/tranches/C/infra/{tls.md, blob-backend-dr.md}` + the deploy precept into the `docs/precepts/` submodule (the submodule seam at `C/waves/W1.md §2.4` / `W2.md "Commit note"`); add the `<app>.babb.dev`/`api.<app>.babb.dev` naming precept | **W2** |
| C.W4 | The frontend `snapshot_hash` discharge is symmetric to the backend band catalogued in §3.1 above | **W3** |
| C various | `--reload` background queue (dev-only) — held as a named residual unless ε surfaces a need; the C4.5/C4.6 visibility-transition guard (decide at Wα — intersects contract v2.0.0) | **W3 or δ** |

Out of D entirely (per `D.md §7`): multi-replica fourier (inv-19); the full value.js `Palette`/`colorScale` domain model (inv-15, library nobody calls); the frontend bundle split (`index` 867 kB, `PaperView` 471 kB — perf only).

---

## §9 — Wα research dispatch — the ratification charter

**Crucial framing** (`D.md §3` row Wα): the D-development audit already did most of Wα's substantive work. The 10+6 lanes covered R1 (CRUD-contract design — DA3, CRUD-COHESION.md), R2 (deploy-safety — DA4, DEPLOY-RECONCILE-equivalent in CRUD-COHESION + the precepts notes), R3 (ingress + DNS + TLS-path resolution — DOMAIN-NAMING.md + NA5, CONSTELLATION-DEPLOY §3.2), R4 (the constellation matrix — NA1–NA6, CONSTELLATION-DEPLOY). Wα at execution is therefore a **ratification + research-dispatch** wave: confirm the dev-era findings hold against the live tree at execution-time, dispatch any narrowed follow-up research (a single lane if anything genuinely changed since 2026-05-27), and produce the binding research-contracts index.

Wα closes when (a) every dev-era finding is re-verified against the live tree + host at execution-time, with deltas (if any) recorded; (b) the binding research-contracts index lands at `docs/tranches/D/research/README.md` referencing the existing audit substrate; (c) any narrowed follow-up research lane has authored its artefact + KISS-ordered verdict; (d) the Wχ probe set (P1–P5) is fully scoped against the ratified evidence.

**Ratification lanes** (the dev-era audit lanes whose findings Wα confirms; no fresh research needed unless a delta surfaces):

| Lane | Subject | Dev-era evidence | Wα ratification check |
|---|---|---|---|
| **R1** | CRUD-CONTRACT v2.0.0 design + fourier↔value.js clause map + the `palette_slug` FK contract | `DA3-valuejs-crud-cohesion.md`, `coordination/CRUD-COHESION.md`, the existing `B/coordination/CRUD-CONTRACT.md` + `CONFORMANCE-MATRIX.md` | confirm the ~11 divergent clauses still hold against live `palette-api` (HTTP 200, 2 months uptime per `validation-matrix.md`); confirm the two KISS relaxations (user-supplied slugs admit; behaviour-not-layout) survive adversarial probing |
| **R2** | prod-deploy-safety on the shared host (dirty-tree reconcile, secret extraction, migration-with-deploy ordering, rollback on a live multi-app host) | `DA4-host-deploy-prod.md`, `C/waves/W1.md`, `C/coordination/DEPLOY-RECONCILE.md`, `C/infra/blob-backend-dr.md`, the §1 baseline above | confirm host state hasn't drifted since `2026-05-27` (HEAD still `8818ae5`, tree still `M` on both compose files, `image_blobs` volume still absent, the Mongo binds still `0.0.0.0`); confirm the dispatcher's fourier arm still points at the un-improved logic |
| **R3** | ingress + domain-naming (the `fourier.babb.dev` / `api.fourier.babb.dev` split shape; the loopback-bind for the exposed Mongos; the palette-api provenance discrepancy — prod deploys from standalone `/home/mbabb/Programming/palette-api`, not `value.js/api/`; how `color.babb.dev` GH-Pages reaches the API today) | `coordination/DOMAIN-NAMING.md`, `coordination/CONSTELLATION-DEPLOY.md §1`/§3.2/§8, the 6 `normalization/NA*` lanes | confirm the TLS path (grey-cloud + origin LE) remains the resolution; confirm the palette-api provenance (separate repo on host, not a `value.js/api/` checkout) bears on the rename + δ cohesion |
| **R4** | the constellation matrix + the pilot-then-rollout ordering | `CONSTELLATION-DEPLOY.md §2`/§7/§8 + `NA1`/`NA4`/`NA6` | confirm the per-app split decisions (fourier/sudoku split, words all-mbabb, grammar deferred, keyframes pure CF Pages) survive a live re-probe; confirm the CF token discipline + the four-perm minimum |

**Narrowed follow-up** (dispatch at most ONE lane if a genuine delta surfaces at the ratification check — e.g. the host tree advanced since 2026-05-27 and the dirty-tree reconcile must re-account; the live `palette-api` cut a new minor version since the audit; the CF token perms were silently rotated; a co-tenant app appeared/disappeared). Else Wα proceeds directly to Wχ with a "no delta" close note. Honesty discipline: the dev-era audit was deep; over-research at Wα is gold-plating (DA6 §1).

**No infra/storage/frontend source changes commit before Wχ closes** (Phase 0 discipline, `D.md §4`).

---

## §10 — File bounds + scope honesty

W0 writes **only** the docs below, all under `docs/tranches/D/`:

| File | Action |
|---|---|
| `docs/tranches/D/waves/W0.md` | **create** (this record) |
| `docs/tranches/D/waves/Walpha.md` | **create** (the Wα ratification charter, separate spec) |
| `docs/tranches/D/waves/Wchi.md` | **create** (the Wχ challenge wave spec, separate file) |
| `docs/tranches/D/D.md` | NOT touched (the §X congruence pass lists findings for team-lead reconcile; team-lead edits centrally) |
| `docs/tranches/D/PROGRESS.md` | NOT touched (team-lead updates at wave-close boundaries) |
| `docs/tranches/D/coordination/*.md` | NOT touched |
| any source under `api/**`, `web/**`, `scripts/**`, `docker-compose*.yml` | NOT touched (W0 is open/baseline/dispatch only) |

**What W0 does NOT do** (scope honesty, mirrors `C/W0-baseline.md §6`):
- No source files touched (the first source change is W1, post-Wχ).
- No research verdicts pre-empted — the R1–R4 ratification confirms or refutes the dev-era findings; it does not re-decide them.
- No Wχ probe pre-empted — the P1–P5 verdicts are Wχ's, not W0's.
- No host mutation — every host claim above is read-only, pasted from SSH output. The first host mutation is W1.
- No CF token usage — the token stays in the gitignored `.env`s.

---

## §11 — Hard-gate ledger

Each gate, how it is proven; W0 gates are repo-local (the baseline is documentation).

| # | Gate | How proven |
|---|---|---|
| W0-G1 | C confirmed closed | `docs/tranches/C/FINAL.md` reachable; `git log` shows the C close commits per `C/FINAL.md §2` |
| W0-G2 | Pre-A prod-state baseline committed (`8818ae5` gap, dirty tree, empty DB, missing `image_blobs` volume) | §1 above; every fact `file:line` or SSH-pasted (cross-cited to `DA4`) |
| W0-G3 | Live Mongo exposure baseline committed | §1.2 above; `ss -tlnp` + `ufw status verbose` output recorded (cross-cited to `NA1 §4`, `NA5 §0`) |
| W0-G4 | Design-debt catalog committed | §2 above; the 14 `.cartoon-card` consumers + the 3 upload affordances + the 2 gallery orphans + the contrast cluster + the missing focus rings — every finding `component:line` (cross-cited to `design/DA-design-A1..A4`) |
| W0-G5 | Backend legacy catalog committed | §3 above; the `snapshot_hash` band, the dead `gallery` stratum, the untyped image-asset shim — every finding `file:line` (cross-cited to `DA1 §1.1`, §3.1, §3.3) |
| W0-G6 | Cross-repo CRUD cohesion catalog committed | §4 above; the live `palette-api` v2.0.0, the ~11 divergent clauses, the `palette_slug` FK, the two KISS relaxations, the provenance discrepancy (cross-cited to `DA3` + `CRUD-COHESION.md`) |
| W0-G7 | Constellation deployment baseline committed | §5 above; the 7-app matrix, the TLS-path resolution, the CF token discipline, the pilot-then-rollout ordering (cross-cited to `CONSTELLATION-DEPLOY.md` + `NA1–NA6`) |
| W0-G8 | Three new invariants recorded by name | §6 above; production parity, code-and-migration-together, token-system-single-source — each with live evidence + testable gate |
| W0-G9 | §8 window ratified provisional | §7 above; restoration wave (W1), reason (the first real A/B/C deploy + the Mongo-exposure closure on a shared host) |
| W0-G10 | C residuals D inherits catalogued | §8 above; the table mapping C.W1/W2/W5 host residuals to D.W1/W2/W3 |
| W0-G11 | Wα dispatched as ratification + research-dispatch | §9 above; the four ratification lanes (R1–R4) + the narrowed-follow-up policy + the no-source-change discipline; the binding Wα charter lands at `waves/Walpha.md` |
| W0-G12 | Wχ probe set scoped | The P1–P5 subjects from `D.md §3` recorded above + scoped at `waves/Wchi.md` |

---

## §12 — Dependencies + archaeology

**Depends on**: C closed (`C/FINAL.md`); the 10+6-lane D-development audit landed (`docs/audits/runs/2026-05-27-D-audit/`); `D.md` + `PROGRESS.md` + the three `coordination/*.md` authored.

**Blocks**: Wα (the ratification + research-dispatch wave); Wχ (the challenge wave); every implementation wave W1–W12 cites this baseline.

**Archaeology**: D.W0 mirrors `C/W0-baseline.md` in shape but inverts in posture. C.W0 baselined a repo-only reality (no prod SSH) and dispatched four greenfield research lanes; D.W0 baselines a **live prod + shared-host reality** (prod SSH now available) and dispatches a **ratification** wave because the dev-era audit already did the substantive research work. The W0 → Wα → Wχ gate discipline is identical to C's; what changed is the substrate's depth (D-development was 10 lanes + 6 lanes + 4 design + the validation matrix vs C-development's 6 lanes), so Wα at D-execution honestly has less to do.

---

## §13 — Summary

W0 commits the canonical baseline doc that subsequent D waves cite: the prod-state gap (pre-A `8818ae5`, dirty host tree, empty DB, missing volumes, three live-exposed Mongos), the design debt (14 dead `.cartoon-card` consumers, the triple upload IA, the gallery orphans, the light-mode contrast cluster, the missing focus rings), the backend NO-legacy debt (the `flags` `snapshot_hash` band + the dead `gallery` stratum + the untyped image-asset shim), the cross-repo CRUD cohesion baseline (the live `palette-api` v2.0.0 + the `palette_slug` FK + the two KISS relaxations + the provenance discrepancy), and the constellation deployment baseline (the 7-app matrix, the grey-cloud + origin-LE TLS path, the CF-token-in-gitignored-`.env`s discipline, the pilot-then-rollout ordering). It records the three new invariants by name with live evidence + testable gate. It ratifies the §8 brittleness window as W1's deploy-chain span only (no dual-read). It dispatches Wα as a **ratification** wave — confirming the dev-era findings hold at execution-time, dispatching at most one narrowed follow-up research lane if a genuine delta surfaces, and producing the binding research-contracts index — and scopes the Wχ probe set (P1–P5). W0 touches no source; the first source change is W1, post-Wχ. The strict W0 → Wα → Wχ → implementation gate opens on this record's close.

---

## §X — Congruence findings (for team-lead reconcile)

The following incongruences between this baseline and `D.md` / `PROGRESS.md` / `coordination/*.md` were surfaced during W0 authoring. Do NOT edit centrally from here; team-lead reconciles.

1. **Wave numbering — close wave is W12 in `D.md §3` but referenced as W7 in `PROGRESS.md` "Completion criterion" + `D.md §4` Phase VI ("close (W7)") and §5 ("W7 close").** `D.md §3` table has W12 as the close (the constellation thread α′ added W8–W11 + W12). `D.md §4` Phase VI and §5 still say `W7`. `PROGRESS.md` row count is W0/Wα/Wχ/W1/W2/W3/W4/W5/W6/W8/W9/W10/W11/W12 (13 waves; W7 not listed) — consistent with `D.md §3` but contradicts the prose. Reconcile: prose says W12 throughout; status board row "W12 — Close" is correct; W7 is a phantom number from the pre-α′ schedule.

2. **Invariant numbering — `D.md §2` records "three new invariants" but assigns them by name only because the C-era numbering of its three additions as 18/19/20 already overlaps B's 18–24 range** (`D.md §2`: "a known doc inconsistency; a γ sub-item reconciles the numbering at execution"). The new D invariants therefore remain unnumbered in `D.md`. W0 records them as "production parity / code-and-migration-together / token-system-single-source" by name only (§6 above). Reconcile path: a γ-thread sub-item at W3 close reconciles the global invariant numbering across A/B/C/D and renumbers consistently; until then, binding by name.

3. **Conformance-matrix DEFERRED cell count — `D.md §3` W5 row and `D.md §1 α` both say "~88 DEFERRED matrix cells against `palette-api`"** but the `CRUD-COHESION.md §4` text says "the ~88 DEFERRED matrix cells" and `DA3 §5` "Net recommendation" likewise. The count is consistent across all D-tranche docs. **NO incongruence** — recording for completeness in case the matrix re-author finds the count off.

4. **CANONICAL-ORDERING — `SYNTHESIS.md §6` reconciles to ordering ε post-D-development** ("The synthesis re-authors CANONICAL-ORDERING as ordering ε"). `PROGRESS.md` row "W12 — Close" says "CANONICAL-ORDERING → ordering ε" (consistent). `D.md §3` row "W12" likewise. **NO incongruence** — recording for completeness.

5. **Palette-api provenance — `coordination/DOMAIN-NAMING.md §3` notes "Provenance discrepancy to reconcile (Wα): is prod `palette-api` a separate repo, a checkout of `value.js/api/`, or a divergent copy?"** but `coordination/CONSTELLATION-DEPLOY.md §2` definitively states it is a "standalone rsync, **not** value.js/api" + NA4 §0 likewise. `DA4 §1.1` records the host working_dir `/home/mbabb/Programming/palette-api` distinctly from value.js. The reconcile question in DOMAIN-NAMING is now ANSWERED by NA1/NA4/CONSTELLATION-DEPLOY: standalone repo on the host, deployed from there by the dispatcher's `mkbabb/value.js` arm. Reconcile: DOMAIN-NAMING.md §3 should be updated to cite NA4 + CONSTELLATION-DEPLOY's answer (no longer an open Wα question).

6. **Wα framing — `D.md §3` Wα row lists "R1 + R2 + R3" as the lanes** (with R3 the ingress + domain-naming lane); `PROGRESS.md` row Wα says "R1 + R2; optional R3 value.js alignment-tranche shape". The two disagree on whether R3 is ingress (D.md) or the optional value.js tranche shape (PROGRESS). NA-folding in `D.md §3 Wα` then adds an R3 = "ingress + domain-naming" + an implicit R4 = "constellation matrix" via NA. Reconcile: there are effectively four ratification lanes (R1 CRUD, R2 deploy-safety, R3 ingress + domain-naming, R4 constellation matrix) — `PROGRESS.md` row Wα should be updated to list R1/R2/R3/R4 consistent with `D.md §3` Wα row.

7. **Conformance suite count — `D.md` does not name a count, but DA3 §5 + the value.js-side I.W4 sketch say "the 88 value.js cross-repo conformance cells in CONFORMANCE-MATRIX.md"** vs the `D.md §3` W5 row's "~88 DEFERRED matrix cells". Approximate counts are consistent. **NO incongruence**.

8. **The `8818ae5` SHA discrepancy mentioned in `DA4` Appendix** — the conversation's opening gitStatus snapshot showed `fc5b3b0` (a B-tranche close commit); live `git rev-parse HEAD` at audit time was `1e47115` (the C-tranche close); DA4 Appendix records this. **NO incongruence with D.md/PROGRESS.md** — `D.md` cites `1e47115` consistently; recording for awareness that any future reconcile must distinguish the snapshot SHA from the live tree SHA.

9. **The "secret hardening + HMAC rotate in lockstep" disposition** — DA4 §6 Phase 1 step 4 recommends hardening `hooks.json` + `/opt/deploy/.env` from `0664 → 0600` **and rotating the HMAC secret in lockstep with the perm change** (the secret was world-readable, so the perm tightening is incomplete without rotation). `D.md §3` W1 row says "hook perms `0664→0600`" without mentioning the rotate. Reconcile: W1 row in `D.md §3` should be updated to add "+ rotate HMAC secret in lockstep" to the perm hardening clause (DA4 §6 Phase 1 step 4).

10. **The C4.5/C4.6 visibility-transition guard** — `D.md §7` "Inherited from B/C (the NO-legacy completion)" says "The C4.5/C4.6 visibility-transition guard (struck at C.W4) → **W3 or δ** (decide at Wα — it intersects the contract v2.0.0)". The Wα ratification will need to issue the W3-vs-δ verdict explicitly; reconciling here so it isn't lost in the ratification close.

End of W0.
