# D.W12 — Close (the tranche close)

**Wave**: W12 (close; 1 agent — team-lead). **Status**: scaffold — authored at the Wχ-harden round, **filled at the actual close** once W0–W11 land. This file is the close-ceremony checklist; the narrative close record is `FINAL.md` (authored at close, mirroring `A/FINAL.md` + `B/FINAL.md` + `C/FINAL.md` §0→§9 structure). **Authored**: 2026-05-27. **Predecessor template**: `docs/tranches/C/waves/W6.md` (the C close-scaffold pattern). **Opens after**: every other D wave's hard gates are met or carry a named successor; the §8 brittleness window is restored (closed); no breaking_changes_during_wave remain.

This wave **does not introduce new feature work** — close ceremony is reconciliation + record only.

---

## §0 — Goal criterion and completion criterion (paired)

**Goal criterion (the aim).** Close D honestly: every wave's hard gates are met or carry a named successor; `PROGRESS.md` reconciles claim against artefact without archaeology; `FINAL.md` cites every commit + gate per §6 of `D.md`; the cross-repo δ disposition + the constellation α′ rollout disposition + the W11 user-re-mandate disposition are all recorded; the dangling host-image / dead-vhost cleanup completes (`gaggle`, `server-api` images; the dead `:8140` speedtest vhost — NA1 §3 + NA5 §1.2); the §8 brittleness window is **restored** (the W1 deploy + migration window closed); the CANONICAL-ORDERING advances to **ordering ε** (or **ζ** if more changes warrant); the precept promotions for the α′ constellation work land in `docs/precepts/infra/`; the CF token is **NOT rotated** (per `D.md §7` user direction; recorded verbatim so NA6 §2.3's rotate-after-migration recommendation is explicitly overridden, never silently complied with).

**Completion criterion (the close checklist — each line is a gate).** D closes when all of the below are green or carry a named, dated residual:

### §0.1 — Reconcile

- [ ] `PROGRESS.md` status board: every W0–W11 row carries a status word (from the canonical set: `planned` / `in_progress` / `closed` / `closed_with_residuals` / `held_pending_user_remandate` / `deferred` / `cancelled`) + a close timestamp + a binding-deliverable note, reconciled against the tree (not against claims). Specifically:
  - W0/Wα/Wχ — closed; each row cites the close commit.
  - W1 — closed; the Mongo bind transcript (front-loaded security), the dirty-tree reconcile, the first A/B/C → prod deploy chain transcript, the rollback proof transcript.
  - W2 — closed; the verified-TLS cutover on prod (the §9 3-site diff applied), the precepts promotion of `C/infra/{tls.md, blob-backend-dr.md}` + the new `<app>.babb.dev`/`api.<app>.babb.dev` naming precept.
  - W3 — closed; the backend `snapshot_hash` band gone, the dead `gallery` stratum deleted, the image-asset typed.
  - W4 — closed; `.cartoon-card` resurrected (or 14 components migrated), workspace IA resolved, gallery orphans resolved, light-mode contrast swept, focus rings restored.
  - W5 — closed; the CRUD-CONTRACT v2.0.0 ratified; the value.js alignment ask recorded.
  - W6 — closed; the cross-env Playwright matrix recorded; CI Mongo lands; the rate-limit harness lands.
  - W8 — closed; `scripts/cf-dns.{sh,py}` lives + idempotent; the pilot-tranche A records resolved + the rollout-tranche CNAMEs landed per W9 cutover.
  - W9 — closed; fourier on CF Pages + keyframes/value.js/color/sudoku off GH-Pages / mbabb-nginx onto CF Pages; grammar **DEFERRED** explicitly recorded.
  - W10 — closed; LE expand for `api.fourier/color/sudoku.babb.dev` lives; the three Apache vhosts active; CORS fixes verified per app.
  - W11 — closed OR `held_pending_user_remandate` (recorded honestly); the provenance reconcile artefact `coordination/PALETTE-API-PROVENANCE.md` exists regardless.
- [ ] `FINAL.md` authored — §0 paired criteria → §9 reflection (mirror `C/FINAL.md`); every gate in `D.md §6` cited with the commit that discharged it (or the residual that names its successor); the §6 hard-gate ledger transcribed with evidence; the §7 "carries" list updated.

### §0.2 — Thread α (fourier prod-deploy + TLS) — gates discharged or residual-named

- [ ] **W1**: `fourier-analysis-mongo` no longer publishes on `0.0.0.0`; the three sibling Mongos (palette + floridify) also bound loopback (cross-app, front-loaded security); the four UFW ALLOW rules withdrawn; the dirty host tree reconciled (any host-side overrides reconciled with the repo source); the hook-secret hardened 0664 → 0600; the `image_blobs` volume created; the recorded commit-to-deploy chain transcript captured; the intentional-bad-commit rollback transcript captured.
- [ ] **W2**: prod `docker-compose.prod.yml` source has zero `tlsAllowInvalid*` and zero `--tlsAllowConnectionsWithoutCertificates` (all sites); the live verified-cert ping succeeded; the `CN=fourier-internal-ca` issuer line lives in the **promoted** `docs/precepts/infra/tls.md` (the precept submodule); the deploy precept + the `<app>.babb.dev`/`api.<app>.babb.dev` naming precept landed in `docs/precepts/infra/`; the fourier domain split lives — `fourier.babb.dev` serves the frontend (CF Pages from W9.a), `api.fourier.babb.dev` serves the backend (LE from W10).

### §0.3 — Thread γ (backend NO-legacy + transpositions) — W3 gates

- [ ] **W3**: `git grep -nE "snapshot_hash|snapshotHash" api/` returns zero on identity paths (symmetric to C.W4's frontend discharge); the dead `gallery` collection stratum deleted (no `_entry_from_doc`, no `GalleryEntryResponse`, no `snapshots`/`gallery` boot indexes); the image-asset typed (a Pydantic model in `api/models/assets.py`; `images.py:140,159` resolve through it — retiring the dict-shim class of bug that produced C9/C10).

### §0.4 — Thread β (design refinement) — W4 gates

- [ ] **W4**: `git grep cartoon-card` returns zero **dead** references (either the class lives again — one shim → 14 components un-flattened — or every consumer is migrated to a token); the workspace upload IA resolved to one hero dropzone + a source-strip (no triple-affordance); the gallery orphans resolved (marquee wired OR both deleted; empty-state CTA in place); the light-mode contrast token sweep clean (axe light-mode pass clean on the measured surfaces); `:focus-visible` rings present on TOC + gallery cards.

### §0.5 — Thread δ (CRUD cohesion) — W5 gates

- [ ] **W5**: `docs/tranches/B/coordination/CRUD-CONTRACT.md` → v2.0.0 (the two KISS relaxations recorded — admit user-supplied slugs, bind observable behaviour not module layout); `CONFORMANCE-MATRIX.md` DEFERRED cells flipped against the live `palette-api`; `coordination/CRUD-COHESION.md` records the value.js alignment ask with the disposition (value.js-side execution is a value.js tranche, user-re-mandate-gated); the `palette_slug` FK contract recorded as the one binding cross-repo artefact.
- [ ] The `sampleToSVGPath` consume disposition recorded: either consumed (value.js published during D's window) OR **named residual** (value.js v0.10.0 still does not export it — verified at W5 close). `coordination/COLOUR-LIFT.md` reconciled.

### §0.6 — Thread ε (test integrity) — W6 gates

- [ ] **W6**: the cross-env Playwright matrix recorded (fourier + value.js × local/dev/prod, prod non-mutating; green or each red cell carries a named cause); a CI Mongo retires the load-bearing `@requires_mongo` skips; the `COMPUTE_RATE_LIMIT` e2e harness landed; `settings-persistence.spec.ts` recorded inert if still inert.

### §0.7 — Thread α′ (constellation normalization) — W8/W9/W10/W11 gates

- [ ] **W8 (DNS-as-code)**: `scripts/cf-dns.{sh,py}` tracked + idempotent; the host run produced the pilot-tranche records (`api.fourier.babb.dev`, `api.color.babb.dev`, `api.sudoku.babb.dev` all return `34.197.214.67`); the rollout-tranche CNAMEs landed per W9 cutover order; the don't-break regression test green (MX/SPF/NS/apex/wildcard byte-identical pre→post).
- [ ] **W9 (CF-Pages migration)**: fourier-pilot green first (the load-bearing proof); then keyframes off GH-Pages onto CF Pages (gh-pages branch deleted, CNAME removed, peaceiris step removed, matrix CI kept); then value.js/color same teardown; then sudoku onto CF Pages; grammar **DEFERRED** explicitly. For each in-scope app: `curl https://<app>.babb.dev/` returns 200 + `cf-ray`.
- [ ] **W10 (origin LE + Apache vhost + CORS)**: `certbot --expand` succeeded; the LE cert SAN list now includes the three api SANs; the three Apache vhosts active; `certbot renew --dry-run` succeeds; CORS fixes per app verified (fourier no-op; palette-api `ALLOWED_ORIGINS=https://color.babb.dev`; floridify `BACKEND_CORS_ORIGINS=["https://words.babb.dev"]`); W1 Mongo bind held (verification — no `0.0.0.0:27017/27018/27020` listeners).
- [ ] **W11 (palette-api → color rename)**: **either** the user re-mandate received + the rename landed (provenance reconciled; canonical-tree rename complete; data preserved via mongodump+restore or volume reuse; `docker ps | grep color` shows the renamed containers; CORS + Mongo-bind held; `git grep "palette-api"` zero on live source) **or** recorded as `held_pending_user_remandate` with the provenance artefact in place and the disposition carried to a value.js / constellation-cleanup successor.

### §0.8 — Coordination + tests

- [ ] `coordination/` updated:
  - the value.js cohesion ask (W5) carried into a candidate value.js tranche thesis (NOT authored here — value.js's choice).
  - the prod-deploy runbook (D's runbook for the W1/W2 first-deploy + the subsequent constellation-deploy operational discipline) recorded — extends `C/coordination/DEPLOY-RECONCILE.md` into D's reality.
  - the constellation runbook (the α′ recipe operationalized — DNS / CF Pages / origin LE / Apache vhost / CORS) recorded in `coordination/CONSTELLATION-DEPLOY.md` (or a successor `CONSTELLATION-RUNBOOK.md`) as the binding operator's guide for the next constellation app (post-grammar).
  - the W11 user-re-mandate disposition recorded.
- [ ] **Dangling-image / dead-vhost cleanup** (the W12 explicit sweep, NA1 §3 + NA5 §1.2):
  - `docker images | grep -E 'gaggle-gaggle|server-api'` → both images removed (`docker image rm` on the host; NA1 §3 — they have no running containers).
  - `docker images | grep -E 'speedtest-api|speedtest-frontend'` → both removed (NA1 §3 — speedtest lives at CF Pages, not on mbabb).
  - The dead `:8140` speedtest vhost (`/etc/apache2/sites-enabled/speedtest.conf`, NA5 §1.2 + NA1 §3) — disabled (`a2dissite speedtest`; or the vhost edited to redirect to `speedtest.friday.institute`).
  - The dead `27019/tcp` UFW rule (NA1 §4b — `speedtest mongo` that never existed) — withdrawn (`sudo ufw delete allow 27019/tcp`).
- [ ] **Apex vhost retirements (optional, agent's call)** — after W9 cutovers, the host-Apache vhosts for `<app>.babb.dev` (fourier `:8100`, sudoku `:8120`) became dead-but-harmless (CF DNS sends traffic to CF Pages). The agent may either retire them at W12 close (`a2dissite babb-dev` segments) or hold them as fallbacks. **Recommendation**: hold the apex vhosts as fallbacks; record in `coordination/` that the apex vhost serves as a backstop iff CF Pages goes down. The agent's call; record either way.
- [ ] `uv run pytest` green; `vue-tsc -b --force` green; `npm run build` succeeds (the tranche-level close gates, `D.md §6`).
- [ ] `CANONICAL-ORDERING.md` reconciled to **ordering ε** (post-D-close; fourier-A/B/C/D closed) — or **ordering ζ** if more cross-repo changes warrant a finer step.
- [ ] Brittleness window §8 — **restored** (closed). At Wχ the window opened only for W1 (the deploy + migration cutover span); W1 closed the window; W2–W11 had no window per `D.md §8` ("W2-W12 close green with no window"); W12 records the final disposition (no suspended gates).

### §0.9 — Residuals ledger (each carries a named destination — P-Inv 28)

- [ ] **The CF token NOT rotated** (per user direction, `D.md §7`; `PROGRESS.md` 2026-05-27 resolution entry; `CONSTELLATION-DEPLOY.md §6` "Do NOT rotate"). The token lives in the gitignored host `.env`s (`fourier-analysis/.env`, `value.js/.env`, `0600`) + the CI secret store. Rotated only on suspicion. NA6 §2.3's recommended rotate-after-migration is **explicitly overridden** by the user; the override is recorded so it cannot be silently walked back at a future close.
- [ ] **The grammar CF-Pages migration** (NA4 §2.8 — author-coordinated quiet-window required; 1009 commits/14d active dev) → a successor wave (a constellation-cleanup tranche or a grammar-specific window).
- [ ] **The value.js cohesion (the v2.0.0 contract on the value.js side)** → a value.js tranche, user-re-mandate-gated (per `D.md §7` + `coordination/CRUD-COHESION.md §6`).
- [ ] **The `sampleToSVGPath` consume** → a fourier successor, fired iff a value.js tranche publishes it.
- [ ] **Multi-replica fourier deployment** → a fourier-E if ever needed (invariant 19 single-replica preserved).
- [ ] **`value.babb.dev` co-frontend disposition** (NA2 §6.1 open item) — recorded at W9.c per the agent's call; if not yet flipped, carry the residual to a value.js tranche.
- [ ] **The W11 rename — IF held** → a value.js tranche residual.
- [ ] **The `--reload` background-queue** (carried from C — explicit trigger: compute outliving a request) — still a fourier-D-or-successor residual; if ε surfaced a need, fired in W6; else carried to a successor.

---

## §1 — FINAL.md authoring (the §0→§9 structure mirroring C/FINAL.md)

The narrative close lands in `docs/tranches/D/FINAL.md`. Required sections (mirror `C/FINAL.md §0→§9`):

- **§0 — Goal criterion and completion criterion (paired)** — cite which closed met / closed-with-residuals / held; honest binary.
- **§1 — Thesis recap** — A retired drift, B converged identity, C hardened infra + discharged residuals + α′ found the constellation drift, **D was the first time the lineage was actually deployed** + the constellation pattern was proven on fourier + rolled bounded-parallel.
- **§2 — Wave-by-wave commit ledger** — W0 / Wα / Wχ / W1 / W2 / W3 / W4 / W5 / W6 / W8 / W9 / W10 / W11 / W12 — each row with thread + commits.
- **§3 — Cumulative metrics** — Mongo exposure closed; LE SANs expanded to six; CF Pages apps deployed (count); the backend `snapshot_hash` band 0; the `.cartoon-card` shim resurrected (1 shim → 14 consumers); contract v2.0.0 ratified.
- **§4 — Hard-gate evidence** — transcribe the `D.md §6` ledger with the commits + transcripts.
- **§5 — Scope-reveals + challenge catches + the host-coupling honesty** — the load-bearing Wχ-P5 finds; the in-tranche security hotfix (Mongo bind front-loaded); the W11 user-re-mandate gate honoured; the grammar DEFER recorded; the W10 LE-expand pre-condition (W8 first) honoured.
- **§6 — Carries (named successors)** — the §0.9 residuals ledger.
- **§7 — Constellation final state** — fourier-A/B/C/D closed; the constellation now uses the `<app>.babb.dev` + `api.<app>.babb.dev` pattern on every in-scope app; the grammar disposition; the W11 disposition; CANONICAL-ORDERING → ordering ε (or ζ).
- **§8 — Tranche close evidence** — `uv run pytest` green; `vue-tsc -b --force` green; `npm run build` green; `dig` returns the target DNS state; `curl` all live endpoints (`https://fourier.babb.dev`, `https://api.fourier.babb.dev/api/health`, `https://color.babb.dev`, `https://api.color.babb.dev`, `https://sudoku.babb.dev`, `https://api.sudoku.babb.dev/api/health` if csp-solver exposes one, `https://keyframes.babb.dev`) — **all 200 with valid certs**.
- **§9 — Reflection** — the discipline lessons (pilot-then-rollout, the in-tranche security hotfix, the cross-repo coordination, the precept promotions, the no-rotate CF token override honoured honestly).

---

## §2 — Out of scope at close

- The close ceremony cannot run while any brittleness window is open — `D.md §8` opened only for W1; W1 closed the window; W12 enters with no open window.
- **No new feature work** at W12.
- **No CF token rotation** (per user; recorded explicitly).
- **No grammar migration** (DEFERRED; recorded).
- **No value.js-side tranche** (the value.js side is value.js's choice).
- **No fourier-E** unless invariant 19's single-replica is to be re-litigated (it is not at W12).
- **No apex-vhost retirement forced** — the agent's call (recommendation: keep the apex vhosts as CF-Pages-down fallbacks).

---

## §3 — Verification artefacts at close

- `PROGRESS.md` — every row reconciled.
- `FINAL.md` — authored per §1.
- The dig-transcript artefacts (from W8.b) — content cited in FINAL §4.
- The openssl SAN-dump (from W10.a) — content cited in FINAL §4.
- The curl transcripts (live endpoints — every `<app>.babb.dev` + `api.<app>.babb.dev`) — captured in FINAL §8.
- `docker images | grep -E 'gaggle|server-api|speedtest-(api|frontend)'` returns empty (the cleanup proof).
- `apache2ctl -S | grep speedtest` returns empty (or shows a redirect) — the dead `:8140` vhost disabled.
- `ufw status verbose | grep 27019` returns empty — the dead Mongo rule withdrawn.
- `git log --oneline docs/precepts/infra/` shows the new `<app>.babb.dev`/`api.<app>.babb.dev` precept and the deploy precept promotion commits.
- `CANONICAL-ORDERING.md` shows ordering ε (or ζ).
- `coordination/` updated with the prod-deploy runbook + constellation runbook + value.js cohesion ask + W11 disposition.

---

## §4 — Filled-at-close sections (placeholders the close agent fills)

When W12 actually runs (after W0-W11 land), the close agent fills:

- **The wave-by-wave commit ledger** — actual SHAs per wave.
- **The hard-gate transcripts** — actual `curl`/`dig`/`openssl`/`docker ps` outputs at close-time.
- **The CANONICAL-ORDERING bump** — ε or ζ.
- **The dispositions for held residuals** — W11 (held vs landed), grammar (deferred residual successor), value.js cohesion (named tranche).
- **The §9 reflection** — the lessons learned; the close-agent's authored narrative.

---

## §5 — Dependencies

- **Depends on**: every other D wave's close (W0/Wα/Wχ/W1/W2/W3/W4/W5/W6/W8/W9/W10/W11). The close ceremony cannot run while any wave is open. W11 may close as `held_pending_user_remandate` — that is an honest close, not a block on W12.
- **Binds**: the `D.md §6` hard-gate ledger; `D.md §7` deferral list; the `D.md §8` window restoration; the `coordination/` charters (`CONSTELLATION-DEPLOY.md`, `DOMAIN-NAMING.md`, `CRUD-COHESION.md`, `PALETTE-API-PROVENANCE.md`).
- **Blocks**: nothing within D (W12 is the close). Blocks the next tranche's W0 (a successor cannot open until D closes honestly).

---

## §6 — Congruence findings

The Wχ-harden pass surfaced these incongruences across the D documents — recorded here for the W12 reconcile pass to apply, NOT edited from any earlier wave:

1. **`D.md §3` W12 row** says "CANONICAL-ORDERING → ordering ε" — congruent for the default case. The spec authored here admits `ζ` as an alternative if more cross-repo changes warrant a finer step (the W11 user re-mandate + value.js side activity could justify ζ if both fire during D's window). **Reconcile at W12 actual close**: the close agent picks ε or ζ based on the actual end-state.

2. **`D.md §3` W12 row** says "**CF token NOT rotated** (per user direction)" — congruent + reinforced. The spec authored here makes the override **explicit** (so NA6 §2.3's contrary recommendation is not silently walked back). **No reconcile**; the W12 row is correct.

3. **`D.md §3` W12 row** says "the dangling-image cleanup (`gaggle`, `server-api` images on the host) + the dead `:8140` speedtest vhost" — congruent. NA1 §3 also names `speedtest-api` + `speedtest-frontend` as dangling — the spec authored here adds these to the §0.8 cleanup list. **Reconcile at W12 close**: D.md §3 W12 row should be updated to add `speedtest-api`, `speedtest-frontend`.

4. **`D.md §3` W12 row** says "restore the §8 window" — interpreted as: the §8 brittleness window was active only for W1; W1 closed the window (the deploy completed); W12 records the final disposition (no suspended gates). The "restore" phrasing reads ambiguously — it could be misread as "re-open the window" rather than "restore the pre-W1 closed state". **Reconcile at W12 close**: edit D.md §8 to read "the W1 deploy window opened and closed within W1; W12 records the closed disposition" — restoration means closure, not re-opening.

5. **`PROGRESS.md` W12 row** says "**CF token NOT rotated** (per user) — saved in gitignored `.env`s + CI secrets" — congruent. **No reconcile needed**.

6. **`coordination/CONSTELLATION-DEPLOY.md §7`** uses `α′.W1` through `α′.W5` wave numbers — the canonical D wave numbers (W8–W12) replace these. **Reconcile at W12**: edit `CONSTELLATION-DEPLOY.md §7` to use the D.md wave numbers (W8/W9/W10/W11/W12).

7. **`coordination/DOMAIN-NAMING.md §3`** notes the palette-api provenance discrepancy with **NA1 §5** as the resolution evidence (standalone repo, rsync-deployed). The W11 spec's `coordination/PALETTE-API-PROVENANCE.md` artefact records the final reconcile. **Reconcile at W12**: confirm the cross-link from DOMAIN-NAMING.md §3 to PALETTE-API-PROVENANCE.md is in place.

8. **`coordination/CRUD-COHESION.md §6`** says "The value.js-side execution is a **value.js tranche** the user must re-mandate" — congruent with the W12 close's "value.js cohesion ask carried into a value.js tranche". **No reconcile**.

9. **NA6 §2.3** recommended "ROTATE the CF API token after the constellation migration completes" — the user **explicitly overrides** this (per `D.md §7` + `PROGRESS.md` 2026-05-27 resolution). The W12 close records the override honestly (NA6 cannot be silently violated; the override is named). **Reconcile at W12 close**: NA6 §2.3's recommendation is logged as superseded by user direction, not as ignored.

10. **`D.md §3` W9 row** + W10 row mention "fourier pilot first" — the W12 close's FINAL.md §5 must cite the pilot-then-rollout discipline as a load-bearing W12 finding. **Reconcile at W12 close**: FINAL.md §5 records the discipline.

11. **The pre-A `8818ae5` prod-state baseline** (from `PROGRESS.md` 2026-05-27 entry: "Pivotal finding (DA4). None of A/B/C is in production — prod serves pre-A `8818ae5`") — the W12 close's FINAL.md §0/§3 cites that D is the first deployment of the A/B/C lineage. This is the headline of the tranche. **Reconcile at W12 close**: FINAL.md §0 + §3 lead with this finding.

12. **Apex vhost retirement (§0.8 optional cleanup)** — `D.md §3` does not name this explicitly. The agent at W12 close decides whether to retire `<app>.babb.dev` apex vhosts (fourier `:8100`, sudoku `:8120`) once CF Pages serves them, or to hold them as fallbacks. **No reconcile in D.md needed** — the disposition lands in `coordination/` at W12 close (per the agent's call).

13. **`D.md §3` W6 row** (test integrity / Playwright matrix) — this is thread ε and overlaps in time with the constellation waves W8/W9/W10. The W12 close's PROGRESS reconcile must order these: W6 lands after W9-W10 prove the new endpoints (the Playwright matrix tests against the new endpoints, not the pre-W9 mbabb-served originals). **Reconcile at W12 close**: PROGRESS.md log records W6's actual landing date relative to W9/W10.
