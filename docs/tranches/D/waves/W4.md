# D.W4 — Frontend design refinement (thread β)

**Name**: W4 — Design refinement — the `.cartoon-card` resurrection + workspace upload IA + gallery orphan reconciliation + light-mode contrast token sweep + `:focus-visible` rings.
**Thread**: β (frontend design refinement — surgical, NOT redesign or rebrand; the per-screen findings from the 4 design lanes A1–A4 distilled into one wave).
**Opens after**: Wχ close (research-first gate). **W4 is INDEPENDENT of W3 at file bounds** (`web/src/**` + `glass-ui/**` carry vs `api/**`) — the two are the headline parallel pair of D's implementation phase. **Independent of W1/W2 at file bounds** (`web/src/**` vs `docker-compose*.yml` + host); the only weak coupling is that W4's visual changes ride into prod via W1's deploy chain — so a green W4 build does not reach the live site until W1 closes.
**Agents**: 2–3 parallel — **A** (the `.cartoon-card` shim + the light-mode token sweep — the two glass-ui carry items; one diff in a shared CSS/token file plus the consumer-side hex-to-token swaps); **B** (the workspace upload IA collapse — VisualizationView + ImageUpload + placeholder); **C** (gallery orphans resolution + `:focus-visible` rings — GalleryView + GalleryMarquee/Grid + GalleryCard + AdminFlaggedPanel cards + PaperSidebar TOC links). A/B/C share no files. **Hard ceiling 3 agents** (per D.md §3 + DA6 guard).
**Status**: provisional (hardened from `D.md §3` W4 row + design lanes A1/A2/A3/A4).
**Binding inputs**: `DA-design-A1-paper.md §2 #1/#2/#3` (TOC `:focus-visible`, light-mode amber 3.54:1, sub-AA section-color text); `DA-design-A2-workspace.md §2 A2-01/A2-02/A2-03/A2-09` (dual/triple upload affordance, canvas placeholder rgba 3.05:1, panel `/60`+`/70` opacity dimming, dropzone border 2.41:1); `DA-design-A3-gallery-admin.md §2 #1/#3/#4/#12` (GalleryMarquee + GalleryGrid orphans, dimmed timestamp `text-foreground/35` 1.9:1, GalleryCard `<div @click>` keyboard-unreachable, empty-state inert text); `DA-design-A4-equation-morph-chrome.md §2 #1/#3/#4` (the `.cartoon-card` dead-class headline — 14 consumers flatten; light-mode amber 3.55:1 — duplicate of A1; hardcoded `#f0b632` text 1.76:1).

---

## Goal criterion (paired with completion)

**Goal.** After W4, (a) the **dead `.cartoon-card` class is alive again** — either as a one-utility shim in `web/src/style.css` (the cheapest stop-gap; one local `@utility cartoon-card { @apply cartoon-surface; border-color: var(--border); background: var(--card); }`) OR as a glass-ui carry (`@mkbabb/glass-ui/dist/styles/cards.css` re-publishes `.cartoon-card` as a recipe class) — and the 14 components A4 enumerated render with their intended 2px cartoon border + `3px 3px 0` hard-offset shadow restored (verified by computed-style probe on every consumer); (b) the **workspace upload affordance is one hero dropzone** in the canvas + a slim source-strip in the panel that expands to a thumbnail/replace card once an image exists (the redundant panel-dropzone and the divergent copy collapse; the global drop-anywhere overlay stays — it only appears mid-drag); (c) the **gallery orphans are resolved** — `GalleryMarquee` mounts as a living empty-state band with a CTA to `/visualize` OR both `GalleryMarquee` and `GalleryGrid` delete, with the decision documented in the wave close-record; (d) the **light-mode contrast cluster is swept** — `--viz-amber` darkens from `hsl(35 70% 42%)` (3.54:1) to ~`hsl(35 76% 35%)` (≥4.6:1), the three hardcoded `#f0b632` text occurrences swap to `var(--viz-amber)`, the `text-foreground/35` + `text-muted-foreground/60` + `text-muted-foreground/70` alpha-dimmed muted text sites swap to `--muted-foreground` (already AA 6.76:1) or a `--muted-foreground-strong` variant, the canvas-placeholder hardcoded `rgba(150,150,150,0.6)` swaps to a resolved `--muted-foreground` hex via the existing `resolveVizColors` plumbing; (e) `:focus-visible` rings land on every TOC link (`PaperSidebar.vue:215`, `MobileFloatingToc.vue:336`, `PaperArticleWindow.vue:124`) AND every gallery card (`GalleryCard.vue:63-67` becomes a real `role="button" tabindex="0"` control with Enter/Space activation + a visible focus ring) — mirroring the canonical pattern already at `AppHeader.vue:174-177`.

**Completion criterion (binding ledger — §6 below).** W4 closes only when: `git grep -n cartoon-card web/src` returns its 14 consumer references AND a computed-style Playwright probe on every consumer reports `border-width: 2px` + `box-shadow` containing `3px 3px 0` (not `0px` / `none`); the visualization workspace empty state renders **exactly one full dropzone** (the canvas hero) — `ImageUpload.vue`'s empty-state-dropzone variant is collapsed to a slim source-strip (the existing thumbnail/replace card at `ImageUpload.vue:51-85` becomes the only "full" panel form); the gallery decision is **acted-upon** (either `GalleryMarquee` is imported + mounted in `GalleryView.vue` OR both `GalleryMarquee.vue` + `GalleryGrid.vue` are deleted; the wave close-record names which); axe-core scan on light mode (`prefers-color-scheme: light` via Playwright) of `/visualize`, `/gallery`, `/equation`, `/morph`, `/paper` reports **zero serious/critical contrast violations** on the measured surfaces (the four contrast-cluster hits — amber nav label, `#f0b632` text, dimmed timestamps, canvas placeholder — all clear AA); keyboard-only tab traversal on `/paper`'s TOC and `/gallery`'s card grid hits every interactive element with a visible ring (Playwright `await page.keyboard.press('Tab')` loop + `expect(focused).toHaveCSS('outline-width', '2px')` style probe); `vue-tsc -b --force` exit 0; `npm run build` exit 0; `cd web && npx playwright test e2e/visualization-ux.spec.ts --project=chromium` green with the existing 4 axe-keystone assertions still zero serious/critical (the W4 changes do not regress the C.W4 axe baseline).

The wave **fails** if: the `.cartoon-card` revival is a hardcoded style rewrite (e.g. consumers stop using `cartoon-card` and inline `border: 2px solid var(--border); box-shadow: 3px 3px 0 var(--border);` — that would *defeat* invariant `the token system is the single source of surface truth`, the cure becoming a worse instance of the disease the `D.md §2` third new invariant explicitly forbids); the workspace upload IA "collapse" leaves the panel `ImageUpload` empty-state intact and the canvas placeholder shrunk (the panel is the redundant copy, NOT the canvas); the gallery decision is *not made* (a "deferred" close on the orphans is the worst outcome — the audit named them, the wave must dispose them); a "darker amber" lands as a hardcoded hex in one consumer rather than as a `--viz-amber` token change (the D.md `token system single source` invariant forecloses); the `:focus-visible` rings land in `glass-ui` (out-of-bounds — see §5) but the fourier-local consumers do not get them; `vue-tsc` regresses or `npm run build` fails; the axe baseline regresses (the C.W4 + B/C-keystone axe work was load-bearing — W4 must not net-add violations).

---

## §1 — The ROOT (verified against the live tree at HEAD `1e47115`)

The five sub-deliverables are independently bound. Each is grounded `file:line` against the live tree.

### §1.1 — The `.cartoon-card` headline (the single highest-leverage fix; 14 consumers un-flatten)

`.cartoon-card` was a glass-ui recipe class carrying the intended 2px border + `3px 3px 0` hard-offset shadow + hover-lift. **A4 confirms via computed-style probe**: at HEAD on `/equation`, `getComputedStyle('.cartoon-card')` reports `border-width: 0px`, `background: rgba(0,0,0,0)`, `box-shadow: none` — the class resolves to *nothing*. The `glass-ui/dist/styles/cards.css:2` comment confirms "recipe classes were removed" at C.W5 (the glass-ui bump that silently flattened the consumers — a **classic dead-class regression** invariant `token system single source` now binds against).

**The 14 consumers** (verified via `git grep -ln cartoon-card web/src` — actual census exceeds A4's count slightly; the canonical list at HEAD):

| # | File | Line(s) |
|---|---|---|
| 1 | `web/src/components/equation/EqCoefficientsPanel.vue` | `:12` |
| 2 | `web/src/components/equation/EquationView.vue` | `:229, :238, :242, :250, :307` (5 sites — the empty-state, computing-state, error-state, equation card, plot card) |
| 3 | `web/src/components/equation/FunctionInput.vue` | `:93, :171` |
| 4 | `web/src/components/equation/InfoCard.vue` | `:18` |
| 5 | `web/src/components/morph/HarmonicLevelGrid.vue` | `:2` |
| 6 | `web/src/components/morph/MorphPhaseConfig.vue` | `:2` |
| 7 | `web/src/components/morph/MorphShapePreview.vue` | `:4` (the morph-button shell — the "border pops in on hover only" finding A4 #2 cascades from here) |
| 8 | `web/src/components/visualization/BasisCanvas.vue` | `:485` (the canvas container) |
| 9 | `web/src/components/visualization/ContourPreview.vue` | `:33` |
| 10 | `web/src/components/visualization/GalleryView.vue` | `:292` (the batch toolbar) |
| 11 | `web/src/components/visualization/ImageUpload.vue` | `:38` |
| 12 | `web/src/components/visualization/VisualizationView.vue` | `:163` (the workspace empty/loading state) |
| 13 | `web/src/components/visualization/gallery/AdminUserList.vue` | `:306, :360` (sticky toolbar + user rows) |

(A4 said "14"; the live `git grep -l` returns 13 consumer files at HEAD — the 14th was `GalleryCardModal.vue` which now uses `.cartoon-card foreground/3 plate` per the comment at `:257` but does not apply the class directly; the discrepancy is benign — the gate is the *computed-style probe on every applied consumer*, not a line-count match.)

**The fix — KISS, fix-at-ROOT, one shim** (A4's recommendation): add **one local Tailwind v4 `@utility` block** to `web/src/style.css` defining `.cartoon-card` against the existing `cartoon-surface` utility (which glass-ui still exports at `cards.css:~40` per A4 — verify at dispatch):

```css
/* web/src/style.css — add adjacent to the existing utility/theme blocks */
@utility cartoon-card {
    @apply cartoon-surface;
    border-color: var(--border);
    background: var(--card);
}
```

**Why fourier-local, not glass-ui**: glass-ui is a *consumed dependency* (a sibling repo pinned via `file:../../glass-ui`); cross-repo coordination is out of D's autonomous bounds (the value.js parallel — D.W5 records cross-repo asks, never authors). The fourier-local shim is the *immediate, KISS, single-PR-bounded* fix; glass-ui can later re-export the recipe (a coordination ask recorded in `D/coordination/`), at which point the shim deletes (or the shim stays as a defensive carry — KISS prefers the simplest stop-gap).

**Three rejected alternatives** (recorded so the cure is honest):

- **Rewrite every consumer to inline the style** (`border: 2px solid var(--border); box-shadow: 3px 3px 0 var(--border);`). **Rejected** — defeats the D.md §2 third new invariant: the token system is the single source of surface truth; an inline rewrite is exactly the kind of "token-system bypass" the invariant forbids.
- **Migrate every consumer to `<Card surface="cartoon">`** (A4's "shipped replacement" path). **Rejected as W4 scope** — 14 components migrating their root element to a glass-ui primitive is a larger structural change than a 3-line shim; KISS prefers the smaller mechanism. (If glass-ui later deletes `cartoon-surface` too, the migration becomes mandatory; recorded as a named successor.)
- **Cross-repo glass-ui patch**: re-publish `.cartoon-card` from glass-ui directly. **Held as a coordination ask** (not blocked, not in-scope-as-the-primary-fix) — D records it in `coordination/` if the shim proves a long-lived workaround; the immediate fix is fourier-local.

### §1.2 — The triple upload affordance (A2-01) — collapse to one hero dropzone

The screenshot (the empty workspace state) shows two competing full dropzones rendering simultaneously: (a) the canvas placeholder ("Drag & drop an image here", via `web/src/lib/canvas-drawing/placeholder.ts:24-60` painted into `BasisCanvas.vue`); (b) the right-panel `ImageUpload.vue:88-110` dashed dropzone ("Drop an image or click to upload"). A third, the global drag-enter overlay at `VisualizationView.vue:144-153` ("Drop image anywhere"), is correctly ephemeral and stays.

**The collapse target** (per A2 §3 #1):

- **Keep the canvas placeholder as the hero** — it owns the largest screen area, owns the spatial-mapping (upload-where-the-result-appears), and is the workspace's natural first-action.
- **Demote `ImageUpload.vue`'s empty-state dropzone (`:88-110`) to a slim source-strip** — a one-line "Source: drop or click to upload" subhead + a small icon, NOT a full dashed-border full-card dropzone. The component's existing **thumbnail/replace card variant** (`ImageUpload.vue:51-85`) — which renders once an image exists, showing the thumbnail + a Replace button — is the only "full" panel form post-W4.
- **Unify the copy**: pick one phrasing across the canvas placeholder + the panel source-strip + the global drag-enter overlay. The user's `feedback_em_dashes.md` precept binds Unicode `—` (U+2014) not `&mdash;` HTML entities (`ImageUpload.vue:47` uses `&mdash;` — rewrite to a literal `—`). The recommended copy (A2-06): one of {"Drop or click to upload — PNG/JPG/SVG ≤ 10 MB"} across all three sites, varying only by context-prefix ("Source:" on the panel strip; "Drop anywhere" on the global overlay; the canvas-hero is the headline form).
- **Delete the redundant code**: remove the `ImageUpload.vue:88-110` empty-state dropzone block; the component's render now branches on `hasImage` to either the slim source-strip (no image) or the existing thumbnail/replace card (image exists).

### §1.3 — Gallery orphans (A3 #1, #12) — decide + act

`GalleryMarquee.vue` and `GalleryGrid.vue` are imported nowhere in `web/src` — confirmed at this Wχ-harden: `git grep -ln "GalleryMarquee\|GalleryGrid" web/src` returns ZERO references (the orphan grep at HEAD). `GalleryView.vue` wires only `GalleryInfiniteGrid` + `GalleryFeaturedCarousel`. The dual-track auto-scroll marquee is the most distinctive motion idea in the surface, **shipped but unreachable**.

**The decision** (the wave makes it; "deferred" is a fail-mode):

- **Option A (the recommended)** — mount `GalleryMarquee` as the empty-state band: when `entries.length === 0 && !loading`, render `<GalleryMarquee :entries="featuredOrSampleEntries" />` behind the centred empty-copy block (the marquee paints a living preview of what the gallery becomes; the empty copy reads "No visualizations yet — [Open the Visualizer →]" with a real `<Button>` routing to `/visualize`); delete `GalleryGrid.vue` (the active grid is `GalleryInfiniteGrid`, not `GalleryGrid` — confirmed at A3 §1; `GalleryGrid` was a deprecated parallel). Add `@media (prefers-reduced-motion: reduce) { .marquee-inner { animation: none } }` to `GalleryMarquee.vue:102-124` (A3 #9 — the missing reduced-motion guard).
- **Option B (the minimum)** — delete BOTH `GalleryMarquee.vue` AND `GalleryGrid.vue`; replace the empty-state inert text with a `<Button variant="outline">` routing to `/visualize` (the A3 #12 fix).

**Either is acceptable**; the wave close-record names which. Option A is "more living"; Option B is "more KISS". The decision threshold: if `GalleryMarquee` mounts cleanly with a 6-entry sample (the existing dual-track auto-scroll works), pick A; if mounting reveals broken bindings (the orphan rotted), pick B. The dispatch agent decides on the live evidence.

### §1.4 — The light-mode contrast token sweep (A1 #2/#3, A2 #02/#03/#09, A3 #3, A4 #3/#4) — the systematic AA-failure cluster

The audit data, consolidated:

| Surface | Token / hex | Measured contrast | Sites |
|---|---|---|---|
| `--viz-amber` light (`hsl(35 70% 42%)`) | nav label | **3.54:1** (fail AA) | `AppHeader.vue:187-190, 326-329`; `FunctionInput.vue` slider tint; the harmonics auto-button (A1 #2, A4 #3) |
| Section colors light (e.g. `sc5` amber, `sc4` forest) | small text | **3.54:1 / 4.45:1** (fail) | `theme.css:476, 481`; `PaperSidebar.vue:76`; section-number at `0.95rem` (A1 #3) |
| Hardcoded `#f0b632` text | per-coefficient hover, auto-active | **1.76:1** (severe fail) | `EquationView.vue:420-421`; `FunctionInput.vue:247-249`; mirror in `lib/colors.ts:12` `STATIC.golden` (A4 #4) |
| `text-foreground/35` | timestamps | **~1.9:1** (severe fail) | `GalleryCard.vue:98`; `GalleryCardModal.vue:108` (A3 #3) |
| `text-muted-foreground/60` | stat suffixes | **~2.8:1** (fail) | `GalleryCardModal.vue:116, 128` (A3 #3) |
| `text-muted-foreground/60` | "PNG, JPG, SVG up to 10MB" hint | **3.28:1** (fail) | `ImageUpload.vue:107` (A2 #03) |
| `text-muted-foreground/70` | "— source input" subhead | **3.96:1** (fail) | `ImageUpload.vue:47` (A2 #03) |
| `rgba(150,150,150,0.6)` placeholder text | canvas "Drag & drop" | **3.05:1** (fail) | `web/src/lib/canvas-drawing/placeholder.ts:33` (A2 #02) |
| `border-2 border-dashed border-border` on dropzone | affordance edge | **2.41:1** (fail; UI 3:1 floor) | `ImageUpload.vue:90-94` (A2 #09) |

**The fix — three coordinated mechanisms** (all token-driven; not a single hardcode):

1. **The glass-ui light-token carry** (the root): `--viz-amber` light from `hsl(35 70% 42%)` → `hsl(35 76% 35%)` (≈ 4.6:1, clears AA for normal text); `--section-color-4`/`--section-color-5` light tighten by ~6% lightness (per A1 #3); the `--muted-foreground-strong` variant — a darker step in the muted-text scale (e.g. `hsl(48 5% 50%)`, L≈50, ≥ 5:1) — declared explicitly in the light token set as an AA-tuned alternative to `--muted-foreground` at the `/60` alpha-modifier sites. **Coordination ask**: glass-ui owns these tokens. Two paths: (a) author the token change locally in `web/src/style.css` as `:root` overrides that win over the imported glass-ui tokens (KISS, fourier-local, single-PR-bound); (b) coordinate a glass-ui PR. **Path (a) is the W4 binding mechanism**; path (b) is recorded as a coordination ask in `D/coordination/` (mirrors the `.cartoon-card` shim pattern — fourier-local first, cross-repo follow-up).

2. **The hardcoded `#f0b632` → token swap**: replace the three text occurrences (`EquationView.vue:420-421`, `FunctionInput.vue:247-249`) with `var(--viz-amber)` (already theme-aware, both modes). `lib/colors.ts:12 STATIC.golden` may also retire (if it has no JS consumer; verify) — if it stays, retint to mirror the token (read the resolved hex at runtime via the existing `resolveVizColors` plumbing).

3. **The alpha-modifier-on-a-muted-token sweep**: replace every `text-muted-foreground/60`, `/70`, and `text-foreground/35` site with `text-muted-foreground` (the un-dimmed token is already AA at 6.76:1 dark, AA when the light-mode carry lands; the `/N` alpha-modifiers were drift, not a coherent dimming-scale). Sites: `GalleryCard.vue:98`, `GalleryCardModal.vue:108, 116, 128`, `ImageUpload.vue:47, 107`. The canvas placeholder (`placeholder.ts:33`) is special — Canvas2D does not resolve CSS vars; route through the existing `resolveVizColors` (`lib/colors.ts:90`) extended to expose `--muted-foreground` as a hex string. The dropzone border (`ImageUpload.vue:90-94`) — collapses with the IA collapse in §1.2 (the dropzone goes; the source-strip carries no dashed border).

### §1.5 — `:focus-visible` rings (A1 #1, A3 #4) — the WCAG 2.4.7 fix

**TOC links** (A1 #1) — `PaperSidebar.vue:215-221` overrides `background:none;border:none` on a glass-ui ghost `Button` and adds NO focus style; `MobileFloatingToc.vue:336`, `PaperArticleWindow.vue:124 .callout-btn` carry the same gap. **The fix** (per A1 §3 #1 — mirroring `AppHeader.vue:174-177`):

```css
/* Applied to .sidebar-link, .floating-toc-item, .callout-btn */
&:focus-visible {
    outline: 2px solid var(--ring);
    outline-offset: 2px;
    border-radius: inherit;
}
```

**Gallery cards** (A3 #4) — `GalleryCard.vue:63-67` is a bare `<div @click>`, not focusable, not keyboard-activatable. **The fix**: promote to `role="button" tabindex="0"`; add `@keydown.enter.space.prevent="emit('click')"`; add `:aria-label` naming the slug; add the same `:focus-visible` outline block as above.

**Cross-cutting gate** (the canonical pattern lives at `AppHeader.vue:174-177`; W4 mirrors it everywhere; D's `:focus-visible` discharge is wave-bound here).

---

## §2 — File bounds, disjointness, agent units

| File | Access | Owning agent | Notes |
|---|---|---|---|
| `web/src/style.css` | modify (add `@utility cartoon-card` block; light-token `:root` overrides for `--viz-amber`/section-colors/muted-foreground-strong) | A | the two glass-ui carries — both fourier-local for KISS |
| `web/src/components/equation/EquationView.vue` | modify (`:420-421` `#f0b632` → `var(--viz-amber)`) | A | hex-to-token swap |
| `web/src/components/equation/FunctionInput.vue` | modify (`:247-249` `#f0b632` → `var(--viz-amber)`; `:47` `&mdash;` → `—`) | A | hex-to-token + em-dash fix |
| `web/src/lib/colors.ts` | conditional (if `STATIC.golden:12` has no JS consumer, delete; else retint via `resolveVizColors` to mirror the token) | A | dispatch-gated by a consumer grep |
| `web/src/lib/canvas-drawing/placeholder.ts` | modify (`:33,41,55-59` rgba literals → resolved `--muted-foreground` hex via `resolveVizColors`) | A | Canvas2D cannot resolve CSS vars — route through the JS resolver |
| `web/src/components/visualization/ImageUpload.vue` | modify (`:88-110` empty-state dropzone collapse → slim source-strip; `:47, :107` opacity-modifier drop) | B | the IA collapse |
| `web/src/components/visualization/VisualizationView.vue` | modify (the dropzone-affordance verification — confirm the canvas placeholder is the only "full" affordance in the empty state) | B | reads the IA flow; minimal-diff |
| `web/src/components/visualization/gallery/GalleryView.vue` | modify (the empty state — either mount `GalleryMarquee` + add the `<Button variant="outline">` CTA, OR replace the inert text with the `<Button>`) | C | the gallery decision |
| `web/src/components/visualization/gallery/GalleryMarquee.vue` | conditionally modify (option A — add `prefers-reduced-motion` guard at `:102-124`) OR delete (option B) | C | the gallery decision |
| `web/src/components/visualization/gallery/GalleryGrid.vue` | DELETE (both options) | C | orphan; not the active grid |
| `web/src/components/visualization/gallery/GalleryCard.vue` | modify (`:63-67` → `role="button" tabindex="0"` + keydown handler + `:aria-label` + `:focus-visible` ring; `:98` `text-foreground/35` → `text-muted-foreground`) | C | a11y keystone — the card is the gallery's primary action |
| `web/src/components/visualization/gallery/GalleryCardModal.vue` | modify (`:108, :116, :128` alpha-modifier drops) | C | inherits with the GalleryCard contrast pass; **NOT** the dialog-role keystone (A3 #5 — `role="dialog"` + focus trap — is out-of-scope per §6) |
| `web/src/components/paper/PaperSidebar.vue` | modify (`:215` → add `:focus-visible` ring on `.sidebar-link`) | C | a11y |
| `web/src/components/paper/MobileFloatingToc.vue` | modify (`:336` → add `:focus-visible` ring on `.floating-toc-item`) | C | a11y |
| `web/src/components/paper/PaperArticleWindow.vue` | modify (`:124` → add `:focus-visible` ring on `.callout-btn`) | C | a11y |
| `PROGRESS.md` | modify (W4 row close-record; the gallery decision; the cartoon-card mechanism) | orchestrator | |

**Do NOT touch**: `glass-ui/**` (out-of-bounds per §5); `web/src/components/visualization/gallery/AdminFlaggedPanel.vue` (the C.W4 reshape is recent — A3 #7/#8 destructive-icon-tint + 10px-type findings are **deferred** per §6 unless the dispatch agent finds a one-line fix); `web/e2e/**` (W6 owns the test integrity; W4's design changes propagate into the existing axe-keystone assertions — see G7); `api/**` (W3 owns); `docker-compose*.yml` (W1/W2 own).

### Agent units (the 2-3 parallel partition)

- **W4.A — `.cartoon-card` shim + the light-mode token sweep.** The two glass-ui carries (the dead-class shim + the token darkening) are both `web/src/style.css` + a small hex-swap consumer pass. Agent A owns the single CSS file + the equation/lib hex swaps + the canvas placeholder rgba-to-token swap (via `resolveVizColors`). Sub-gate: G1 (the 14 consumers' computed-style probe) + G4 (the axe contrast pass).
- **W4.B — workspace upload IA collapse.** `ImageUpload.vue` + `VisualizationView.vue`. The redundant empty-state dropzone removes; the slim source-strip lands; the copy unifies. Sub-gate: G2 (the workspace empty state shows exactly one full dropzone).
- **W4.C — gallery orphans + `:focus-visible` rings.** Decide + act on the gallery; lift the GalleryCard to a real keyboard control; add the `:focus-visible` rings on the three TOC link classes + the gallery card. Sub-gate: G3 (the gallery decision recorded) + G5 (the keyboard-tab traversal hits every interactive element with a ring).

A/B/C share no file. **Hard ceiling 3 agents** (D.md §3 + DA6).

---

## §3 — Sequenced steps (per agent)

The five sub-deliverables order is *per-agent*; the agents run in parallel. Within each agent, the order minimises re-work and keeps `vue-tsc`/`build` green at each step.

### Agent A — cartoon-card shim + token sweep

1. **A.1 — Add the `@utility cartoon-card` block to `web/src/style.css`.** Computed-style probe at `/equation` immediately after: `getComputedStyle('.cartoon-card')` reports `border-width: 2px`, `box-shadow` containing `3px 3px 0`. (If `cartoon-surface` is also missing from glass-ui — verify at dispatch — the shim's body inlines the literal `border: 2px solid var(--border); box-shadow: 3px 3px 0 var(--border);` as a transitional carry, recorded as a coordination ask. The probe is the gate, not the mechanism.)
2. **A.2 — The light-token `:root` overrides in `web/src/style.css`.** Darken `--viz-amber` from `hsl(35 70% 42%)` to `hsl(35 76% 35%)`; tighten `--section-color-4`/`-5`; declare `--muted-foreground-strong`. Axe scan in `prefers-color-scheme: light` against `/visualize` + `/gallery` + `/equation` + `/morph` + `/paper`: each route's contrast violation count → zero on the cluster surfaces.
3. **A.3 — The hex-to-token swap.** Three `#f0b632` text occurrences → `var(--viz-amber)`. `lib/colors.ts:12 STATIC.golden` retires (if dead) or retints (if live).
4. **A.4 — The canvas placeholder rgba-to-token via `resolveVizColors`.** Extend `lib/colors.ts:90 resolveVizColors` to expose `--muted-foreground` as a hex; `placeholder.ts:33,41,55-59` consume the resolved hex. Probe: the placeholder text resolves to ≥ 4.5:1 against `--background` in both modes.

### Agent B — workspace upload IA collapse

1. **B.1 — Collapse the `ImageUpload.vue:88-110` empty-state dropzone to a slim source-strip.** Replace the full dashed-border block with a single-line subhead + icon ("Source: drop or click to upload — PNG/JPG/SVG ≤ 10 MB"). The component branches on `hasImage` to either this slim strip (no image) or the existing thumbnail/replace card (image exists).
2. **B.2 — Verify `VisualizationView.vue` empty state.** The canvas placeholder remains the hero; the global drag-enter overlay stays (only appears mid-drag). No structural change required beyond the `ImageUpload` collapse.
3. **B.3 — Unify the copy + em-dash convention.** Replace `&mdash;` HTML entities with literal Unicode `—` (U+2014) per `feedback_em_dashes.md`. The three upload-affordance copy strings settle on one phrasing.

### Agent C — gallery orphans + focus rings

1. **C.1 — Decide the gallery orphan disposition** (option A vs option B per §1.3). Verify at dispatch: import `GalleryMarquee` into a temporary `GalleryView.vue` test harness with a 6-entry sample; if it mounts + animates cleanly, pick A; else pick B.
2. **C.2 — Act**: either mount `GalleryMarquee` + add the prefers-reduced-motion guard + add the CTA `<Button>`, OR delete both orphans + add the CTA `<Button>`.
3. **C.3 — Promote `GalleryCard` to a real keyboard control** (`:63-67` → `role="button" tabindex="0"` + keydown.enter.space + `:aria-label` + `:focus-visible` ring).
4. **C.4 — Add `:focus-visible` rings on the three TOC link classes** (`.sidebar-link`, `.floating-toc-item`, `.callout-btn`), mirroring `AppHeader.vue:174-177`.
5. **C.5 — Drop the `text-foreground/35` + `text-muted-foreground/60` alpha-modifiers** on GalleryCard + GalleryCardModal (5 sites). Coordinate with Agent A's token sweep (the un-dimmed `text-muted-foreground` is the AA target).

---

## §4 — Hard-gate ledger (completion criterion)

1. **G1 — `.cartoon-card` ALIVE.** `git grep -l cartoon-card web/src` returns the 13 consumer files (CSS class still referenced); Playwright computed-style probe on the canonical 5 consumers (one from each viewport — `/equation` EquationView, `/morph` MorphShapePreview, `/visualize` BasisCanvas, `/visualize` ImageUpload, `/gallery` AdminUserList) reports `border-width: 2px` AND `box-shadow` containing `3px 3px 0` (NOT `0px` / `none`).
2. **G2 — One upload affordance in the workspace empty state.** Playwright load `/visualize` (empty DB); `await page.locator('[data-dropzone-full]').count()` (or the equivalent selector for the dashed-border full dropzone) returns **exactly 1** (the canvas hero); `ImageUpload.vue` renders the slim source-strip variant, not the full dropzone. (The harness asserts the empty-state IA; the global drop-anywhere overlay is gated on `dragenter` and out-of-frame for this assertion.)
3. **G3 — Gallery orphan decision recorded + acted-upon.** `git grep -ln "GalleryMarquee\|GalleryGrid" web/src` returns the expected state: option A → `GalleryView.vue` imports `GalleryMarquee`, `GalleryGrid.vue` does NOT exist; option B → neither file exists, neither is imported. The PROGRESS.md W4 row names the decision.
4. **G4 — Light-mode axe contrast pass clean.** `cd web && npx playwright test e2e/visualization-ux.spec.ts --grep "axe" --project=chromium` — extend the axe-keystone suite (or author a sister spec `e2e/light-mode-axe.spec.ts`) to also run with `colorScheme: 'light'` against `/visualize`, `/gallery`, `/equation`, `/morph`, `/paper`. Each axe scan reports **zero serious/critical contrast violations** on the measured surfaces. Specifically: `--viz-amber` light → ≥ 4.5:1 ratio against the page; the three former `#f0b632` text sites → ≥ 4.5:1; the `text-foreground/35` timestamp sites → ≥ 4.5:1; the canvas placeholder → ≥ 4.5:1.
5. **G5 — `:focus-visible` rings on every TOC + gallery card.** Playwright keyboard-tab traversal on `/paper` (`page.keyboard.press('Tab')` until back to start) records that every visited TOC element has `outline-width: 2px` (or `box-shadow` containing the ring inset). Same on `/gallery` (with a populated grid; if empty-DB, the test loads a fixture). The GalleryCard is reachable AND activatable by keyboard (Enter/Space fires the modal-open).
6. **G6 — `vue-tsc -b --force` exit 0.** No type regression from the GalleryCard role/handler additions, the IA collapse, or the gallery-orphan changes.
7. **G7 — `npm run build` exit 0.** Build green; the existing C.W4 axe-keystone assertions (`e2e/visualization-ux.spec.ts:63,80,99,115`) stay green — W4's changes do not regress the C-baseline (zero serious/critical violations).
8. **G8 — `prefers-reduced-motion` guard** on `GalleryMarquee.vue:102-124` if option A; recorded as N/A in PROGRESS if option B.
9. **G9 — `em-dash convention** — `git grep -n "&mdash;" web/src/components/visualization/ImageUpload.vue` → ZERO (the literal `—` consumed).
10. **PROGRESS reconciled** — the W4 row cites the close commit, the `.cartoon-card` mechanism (shim vs migration), the gallery option (A vs B), the axe-clean evidence, the keyboard-traversal evidence.

**Invalid hard gates (rejected at challenge)**: "`.cartoon-card` fixed" by hardcoding the border+shadow into each consumer rather than the token/shim (the D.md §2 token-system invariant forecloses); "upload IA collapsed" by removing the canvas placeholder hero rather than the panel dropzone (B owns the IA flow, not the canvas); "gallery orphan resolved" without a recorded decision (the audit named them; the wave must dispose); "axe clean" via a hardcoded `#xxxxxx` text-color rather than a token darkening (G4 measures the token, not the hex); "`:focus-visible` added" only on the canonical `AppHeader` (the gap is on TOC + cards — those are the binding sites); a regression in the C.W4 axe-keystone baseline (G7 forecloses).

---

## §5 — Out of scope

- **`glass-ui/**` source edits** — fourier consumes glass-ui via `file:../../glass-ui`; cross-repo edits are coordination asks, not autonomous W4 surface. The light-token darkening + the `.cartoon-card` re-export are *coordination asks* recorded in `D/coordination/` if the shim/local-override proves long-lived; the W4 binding mechanism is fourier-local.
- **`/demo/shape-extractor` redesign** (A4 #5) — the dev-only tool unreachable from nav; the 10-minute polish pass is **optional**, NOT a W4 gate. If the dispatch agent has bandwidth, the four micro-changes (red→`var(--viz-fourier)`; #ccc→`var(--border)`; bare h1/h2→`.cm-serif`; gate behind `import.meta.env.DEV` in the router) land as a tidy sub-task; otherwise deferred.
- **Equation/Morph button vocabulary unification** (A4 #6) — replacing the bespoke `.btn-export`/`.btn-reset` CSS with glass-ui `<Button variant="default">`/`"outline">`; a coherence-not-correctness item. **Held as a named D successor or W12 tidy**; W4 owns the contrast/IA/orphan/focus gates.
- **Page-title type scale unification** (A4 #7) — `.page-title` utility/Heading size; same coherence rationale; held.
- **A3 #5 dialog-role keystone** — `GalleryCardModal` adopting `role="dialog"` + focus-trap + return-focus. **Deferred to a D successor or W12** — it requires either adopting the glass-ui `<Dialog>` primitive (cross-repo coordination) or hand-rolling focus-trap/return-focus (non-trivial); W4 owns the contrast token sweep on the modal (A3 #3), not the dialog-role reshape.
- **A3 #7 destructive icon tint at rest** (AdminFlaggedPanel.vue:201-228 — Delete glyph indistinguishable from Dismiss by colour at rest) — admin moderation a11y; **held**.
- **A3 #8 10px admin-label type** (`text-admin-label` on flag-reason lines) — typography-token surface; **held**.
- **A3 #10 AuditLog pagination idiom** (raw `<button>` vs glass-ui `Button`) — coherence tidy; **held**.
- **A3 #11 unused `VIZ_COLORS` import** — one-line tidy; **held or one-line fold**.
- **A2 #4 empty-state value-prop hero** — the "Upload a shape — watch Fourier epicycles trace its outline" + "Try an example" affordance; a *new* feature, not a *refinement*; **held**.
- **A2 #7/#8 unified compute language + early contour reveal** — a structural compute-loading change; **held**.
- **A2 #10 mobile workspace observability** (the Controls|Canvas tab toggle) — IA structural change; **held**.
- **A2 #13 `is-default` button `aria-disabled` semantics** — small a11y tidy; **held or one-line fold**.
- **`api/**` — W3 owns**.
- **`docker-compose*.yml`, host infra — W1/W2 own**.
- **The C.W4 frontend slug-identity discharge** — already landed; W4 does not re-touch.

---

## §6 — Dependencies + ordering

- **Depends on Wχ close** (research-first gate).
- **Independent of W1/W2 at file bounds** (`web/src/**` vs `docker-compose*.yml`/host); but the *visual changes ride into prod via W1's deploy chain* — a green W4 build does not reach the live site until W1 closes. Recorded so the close-record cites the deploy-chain commit (W1) alongside the W4 commit.
- **Independent of W3 at file bounds** (`web/src/**` vs `api/**`); parallel-capable — the headline parallel pair of D's implementation phase.
- **Blocks**: W6 (test integrity — the e2e cross-env matrix needs the design changes to compute against a single canonical surface; the axe-keystones extend in W6 to cover the light-mode pass).
- **Coordination outputs**: a brief `D/coordination/DESIGN-CARRIES.md` (or section in `D/coordination/CONSTELLATION-DEPLOY.md`) records the two glass-ui asks (the `.cartoon-card` recipe re-export; the light-mode `--viz-amber`/section-color/muted-foreground-strong token carry) so a future glass-ui release lifts the shim.

---

## §7 — Verification artefacts

- A Playwright computed-style probe spec (the 5-consumer cartoon-card probe — extends or sister-specs `e2e/visualization-ux.spec.ts`).
- An axe light-mode pass against the five routes (extends the axe-keystone harness; runs with `colorScheme: 'light'`).
- A Playwright keyboard-traversal test on `/paper` TOC + `/gallery` card grid (the `:focus-visible` proof).
- A computed-style probe on `--viz-amber` light resolving to `hsl(35 76% 35%)` (or whichever AA-clearing value lands).
- The W4 close-record commit cites: the cartoon-card shim mechanism (a one-line `git diff` on `style.css`); the gallery decision (option A vs B + the deleted/added files); the axe-clean evidence (the test artifact); the keyboard-traversal evidence.

---

## §8 — Summary

**The headline lift.** Fourteen components flat-render today because `.cartoon-card` is a dead class — a glass-ui bump silently removed it. One Tailwind `@utility cartoon-card { @apply cartoon-surface; … }` block in `web/src/style.css` (the fourier-local KISS shim) restores the intended 2px border + `3px 3px 0` hard-offset shadow + hover-lift across all 14 consumers AT ONCE. This is the single highest-leverage fix in the entire D design thread — one change un-flattens equation, morph, gallery, visualize, admin, and the workspace in one sweep.

**The contrast cluster.** The light-mode AA-failure cluster — `--viz-amber` 3.54:1, hardcoded `#f0b632` 1.76:1, dimmed `text-foreground/35` 1.9:1, `text-muted-foreground/60` 2.8:1, canvas placeholder rgba 3.05:1 — is one coordinated token sweep: darken `--viz-amber` light to `hsl(35 76% 35%)` (≈ 4.6:1), declare `--muted-foreground-strong` as the AA-tuned dimmed variant, swap the three hardcoded hex sites to `var(--viz-amber)`, drop every alpha-modifier-on-a-muted-token (the `/60`/`/70`/`/35` modifiers were drift, not a scale), route the canvas placeholder through the existing `resolveVizColors` plumbing. One coordinated change, axe-clean across all five routes.

**The IA collapse.** Three upload affordances (canvas placeholder, panel dropzone, global drop-anywhere) compete for the same empty-state first-action — collapse to ONE (the canvas hero) + a slim source-strip in the panel; the global drop-anywhere stays (gated on `dragenter`, ephemeral). The panel's existing thumbnail/replace card (`ImageUpload.vue:51-85`) becomes the only "full" panel form post-W4.

**The gallery orphans.** `GalleryMarquee.vue` + `GalleryGrid.vue` import nowhere — confirmed at HEAD. The wave makes the decision (option A: mount `GalleryMarquee` as a living empty-state band with prefers-reduced-motion guard + delete `GalleryGrid`; option B: delete both + a `<Button>` CTA to `/visualize`). "Deferred" is a fail-mode — the audit named them, the wave disposes them.

**The `:focus-visible` discharge.** TOC links (`.sidebar-link`, `.floating-toc-item`, `.callout-btn`) and gallery cards (`GalleryCard.vue:63-67` becomes a real `role="button" tabindex="0"` keyboard control with Enter/Space activation) all carry the canonical focus ring already at `AppHeader.vue:174-177`. WCAG 2.4.7 discharged across the surfaces.

**The binding mutual-exclusion**: G1 (cartoon-card alive, computed-style probe) + G4 (axe contrast clean) + G6 (vue-tsc green) — together they foreclose the three β-cheats: a hardcoded-style rewrite would defeat G1's *token* shim; a hex-not-token amber would defeat G4's *token-measure* axe; a partial role-promotion (button without tabindex/keydown) would defeat G6's type check on the Vue prop contract.

---

## §9 — Congruence findings

These are incongruences surfaced during this Wχ-harden of W4 against `D.md §3`, `D/PROGRESS.md`, and `D/coordination/*`. **Recorded only — D.md is not edited herein** (the orchestrator reconciles at Wχ close or W12 close).

1. **D.md §3 W4 row says "1 shim → 14 components un-flattened"** — the live `git grep -l cartoon-card web/src` at HEAD returns **13** consumer files. The discrepancy is benign (the 14th was likely counted via two `:` references in `EquationView.vue` at A4 audit time; the canonical gate is the *computed-style probe*, not the file count). **Suggested D.md reconcile at W12**: read "13 consumer files (14 application sites, one component applies the class twice)".
2. **D.md §3 W4 row says "the gallery orphans resolved (marquee wired or both deleted)"** — matches the §1.3 decision. PROGRESS.md does not yet name the decision criterion; **suggested**: PROGRESS.md W4 notes augment with "decided at dispatch via the marquee mount-test".
3. **D.md §5 row "Design (β)" lists "the light-mode token sweep (`web/src/**/*.css` / glass-ui tokens)"** — the file-bounds wording suggests `web/src/**/*.css` would be edited everywhere; the W4-harden narrows this to **`web/src/style.css` only** (the single `:root` override block — fourier-local; glass-ui carries recorded as a coordination ask). **Suggested**: the §5 row reads "the light-mode token sweep (`web/src/style.css` `:root` overrides; the glass-ui carry recorded as a coordination ask)".
4. **D.md §3 W4 row says "axe light-mode clean"** but D.md §6 hard gates do NOT include a binding contrast-pass list (the cluster of 4 sites). **Suggested**: D.md §6 hard gates "Design" row adds "the four named cluster sites — amber nav label, `#f0b632` text, dimmed timestamps, canvas placeholder — each clear AA".
5. **D.md §7 inherited-from-C/B does NOT list the `.cartoon-card` regression** — it's a C.W5-introduced (glass-ui bump) regression on a B-era class, but D.md §1 thesis names it ("a glass-ui bump silently introduced `.cartoon-card` died, 14 components flattened"). The §7 deferred-debt section should mirror by adding the row: "the glass-ui `.cartoon-card` recipe carry (a coordination ask if the shim proves long-lived) — W4 lands the shim; a future glass-ui PR retires the shim".
6. **The cross-cutting `:focus-visible` discharge** is W4's binding output — D.md §6 close gates name "`:focus-visible` rings present on TOC + gallery cards" but the implementation list at §3 W4 row says "(TOC + cards)" generically. **Suggested**: §3 W4 row reads "(TOC: `PaperSidebar.vue:215`, `MobileFloatingToc.vue:336`, `PaperArticleWindow.vue:124`; gallery cards: `GalleryCard.vue:63-67`)".
7. **The `feedback_em_dashes.md` precept** binds Unicode `—` (U+2014) not `&mdash;` HTML entities; the `ImageUpload.vue:47 &mdash;` site is the only known violation in the touched bounds — captured as a W4.B sub-step. **Suggested**: PROGRESS.md W4 notes record "em-dash convention enforced on `ImageUpload.vue:47`".
