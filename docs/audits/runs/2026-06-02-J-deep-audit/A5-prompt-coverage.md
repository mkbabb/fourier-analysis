# A5 — PROMPT-COVERAGE recap (the whole work-arc)

**Auditor**: A5 (prompt-coverage). **Run**: `2026-06-02-J-deep-audit`. **Date**: 2026-06-02.
**Method**: recap every canonical user prompt P1→P8 across the H→I→J arc; verify each was addressed with artefact evidence (`file:line`/commit); flag anything PARTIAL/OPEN. Read: `docs/tranches/{H,I,J}/`, `docs/tranches/J/design/WC-*.md` + `J.W1-crud-remix.md`, `docs/audits/runs/2026-06-01-{constellation-ui,modern-web,modern-web-audit}/`, `docs/constellation/ADOPTION-ASKS.md`.

**Verdict**: the arc is **substantially addressed**. P1, P2, P3, P4, P5(prose+root-patch), P7 are FULLY addressed; P6 is FULLY-with-a-dangling-ref; **the surviving gaps are all forward (J-execution) gaps**: P5's squared-`ConfiguratorLayer` DEFECT is **booked-not-shipped** (glass-ui-owned, mid-flight), the value.js-J atom-diff arm is **named-in-spec but not yet entered in `ADOPTION-ASKS.md`** (P6/P8 value.js arm), and P8 (this prompt) requires the forward tranche doc this audit feeds.

---

## Coverage matrix

| Prompt | What it asked | Addressed? | Artefact evidence (file:line / commit) | Gap |
|---|---|---|---|---|
| **P1** | Begin+continue tranche **H** in totality; read all docs; agent orchestration + deep parallelization; idiomatic/gestalt, no workarounds | **FULLY** | `docs/tranches/H/FINAL.md` (CLOSED GREEN 2026-06-01); CI run `26773946417` + 4 consecutive green runs `26776247004/45779/50394/54065` on HEAD `b52b945`; six threads α–ζ in §1; commits `62cafc3…b52b945` §3. The e2e gate was actually repaired (4 breakage classes + 3 buried app bugs); `pytest api/tests/`→`pytest api/` widened (225/0). | none |
| **P2** | Recap the tranche + sibling-repo status + deploy repo + value.js & fourier CRUD facilities | **FULLY** | H.ε recon recorded the 5-sibling mid-flight state (keyframes +19, glass-ui +111, words +11, speedtest +563, value.js in-sync) — `H/FINAL.md §1 ε`, `ADOPTION-ASKS.md §6`. Deploy repo: `H/FINAL.md §1 ζ` (DNS reconcile deploy `3c3fbd2`). CRUD facilities recapped at depth in the J authoring (`J/J.md §1`, `J/PROGRESS.md` log): the fourier phantom `most-forked`/`fork_count` + value.js `forkPalette`/`PaletteVersion`/`getProvenance` asymmetry, file-verified. | none — recap was delivered in-session; J formalizes the CRUD half |
| **P3** | Why did the visualizer UI move left→right? Why are the borders squared not rounded? Deploy 6 frontend-design agents to audit all app UIs + a bidirectional design-token/utility Style-Audit | **FULLY** | **6-agent audit** = `docs/audits/runs/2026-06-01-constellation-ui/` (6 per-app reports `fourier.md`/`color-valuejs.md`/`glass-ui-self.md`/`keyframes.md`/`speedtest.md`/`words.md` + the MERGED `style-audit.md`). **Q1 (left→right)** answered: `style-audit.md:9` — "fourier adopted glass-ui `Configurator`/`ConfiguratorLayer` (B.W2); the move went live only now because the SPA deploy chain was dead ~2 months and just got restored." **Q2 (squared)** answered: `style-audit.md:10` — a **DEFECT** (outer `Configurator` is `rounded-card`; inner `ConfiguratorLayer` uses `border-b` dividers with no radius — rounding stops one level too high). Bidirectional token+utility audit present (`style-audit.md` 7 axes, glass-ui-gaps + union-candidates). Forward design-refinement folded into J as the 4 `WC-design-*.md` specs. | none for the *questions*; the squared-edge **fix** is P5's concern (see P5) |
| **P4** | Continue. Redeploy all workflows and agents | **FULLY** | The redeploy produced two further 6-agent runs that continue P3's audit: `docs/audits/runs/2026-06-01-modern-web/` (6 posture reports + `modern-web-tranche-plan.md`) and `docs/audits/runs/2026-06-01-modern-web-audit/` (6 conformance reports + `modern-web-audit.md`). These fed the I tranche authoring (`I/I.md`, `I/PROGRESS.md`). | none |
| **P5** | glass-ui should be **fully rounded by default, at the root**. Continue. | **PARTIAL** | CANON recorded verbatim: `style-audit.md:10` "User CANON is now explicit: glass-ui must be fully rounded by default, at the root." Root-cause traced to glass-ui's own `Configurator.vue:94` (`glass-floating rounded-card`) at `glass-ui-self.md:50`, `style-audit.md:123,259,266`. A **`glass-ui-root-rounding.patch`** was drafted (`docs/audits/runs/2026-06-01-constellation-ui/glass-ui-root-rounding.patch`). glass-ui committed root-rounding `b6d6cf4` (referenced `I/I.md:5,125,136`, `CANONICAL-ORDERING.md:668`). **BUT** the actual P5 defect — the inner `ConfiguratorLayer` sections rendering squared (`border-b`, no radius) — is **byte-identical in 2.x and 3.x** (`style-audit.md:10,266`): the `^2→^3` bump does NOT fix it; it needs a code change at the `Configurator`/`ConfiguratorLayer` root which is **glass-ui-owned and the constellation was mid-flight** → booked-not-shipped under inv-16′. | **the squared-`ConfiguratorLayer` inner-section rounding is BOOKED-NOT-SHIPPED** — the drafted patch + `b6d6cf4` address the outer container's `rounded-card→rounded-panel` axis, but no fourier-side or shipped-glass-ui evidence confirms the *inner section* rounding (the exact user-reported defect) is live. Not in `ADOPTION-ASKS.md` as a discrete entry. |
| **P6** | Fold into the tranche with an idiomatic fix; download/read/use **modern-web-guidance** with 6 agents to analyze all child repos (focus fourier, value.js, glass-ui + others); long-horizon expand+optimize the tranche set with more waves | **FULLY** (one dangling ref) | modern-web-guidance v0.0.170 used (`/tmp/mwg/.../guides`, 12 categories) across two 6-agent runs (P4 evidence). All child repos covered incl. value.js (`value.js-color.md`/`value.js.md`), glass-ui, keyframes, words, speedtest. Tranche-set expansion = **I authored** (9 waves α–ι, `I/I.md`) then closed with ~65% converged into glass-ui AQ (`I/FINAL.md §4`), then **J authored** (CRUD/REMIX + leaf perf, `J/J.md`). The "idiomatic fix" fold is the leverage principle (glass-ui-rooted first) honored throughout. | **dangling ref**: `J/J.md:5,37` + `J.W1-crud-remix.md:6` cite `docs/constellation/next/audit/A3-fourier-valuejs.md` as the WAVE-D seed, but that file does NOT exist in fourier — it lives in `/Users/mkbabb/Programming/glass-ui/docs/constellation/next/audit/A3-fourier-valuejs.md`. The fourier-relative path is broken. |
| **P7** | Deploy another 6 agents to properly audit the constellation with the precepts from the Google reference | **FULLY** | `docs/audits/runs/2026-06-01-modern-web-audit/` — 6 conformance reports + the MERGED `modern-web-audit.md` (12-category scorecard matrix, inv-29/inv-30 lens, P0–P3 severity, per-row inv-30 flag). It CONFIRMS+EXPANDS the P3 style-audit and the P4 posture pass, and validates the I plan (`modern-web-audit.md:30-34`). | none |
| **P8** | DEEPLY audit (6 agents) the original plan + waves + all changes; devise a path forward; no workarounds/legacy; delineate chronic/deferred items and fold them; recap ALL prompts; tranche-development only; for fourier AND value.js spec a fully-formed CRUD remix facility (diff + provenance, git-like atomic diffing, KISS); analyze current + fourier + value.js tranches | **PARTIAL (in-flight — this audit)** | The fourier half of the CRUD/REMIX spec is FULLY authored: `J/J.md` + `J/design/J.W1-crud-remix.md` (5 config atoms, per-atom + set-hash, `visualization_versions` collection + `{parent_hash,set_hash,atom_diff}` edge, 5 endpoints, `lib/crud/atomdiff.{py,ts}` shared PATTERN, single-parent LINEAR, no DAG/merge/CRDT — KISS honored). Chronic/deferred folded: `J/J.md §8` (e2e/axe CI evidence, scheduler.yield, CSP/fetchLater, content-visibility gap; VAL-1/VAL-9 disclaimed; DAG declined). This A5 recap + the sibling A-agents are the 6-agent deep-audit. | **OPEN by construction**: (a) the **value.js-J arm is spec'd in `J.W1-crud-remix.md §6` but NOT yet a discrete `ADOPTION-ASKS.md` entry** — J.md §6/§8 claims it "is a NAMED, ledgered ask in `ADOPTION-ASKS.md`", but `ADOPTION-ASKS.md` (last-touched 2026-06-01) contains zero J-era/atom-diff entry; the claim is forward-tense, the booking does not yet exist. (b) P8 still requires the **forward tranche doc** this audit feeds (the consolidated path-forward + folded ledger). (c) J is AUTHORED-only (W0/W1 done; W2+ planned) — no execution, correct for tranche-dev-only. |

---

## Severity-ranked gap summary

- **P1 (NIT)** — book the `A3-fourier-valuejs.md` source either by copying/symlinking it into fourier or by re-citing the glass-ui-repo path explicitly (it is the WAVE-D seed the whole J tranche rests on; a dangling provenance pointer is an inv-traceability hole).
- **P2 (NIT)** — the value.js-J ADOPTION-ASKS booking the J docs assert ("NAMED, ledgered in `ADOPTION-ASKS.md`") must actually be written; today it is forward-tense prose without the ledger row.
- **P5 (P1)** — the squared-`ConfiguratorLayer` inner-section rounding (the literal user-reported defect) is booked-not-shipped: a drafted patch + glass-ui `b6d6cf4` address the OUTER container axis (`rounded-card→rounded-panel`), but no evidence confirms the INNER `border-b`-divider sections are now rounded, and the fix is glass-ui-owned + was mid-flight. This is the one prompt whose *fix* (not its diagnosis) is genuinely incomplete.
- **P8 (in-flight)** — the value.js arm of "for fourier AND value.js, a fully-formed CRUD remix spec" is authored as a cross-repo parity TABLE inside fourier's spec, not as value.js's own spec doc / booked ask; the forward tranche doc is owed.

---

## Fold recommendations (into the forward J tranche)

1. **Fix the `A3-fourier-valuejs.md` dangling path** (P1/P6) — copy the WAVE-D seed into `fourier-analysis/docs/` or re-cite the glass-ui-repo path; J's CORE provenance must resolve in-repo.
2. **Write the value.js-J `ADOPTION-ASKS.md` entry** (P2/P6/P8) — a discrete, file-verified, per-repo-green-CI-gated inv-16′ row: value.js extends `forkPalette`→`remixPalette`, `PaletteVersion` gains `atom_diff`, `GET /:slug/diff?from=`, `lib/crud/atomdiff.ts`; this is the booking J.md already *claims* exists.
3. **Promote the squared-`ConfiguratorLayer` fix to a shipped/booked line item** (P5) — confirm whether the inner-section rounding is live in glass-ui post-`b6d6cf4`; if not, book it as a discrete `ADOPTION-ASKS.md` glass-ui entry (the user CANON "fully rounded by default, at the root" is not satisfied until the inner sections round). This is the highest-severity carry.
4. **Land the J WC design-refinement specs as a WAVE in the forward tranche** (P3 forward-design) — the 4 `WC-design-*.md` specs (layout/motion/atmosphere-a11y/typo-color) are authored but currently orthogonal to J's CORE; they answer the *aesthetic* half of P3 and should be a sequenced wave, not loose design docs.
5. **Carry the I-deferred + ι-tail ledger forward as J W3–W6** (P8 chronic/deferred) — already folded in `J/J.md §8` (scheduler.yield, content-visibility gallery gap, e2e/axe CI evidence, CSP/fetchLater); keep them gated by inv-27 green CI at execution.
