# A6 — CHANGES-MADE + NO-LEGACY + INTEGRITY audit (tranche J, both repos)

**Auditor**: A6.
**Date**: 2026-06-02.
**Scope**: everything actually changed/authored across the J arc in `fourier-analysis` and `value.js` — half-done work, internal inconsistency, workarounds, legacy cruft, dangling references.
**Repos at audit**: fourier `7ad8fce` (clean tree + untracked `docs/audits/runs/2026-06-01-constellation-ui/`); value.js `2f7fc87` (clean tree + untracked `docs/tranches/C/`).
**Verdict**: **SOUND-WITH-REFINEMENTS.** J is planning-only and touches no source (correct for an AUTHORED tranche). The thesis (CRUD/atom-diff, single-parent linear, no DAG) is KISS-faithful and well-grounded against real file:line. But there are real integrity gaps: (a) a cross-repo dangling seed citation, (b) an orphaned WAVE-C bolt-on never wired into J's own plan, (c) an unbacked ADOPTION-ASKS claim, and (d) a three-way module-name inconsistency in the "shared PATTERN."

---

## What the arc actually changed (the diff)

The J arc is **docs-only**. `git diff 0c4d806..HEAD` (fourier) = 7 files, +1256 lines, all under `docs/tranches/J/`. No `api/`, no `web/src/`. Same shape in value.js (`5558f8d`, `2f7fc87`). This is correct: J is AUTHORED-only, awaits "Begin." **No legacy code, no commented-out blocks, no TODO/FIXME/HACK was introduced this arc** — the sweep over both J doc trees is clean. The 46 "fallback/floor" mentions are all inv-29 progressive-enhancement framing (`scheduler.yield`→`setTimeout` floor; `CSS.highlights`→wrapper-span fallback), which is the *correct* idiom, not legacy.

Two factual claims J leans on were verified true against live source:
- The **phantom `fork_count`** is real: `api/lib/crud/cursors.py:21` sorts `most-forked` by `fork_count`, and that field is **not on** `api/models/visualization.py` — a genuine read-side-without-write-side. J's CORE thesis stands.
- The **`cartoon-card` "dead-class resurrection"** the WC-layout doc cites is real and *self-documented* at `web/src/style.css:98-108` ("glass-ui removed the `.cartoon-card` recipe at C.W5 … The shim re-binds the class"). This is a true legacy smell — but it is a **D.W4 artifact, not introduced by J** (so it is a fold candidate, §findings F1, not a J regression).

---

## Findings

| # | Sev | Title | Location | Issue | Idiomatic fix |
|---|---|---|---|---|---|
| A6-1 | P1 | Dangling cross-repo seed citation — the A3 "WAVE-D seed" lives in a THIRD repo (glass-ui), cited as repo-local | fourier `J/J.md:5,29,56,116,123`; `J/PROGRESS.md:37,48`; `J/design/J.W1-crud-remix.md:6`; value.js `J/J.md:6,7,37`; `J/PROGRESS.md:48` | Both tranches cite `docs/constellation/next/audit/A3-fourier-valuejs.md` as the authoritative seed. That path **does not exist** in fourier (`docs/constellation/` holds only `ADOPTION-ASKS.md` + `DEPLOY-STANDARDIZATION-DESIGN.md`) nor in value.js (no `docs/constellation/` at all). The file actually lives at `/Users/mkbabb/Programming/glass-ui/docs/constellation/next/audit/A3-fourier-valuejs.md` (committed glass-ui `d2aa67f`, "WAVE A deep 6-agent next-round audit"). The seed is REAL — its content (§4 the CRUD/REMIX gap, §5.1/§5.2 the J sketches) exactly matches what J claims — but every citation is a bare repo-local relative path that resolves to nothing in the repo it sits in. A tranche cannot cite a seed by a path that 404s in its own tree. | The seed is glass-ui's (the constellation hub repo owns `docs/constellation/next/`). Make every citation **repo-qualified**: `glass-ui:docs/constellation/next/audit/A3-fourier-valuejs.md`, the same way INVARIANTS.md already qualifies glass-ui precepts. Do NOT copy the file into fourier/value.js (that forks the seed). One find-replace per repo across the 8/5 cited lines. |
| A6-2 | P1 | WAVE-C (4-lens frontend-design refinement) is an orphaned bolt-on — never referenced by J's own plan | fourier `J/design/WC-*.md` (4 files, +707); value.js `J/design/WC-*.md` (4 files, +257); committed `7ad8fce` / `2f7fc87` | The commit titles it "WAVE C frontend-design refinement," but **neither J.md, PROGRESS.md, nor the wave schedule mentions WAVE-C / J.WC / "Wave C" anywhere** (grep returns empty in both repos). The fourier wave schedule is W0–W7; value.js is W0–W5; there is no "WC" slot, no disposition row, no §-anchor. The docs are pure UI design specs (typography ladder, glass-ladder altitude, aurora, motion orchestration, a11y) grounded in glass-ui levers — **orthogonal to J's CRUD/atom-diff thesis**, which the fourier WC-layout doc *itself admits* at line 96: "The CRUD/remix backend (J.W1) is orthogonal; this spec touches only `web/src/` chrome." A 4-lens frontend-design refinement is not a wave of a data-model tranche. It is either (a) a misfiled artifact that belongs to an I-style modern-web/design tranche, or (b) a legitimate-but-unwired wave that J never declared. As authored, it is substrate-without-a-home: it appears in `design/` but no plan consumes it (a doc-level inv-15 violation). | Decide and wire explicitly. EITHER promote it to a real declared wave (add a "WC — frontend-design refinement" row to both wave schedules with a disposition, fold it into J's thesis as "the design surface the remix/diff features land on," and cross-link J.md↔WC), OR demote it out of J (move to a `docs/tranches/J/design/refinements/` clearly marked "exploratory, not a J wave," or relocate to its own design tranche). It cannot stay in `J/design/` as an undeclared peer of the binding `J.W1` spec. |
| A6-3 | P1 | The value.js-J cross-repo ask is claimed as a NAMED ADOPTION-ASKS entry, but the ledger has no such entry | fourier `J/J.md:82,96` ("a NAMED, file-verified `ADOPTION-ASKS.md` entry"); §3 close-gate `:43`; §7 `:109` | inv-16′ requires every cross-repo arm to be a NAMED, ledgered, per-repo-green-CI-gated ask. J.md §6 asserts the value.js-J atom-diff adoption *is* "a NAMED, file-verified `ADOPTION-ASKS.md` entry." Grep of `docs/constellation/ADOPTION-ASKS.md` for J / value.js-J / atom-diff / remix / WAVE-D returns **nothing**; the file was last touched at H (`013a466`, "inv-16′ book-all"). The claim is **currently false** — the ledger entry does not exist. For an AUTHORED (not yet opened) tranche this is forgivable as a W0 deliverable, but the prose states it in the present tense as already-done, which is an inv-27/inv-16′ honesty gap (a "booked" claim with no artifact). | Either (a) soften the tense to "will be seeded at W0" everywhere J asserts the entry exists (J.md §6, §3, §7), OR (b) actually author the ADOPTION-ASKS entry now as part of the authoring (it is a doc edit, in-scope for a planning tranche). Option (b) is the stronger fix: it makes the inv-16′ gate real at authoring time rather than a forward promise. |
| A6-4 | P2 | The "shared PATTERN" has three different module names across the cohort — undermines the cross-repo parity claim | fourier `J/J.md:31,57,59,81` (`lib/crud/atomdiff.py/.ts`); value.js `J/J.md:5,31,83,152` (`lib/crud/remix`) vs `J/J.md:83,86` + `J.W1:77,268` (`api/src/services/palette/diff.ts`) | J's spine is "authored once, adopted twice, same PATTERN." But the pattern is named **three incompatible ways**: fourier calls it `lib/crud/atomdiff.py` (+ `.ts` for the twin); value.js prose calls it the canonical `lib/crud/remix` pattern; value.js's own §5 critical-files table + W1 spec locate the actual file at `api/src/services/palette/diff.ts` — which is **not under `lib/crud/` at all**. So the "same module, two adoptions" claim is internally contradicted: there is no `lib/crud/remix` file in the spec, and fourier's `lib/crud/atomdiff` has no value.js counterpart at the same path. inv-16 (shared-by-CONTRACT, per-language module) is satisfiable, but only if the two repos name the contract consistently. | Pick ONE canonical pattern name and reconcile both repos. The cleanest is `lib/crud/atomdiff.{py,ts}` (fourier already uses it; "atomdiff" names the actual artifact, "remix" names the feature). Then value.js's `services/palette/diff.ts` either becomes `lib/crud/atomdiff.ts` (matching the contract path) or the prose stops calling it `lib/crud/remix`. The cross-repo `/diff` shape-parity gate is meaningless until the module name parity is fixed first. |
| A6-5 | P2 | WAVE-C symmetry is broken between repos — value.js's WC threads into J.W1, fourier's is fully orphaned | fourier `J/design/WC-design-layout.md:96`; value.js `J/design/WC-design-layout.md:5,37` | A6-2 manifests asymmetrically. value.js's WC-layout *does* bind to J's thesis: line 5 names `J.W1-palette-remix.md` as its sibling ("this doc governs the SPATIAL surface those features land on — the BrowsePane grid, the diff render's home"), and line 37 wires the aurora to the booked VAL-1 OKLab-LUT as "the aurora-LUT's second consumer" (a real inv-15 consumer thread). fourier's WC has none of that — its only mention of J.W1 is to declare it "orthogonal." So one repo's WC is a thin-but-real J wave; the other's is a free-floating design essay. Since J's headline is the *symmetric* cross-repo close, the design layer being asymmetric is a coherence defect. | Resolve A6-2 first. If WC is promoted to a wave, make fourier's WC thread into J.W1 the same way value.js's does (name the gallery/diff-viewer as the surface the remix/diff features render on — the diff-viewer IS a fourier consumer per J.W1 §5.2). If WC is demoted, demote it in both repos identically. |
| A6-6 | NIT | Minor I-close framing — verify fourier-I "A–I all CLOSED GREEN" against the e2e/axe deferral | fourier `J/J.md:4` | J.md:4 states "A–I all CLOSED GREEN" while the same sentence (and fourier-I FINAL.md:4) concedes "e2e/axe deferred to CI" — i.e. fourier-I closed without a covering green CI run for the e2e arm (the exact inv-27 gap J's W5 EVIDENCE wave exists to repair). Calling I "CLOSED GREEN" while its own evidence wave is the thing J must still execute is a soft inv-27 tension (not a contradiction — I closed on a documented deferral). | Phrase as "A–I CLOSED (I with the e2e/axe evidence honestly deferred to J.W5 per inv-27)" so the green-claim is not asserted over the gap J is chartered to close. The I-close *dates* (fourier 2026-06-02, value.js 2026-05-28) are each cited correctly against their own predecessor — no finding there. |

---

## What is SOUND (no finding — recorded so the close doesn't re-litigate)

- **KISS discipline is faithful.** Single-parent LINEAR, flat content-addressable atom bag, per-atom set-difference, no DAG/merge/rebase/CRDT — stated in J.md §1/§8/§9, J.W1 §0/§9, value.js J.md §1/inv-J-1, and the design specs. The guardrails are named, not assumed. Matches the precept verbatim.
- **inv-15 substrate-without-consumer** is honored in the CORE: J.W1 §5 names ≥2 real consumers per endpoint (gallery `most-forked` write-side, diff-viewer, provenance breadcrumb; WebMCP booked-not-built). The 5-atom set is closed with per-atom NOT-an-atom rationale (J.W1 §1.1) — overfitting-disciplined.
- **inv-16 / inv-26** held in intent: a shared PATTERN (per-language module), explicitly NOT a shared package, NO codegen, hand-typed-canonical. (The *naming* of that pattern is A6-4 — the discipline is right, the label is inconsistent.)
- **VAL-1 / VAL-9 ownership is symmetric and correct.** value.js-J OWNS them (binary ≥2-consumer-gated, J.W4); fourier-J §8 DISCLAIMS them as "not a fourier residual." The two sides agree.
- **inv namespaces are clean.** fourier-J uses inv-16′/inv-27/inv-29/inv-30; value.js-J uses its own inv-I-1/inv-I-2/inv-J-1/inv-J-2 lineage. No cross-contamination (fourier-J never wrongly cites inv-I-1).
- **No half-authored text.** J.md, PROGRESS.md, and the W1 specs are internally complete; every §-anchor referenced resolves within its own doc; FINAL.md/CANONICAL-ORDERING/INVARIANTS targets all exist.

---

## Fold recommendations (for the forward tranche)

| Item | Current status | Recommended fold | Rationale |
|---|---|---|---|
| A3 seed citation repo-qualification | dangling (resolves to non-existent path in both repos) | **W0** (audit intake) — fix the citation as part of "intake A3's WAVE-D seed" | W0 already claims to intake the seed; it must first make the seed resolvable. One sed per repo. |
| WAVE-C disposition (promote-as-wave OR demote-out) | orphaned/dangling — committed but unwired | **W0 / pre-open ratification** — the user must rule whether WC is a J wave | A 4-lens design refinement in a CRUD tranche is an architectural decision, not an editorial one; it cannot ship undeclared. If kept, it changes J's wave count + thesis scope. |
| value.js-J ADOPTION-ASKS entry | claimed-present, actually missing | **W0** (the §charter already says "seed the value.js-J inv-16′ ask") | The claim is stated present-tense; make it true at authoring or soften the tense. inv-16′ gate. |
| `lib/crud/atomdiff` vs `lib/crud/remix` vs `services/palette/diff.ts` | inconsistent across 3 names | **W1** (the CORE spec is the contract — pick the canonical name there) | The `/diff` shape-parity close-gate depends on a single agreed pattern name. |
| `cartoon-card` dead-class shim (`web/src/style.css:98-108`) | chronic legacy (D.W4 artifact; 14 consuming sites) | **the WC layout wave, IF WC is promoted** — else book as a standalone hygiene residual | This is a genuine NO-LEGACY violation (a resurrected retired class), correctly diagnosed by WC-layout R1 but only actionable if WC becomes a real wave. It predates J; it should not silently ride J's docs without a home. |
