# 2026-05-26 — Wave-2 audit synthesis (six-agent CRUD fan-out)

Authored by **agent S2 — Wave-2 audit synthesis + B-development close ceremony** against HEAD `f8db2c6` (the post-Wave-1 synthesis commit). This document consolidates the six concurrent Wave-2 audit lanes (C1–C6) into a single artefact and applies the final B-plan amendments before the B-development authoring phase closes. The six lane docs remain available as evidence; this synthesis is the substrate-of-record for B's W0 dispatch.

## §0 — Goal criterion and completion criterion (paired)

**Goal criterion.** This document succeeds when a reader of tranche B can — without recourse to the six underlying Wave-2 audit artefacts — reconstruct (a) each lane's headline finding, (b) the cross-lane themes that surface in more than one lane, (c) the consolidated inheritance ledger enumerating every Wave-2 finding that warrants a B-plan amendment, and (d) the prescribed B amendments that discharge each load-bearing row. The synthesis is the *substrate of record* the final B-authoring revision consumes; the six C-lane docs remain available as evidence.

**Completion criterion.** The document carries: the six-lane recap (§1); the cross-lane theme synthesis (§2); the consolidated inheritance ledger (§3 — every Wave-2 amendable finding gets a row with severity + named-destination disposition); the prescribed B amendments (§4); and the honest reckoning (§5). Both criteria hold at this writing.

## §1 — The six lanes recapped

**C1 — fourier CRUD substrate audit** (`C1-fourier-crud-substrate.md`, 120 lines, 8 collections × 8 routers × 5 identity schemes × 10 non-conforming clauses × 4 HIGH defects). Walked fourier's present CRUD surface end-to-end at HEAD `f8db2c6`: enumerated the eight Mongo collections (`images`, `contours`, `snapshots`, `users`, `sessions`, `gallery`, `flags`, `admin_audit`), the eight routers, and the five identity schemes (human slug, content hash, UUIDv4, ObjectId, IndexedDB `imageSlug`); scored §1–§9 of `CRUD-CONTRACT` row-by-row; surfaced ten non-conforming clauses (7 DRIFT + 2 NOT-YET-IMPLEMENTED + 1 PARTIAL) and 13 defects ranked by B-wave destination. **Headline finding:** 4 HIGH defects bind B.W1 ratification + B.W3 entity migration — the `gallery.py:162` orphan-publish path; no `visualization` entity; the over-loose `SLUG_PATTERN` at `dependencies.py:27`; and the **non-cryptographic RNG** behind `coolname` (CPython Mersenne, not `secrets`). **Deliverable:** §6 defect ledger with 4 HIGH / 5 MED / 4 LOW rows, named-destination per row, plus §7 W1 ratification recommendations.

**C2 — value.js CRUD substrate audit** (`C2-valuejs-crud-substrate.md`, 79 lines, 9 palette-api collections × 13-section contract scoring × 6 incidental landings). Audited value.js v0.10.0 at the HEAD of its own sequence (post-D / E / F / G / H thesis runs, C never opened); enumerated nine palette-api Mongo collections, scored value.js's substrate against the 13-section CRUD-CONTRACT, and confirmed the **six-of-thirteen incidental-landings tally**: content hash (§2), sessions (§3), bounded cron (§8), envelope (§9), pagination (§10), user-slug shape (§1). **Headline finding:** the cohort is empirically **dissolved-not-delayed** — value.js's substrate is structurally complete under its own theses but conforms to only 6 of 13 contract sections, and the seven non-conformances cluster on the SOTA-2026 conventions the contract proposes (soft-delete grace, RFC 9457, ETag, Idempotency-Key, three-state visibility) — adopting those upgrades would be a new tranche, not a continuation. **Deliverable:** §7 implication paragraph naming the cohort dissolution and binding fourier-B to treat CRUD-CONTRACT as fourier-internal coherence per R4 §5.

**C3 — Convergence shape audit** (`C3-convergence-shape.md`, 73 lines, 13-section binding-force scoring × 3-tier disposition × 4 Wχ probe re-validations). Re-read CRUD-CONTRACT against C1's and C2's substrates; classified each of the 13 contract sections by convergence verdict; re-validated the three-tier disposition (shared-data / per-language-utility / framework-rejected); re-ran the four Wχ probes against the orphan verdict; recommended the binding-force the contract should carry going forward. **Headline finding:** the contract is **9 both-bound (B) / 2 fourier-only (F) / 2 advisory (A)** — fourier-mandatory + advisory-both-sides on cohort-reopen. The Wχ.P1 framework-in-disguise probe **passes at 0 % code-fraction**; the three-tier disposition holds unchanged (1 shared-data item at `docs/precepts/data/slug-words.json`; 8 per-language sub-modules per repo; framework class rejected). **Deliverable:** §6 four-row recommendation set binding the amended §10 close-rule to fourier-only ratification with value.js cells held DEFERRED.

**C4 — Schema + Conformance Matrix audit** (`C4-schema-matrix.md`, 92 lines, 9 schema sections × 26 matrix sub-sections × 182 rows). Validated `SCHEMA.md` (864 L; 9 numbered sections) and `CONFORMANCE-MATRIX.md` (529 L; 26 sub-sections; 182 rows = 176 cross-repo + 6 §F) against the post-Wave-1 substrate; pinned the gate-or-bind threshold for B.W1. **Headline finding:** **0 / 182 rows PASS at HEAD `f8db2c6`** — the corpus is *entirely aspirational*; no test file exists at any of the 176 named paths; no `scripts/conformance/`; no `api/lib/crud/`. Paper-binding PASSES (every row carries a non-empty Run command cell); empirical-binding FAILS. **Schema verdict**: 9 / 9 conforming with 3 minor citation deltas (RFC 4648 absent, `AnimationData` body undefined, `Idempotency-Key` parameter undefined). **Matrix verdict**: 4 schema gaps + 5 matrix gaps + 3 test-surface gaps = 12 gaps. **Deliverable:** §6 W1 recommendations naming the 4 schema addenda, the matrix CS5.2 reconciliation, the §F `AnimationData` row, and the 14-file + 5-script test-surface skeleton authoring.

**C5 — Migration story audit** (`C5-migration-story.md`, 144 lines, 9 entities × 9 transformations × 10 risks × 9 W3 scope-item gaps). Audited the migration path by which the five identity schemes collapse to one `visualization` entity; enumerated every pre-migration entity (7 server collections + IndexedDB drafts + embedded epicycle payload); named detection-transformation-validation-cutover quadruples; surfaced risks the W3 spec does not yet absorb; issued discrete scope-item additions for `W3.md`. **Headline finding:** the migration is **W3-load-bearing** with image-blob fork **Option B** (defer entirely; visualization carries `image_slug` FK that resolves wherever the blob lives — today Mongo-inline, future fourier-C external). 10 risks surfaced (top-3: orphan snapshots assigned `anon-migrated-NNN`; dangling contour-hash post-condition; `migrated_from` marker for idempotency). 9 W3 scope-item gaps (W3.16–W3.24): orphan-snapshot detection mechanism; anon-migrated minting function; dangling-contour post-condition; `migrated_from` marker; epicycle-animation denormalisation canonicalisation; image-blob FK survival; IndexedDB lift deferral; integration/transform pytest specs; `--reload` interference docstring. **Deliverable:** §5 migration test surface (9 specs) + §6 image-blob fork Option-B recommendation + §7 W3.16–W3.24 scope-item ledger.

**C6 — Risks + SOTA opportunities** (`C6-risks-sota.md`, 122 lines, 9-wave risk matrix × 5+4 chronic items × 16 SOTA candidates × 9 KISS-rejection re-validations × 5 orphan-resurgence preventers). Catalogued execution risks per B wave; re-examined the five chronic-deferral items L6 ratified; scored 16 SOTA candidates ADMIT / REJECT / DEFER under KISS; re-validated the nine 2026-05-19 KISS rejections; verified the five orphan-verdict-resurgence preventers; emitted a severity-classified risk matrix. **Headline finding:** **5 HIGH-flag instances + 4 MEDIUM-flag instances** across 9 waves; the top-3 SOTA ADMITs are **RFC 9457 problem+json** (→ B.W1 + B.W3), **RFC 9110 ETag + If-Match** (→ B.W3 + B.W4), **RFC 9239 RateLimit headers** (→ B.W4); 9 / 9 KISS rejections HOLD; 5 / 5 orphan-resurgence preventers in place. **Deliverable:** §4 sixteen-row SOTA matrix (ADMIT 8 / REJECT 6 / DEFER 2) + §7 top-3 ADMITs with B-wave routing + §8 risk matrix.

## §2 — Cross-lane themes

**Theme α — The orphan-verdict empirically confirmed (C2 + C3 + C6 converge).** C2's empirical scoring of value.js v0.10.0 against the 13-section contract returns **6 of 13 incidental landings** with the seven non-conformances clustering on the SOTA-2026 conventions the contract proposes — confirming the cohort is *dissolved, not delayed*. C3's section-by-section binding-force scoring concurs at the meta level: §10's "both columns PASS" cohort gate is structurally unmeetable. C6 §6 verifies the five orphan-verdict-resurgence preventers (banner, FILED-not-deleted, named-residual destinations, DEFERRED cell status, latent-affordance posture) are in place at spec level. The verdict that B.W1 ratifies on fourier-only authority — value.js cells held DEFERRED — is no longer a guess; it is empirically substantiated by three independent Wave-2 lanes.

**Theme β — The contract is fourier-mandatory + advisory-both-sides on cohort-reopen (C3 ratifies; C6 endorses).** C3 §6 recommendation 1 binds the contract's authority shape: *mandatory-fourier-side-only; advisory both-sides on cohort-reopening*. The §10 close-rule (already amended at `CONFORMANCE-MATRIX.md:515`) becomes "all 88 fourier cells PASS + all 6 §F cells PASS; the 88 value.js cells DEFERRED" — the cohort-level "176 cells PASS" gate is structurally unmeetable but the fourier-only path remains. C6 §4 endorses by ranking the three top SOTA ADMITs (problem+json, ETag, RateLimit headers) as **all standards-compliant projections** of existing fourier substrate — none reopens value.js dependence; all bind fourier alone. The binding force preserves the cohort affordance for any successor tranche.

**Theme γ — The migration story is W3-load-bearing (C1 + C5 + C6 W3-HIGH-risks).** C1 §6 #1 names the canonical regression case (`gallery.py:162` orphan-publish path → 401 enforcement at the dependency layer is a B.W3 entity-migration concern, not a contract-text concern). C5 §3 + §4 enumerate the 9 transformations and 10 risks — *every* risk lands in W3's brittleness window. C6 §2's risk matrix flags W3 with **3 HIGH-flag instances** (data-loss; within-wave brittleness; framework-in-disguise discipline). C5 §7 issues 9 scope-item additions (W3.16–W3.24) that must fold into `W3.md` before W3 dispatches honestly. The migration is the load-bearing seam — entity definition (model), data preservation (counts + spot-check), idempotency (marker field), and post-condition straggler check all converge there.

**Theme δ — The empirical-binding is FAIL at HEAD; B.W1 ratifies on paper-binding; B.W3 + W4 + W5 empirical-bind (C4 + C6).** C4 §5 records **0 / 182 rows PASS at HEAD `f8db2c6`** — the matrix is aspirational. The hard-gate threshold for B.W1 is therefore the *paper binding*: every row has a non-empty `Run command` cell (which holds today). The *empirical binding* — `94 fourier rows PASS` (88 cross-repo + 6 §F) — closes at B.W3 (entity + utility-module landing) and B.W4 (consumer adoption). C6 §2 risk #5 binds the helper-adoption hard-gate (≥ 3 `from api.lib.crud` imports in `admin.py`; ≥ 10 helper sites across migrated routers) to prevent framework-in-disguise resurgence at the adoption boundary. The empirical-binding-FAIL is the honest gate.

**Theme ε — The SOTA opportunities cluster on standards-compliant projections, NOT framework-in-disguise (C3 + C6).** C6 §4's 16-row SOTA matrix admits 8 candidates, rejects 6, defers 2. **Every ADMIT is a standards-compliant projection of existing substrate** (RFC 9457 envelope of existing `HTTPException`; RFC 9110 ETag on existing PATCH/DELETE; RFC 9239 RateLimit headers on existing rate-limiter; RFC 4648 base64url on existing cursor; RFC 8288 Link on existing cursor; Idempotency-Key on existing POST; `extra=forbid` on existing Pydantic; pnpm workspaces already adopted). **Every REJECT is a topology change** (MongoDB Change Streams; msgpack; Tanstack Query; WebTransport; HTTP/3; time-series collections for audit). C3 §3 confirms the three-tier disposition holds unchanged: 1 shared-data item + 8 per-language sub-modules per repo + framework class rejected. The Wχ.P1 framework-in-disguise probe re-runs at **0 % code-fraction** — the rejected-list is empirically the right discipline.

## §3 — Consolidated inheritance ledger

Every Wave-2 finding that warrants a B-plan amendment gets a row. Severity: **HIGH** (load-bearing — closes a load-bearing gate); **MED** (load-bearing — closes a non-blocking gate); **LOW** (cosmetic / hygiene); **INFO** (tracking-only). Disposition codes: `NEW-INVARIANT`, `NEW-SCOPE-ITEM-IN-EXISTING-WAVE`, `NEW-COORDINATION-DOC-ROW`, `ALREADY-ABSORBED`, `CONFIRM-AS-IS`.

| # | Source | Finding | Severity | Proposed B-amendment | Disposition |
|---|---|---|---|---|---|
| 1 | C1 §6 #1 | Orphan publish path — `gallery.py:162` writes `user_slug: null` rows | HIGH | Already absorbed in W3 scope item 6 + C-mig-3 post-condition | ALREADY-ABSORBED |
| 2 | C1 §6 #2 | No `visualization` entity; gallery URL is sha256 | HIGH | Already absorbed in W3 scope item 1–5 (entity carve) | ALREADY-ABSORBED |
| 3 | C1 §6 #3 | Slug pattern admits non-4-word at `dependencies.py:27` | HIGH | Already absorbed in W3 scope item 6 (tighten `SLUG_PATTERN`) | ALREADY-ABSORBED |
| 4 | C1 §6 #4 | Slug RNG not cryptographic — `coolname` → CPython Mersenne | HIGH | **NEW-INVARIANT 21** (slug-mint cryptographic-RNG); CRUD-LIB-PY `slugs.py` must use `secrets.choice` | NEW-INVARIANT + COORDINATION-DOC |
| 5 | C1 §6 #5 | Slug collision handling asymmetric (`image_storage.py:75–77` TOCTOU; `sessions.py:47` no retry) | MED | Already absorbed in W3 scope item 8 (migration uses `slug_with_retry`) + W4 helper adoption | ALREADY-ABSORBED |
| 6 | C1 §6 #6 | Batch return shape `{ok, affected}` vs contract's `{processed, errors[]}` | MED | Already absorbed in W4 endpoint table swap; pair with problem+json | ALREADY-ABSORBED |
| 7 | C1 §6 #7 | Session TTL 7 d vs contract 30 d | MED | NEW W4 scope item — one-line `session_ttl_days` bump + live `expires_at` extension | NEW-SCOPE-ITEM-IN-EXISTING-WAVE (W4) |
| 8 | C1 §6 #9 | Frontend `WorkspaceDraft.savedSnapshots: []` dead field | LOW | Already absorbed in W4 store re-point (workspace.ts) | ALREADY-ABSORBED |
| 9 | C2 §6 | value.js `Palette`/`colorScale`/`sampleToSVGPath`/shared slug-words structurally absent | INFO | Already absorbed at W4 fallback-primary + B.md §7 named-residual | CONFIRM-AS-IS |
| 10 | C2 §7 | Cohort dissolved-not-delayed; B.md §1 thesis binding | INFO | B.md §1 thesis already records orphan-verdict effect; no thesis bullet contradicts | CONFIRM-AS-IS |
| 11 | C3 §6 recommendation 1 | Binding force: mandatory-fourier-side + advisory-both-sides on cohort-reopen | HIGH | NEW-COORDINATION-DOC row in CRUD-CONTRACT §0 (binding-force clause); CONFORMANCE-MATRIX §10 already amended | NEW-COORDINATION-DOC + ALREADY-ABSORBED |
| 12 | C3 §6 recommendation 2 | Slug-words location confirmed at `docs/precepts/data/slug-words.json` | INFO | Already in SLUG-WORDS.md §1; B.W3 scope item 12 names the loader; file empirically absent at HEAD — owed at W3 close | CONFIRM-AS-IS + W3-OWED |
| 13 | C4 §6 #1 (a) | RFC 4648 citation absent in SCHEMA §1 and §9 | LOW | NEW-COORDINATION-DOC row — SCHEMA §1 cursor convention adds RFC 4648 §5 citation; §9 table adds RFC 4648 row | NEW-COORDINATION-DOC |
| 14 | C4 §6 #1 (b) | `AnimationData` type body undefined under `components.schemas` | LOW | NEW-COORDINATION-DOC row — SCHEMA §3 (or §8 addendum) adds `AnimationData` schema block | NEW-COORDINATION-DOC |
| 15 | C4 §6 #1 (c) | `Idempotency-Key` cited but not defined as OpenAPI parameter | LOW | NEW-COORDINATION-DOC row — SCHEMA §1 adds the parameter under `components.parameters` | NEW-COORDINATION-DOC |
| 16 | C4 §6 #1 (d) | `urn:contract:slug-exhausted` referenced at MATRIX U-slugs-3 but not catalogued in SCHEMA §5 | LOW | NEW-COORDINATION-DOC row — SCHEMA §5 catalog adds the row | NEW-COORDINATION-DOC |
| 17 | C4 §6 #2 | MATRIX CS5.2 says "18 catalogued URIs" — table is 20 rows | LOW | NEW-COORDINATION-DOC row — MATRIX CS5.2 row updated to "20 URIs" | NEW-COORDINATION-DOC |
| 18 | C4 §4 gap (b) | MATRIX §S5 references `Problem.model_validate` but no Python `Problem` class extant | LOW | Already absorbed in W3 scope item 11 — `errors.py` lands with `Problem` Pydantic class | ALREADY-ABSORBED |
| 19 | C4 §4 gap (c) | No matrix row asserts `AnimationData.partial_sums` JSON round-trip | LOW | NEW-MATRIX-ROW under §F — F.partial-sums round-trip assertion | NEW-COORDINATION-DOC |
| 20 | C4 §4 gap (d) | No matrix row asserts `urn:contract:slug-exhausted` 503 emission | LOW | NEW-MATRIX-ROW — U-slugs-3 already exists; add a §S5 row asserting catalog coverage post-addition | NEW-COORDINATION-DOC |
| 21 | C4 §6 #4 | Test-surface authoring — `api/tests/conformance/`, `scripts/conformance/`, `api/lib/crud/` empty at HEAD | HIGH | NEW W1 scope item — author the conformance harness skeleton at W1 close (14 pytest files + 5 grep scripts); fills the "named test path exists" half of the W1 gate | NEW-SCOPE-ITEM-IN-EXISTING-WAVE (W1) |
| 22 | C5 §7 G1 (W3.16) | Orphan-snapshot detection mechanism not named in W3 | HIGH | NEW W3 scope item — chunked `$lookup` aggregation; reject unbounded `$nin` | NEW-SCOPE-ITEM-IN-EXISTING-WAVE (W3) |
| 23 | C5 §7 G2 (W3.17) | `anon-migrated-NNN` minting function not named | MED | NEW W3 scope item — `mint_anon_migrated_slug(n)` separate from `slug_with_retry` | NEW-SCOPE-ITEM-IN-EXISTING-WAVE (W3) |
| 24 | C5 §7 G3 (W3.18) | Dangling contour-hash post-condition not named | MED | NEW W3 scope item — post-condition `$lookup contours` assertion | NEW-SCOPE-ITEM-IN-EXISTING-WAVE (W3) |
| 25 | C5 §7 G4 (W3.19) | `migrated_from` marker for idempotency not named | HIGH | NEW W3 scope item — stamp marker; first-action find on `migrated_from: {$exists: false}` | NEW-SCOPE-ITEM-IN-EXISTING-WAVE (W3) |
| 26 | C5 §7 G5 (W3.20) | Epicycle-animation denormalisation canonicalisation source-of-truth | MED | NEW W3 scope item — canonicalise on snapshot's `animation_settings` | NEW-SCOPE-ITEM-IN-EXISTING-WAVE (W3) |
| 27 | C5 §7 G6 (W3.21) | Image-blob FK survival recommendation (Option B) not recorded in W3 | MED | NEW W3 scope item — explicit `image_slug` FK stability statement; defer blob entirely | NEW-SCOPE-ITEM-IN-EXISTING-WAVE (W3) |
| 28 | C5 §7 G7 (W3.22) | IndexedDB `WorkspaceDraft` lift not cross-referenced in W3 | LOW | NEW W3 scope item — cross-reference deferral; W4 owns the lift | NEW-SCOPE-ITEM-IN-EXISTING-WAVE (W3) |
| 29 | C5 §7 G8 (W3.23) | Integration + transform pytest specs not enumerated | MED | NEW W3 scope item — author `test_migrate_transform.py` + `test_migrate_integration.py` | NEW-SCOPE-ITEM-IN-EXISTING-WAVE (W3) |
| 30 | C5 §7 G9 (W3.24) | `--reload` interference mitigation absent | LOW | NEW W3 scope item — docstring constraint on `python -m` invocation | NEW-SCOPE-ITEM-IN-EXISTING-WAVE (W3) |
| 31 | C5 §6 image-blob | Option B (defer entirely; FK-stable) | MED | NEW B.md §7 cross-tranche debt row — reaffirm Option B | NEW-SCOPE-ITEM-IN-EXISTING-WAVE (B.md §7) |
| 32 | C6 §4 ADMIT 1 | RFC 9457 problem+json envelope | HIGH | **NEW-INVARIANT 22** (RFC-compliant error contract); W3 `errors.py` already named; helper adoption hard-gate ≥ 5 sites already in W3 | NEW-INVARIANT |
| 33 | C6 §4 ADMIT 2 | RFC 9110 ETag + If-Match | HIGH | **NEW-INVARIANT 23** (Optimistic concurrency via ETag/If-Match); W3 `etag.py` + W4 consumer adoption already named | NEW-INVARIANT |
| 34 | C6 §4 ADMIT 3 | RFC 9239 RateLimit headers | MED | **NEW-INVARIANT 24** (RateLimit header transparency); NEW W4 scope item — middleware emits headers | NEW-INVARIANT + NEW-SCOPE-ITEM-IN-EXISTING-WAVE (W4) |
| 35 | C6 §4 ADMITs 4–8 | RFC 4648 cursors; RFC 8288 Link header; Idempotency-Key; JSON Schema `extra=forbid`; pnpm workspaces | INFO | Already covered by extant spec or extant adoption; no new wave routing | CONFIRM-AS-IS |
| 36 | C6 §4 REJECTs | MongoDB Change Streams; msgpack; Tanstack Query; WebTransport; HTTP/3; time-series audit; Argon2id for ADMIN_TOKEN | INFO | KISS-rejected; preserves invariant 16 | CONFIRM-AS-IS |
| 37 | C6 §5 | 9 of 9 KISS rejections HOLD | INFO | Already in CRUD-CONTRACT §0; no re-evaluation warranted | CONFIRM-AS-IS |
| 38 | C6 §2 W2-risk | A11y gates slip past initial pass without axe-core | HIGH | NEW W2 scope item — fold `@axe-core/playwright` integration forward from W4.d into W2's hard gate | NEW-SCOPE-ITEM-IN-EXISTING-WAVE (W2) |
| 39 | C5 D1 / C6 W2-risk | Auto-recompute discipline (Invariant 19) testing seam | MED | NEW W2 scope item — Playwright spec includes the `saveContourPoints` auto-recompute assertion that fails if W3's seam regresses | NEW-SCOPE-ITEM-IN-EXISTING-WAVE (W2) |
| 40 | C6 §2 W4-risk | Helper-adoption is shape-only, not actually consumed (framework-in-disguise resurgence at adoption boundary) | HIGH | Preserve via W4 hard-gate item 9 binding (≥ 3 imports in admin.py; ≥ 10 helper sites across migrated routers); NEW W5 carry-forward assertion | CONFIRM-AS-IS + NEW-SCOPE-ITEM-IN-EXISTING-WAVE (W5) |
| 41 | C6 §6 | `docs/precepts/data/slug-words.json` empirically absent at HEAD; owed at W3 close | INFO | Already in W3 scope item 12 + SLUG-WORDS.md §1 | CONFIRM-AS-IS |
| 42 | C3 + C6 | The empirical-binding gate fires at B.W1 (paper-binding PASS) / W3 (entity + utility) / W4 (consumer adoption) / W5 (carry-forward) | INFO | Already in MATRIX §10 close-rule (88 fourier + 6 §F at B.W3) | CONFIRM-AS-IS |

**Tally.** 42 rows total. **NEW-INVARIANT**: 4 rows (21, 22, 23, 24 — slug RNG, problem+json, ETag/If-Match, RateLimit headers). **NEW-SCOPE-ITEM-IN-EXISTING-WAVE**: 12 rows (W1: 1; W2: 2; W3: 9; W4: 2; W5: 1; B.md §7: 1). **NEW-COORDINATION-DOC**: 6 rows (SCHEMA: 4; MATRIX: 2). **ALREADY-ABSORBED** (Wave-1-folded or already in B's plan): 8 rows. **CONFIRM-AS-IS** (no amendment required): 12 rows. **Zero silent deferrals.**

## §4 — Recommended B amendments

The 22 amendable rows collapse to the following surgical edits across B's plan documents:

**A. Four new invariants** (B.md §2):

- **Invariant 21 — Slug-mint cryptographic RNG.** Slug minting uses `secrets.choice` / `crypto.randomInt` (cryptographically-strong RNG). The legacy `random.choice` (Mersenne) path is retired by W3. Discharges C1 §6 #4 HIGH.
- **Invariant 22 — RFC-compliant error contract.** Every non-2xx response carries `application/problem+json` per RFC 9457 with `{type, title, status, detail, instance}`. The `urn:contract:<kebab>` namespace is closed-set. Discharges C6 §4 ADMIT 1.
- **Invariant 23 — Optimistic concurrency via ETag / If-Match.** Mutating endpoints on the `visualization` entity require `If-Match` per RFC 9110 §8.8 / §13.1.1; missing → 428 `urn:contract:precondition-required`; mismatch → 412 `urn:contract:etag-mismatch`. The strong validator is `"<sha256-hex>"`. Discharges C6 §4 ADMIT 2.
- **Invariant 24 — RateLimit header transparency.** Every response carries `RateLimit-Limit`, `RateLimit-Remaining`, `RateLimit-Reset` per RFC 9239 (draft); 429 additionally carries `Retry-After`. Single-replica posture is documented; the headers make the budget honest at the response boundary. Discharges C6 §4 ADMIT 3.

**B. Existing-wave amendments**:

- **W1 (contract ratification)**: add scope items for (i) the 4 schema gaps (RFC 4648 citation; `AnimationData` body; `Idempotency-Key` OpenAPI parameter; `slug-exhausted` catalog row); (ii) the 5 matrix gaps (CS5.2 drift; `Problem` class realisation note; `AnimationData` matrix row; slug-exhausted §S5 row; §F-count clarification); (iii) the test-surface authoring (3 directories empty at HEAD: `api/tests/conformance/`, `scripts/conformance/`, `api/lib/crud/`).
- **W2 (UX coherence)**: add the W3.5.d D1 auto-recompute Playwright assertion (regression-guards the W3 seam); add `@axe-core/playwright` integration forward from W4.d (C6 §2 W2-risk — measurement binding for Invariant 18 at the same wave that lands it).
- **W3 (entity migration)**: add scope items W3.16–W3.24 per C5 §7 (9 gaps); explicitly state image-blob Option B (defer; `image_slug` FK stable); confirm cryptographic-RNG (Invariant 21) lands in `api/lib/crud/slugs.py` (already named in CRUD-LIB-PY but now invariant-binding).
- **W4 (convergence wiring)**: add scope items for (i) session TTL 7 → 30 d bump (C1 §6 #7); (ii) RFC 9239 RateLimit-header middleware emission (Invariant 24); (iii) RFC 9110 ETag/If-Match consumer adoption — `gallery.ts` / `workspace.ts` send `If-Match` on PATCH/DELETE (Invariant 23 consumer half); preserve helper-adoption-is-shape-only risk via existing hard-gate item 9 binding (≥ 3 imports in `admin.py`; ≥ 10 helper sites).
- **W5 (close)**: add the carry-forward assertion from W4 — verify helper adoption holds through W5 (the W4 hard-gate item 9 thresholds remain satisfied at W5 close).

**C. Coordination updates**:

- **CRUD-CONTRACT.md**: add §0 binding-force clause naming "fourier-mandatory + advisory-both-sides on cohort-reopen" (C3 §6 recommendation 1; CONFORMANCE-MATRIX §10 already amended). The §11 (problem+json), §12 (ETag), §13 (Idempotency-Key) clauses are already authored in SCHEMA.md §1 — cite them from CRUD-CONTRACT and ratify as fourier-mandatory + advisory-both-sides under the new §0 clause.
- **SCHEMA.md**: add the 4 missing rows (RFC 4648 citation in §1 + §9; `AnimationData` schema body under `components.schemas`; `Idempotency-Key` parameter under `components.parameters`; `urn:contract:slug-exhausted` row in §5 catalog); reconcile CS5.2 to **20 URIs**.
- **CONFORMANCE-MATRIX.md**: add the 5 matrix gaps (CS5.2 reconciliation; `Problem` class realisation row; `AnimationData` round-trip row in §F; slug-exhausted §S5 row; §F-count clarification — 6 fourier-only). Add §G section if SOTA ADMITs warrant new dedicated rows (the new invariants 22–24 are bound by existing §S2/§S3/§S4/§S5 rows; no new §G section needed — the §S rows already test the SOTA shape).
- **CRUD-LIB-PY.md** and **CRUD-LIB-TS.md**: confirm the 8-sub-module decomposition holds (no change). The `errors`, `etag`, `idempotency` modules are already in the spec (§3, §4, §5 of each); a `ratelimit` module is **not** required — the RateLimit headers ride existing middleware. The Python `slugs.py` (§1) is amended to explicitly cite `secrets.choice` per Invariant 21.
- **SLUG-WORDS.md**: confirm `docs/precepts/data/slug-words.json` is the canonical location; cite C3 §6 recommendation 2 + the empirical absence at HEAD (C3 §1 + C6 §6); the file is owed at W3 close per W3 scope item 12.

## §5 — Honest reckoning

Tranche B's plan, after the Wave-1 fold and now the Wave-2 fold, inherits a coherent substrate and surfaces no unrouted load-bearing work. Wave-2's six independent lanes converged — independently — on the same load-bearing observation: the contract is fourier-mandatory with the value.js side held DEFERRED; the migration is W3-load-bearing with image-blob Option B (defer entirely); the SOTA opportunities (problem+json, ETag, RateLimit headers) are standards-compliant projections of existing substrate rather than framework-in-disguise; and the empirical-binding is FAIL at HEAD — the matrix is *aspirational* until B.W3 lands the entity + utility + test surface, B.W4 lands the consumer adoption, and B.W5 closes on the carry-forward assertion. The four new invariants (21–24) codify the SOTA-and-discipline shape that the next coherence pass — whether at a B sub-wave or under a successor tranche — does not have to re-discover. The cohort-orphan absorption holds across all six C-lanes without drift; the orphan-resurgence preventers are in place; the binding-force clause (mandatory-fourier-side; advisory cross-repo) is the honest authority shape. **B's authoring phase closes at this synthesis.** The implementation phase (W0 dispatch → Wα research → Wχ challenge → W1–W5 execution) remains future work. The 12 audit artefacts (6 Wave-1 + 6 Wave-2 + 2 syntheses) collectively form B's substrate-of-record at the boundary; no part of B's plan exists without empirical citation against HEAD `f8db2c6` or named-destination routing.

---

**Final tally.** 6 lanes × 42-row consolidated ledger. **4 new invariants** (21 — slug RNG; 22 — problem+json; 23 — ETag/If-Match; 24 — RateLimit headers). **5 amended waves** (W1, W2, W3, W4, W5). **5 coordination updates** (CRUD-CONTRACT §0 binding-force clause; SCHEMA 4 schema addenda; CONFORMANCE-MATRIX 5 row additions; CRUD-LIB-PY slugs.py RNG citation; SLUG-WORDS empirical-absence note). **Empirical-binding gate verdict: PAPER-BINDING PASS; EMPIRICAL-BINDING FAIL at HEAD; fires at B.W1 / W3 / W4 / W5 execution.** **B-plan-coherence verdict: COHERENT — every Wave-2 load-bearing row has a named B-destination; no B wave duplicates another; the orphan-verdict context is preserved verbatim; the CRUD-CONTRACT remains coherent under fourier-mandatory + advisory-both-sides; the SOTA ADMITs are KISS-compatible (no framework-in-disguise resurgence).** **Zero silent deferrals.**
