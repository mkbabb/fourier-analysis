# A6 — CHRONIC + DEFERRED LEDGER + NO-LEGACY SWEEP (J post-impl audit)

**Auditor**: A6 (post-impl, dimension A6). **Date**: 2026-06-04. **Repo**: fourier-analysis @ origin/master `9d7c387` (NOT deployed). **Scope**: tranche-development audit only — enumerate EVERY chronically-deferred + deferred item across J + the constellation with a TERMINAL verdict (zero perpetual punts); SEPARATELY, a NO-LEGACY sweep of every change this session.

**Governing frame** (user, verbatim): "NO quick solutions, NO workarounds: idiomatic, gestalt approaches… NO legacy code. Delineate any chronically deferred items and fold them into this new tranche… This is NOT an implementation phase. Tranche development only."

**One-line verdict**: GAPS-FOUND. The chronic ledger is honest and mostly terminal, but the W2 CORE shipped a **7-endpoint server with ZERO frontend/UI consumer** (a real inv-15 binary breach masquerading as "booked W5/W6"); the deploy chronic is now a 29-commit-deep, 4-tranche-spanning host stall that J inherits silently; and the W2 no-legacy sweep is clean EXCEPT for two reads that warrant a verdict (`set_hash=""` default; the `gallery.publish()` PATCH path that the new publish verb was authored to replace but does not).

---

## PART I — THE CONSOLIDATED CHRONIC + DEFERRED LEDGER (terminal verdicts)

Status legend: EXECUTED · BOOKED (owner+gate) · CHRONIC (≥2 tranches) · DISCLAIMED (other-repo) · DECLINED · KILLED. Every row below carries one of: **KILL / BOOK-with-hard-kill-date / SHIP-as-wave / FOLD-into-next-tranche / DECLINE-recorded**.

### §1 — fourier-LOCAL deferred items (the executable surface)

| # | Item | Origin | Deferred-count | This-session status | TERMINAL VERDICT |
|---|---|---|---|---|---|
| L-1 | **Frontend/UI consumer for the 7 new W2 endpoints** (remix, publish, unpublish, forks, provenance, diff, versions) | J.W2 (NEW gap) | new | **MISSING** — `grep web/src/lib/api.ts` for any of the 7 endpoints = 0 hits; only `web/src/lib/types.ts` carries the twins. The server + TS twins shipped with NO client method, NO caller. See finding A6-NL-1. | **FOLD-into-J (the open tranche) as the W5/W6 binding gate** — the endpoints are inv-15 substrate-without-a-consumer at HEAD. The diff-viewer/publish-UI are NAMED in J.md §4 W5 + the constellation fold §D, but until a client method + a UI surface lands, the CORE is half-shipped. W5/W6 must ship the consumer or the close is dishonest. |
| L-2 | e2e/axe CI evidence (γ LCP/INP delta, δ scroll-anchor, remix-flow e2e, publish-flow e2e) | I.γ/I.δ → J | I→J = 1 | **BOOKED W6** (PROGRESS W6 "planned"); the I-close deferral J exists to repair | **SHIP-as-wave W6** — hard gate: a GREEN fourier CI run id covering the e2e job (inv-27). Currently RED (run `26913592291`, e2e ✗). Cannot close J until W6 lands green. |
| L-3 | Measured INP delta (W3) + measured CWV delta (W4) | J.W3/W4 | new | **BOOKED W6** — both deltas explicitly deferred to "the instrumented gate" (PROGRESS W3/W4); shipped as code, NOT measured | **SHIP-as-wave W6** — hard gate: the instrumented W6 run cites a real number. If W6 cannot measure a delta (L-5 says the long-task villain may not exist), the honest verdict is "no measurable delta — shipped as the reusable floor," NOT a fabricated number. |
| L-4 | `scheduler.yield()` floor | I.ι → J | I→J = 1 | **EXECUTED W3** (`web/src/lib/scheduler.ts`) | **EXECUTED** — closed. Floor + `processInChunks` applied to the gallery accumulation. (No-legacy verdict: SOUND, §II.) |
| L-5 | `scheduler.yield()` on the epicycle/morph RENDER hot path | I.ι → J | I→J = 1 | **DECLINED-in-code** — named NOT a yield target (rAF-paced + I.γ off-screen-gated; mid-frame chunking tears) | **DECLINE-recorded** — honest (inv-15: no dominant client long-task found). The risk: the constellation fold §C names a *PRM gate* on `animation.ts:51-75` as a real P1 SHIP-at-W5 item — that is a DIFFERENT lever (prefers-reduced-motion, not yield) and must NOT be conflated with L-5's decline. Keep both: L-5 declined; PRM-gate folds to W5. |
| L-6 | `content-visibility` on the gallery grid | A3 audit → J | new | **EXECUTED W4** (`GalleryCard.vue` `.deferred-section`) | **EXECUTED** — closed. (No-legacy: SOUND, §II.) |
| L-7 | CSP propagation + `fetchLater()` analytics tail | I.ι → J | I→J = 1 | **BOOKED W7** ("planned") | **SHIP-as-wave W7** — hard gate: a verified CSP header + `fetchLater()` behind `sendBeacon` floor. fourier's own CSP already shipped (H.γ/I.θ); W7 confirms intact post-endpoints. |
| L-8 | WC frontend-design refinement (4 lenses + the grand-audit fold §A–§F) | I-design / grand-audit → J | new | **BOOKED W5** ("planned"); gated on **glass-ui 3.2.0** (`Configurator asideSide`, `useTextHighlight`) | **SHIP-as-wave W5** — hard gate: glass-ui 3.2.0 publishes the 2 named primitives. This is the wave that lands L-1's UI consumer. |
| L-9 | `cartoon-card` dead-class shim (`web/src/style.css:98-108`, D.W4 artifact, 14 sites — a resurrected retired class) | D.W4 → B/C/D/J | D→J chronic | **BOOKED W5** (retired in the WC wave) — NOT yet retired; still in tree at HEAD | **SHIP-as-wave W5 (retire)** — a genuine NO-LEGACY violation already correctly diagnosed. Hard gate: W5 lands. If W5 slips again (glass-ui 3.2.0 gate), this becomes a 4th-tranche carry → at that point KILL the shim standalone (it is chrome-only, revertible) rather than re-defer. |
| L-10 | WebMCP tool surface (`remix-visualization`, `diff-visualizations`, `publish-visualization`) | J authored | new | **BOOKED — successor K** (Early-Preview) | **BOOK-with-hard-kill-date** — hard external gate: Chromium 146 ships stable. The endpoints are authored agent-legibly NOW so K is thin. Kill-date: if Chromium 146-stable has not shipped by the J+2 successor open, re-evaluate whether the agent-legible shaping was speculative overfitting. |
| L-11 | F-5 4th hand-type island (`web/src/lib/equation/types.ts`) inv-26 re-confirm | G → J | G→J | **NOT re-confirmed this session** — J.md §8 / A4 G-4 booked a "one-line W1 re-confirm"; W1 closed but no artifact records it | **FOLD-into-J W8 close** (one-line) — distinct equation-domain contract (10 importers), not an inv-26 duplicate. Confirm-once at close, then DROP from every ledger. Trivial but currently an open loose end. |
| L-12 | DAG/merge/rebase/CRDT remix; cross-viz subject remix; save-as/publish-as-copy; shared atom-diff PACKAGE; OpenAPI→TS codegen revival | J authored | — | **DECLINED-in-design** (single-parent linear; in-place publish; PATTERN not package; hand-typed-canonical) | **DECLINE-recorded** — all correct, all grounded (`J.md §8 Declined`). No carry. |

### §2 — CONSTELLATION cross-repo asks (inv-16 maintainer-owned; `ADOPTION-ASKS.md`)

These are NOT fourier residuals (inv-16 — fourier holds no lever) but J re-triggers the 30-day stale-watch over them at close (J.md §10), so each needs a terminal disposition in J's lineage.

| Ask | Target | Deferred-count | TERMINAL VERDICT |
|---|---|---|---|
| **valuejs-J-atomdiff** | value.js | new (J.W0-seeded) | **BOOK-with-hard-kill-date** — hard gate: value.js's own green CI on `remixPalette`+`atom_diff`+`/diff` binding to `J-diff-shape.md`. Kill-date: the value.js-J close. The `/diff`-envelope parity probe is the J cross-repo close-gate; if value.js-J does not execute by fourier-J close, the parity verdict is "fourier impl asserted against `J-diff-shape.md`; value.js arm booked-not-executed" — honest, not green-over-a-gap. |
| **valuejs-J-publish** | value.js | new (J.W0-seeded) | **BOOK-with-hard-kill-date** — same gate; carries the named [P0] `listPalettes` public-view-filter leak (`crud-list.ts:85`) that MUST land WITH the verb (inv-15). Kill-date: value.js-J close. |
| **glass-ui-P5-inner-rounding** | glass-ui | booked-not-shipped (the surviving user-prompt carry, A5) | **BOOK-with-hard-kill-date = glass-ui 3.2.0 publish** — the LITERAL user-reported P5 defect (squared `ConfiguratorLayer` inner sections). Byte-identical 2.x↔3.x; needs a glass-ui ROOT code change. NOT satisfied until the inner sections round (visual-evidence test at W5). If glass-ui 3.2.0 ships WITHOUT it, the verdict converts to a re-affirmed ask with the NEXT glass-ui tranche as the new hard date — but it must never become a silent carry; the W5 DELTA.md is the structural check. |
| **glass-ui-a11y** (`inert` on `ConfiguratorLayer`) | glass-ui | H→I→J | **BOOK-with-hard-kill-date = glass-ui 3.2.0** — both app keystones `test.fixme`'d (axe `aria-hidden-focus` serious). Un-fixme on the glass-ui `inert` release. Same date as P5 (both glass-ui-owned, both 3.2.0-gated); if 3.2.0 lands without either, BOTH re-date to the next glass-ui tranche together — flagged as a glass-ui cohort-health item, not two independent silent carries. |
| **cascade-vjs / -kf / -gui** (lockfile/peer bumps) | value.js / keyframes / glass-ui | H→J | **BOOK** — booked H.W6 with precise file-verified fixes; per-repo-green-CI-gated; maintainer-owned. No fourier lever. Stale-watch only. |
| **words-spa** (publish the `frontend/` SPA; genuine 404) | words/floridify | H→J | **BOOK-with-hard-kill-date** — P1 (a real prod 404 outage). The 30-day stale-watch must escalate this above the P3 hygiene asks; it is the one live-correctness ask in the bucket alongside Ask 3/6. |
| **Ask 1 (CI), Ask 4 (hardening), Ask 5 (CF-Pages)** | words/speedtest/csp-solver/value.js/keyframes | F→G→H→J (2–4×) | **BOOK (P3 hygiene)** — drift, not fire. Stale-watch with lowest urgency. Honest continued-book (inv-16, no lever). |
| **Ask 2 + Ask 3 + dispatch.sh retirement** | 4 non-fourier repos + operator | E→F→G→H→J (4×) | **BOOK-GATED (hard mechanical gate)** — `dispatch.sh` deletion is gated on all-4-migrate (deleting sooner 404s their deploy). Ask 3 (value.js palette-api rsync→git) is the gating P1 4th migration. NOT a punt — per-repo URL+HMAC isolation is DONE; only the final `rm` is gated. Escalate Ask 3. |
| **Ask 6 (csp-solver routes 404)** | csp-solver | D→E→F→G→H→J (5×) | **BOOK-with-hard-kill-date** — a real prod API-surface regression (1-line `app.include_router`). 5 tranches is a chronic; the 30-day watch must convert this to "owner confirms by date N or it is reclassified as accepted-prod-state." No lever, but no silent forever-shelf. |
| **Ask 7 (floridify Mongo-bind)** | words/floridify | D→…→J (4×) | **BOOK** — data-exposure-adjacent hardening; P2 stale-watch. No fourier lever. |
| **inv-22-color** (`api.color` 4-endpoint vhost) | value.js | G→H→J | **BOOK** — value.js-owned vhost; honest `F-Inv 22*` scope; rides Ask 5. |
| **CH-8 palette-api→color volume rename** | operator | D→E→F→J | **MOVED-OUT (reclassified)** — per A4 CH-8: out of the fourier stale-watch, into the operator runbook. NOT a fourier residual. Terminal: removed from fourier's ledger. |

### §3 — DISCLAIMED (value.js-J's concern; terminal verdicts at value.js-J.W0)

| Chronic | Chain | TERMINAL VERDICT (value.js-J's obligation; fourier disclaims correctly) |
|---|---|---|
| **VAL-9** (`spring()→LinearStop[]` emitter) | G→H→I→J (3×) | **KILL** — keyframes owns the emitter, glass-ui consumes it from there; lifting to value.js inverts a dependency for zero de-dup. ≥2-consumer gate structurally unmeetable. fourier holds no lever. |
| **VAL-1** (OKLab aurora-LUT) | G→H→I→J (3×) | **BOOK-with-hard-kill-date** — ship IFF glass-ui `deriveAurora()` + a 2nd consumer live at value.js-J.W0 re-check, else KILL at close. Conversion math stays in `oklab.ts`. |
| **CH-6** value.js 6-tranche glass-ui-primitive carry | value.js A→…→H (6×!) | **FORCE-TERMINAL at value.js-J.W0** — each of the 7–8 asks: KILL-as-moot / RE-EXPRESS-as-inv-16′-ask / SHIP. The worst chronic in either repo; a cohort-health flag for the paired close. fourier names-to-flag only. |
| **CH-3 / C1 colour-lift** (`sampleToSVGPath` consume) | B→C→D→G (3×) | **KILLED (recorded)** — `easings.ts` self-sufficient; no fourier consumer (inv-15). The two-repo deadlock dissolves; nothing owed. J.md §8 records it. Terminal. |

### §4 — THE TWO HOST/CI CHRONICS J INHERITS THIS SESSION (the BIG ones)

| Chronic | TERMINAL VERDICT |
|---|---|
| **A3 — the babb.dev API deploy stall (host stuck at `f2fe447`, 29 commits / G+H+I+J behind origin/master; health-gate fails → rollback → migrations never run)** | **FOLD-into-J as a NAMED W6/W7 close-blocker** — this is NOT a "booked adoption," it is a verified-broken deploy-of-record (inv-28). The new J backend is NOT the cause (prod Mongo empty → migration is a no-op; `connect_db OK`); the gate failure surfaces from the never-deployed 29-commit G/H/I/J jump (inv-22 `/→404`, frontend, or WORKERS=1). **Verdict: J cannot honestly claim "CORE deployed" until the host advances past the gate.** Either (a) the inv-28 API-arm green-CI gate (booked-not-built, needs a host read-only PAT) lands and the 29-commit jump is health-debugged, OR (b) J's FINAL explicitly books "CORE shipped to origin/master, NOT to babb.dev; host stall is a NAMED forward blocker with owner=operator + kill-date." Echoes F's "webhook silently broken ~2 months" — the anti-pattern the user has flagged repeatedly. Must not be a silent "booked." |
| **A4 — the e2e CI chronic (dock VT-name collision `glass-dock-1`, ~13 specs, RED on every master run incl. the prior `26831216131`)** | **BOOK-with-hard-kill-date = glass-ui 3.2.0** — VERIFIED root cause: `node_modules/@mkbabb/glass-ui/dist/dock.js:234` assigns `w = \`glass-dock-${++n}\`` from a module counter; fourier mounts TWO docks (CanvasControlsDock + EditorControlsDock) → both get `glass-dock-1`; GlassDock exposes NO vt-name prop. This session's diff touches NO dock/VT code (e2e/ last touched at H `b52b945`), so it is genuinely pre-existing. USER DECISION recorded: book to W6/glass-ui 3.2.0 as an inv-16′ ADOPTION-ASK for unique dock VT-names; NO fourier `!important` workaround (honors NO-LEGACY). **This is a NEW inv-16′ row that is NOT yet in `ADOPTION-ASKS.md`** — it must be seeded (see finding A6-NL-7). Hard gate: glass-ui 3.2.0 ships unique dock VT-names → fourier CI goes green → inv-27 claim becomes makeable. Until then, EVERY "J green" claim is blocked (the linchpin: inv-28 SPA deploy correctly skips on red CI). |

### §5 — DEFERRED-ITEM HYGIENE (cruft, submodule, protocol — the constellation-adoption fold)

| Item | This-session status | TERMINAL VERDICT |
|---|---|---|
| **dev.sh-P0 + deploy.sh wrapper** (constellation adoption #1/#2) | **EXECUTED** — `scripts/dev.sh` conformance rewrite + `scripts/deploy.sh [all\|api\|frontend]` confirmation-gated wrapper landed; `.gitignore !scripts/deploy.sh` | **EXECUTED** — closed. (No-legacy: SOUND, §II; the bare-`mongo:8.0` "fallback" is a sanctioned per-repo divergence, NOT a legacy shim.) |
| **precepts submodule bump** (`63240e6` → SHA carrying §"Before/after + compare-at-close") | **NOT DONE** — `git submodule status` = `63240e6` still; the §"Before/after" subsection absent (gated on value.js committing the canonical precepts commit) | **FOLD-into-J W8 close (BOOK-with-hard-kill-date)** — two-step: value.js commits+pushes the precept, then fourier bumps the pointer. Hard gate: value.js's precepts commit lands. The J ι-sweep at close walks `git log -- docs/precepts/` and must record the bump as EXPECTED (not halt close). If value.js has not committed it by J close, J close records the pointer is UNCHANGED with the gate named. |
| **screenshot/visual-evidence protocol** (paired before/after π lane; `w2-workspace-configurator.png` root archival) | **BOOKED W5/W8** — `w2-workspace-configurator.png` still untracked at root; no `DELTA.md` yet | **SHIP-as-wave W5→W8** — binding at J close (J IS a visual-shipping tranche). Archive-not-delete the root PNG into `J.W5-visual-runtime/baseline/`; emit per-page DELTA.md; close-blocker on any unintended delta incl. P5. |
| **`.DS_Store` (10 on disk) + `.playwright-mcp/` (114 MB) + `paper/*.log`** | gitignored (`.gitignore:25,67,74`) — local scratch, untracked, NOT in history | **DECLINE-recorded (no repo action)** — all three are already gitignored (verified: `git status --ignored` shows `!!` prefix). Local-machine prune only, no commit. Terminal: not tracked debt. The one untracked-NOT-ignored item is `w2-workspace-configurator.png` (→ archived per the row above). |
| **P5 inner-rounding** (separate from the ADOPTION-ASK row) | booked-not-shipped | = §2 `glass-ui-P5-inner-rounding`. Single owner (glass-ui), single gate (3.2.0). No double-booking. |

---

## PART II — NO-LEGACY SWEEP of every change this session

The W2/W3/W4 diff (`git diff 7d95af6^..9d7c387`) introduces NO `TODO`/`FIXME`/`HACK`, NO commented-out blocks, NO `try/except: pass`-as-control-flow beyond the two content-addressed-idempotency `DuplicateKeyError: pass` (which are CORRECT — the §11 crash-safe property, not a swallow). The 46 "floor/fallback" mentions are inv-29 progressive-enhancement framing, the correct idiom. The sweep is clean EXCEPT the items below, each with a verdict + fix.

| # | Change | Reads as | Verdict | Fix |
|---|---|---|---|---|
| NL-A | **`Visualization.set_hash: str = ""`** (`models/visualization.py:145`) | A legacy/fallback default — an empty-string sentinel for "pre-backfill legacy row." `_head_set_hash()` (`visualizations.py:480`) + `migrate_..._forks.py:88` both compute-on-read when `set_hash` is falsy. | **SOUND-as-shipped, but harden** — the `""` default is the additive-migration safety valve (legacy rows validate before backfill); the compute-on-read fallback is NOT a legacy shim, it is the migration's own idempotency property (`row.get("set_hash") or compute`). HOWEVER: an empty `""` is a magic sentinel. The cleaner gestalt is `set_hash: str = ""` → compute-on-validate via a model validator, OR document that `""` is transient-until-backfill. Since prod Mongo is EMPTY (0 vizzes — deploy diagnosis), NO legacy row exists; the default is dead-safe today. **FOLD-into-W8**: a one-line assertion at close that no live row carries `set_hash == ""` post-migration (the backfill's own evidence), then the default is provably transient. Do NOT rip it out (it is the migration floor). |
| NL-B | **Compound `_id = f"{viz_slug}:{set_hash}"`** (`models/visualization.py:235`, `visualizations.py:129`) | A composite-key workaround for a Mongo limitation. | **SOUND (idiomatic)** — this is the deliberate §2.3-vs-§11 resolution: per-viz content-addressing so two vizzes sharing an atom-set never collide, while keeping content-addressed idempotent inserts. It is git-like (the natural key is `(viz, content)`), not a shim. NO change. One real consequence is correctly handled: `/versions` cannot cursor-paginate (string `_id` breaks ObjectId casting) → bounded ≤50, F-11. Consistent. |
| NL-C | **`palette_slug` SOFT reference, NO FK validation** (`visualizations.py:530-533`) | A skipped validation / "degrade not fail" shim. | **SOUND (inv-16-forced)** — fourier owns no `palettes` collection (palettes live in value.js); it CANNOT FK-validate without crossing the repo boundary. A dangling palette degrades the render, not the data model. This is the correct cross-repo posture, not laziness. The comment is honest. NO change. |
| NL-D | **The child root version's `atom_diff` CARRIES the source→child remix delta** (`visualizations.py:592-596`; `_write_root_version` `atom_ops=atom_ops`) | Potential internal inconsistency vs the spec's §4.1-step5 "atom_diff=[]" throwaway line. | **SOUND + internally consistent — VERIFIED** — traced across all 3 readers: `/diff` (`get_diff:823` reads `head_version.atom_diff` as `recorded_ops`, returns `from=fork_of_hash → to=head`), `/versions` (`list_versions:881` surfaces `v.atom_diff` per entry), `/provenance` (chain nodes carry `is_fork` from `forked_from_hash`). The root version of a FORK carries the delta; the root of an ORIGINAL carries `[]` (create path `atom_ops=[]`, `:222`). The migration backfills legacy roots with `atom_diff=[]` (`:125`). All three endpoints agree. The chosen reading (J.W1-crud-remix §2.2 over §4.1-step5) is the correct one — a root that records its own remix delta is what makes `/diff` answer "what changed from parent" for a depth-0 fork. NO change. |
| NL-E | **W3 "no dominant client long-task" scoping** (`scheduler.ts` doc; gallery-only application) | Under-delivery — a perf wave that admits it found no perf villain. | **SOUND (honest, inv-15) — but verify at W6** — the floor is a real reusable primitive applied to the ONE genuinely-unbounded consumer (gallery infinite-scroll accumulation, `gallery.ts:74` `processInChunks`). Naming why the rAF render loop is NOT a yield target (mid-frame tear) is the correct inv-15 posture, not theatre. **The risk** (flagged): W6's "measured INP delta" gate (L-3) may find NO measurable delta because there is no villain — at which point W6 must report "0 measurable delta; shipped as the floor" honestly, NOT manufacture a number. The constellation fold §C's PRM-gate (a DIFFERENT lever) is the real motion P1 — keep it separate (do not let it backfill the INP-delta claim). |
| NL-F | **`gallery.publish()` still PATCHes `visibility:"public"`** (`stores/gallery.ts:219-235`) | LEGACY PATH — the new `POST /:slug/publish` verb was authored to BE this, but the frontend still routes publish through the generic PATCH. | **FLAG — FOLD-into-W5/W6** — this is the inv-15 mirror of NL-1: the new publish verb has NO frontend caller, and the OLD PATCH-to-public path (`gallery.ts:219`) is still live. The verb's stated reason-to-exist (J.W1c: "the bright line vs remix; idempotent in-place; the guard's first live caller") is undercut while the UI bypasses it via PATCH. NOT a regression (PATCH-to-public is contract-legal), but the new verb is substrate-without-its-intended-consumer until `gallery.publish()` re-points onto `POST /publish`. W5/W6 must migrate the caller or the publish CORE is half-shipped. |
| NL-G | **The "publish guard's first live caller" claim** (commit msg + `_visibility_verb` comment) | Possible over-claim — does the publish path actually exercise the guard? | **SOUND (code is honest)** — VERIFIED: `_FORBIDDEN_VISIBILITY_TRANSITIONS = {("public","draft")}` only. `publish`→`public` and `unpublish`→`unlisted` are BOTH always legal, so the publish/unpublish verbs NEVER reject. The code comment says exactly this ("never rejects, but the guard is COMPOSED, not bypassed"). The FIRST live REJECTION is the PATCH path (`update_visualization:373`, a `public→draft` PATCH). The commit message's phrasing ("first live caller… PATCH = first live REJECTION") is precise. NO change — but the close FINAL must not slip into "publish exercises the guard rejection" (it composes it; PATCH rejects). |
| NL-H | **`enumerate_atoms` OMITS `palette_slug` when null** (`atomdiff.py:96-98`) | Special-case asymmetry — one atom add/removes, four only change. | **SOUND (designed)** — null `palette_slug` = atom-absent (the only fourier atom with add/remove semantics, per J-diff-shape §5.4); the other four always present. Verified consistent in `diff_atoms` (`_ABSENT` sentinel) + `_write_root_version` (`atoms.get("palette_slug")` → None). The `_ABSENT = object()` sentinel is the idiomatic "distinct from a present None" pattern, not a hack. NO change. |
| NL-I | **`.deferred-section` `--deferred-section-size: 17rem` estimate** (`GalleryCard.vue`) | Magic number. | **SOUND** — a documented `contain-intrinsic-size` estimate (aspect-[4/3] thumb + meta footer); `auto` keyword caches real size post-paint. inv-29 floor (absent ⇒ renders as before). A justified, commented estimate, not a shim. NO change. |

**NO-LEGACY net**: the W2/W3/W4 code is idiomatic and clean. The TWO real flags are **NL-F + NL-1/L-1** — the SAME defect from two angles: the W2 CORE shipped a complete server with NO frontend consumer, and the old publish PATH was not retired onto the new verb. Both are inv-15 substrate-without-a-consumer at HEAD, both fold into W5/W6. Everything else is a verified-sound design call.

---

## PART III — PROMPT-COVERAGE RECAP (the user's "recap ALL of our prompts")

| User directive (across the J arc) | Addressed? |
|---|---|
| "publish two ways, private/public; re-publishing a private item must NOT duplicate — flip the flag in place" | **YES (server)** — `POST /:slug/{publish,unpublish}` in-place `$set`, 0 new rows (verified `:656-659`). **NO (UI)** — no frontend caller; `gallery.publish()` still PATCHes (NL-F). Server-complete, UI-incomplete. |
| "DEEPLY audit our original plan + all changes; devise a path forward; NO workarounds; NO legacy; architectural transpositions for elegance/simplicity/performance" | **PARTIAL** — the canonical_digest 3→1 transposition + atomdiff PATTERN are genuine elegance wins; the chronic ledger is honest. The gaps: L-1 (no UI consumer), A3 (the deploy stall folded as "booked" not "blocker"). |
| "Delineate chronically-deferred items and fold them — NO perpetual punts" | **YES** — every chronic in §1–§5 above carries a terminal verdict; the 9-chronic A4 ledger is enforced; CH-8 moved to the operator runbook; the 2 NEW host/CI chronics (A3/A4) now have terminal dispositions. |
| P5 inner-rounding (the surviving prompt carry) | **BOOKED-with-hard-date** (glass-ui 3.2.0; not-satisfied-until-inner-sections-round; W5 visual-evidence test). The one un-shipped user-prompt item. |
| "fourier CI is actually green / green-means-green" (inv-27, the H headline) | **REGRESSED to RED** — e2e ✗ on `26913592291` (the dock VT chronic, A4). The inv-27 green claim J's close needs is BLOCKED until glass-ui 3.2.0. |

---

## §6 — TOP TERMINAL ACTIONS (the forward fold)

1. **L-1 / NL-F (P0)**: FOLD the missing frontend consumer of the 7 W2 endpoints into W5/W6 as a binding close-gate; re-point `gallery.publish()` onto `POST /publish`. The CORE is inv-15-incomplete (substrate without a consumer) until this lands.
2. **A3 (P0)**: RE-FOLD the babb.dev deploy stall from "booked adoption" to a NAMED W6/W7 close-blocker with owner=operator + a kill-date; J cannot claim "CORE deployed" over a 29-commit host stall (it can claim "shipped to origin/master").
3. **A4 (P1)**: SEED the missing `glass-ui:dock-vt-name` inv-16′ ADOPTION-ASK row (it is the linchpin of every J green claim; currently a USER-DECISION not yet ledgered).
4. **L-9 (P1)**: retire the `cartoon-card` shim at W5 or KILL it standalone if W5 slips again (4th-tranche carry).
5. **Hygiene (P2)**: L-11 inv-26 one-line re-confirm at W8; NL-A close-time assertion that no live row carries `set_hash==""`; precepts pointer bump booked with its value.js gate.

End of A6-chronic-deferred-nolegacy-ledger.md.
