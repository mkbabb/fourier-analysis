
### A1 — A1 — RECAP & COVERAGE (every user prompt across the fourier engagement → addressed? → where; emit only PARTIAL/NO gaps M must fold)
VERDICT: I reconstructed the full prompt corpus across tranches A→K (memory MEMORY.md + project_tranche_{a..k}.md + the four 2026-06 audit SYNTHESIS files + J/K PROGRESS.md). Coverage matrix = ~34 distinct user requests. Of these: 22 ADDRESSED/CLOSED (A–I disciplines, the deploy webhook root-cause, CI-actually-green, the J atom-diff/remix/publish CORE backend, the publish-no-duplicate structural guarantee, the chronic-ledger terminal-verdict discipline, the recap meta-directive itself honoured every tranche). 12 are PARTIAL/NO and are the M fold: (1) the deploy-of-record has NEVER landed — host 29 commits behind since G; every "closed" tranche since G closed against a tree prod never saw (PARTIAL, chronic, the single largest gap); (2) the inv-15 consumer gap is a binary breach still OPEN at HEAD (0 api.ts callers for the 7 J endpoints; gallery.ts:226 still PATCHes); (3) the dirty-tree glass-quiet rename + the stale ^3.1.0/^2.2.0/^0.10.0 pins mean the WHOLE re-ground (siblings shipped 4.0.0/4.2.0/0.12.0) is unconsumed — RUN-BOARD's E1 gate is 11 days stale and FALSE; (4) the standing DESIGN-LANGUAGE directive (Fraunces display, audacious colour pops, animation targets, large typography) was authored as the J WC lenses but NEVER executed (Fraunces grep = 0 in PaperView/style.css); (5) the phantom version chain the audit said DELETE is still fully present at HEAD; (6) the control-pane four-dialect schism (A-1/A-2 glass-ui asks + the fourier-local hierarchy fix) unexecuted; (7) the e2e/inv-27 red gate; (8) measured CWV/INP deltas owed; plus 5 booked tails (CSP/fetchLater, dispatch.sh retirement, L-webmcp, operator-entrypoint adoption, the cohort parity probe). The recurring "DEEPLY audit / idiomatic gestalt / fold chronics / recap all prompts / NOT implementation" meta-directive WAS honoured structurally every tranche (each produced a tranche plan not code, each recapped prompts, each booked chronics with terminal verbs) — BUT it has now recurred ~6 times while the two physical chronics it keeps re-booking (deploy-of-record + the consumer wire-up) remain unshipped: the meta-directive is being satisfied at the planning layer and deferred at the execution layer, which is itself the deepest chronic M must confront.
findings: 11
  [critical] A1-01 Deploy-of-record never landed — prod is 29 commits behind since G; every tranche since closed against a tree the world never saw  (wave:M-W1 (CORE infra; absorbs K.W1-W4) CHRONIC DEFERRED)
  [critical] A1-02 inv-15 consumer gap — the J CORE's 7 endpoints have ZERO api.ts callers; gallery.publish still PATCHes (binary breach at HEAD)  (wave:J.W5 (folds into M as the inv-15 close-gate) DEFERRED)
  [critical] A1-03 The whole sibling re-ground is unconsumed — pins still ^3.1.0/^2.2.0/^0.10.0 while siblings shipped 4.0.0/4.2.0/0.12.0; RUN-BOARD E1 gate is 11 days stale and FALSE  (wave:M-W0/W1 (the FRONT reconciliation wave) CHRONIC DEFERRED)
  [high    ] A1-04 The standing DESIGN-LANGUAGE directive (Fraunces display, audacious colour pops, large typography, animation targets) was authored as J WC lenses but NEVER executed  (wave:M-W (WC design-language execution; rides J.W5 surfaces) CHRONIC DEFERRED)
  [high    ] A1-05 The phantom within-viz version chain the audit ordered DELETED is still fully present at HEAD  (wave:J.W2-transpose + J.W2-fix (folds into M's data-model integrity wave) DEFERRED)
  [high    ] A1-06 Control-pane four-dialect schism — the convergent hierarchy fix (A-1/A-2 glass-ui asks + fourier-local R1-R4) is unexecuted  (wave:M-W (control-pane convergence; rides J.W5) CHRONIC DEFERRED)
  [high    ] A1-07 e2e/inv-27 green gate never reached — fourier CI red since before J on the dock-VT blocker; no green run id over HEAD  (wave:J.W6 EVIDENCE (folds into M; downstream of A1-01/02/03) CHRONIC DEFERRED)
  [high    ] A1-10 The recap/idiomatic/fold-chronics meta-directive is satisfied at the planning layer but deferred at the execution layer — ~6 recurrences, two physical chronics still unshipped  (wave:M-W0 (charter / shape decision) CHRONIC DEFERRED)
  [medium  ] A1-08 Measured CWV/INP deltas for the W3/W4 leaf perf waves are owed but never measured  (wave:J.W6 EVIDENCE DEFERRED)
  [medium  ] A1-09 Booked tails never executed across ≥2 tranches: CSP/fetchLater (J.W7), dispatch.sh retirement (K.W7), operator-entrypoint adoption (K.W5), cohort /diff+publish parity probe (K.W6)  (wave:M-W (tail hygiene sweep) CHRONIC DEFERRED)
  [low     ] A1-11 L-webmcp booked-not-built across multiple tranches; the agent-legible remix API surface it presupposes rides the still-unshipped consumer layer  (wave:L (stays booked; re-affirmed at M close) DEFERRED)
  glassui_asks:
    - control-pane-divider-rule (A-1): An opt-in inter-ConfiguratorRow divider-rule on the Configurator/ConfiguratorLayer chassis — the twin-line catch-light/under-shadow groove that today exists only on .instrument-rail (instrument-rail.css:68) and the explicit .chassis-divider slot, NOT auto on the Configurator chassis
    - configurator-title-ladder-hook (A-2): Bind ConfiguratorLayer's label/sub props to the text-title/text-heading φ-ladder at the component root (a token hook) instead of the literal text-sm font-semibold they emit today
    - verify-4.0-surface-for-DEC-2-and-highlight: Confirm at glass-ui 4.0.0: asideSide (Configurator controls-LEFT, DEC-2), useTextHighlight (on /dom), the inert-on-collapsed-ConfiguratorLayer a11y fix, and the dock useId() VT-name fix are all present (they were authored for 3.1.1/3.2.0)

### A2 — A2 — J data-model coherence (post-impl, true sibling-version reality: glass-ui 4.0.0 / keyframes 4.2.0 / value.js 0.12.0)
VERDICT: The executed J CORE (atomdiff + canonical_digest + the remix/publish surface) is genuinely sound and idiomatic — the one-primitive canonical_digest, the ordered-idempotent no-transaction remix, the WIRE/STORED diff split, and the in-place idempotent publish flip are all correct and confirmed against code. But three data-model transpositions remain OWED and are now MORE urgent at the true sibling reality: (1) the within-viz version chain is structurally degenerate — every row holds exactly ONE depth-0 version forever, version_count is set-to-1-and-never-incremented, parent_hash is always None — so /versions + the provenance `chain` + parent_hash/root_hash/depth are a phantom substrate no operation grows; DELETE them (W2-transpose) and keep only the per-viz atom snapshot /diff actually reads plus the cross-viz fork_of breadcrumb. (2) palette_slug is mutable via PATCH outside the version model, leaving set_hash and the immutable-1yr-cached /diff ETag stale (W2-fix). (3) the /diff `to` param is validated on-chain then silently ignored — to_hash is always HEAD regardless. None of these are blocked by the stale RUN-BOARD glass-ui-3.2.0 hinge (E1 cleared; glass-ui is 4.0.0); they are pure fourier-local data-model work and should land in J before close.
findings: 8
  [critical] A2-M5-core-zero-frontend-consumer The 7 CORE endpoints have ZERO web/src/lib/api.ts caller; gallery.publish still PATCHes visibility=public instead of POST /:slug/publish  (wave:J (W5/W6) CHRONIC DEFERRED)
  [high    ] A2-M1-phantom-version-chain The within-viz version chain is a phantom — DELETE depth/parent_hash/root_hash/version_count/GET-versions, keep only the atom-snapshot /diff reads  (wave:J (W2-transpose) DEFERRED)
  [high    ] A2-M2-palette-patch-stale-set-hash PATCH mutates palette_slug (a remix atom) outside the version model — set_hash + the immutable-1yr /diff ETag go stale  (wave:J (W2-fix) CHRONIC)
  [medium  ] A2-M3-diff-to-param-ignored GET /diff validates the `to` param on-chain then silently ignores it — to_hash is always HEAD  (wave:J (W2-fix) DEFERRED)
  [medium  ] A2-M4-migration-cements-empty-diff-on-orphaned-fork Backfill writes atom_diff=[] on a fork's root version, cementing a false 'no remix delta' if the fork's root version was never written (crash-orphan)  (wave:J (W2-fix) CHRONIC)
  [low     ] A2-M6-content-hash-vs-set-hash-coherence content_hash and set_hash are two content-addresses over overlapping-but-different projections; a palette/settings-only remix changes set_hash while content_hash stays identical  (wave:J (W2-fix, doc/test))
  [low     ] A2-M7-fork-count-self-heal-honesty fork_count is a denormalized counter bumped last-and-unconditionally in remix; an over-count is self-described as 'cosmetic' but the most-forked sort orders on it  (wave:J (W2-transpose) DEFERRED)
  [low     ] A2-M8-set-hash-empty-sentinel set_hash default '' is a migration sentinel that _head_set_hash silently recomputes — acceptable transient, but no post-backfill assertion that it is gone  (wave:J (W6) DEFERRED)

### A3 — A3 — inv-15 consumer-gap architecture (the wire-the-UI critical path for J's 7 CORE endpoints)
VERDICT: J's CORE shipped a complete server substrate (POST /remix, /publish, /unpublish; GET /forks, /provenance, /diff, /versions — 266 api tests green) and even the hand-typed TS response twins in web/src/lib/types.ts (AtomOp, DiffResponse, ProvenanceResponse, VersionsResponse, ForkCrumb), yet web/src/lib/api.ts has ZERO callers for any of them and no store action or component touches the remix/diff/provenance/versions surface. The single live visibility mutation, gallery.publish(), is still a PATCH {visibility:"public"} (stores/gallery.ts:219-238) rather than the idempotent POST /:slug/publish — and there is no unpublish path at all. This is a binary inv-15 breach across seven endpoints: the substrate exists with no consumer. The fix is a coherent wire-the-UI layer — seven api.ts functions, a fork/provenance Pinia slice, a /v/:slug detail surface that hosts a remix action + provenance breadcrumb + version timeline, and a diff-viewer that paints atom changes via glass-ui 4.0 useTextHighlight (CSS Custom Highlight). All twins/contracts are already in place; this is consumption, not new backend design.
findings: 8
  [critical] A3-01 publish/unpublish: replace the PATCH-visibility shim with the idempotent verb routes  (wave:M-W1 (wire-the-verbs; also satisfies the J.W6 §D e2e publish/unpublish-no-duplicate gate so J can close))
  [critical] A3-02 remix flow: no fork action exists — the most-forked sort writer is still unwired client-side  (wave:M-W2 (remix flow; pairs with J.W6 §D remix e2e + fork_count-write-back assertion → unblocks J close))
  [high    ] A3-03 diff-viewer: GET /diff has no consumer — paint the atom-diff via glass-ui useTextHighlight (CSS Custom Highlight)  (wave:M-W3 (diff-viewer; the J.WC §F axe gate consumer))
  [high    ] A3-04 provenance breadcrumb: the cross-viz fork_of trail has a twin and an endpoint but no header consumer  (wave:M-W3 (provenance header; pairs with the diff-viewer on the same detail surface))
  [high    ] A3-06 No /v/:slug detail host for the J surfaces — the remix/diff/provenance/versions consumers have no page to live on  (wave:M-W1 (the host scaffold lands first; the four read consumers + remix mount onto it across W2-W4))
  [medium  ] A3-05 version timeline + forks list: /versions and /forks are unconsumed (the within-viz chain and the children list)  (wave:M-W4 (version timeline + forks drill-down; lowest-risk, builds on A3-03/04))
  [medium  ] A3-08 workspace publish path bypasses the verb too — createSnapshot→PATCH instead of save-draft→POST /publish  (wave:M-W1 (folds into the A3-01 verb wiring))
  [low     ] A3-07 like() is a pure local optimistic bump with no endpoint — a latent inv-15 inversion (consumer with no substrate)  (wave:M-residual (booked, not in CORE; or a thin M-W4 add-on if the endpoint lands) CHRONIC DEFERRED)
  glassui_asks:
    - useTextHighlight export-path + version doc correction (no code change needed — adoption-ledger fix): fourier's J.WC audit + W1 re-ground recorded useTextHighlight as shipping on glass-ui 3.2.0 at the '/dom' subpath. In shipped glass-ui 4.0.0 it lives at src/composables/motion/useTextHighlight.ts, exported from '@mkbabb/glass-ui/motion' (and the root index). No glass-ui code change is requested; this is a fourier-side adoption-ledger correction so the diff-viewer (A3-03) imports the real path. Booking it as a glass-ui-facing ask so the constellation ADOPTION-ASKS row reflects the 4.0 reality, not the stale 3.2.0/dom claim.
    - A diff-row highlight token convention (optional refinement, not a net-new primitive): useTextHighlight paints under a name styled by a consumer-authored ::highlight(<name>) rule. The diff-viewer needs three semantic colours (added/removed/changed). Ask: does glass-ui intend to ship a canonical set of semantic highlight tokens (e.g. --glass-highlight-added/-removed/-changed mapped to value.js diff hues) so consumers don't each invent ::highlight colours and drift? If yes, the diff-viewer binds to them; if no, fourier defines them locally (no blocker either way).

### A4 — A4 — K-deploy spine coherence (the months-class babb.dev API deploy outage + observability floor)
VERDICT: The K-deploy charter's root-cause diagnosis is CORRECT and the proposed spine (container HEALTHCHECK + depends_on:service_healthy + fail-closed green-CI gate + paging sink + inv-31) is the right idiomatic shape — but NONE of it is built yet, and the chronic has gotten WORSE since authoring (host now 44 commits behind, not 29; K.md still says 29). I confirm the two structural blind-spots at code level: (1) backend liveness is invisible to Docker — there is no HEALTHCHECK on backend/frontend/nginx (only mongo has one), and the FastAPI lifespan `await connect_db()` serially builds ~35 indexes BEFORE `/api/health` can answer, so nginx proxies to a not-yet-live backend during a cold WORKERS=1 start that easily exceeds the 60s blind poll; (2) the rollback is silent — neither scripts/deploy-hook.sh nor the deploy template emits any off-host notification, and the API webhook arm has no green-CI gate, so a red SHA still attempts a deploy. The fix must NOT be a longer blind poll. One re-grounding note: K.md's W1 bump to glass-ui ^3.2.0 is itself now stale (glass-ui is 4.0.0); the deploy spine waves W2-W4 are version-independent and should not wait on the bump's exact target.
findings: 9
  [critical] A4-01 No container HEALTHCHECK on backend/frontend/nginx — Docker cannot know the app is live  (wave:M (deploy-spine wave 1) / formerly K.W2 CHRONIC)
  [critical] A4-02 FastAPI lifespan blocks /api/health behind ~35 serial index builds — the root cause of the gate timeout  (wave:M (deploy-spine wave 1) / formerly K.W2 CHRONIC)
  [critical] A4-03 Rollback reaches no off-host sink — the silent-for-months failure mode, structurally unfixed  (wave:M (deploy-spine wave 3) / formerly K.W4 CHRONIC)
  [high    ] A4-04 API webhook arm has no green-CI gate — a red-CI SHA still attempts a prod deploy  (wave:M (deploy-spine wave 2) / formerly K.W3 CHRONIC)
  [high    ] A4-05 inv-31 observability-floor should be authored as a real invariant, not left provisional  (wave:M (deploy-spine wave 3, authored alongside the paging sink) CHRONIC)
  [medium  ] A4-06 Charter drift: host is now 44 commits behind (not 29) and the W1 glass-ui bump target is stale  (wave:M.W0 (re-ground) + spine decoupled from dep adoption CHRONIC)
  [medium  ] A4-07 deploy.sh advisory gate cannot see the inv-22 root contract the on-host gate enforces  (wave:M (operator-entrypoint wave) / formerly K.W5)
  [low     ] A4-08 Migrations run only on a GREEN gate — a permanently-failing gate means schema never migrates  (wave:M (deploy-spine wave 1, verification step))
  [low     ] A4-09 dispatch.sh retirement remains gated on four sibling migrations — confirm still booked, not silently dropped  (wave:M (gated-retirement wave, booked) / formerly K.W7 CHRONIC DEFERRED)

### A5 — A5 — Dependency bump at true versions: glass-ui 4.0.0 / keyframes.js 4.2.0 / value.js 0.12.0
VERDICT: fourier pins glass-ui ^3.1.0 / keyframes.js ^2.2.0 / value.js ^0.10.0 while siblings have shipped glass-ui 4.0.0 (one major), keyframes.js 4.2.0 (two majors), and value.js 0.12.0 (two minors past the pin). The installed versions in node_modules are exactly the floor pins (3.1.0 / 2.2.0 / 0.10.0), so every breaking change introduced since those floors is live debt against fourier's working tree. The glass-ui 4.0.0 major carries five categorical breaking changes that fourier hits directly: the BA.W-TABS retirement of UnderlineTabs (3 call sites), the BA.W-SURFACE-AXIS Dialog variant→surface rename (3 call sites), the AZ.W-METRIC-UNIFY MetricBadge :amount→:value prop rename (12 call sites across 7 files), the v0.8.0 glass-tier ladder rename (confirmed: glass-subtle/medium/elevated do not exist in the 3.1.0 dist either — fourier has been running with zero glass styling on those elements since initial adoption, a silent critical regression the in-flight working-tree rename partially addresses), and the root-barrel SCC-trap movements of useClipboard/Checkbox/supportsViewTransitions. keyframes.js 4.0.0's breaking renames (tick→advanceTo, pause toggle-split) do not hit fourier's narrow surface (fourier calls only .play()/.stop()/.addFrame()/.parse()). value.js easing exports are stable across the bump. The bump sequencing and migration complexity make a single-commit all-at-once upgrade feasible and preferable to staged, given the low actual breakage count.
findings: 10
  [critical] A5-01 glass-subtle / glass-medium class names produce zero styles at 3.1.0 — surfaces silently unstyled  (wave:M.W1 (prereq before or bundled with the glass-ui 4.0.0 bump) CHRONIC)
  [high    ] A5-02 UnderlineTabs retired in glass-ui 4.0 — 3 call sites broken on bump  (wave:M.W1 (glass-ui 4.0 bump wave))
  [high    ] A5-03 DialogContent variant='opaque' → surface='opaque' — 3 call sites broken on bump  (wave:M.W1 (glass-ui 4.0 bump wave))
  [high    ] A5-04 MetricBadge :amount prop renamed to :value — 12 call sites broken on bump  (wave:M.W1 (glass-ui 4.0 bump wave))
  [medium  ] A5-05 Root barrel imports of useClipboard and Checkbox need subpath audit for 4.0  (wave:M.W1 (glass-ui 4.0 bump wave))
  [medium  ] A5-07 keyframes.js 4.0 → 4.2.0: loadAnimationEngine / Animation surface used by fourier is non-breaking, but the pin is two majors stale  (wave:M.W1 (dep bump wave) CHRONIC)
  [medium  ] A5-09 Dep bump sequencing: all three siblings bump in one wave, not staged  (wave:M.W1)
  [low     ] A5-06 Collapsible imported from root barrel — should be subpath @mkbabb/glass-ui/collapsible  (wave:M.W1 (glass-ui 4.0 bump wave))
  [low     ] A5-08 value.js 0.10.0 → 0.12.0: easing surface is stable; bump is low-risk additive  (wave:M.W1 (dep bump wave) CHRONIC)
  [low     ] A5-10 glass-ui 4.0 additive gains not yet adopted: HandMark, SegmentedTabs underline, onFlipSettled, useTextHighlight  (wave:J or M (post-bump adoption, not blocking) DEFERRED)
  glassui_asks:
    - glass-tier-migration-codemod: A documented codemod or proof:consumer-staleness gate that catches glass-subtle/glass-default/glass-medium/glass-elevated usage in external consumers, emitting a build-time warning on the first npm install after v0.8.0. The current situation (fourier silently running with zero glass styling for multiple tranches) proves the rename was invisible to consumers.
    - dom-subpath-clipboard-composable: Confirm useClipboard is intentionally re-exported on the root barrel at 4.0 (currently re-exported via export * from ./composables/dom in index.ts) OR move it to the @mkbabb/glass-ui/dom subpath as the sole public surface. The MIGRATION.md Phase 2 language implies root-barrel removal was planned.

### A6 — Chronic-Deferred Inventory (A6)
VERDICT: Fourier's chronic landscape divides into four strata: (1) a CRITICAL deploy spine that has been silently failing for months — the host is still pinned at the pre-K state with no HEALTHCHECK, no inv-28 API-gate, and no rollback paging, meaning every push since G silently rolls back without alerting anyone; (2) a dependency-version stratum where the installed `@mkbabb/glass-ui` is 3.1.0 and `@mkbabb/keyframes.js` is 2.2.0 against published 4.0.0 and 4.2.0 respectively — a MAJOR version gap the RUN-BOARD (dated 2026-06-05, 11 days stale) still describes as "gated on 3.3.0 via E1," a framing that is now doubly stale; (3) a data-model and frontend gap where J's 7 CORE endpoints have zero frontend consumers and CI is red on a pre-existing blocker; and (4) a long-running cross-repo coordination tail (dispatch.sh, constellation adoption asks, Actions deprecation) where fourier holds no lever but must stop carrying as open.
findings: 16
  [critical] CHR-01 Host API deploy silently failing since G — no HEALTHCHECK, no inv-28 API-gate, no rollback paging  (wave:M.W1-W3 (the deploy spine) CHRONIC)
  [critical] CHR-02 glass-ui pinned at ^3.1.0 (installed 3.1.0) — 4.0.0 published; RUN-BOARD frames gap as 'E1 gated on 3.3.0', doubly stale  (wave:M.W-bump (should be front of chain) CHRONIC)
  [critical] CHR-05 J CORE has zero frontend consumers — inv-15 binary breach since W2 (7 endpoints, 0 api.ts callers)  (wave:M.W5 (folds J.W5 + J.W2-fix + J.W2-transpose) CHRONIC)
  [critical] CHR-06 e2e CI red on pre-existing glass-dock-1 duplicate view-transition-name — suite red since before J  (wave:M.W-bump (unblocks e2e) CHRONIC)
  [high    ] CHR-03 keyframes.js pinned at ^2.2.0 (installed 2.2.0) — 4.2.0 published, TWO majors ahead  (wave:M.W-bump (pair with glass-ui bump) CHRONIC)
  [high    ] CHR-04 value.js pinned at ^0.10.0 — 0.12.0 published; K.W6 parity confirm never landed  (wave:M.W-parity DEFERRED)
  [high    ] CHR-09 GitHub Actions Node 20 deprecation — forced to Node 24 by 2026-06-16 (today), CI may start failing  (wave:M.W-bump (fold with dep bump))
  [high    ] CHR-10 inv-28 API-arm green-CI gate — booked since H (Jun 1), never built; the SPA arm is gated, the API arm is not  (wave:M.W2 (folds K.W3) CHRONIC)
  [high    ] CHR-13 Uncommitted glass-subtle→glass-quiet class-vocab rename in 7 web files — partial 4.0.0 adoption in working tree  (wave:M.W-bump (resolve as part of glass-ui 4.0.0 adoption))
  [medium  ] CHR-07 dispatch.sh retirement gated on 4 non-fourier repos — stuck since F (2026-05-29), 18 days  (wave:M.W7 (book-with-kill-date 2026-07-16) CHRONIC DEFERRED)
  [medium  ] CHR-08 Constellation adoption asks 1-7 — 18+ days open, no migration confirmed, stale-watch lapsed  (wave:M.W7 (coordination + stale-watch resolution) CHRONIC DEFERRED)
  [medium  ] CHR-11 I.e2e/axe deferred to CI — still unexecuted; I closed on a documented deferral, J.W6 gate carries it  (wave:M.W6 (folds J.W6 + I deferred evidence) CHRONIC DEFERRED)
  [medium  ] CHR-12 RUN-BOARD tri-tranche coordination doc — 11 days stale, describes glass-ui 3.2.0/3.3.0 world that is now 4.0.0  (wave:M.W0 (opening audit + dirty-tree resolution))
  [low     ] CHR-14 Configurator A-1/A-2 asks — BOOK-with-kill-date = glass-ui AT/3.3.0; glass-ui AU tranche now targets 3.3.0, kill-date approaching  (wave:M.W5 (re-verify at glass-ui 4.0.0) CHRONIC DEFERRED)
  [low     ] CHR-15 anyCanvasVisible over-export in stores/animation.ts — flagged by I overfitting audit, never trimmed  (wave:M.W5 (fold into consumer wire-up wave) DEFERRED)
  [low     ] CHR-16 C1 colour-lift (sampleToSVGPath) — KILLED at J.W0; needs a clean record closure  (wave:M.W0 (doc hygiene) CHRONIC)
  glassui_asks:
    - A-1: ConfiguratorLayer/ConfiguratorRow inter-row divider-rule opt-in: Add an opt-in inter-row divider-rule groove (twin-line hairline catch-light/under-shadow, dark-aware) to the Configurator chassis — an auto sibling-rule on .configurator-layer adjacent to .configurator-row, analogous to the .instrument-rail groove. Absent at 3.2.0 (ConfiguratorLayer.vue:100 still emits flat border-b). glass-ui SELF-BOOKED to AU/3.3.0 (AS/FINAL.md:146-152); fold is scheduled as AU.W9 FOLD-W9 in deferred-ledger §4 row #30.
    - A-2: ConfiguratorLayer label/sub → typography ladder at component root: Bind the ConfiguratorLayer label/sub props to the glass-ui typography ladder at the component root (ConfiguratorLayer.vue:118 today uses a literal text-sm font-semibold magic string). A single ladder-token change would restyle every pane title across all routes. glass-ui SELF-BOOKED to AU/3.3.0 (AS/FINAL.md:153-155); fold is scheduled as AU.W9 FOLD-W9 in deferred-ledger §4 row #31.

### A7 — A7 — Invariants audit + recent deferrals (contradictions under 4.0.0 reality; new-invariant candidates for M; deferral fold)
VERDICT: No existing invariant (inv-1..inv-30) is semantically CONTRADICTED by the 4.0.0 reality or the in-flight glass-subtle->glass-quiet rename — the rename is a clean RETIRED-class swap that conforms to no-legacy/inv-3 and does NOT trigger inv-29's PE-floor (a CSS class name is not a platform-API gate). But the invariant LEDGER is materially stale: it stops at inv-30, the whole I-era PE-floor framing was authored against a "wait for a future glass-ui release" world that is now false, and three genuinely-new disciplines M needs have no invariant: a version-currency floor (siblings are 1/2/0 majors ahead and the RUN-BOARD still treats a long-cleared edge as a blocked root hinge), a design-hierarchy floor (the control-pane audit found four dialects but only inv-10 token-first governs it), and a motion-proportion/PRM gate (M's motion targets have only scattered WC-doc PRM carves, no codified invariant). inv-31 is already provisionally reserved by K for observability-floor — M must not collide. Recent non-chronic deferrals to fold are concrete and enumerable: the J.W6 e2e/axe evidence, the inv-15 consumer-wire gap, the W2-transpose/W2-fix data-model items, A-1/A-2 glass-ui asks (book-to-AT), and the fourier-local /diff+publish parity confirm.
findings: 11
  [critical] A7-5 The bump is now ^3.1.0->^4.0.0 (a MAJOR), not the ^3.2.0 the J/K charters and RUN-BOARD assume — every gate-prose site is stale and the inv-28 red-SHA contradiction persists  (wave:M.W0 (re-ground) + M.W1 (the ^4.0.0 major adoption wave, fronts the chain) CHRONIC)
  [critical] A7-8 DEFERRAL FOLD: the inv-15 consumer-wire gap (7 CORE endpoints have ZERO api.ts caller; gallery.publish still PATCHes) — the TRUE J critical path  (wave:J.W5 (inherited into M; the true critical path) DEFERRED)
  [high    ] A7-1 Version-currency invariant: codify a max-major-drift floor (siblings now 1/2/0 majors ahead; a cleared edge is still treated as a blocked root hinge)  (wave:M.W0 (re-ground sweep) authors it; binding for the ^3.1.0->^4.0.0 bump that fronts M CHRONIC)
  [high    ] A7-2 Design-hierarchy floor invariant: the control-pane 'one instrument face' discipline has no codified invariant (only inv-10 token-first, which it under-specifies)  (wave:M (the design-language / control-pane-hierarchy wave that lands WC H1-H10))
  [high    ] A7-7 DEFERRAL FOLD: the J.W6 e2e/axe CI evidence (the I-deferred, inv-27-booked-not-asserted gap) — non-chronic, J-owned  (wave:J.W6 (inherited into M's evidence wave; predecessor = the ^4.0.0 bump) DEFERRED)
  [medium  ] A7-3 Motion-proportion / PRM gate invariant: M's motion targets are governed only by scattered WC-doc PRM carves, no codified discipline  (wave:M (the motion / animation-target wave))
  [medium  ] A7-4 inv-29 (progressive-enhancement-floor) must NOT be misapplied to the glass-subtle->glass-quiet rename or the 4.0.0 bump — a class rename is not a platform-API gate  (wave:M.W0 (edit inv-29 scope) + M bump wave (commit + ledger the rename))
  [medium  ] A7-6 inv-31 is reserved by K for observability-floor — M must not re-spend it; M's fresh integers start at 32  (wave:M.W0 (numbering hygiene; coordinate with K's 31 reservation))
  [medium  ] A7-9 DEFERRAL FOLD: W2-transpose (DELETE the phantom within-viz version chain) + W2-fix (palette-PATCH force-resolve) — glass-ui-independent NO-LEGACY/integrity items  (wave:J W2-fix + W2-transpose (inherited into M) DEFERRED)
  [low     ] A7-10 DEFERRAL FOLD: the fourier-local /diff + publish envelope PARITY confirm (cohort peers DONE-in-value.js-L; fourier owes only its own probe)  (wave:K.W6 (inherited into M cohort-parity) DEFERRED)
  [low     ] A7-11 DEFERRAL FOLD: A-1/A-2 glass-ui asks (ConfiguratorLayer inter-row divider + label/sub ladder hook) — genuinely absent at 3.2.0, glass-ui SELF-BOOKED to AT  (wave:M.W0 re-verify; glassui_asks if still absent DEFERRED)
  glassui_asks:
    - glass-ui-configurator-inter-row-divider (A-1, re-verify at 4.0.0): An opt-in inter-row divider-rule on the ConfiguratorLayer/ConfiguratorRow chassis — the twin-line machined hairline groove (catch-light over under-shadow, dark-aware) that today ships ONLY on .instrument-rail (dist/styles/instrument-rail.css). .configurator-row emits only gap/padding; .configurator-layer emits a flat border-b. fourier cannot retrofit the rail's sibling-rule onto the Configurator chassis without a hand-rolled hairline (the forbidden NO-LEGACY workaround).
    - glass-ui-configurator-label-ladder-hook (A-2, re-verify at 4.0.0): Bind ConfiguratorLayer's label/sub typography to the text-title/text-heading ladder at the component root, replacing the literal text-sm font-semibold magic literal at ConfiguratorLayer.vue:118, so one ladder-token change restyles every pane title across all routes.

### A8 — (NULL / failed agent)

### B1 — (NULL / failed agent)

### B2 — B2 — Per-pane design audit: WORKSPACE (/w)
VERDICT: The empty workspace is the app's worst single-screen composition: ~75% of the viewport is a dead, canvas-painted dot-grid with a forlorn ~280px dashed box marooned at center, while the entire control instrument is pinned to a narrow RIGHT rail (DEC-2 reversal target — it belongs LEFT as the authoring rail). The prior audits diagnosed this correctly but assumed a glass-ui 3.2.0 world; the re-ground changes the verb from "wait" to "adopt now" — glass-ui 4.0.0 SHIPS `asideSide="left"` AND `asideWidth` natively, so the entire left-rail flip is a one-prop adoption plus DELETING the three hand-rolled `grid-template-columns` overrides in VisualizationView (lines 322-329) that currently fight the chassis. The deeper sin is that the stage is composed by a 60-line imperative canvas-paint routine (`placeholder.ts`) that re-draws its own grid + dashed box + arrow + Fira-Code text — a bitmap re-implementation of DOM the page already owns — instead of letting a real, generous, audacious DOM dropzone claim the void. Six lenses below; the headline fix is: controls LEFT (one prop), the dropzone claims the stage as a hero (compose-the-void), and the canvas placeholder retires.
findings: 8
  [critical] B2-01 DEC-2: controls pinned RIGHT over dead stage — flip to LEFT authoring rail via shipped asideSide (one prop, delete 3 overrides)  (wave:M (the glass-ui 4.0 adoption wave) — gates B2-02/03 CHRONIC DEFERRED)
  [critical] B2-02 Compose-the-void: the dropzone must claim the stage as a generous DOM hero — retire the canvas-painted placeholder  (wave:M (compose-the-void wave, after B2-01 lands LEFT))
  [high    ] B2-03 Empty-state inverts altitude: ImageUpload floats as the lone titled cartoon-card above the chassis with a visible seam  (wave:J.W5 (fourier-local; chassis already present on this route) / folds into M CHRONIC DEFERRED)
  [high    ] B2-04 Mobile stage collapses to ~0px and the canvas renders ~4px — the chassis flex-fill is hand-patched, not idiomatic  (wave:M (glass-ui 4.0 adoption — same wave as B2-01, these overrides die together) CHRONIC)
  [high    ] B2-05 Hierarchy lens: the basis-pill row + value-row void — orphaned '200'/'1024' numerals, no Basis group heading  (wave:J.W5 (fourier-local) / folds into M CHRONIC DEFERRED)
  [medium  ] B2-06 Atmosphere lens: the stage is the most thematically on-the-nose place for an aurora field, and it's a flat painted grid  (wave:M (compose-the-void / atmosphere wave; parallel-safe, isolated chunk))
  [medium  ] B2-07 Motion lens: empty→populated is a hard cut; no orchestrated reveal when controls/coeffs cascade in  (wave:M (motion polish, after B2-01/03 land the layer targets) DEFERRED)
  [low     ] B2-08 Error/loading empty-states still use cartoon-card + are stage-blind (don't compose the void either)  (wave:M (compose-the-void wave, low priority tail) DEFERRED)
  glassui_asks:
    - A-1 (re-confirm at 4.0.0): ConfiguratorRow inter-row twin-line divider-rule opt-in: An opt-in `data-divider-rule` (twin-line catch-light/under-shadow groove, today on `.instrument-rail` only) for adjacent `<ConfiguratorRow>`s inside a `<ConfiguratorLayer>`, so stacked rows render the machined groove instead of running flush.
    - A-2 (re-confirm at 4.0.0): ConfiguratorLayer label/sub bound to the text-title ladder: Bind `ConfiguratorLayer` `label`/`sub` typography to the glass-ui `text-title`/`text-heading` ladder at the component root (a token hook), so one rung change restyles every section title.

### B3 — B3 — Per-Pane Design Audit: GALLERY (/gallery)
VERDICT: The gallery is structurally sound (cursor-paginated InfiniteScroll, content-visibility shipped on cards, glass-ui Dialog modal, debounced search) but design-flat and CORE-blind. It is the named host of the inv-15 consumer gap: the 7 remix/provenance/diff/versions/publish/unpublish endpoints have ZERO api.ts callers, the card carries no provenance/publish-state affordance at all, and `gallery.publish()` still PATCHes `{visibility:'public'}` rather than the idempotent verb. Beyond the J-blocker, the card hierarchy is monotone (every meta row is text-sm/muted-foreground — slug, time, views, likes all the same weight; no audacious typographic anchor, no colourful POP beyond the basis pills), the empty-state CTA is a DEAD link to a non-existent `/visualize` route, and several surfaces hardcode Tailwind-palette colours (`text-amber-400`, `text-red-300`) instead of the tier/semantic tokens. The fix is one M wave (card-as-provenance-object + publish-state ribbon + pops) layered on the J inv-15 wire-up.
findings: 10
  [critical] B3-01 inv-15 CORE gap — gallery has zero entry point to remix/provenance/diff/versions; publish still PATCHes  (wave:J.W5/W6 (the inv-15 wire-up); card affordance design is M CHRONIC DEFERRED)
  [high    ] B3-02 Empty-state CTA is a DEAD link — routes to /visualize which does not exist  (wave:M (fix is trivial; fold into the card/empty-state pass))
  [high    ] B3-03 Card hierarchy is monotone — slug, time, views, likes all text-sm/muted-foreground; no typographic anchor  (wave:M)
  [high    ] B3-04 No publish-state affordance on cards or drafts — public/unlisted/draft is invisible  (wave:M (affordance); J.W5 (the publish verb wiring) DEFERRED)
  [high    ] B3-10 Dependency pins are 3-4 majors stale — gallery cannot adopt the 4.0 primitives it needs  (wave:M.W0 / K.W4 (the dep bump is shared precondition) CHRONIC)
  [medium  ] B3-05 Hardcoded Tailwind-palette colours bypass the tier/semantic token system  (wave:M)
  [medium  ] B3-06 Likes are optimistic-local-only and unbounded — no server toggle, increments forever  (wave:M (UI honesty); backend like-endpoint is a J/K residual DEFERRED)
  [medium  ] B3-07 Search is slug-only — title/tags/description are unsearchable though they exist on the entity  (wave:M DEFERRED)
  [low     ] B3-08 Tabs + search + filter-drawer hierarchy is left-aligned and cramped on wide viewports  (wave:M)
  [low     ] B3-09 Card lacks animation/motion identity — only a hover-lift; no entrance stagger, no live preview  (wave:M)
  glassui_asks:
    - GalleryCard provenance-cluster primitive: A compact fork/version counter cluster (fork glyph + count, branch glyph + count, optional 'forked from' breadcrumb chip) that reads a {fork_count, version_count, fork_of} shape and renders the canonical provenance affordance. fourier needs it on every gallery card; value.js's palette gallery needs the identical thing (both inherited the fork/version substrate).
    - PublishStateDot toggle affordance: A <StatusDot>-derived in-place publish-state control: green=public, amber=unlisted, grey=draft, with an owner-only click that emits a toggle (the idempotent publish/unpublish flip-in-place). Honours prefers-reduced-motion on the state-change pulse.
    - Diff-viewer atom-op renderer: A presentational component that renders a DiffResponse (ops: added/removed/changed over the 5 closed atoms) as a readable added/removed/changed list — the canonical atomdiff visual. Takes the J-diff-shape op vocab; theme-tinted (green add / red remove / amber change via value.js tokens).

### B4 — B4 — Per-pane design audit: EQUATION (/equation)
VERDICT: The equation pane is the most math-dense surface in the app yet its typography is the most timid: the hero equation tops out at 1.8em KaTeX inside a 10rem fixed-height clipping card, while the actual subject — the convergence animation — is driven by a hand-rolled rAF cycle-counter and a `Math.sin(performance.now())` shimmer that ignore prefers-reduced-motion entirely. The colour story is a parallel, hard-coded HSL ramp (`spectrumColor`, three notation-pill literals, three tier literals) that bypasses value.js 0.12 and the icon palette the user wants echoed. Glass/grid is honest but rests on two debts: the resurrected `.cartoon-card` dead-class shim and a `variant="default"` Compute button that renders as an opaque black blob against the glass field. This is M-wave territory: a typography-up transposition (audacious display math), a keyframes.js-owned convergence timeline, and a value.js-sourced harmonic spectrum, all proportion-governed and reduced-motion-honoured.
findings: 13
  [high    ] B4-01 Hero equation typography is timid and clipped — the math subject is undersized  (wave:M (typography wave))
  [high    ] B4-02 Convergence animation is a hand-rolled rAF cycle-counter — should be a keyframes.js timeline  (wave:M (motion wave) CHRONIC)
  [high    ] B4-03 Auto-play convergence loop ignores prefers-reduced-motion  (wave:M (motion wave))
  [high    ] B4-06 Compute button renders as an opaque black blob against the glass field  (wave:M (glass/layout wave))
  [medium  ] B4-04 Harmonic spectrum colour is a hand-rolled HSL ramp, not value.js — bypasses the colour authority  (wave:M (colour wave); dep-bump prerequisite)
  [medium  ] B4-05 Notation-pill and tier colours are duplicated hard-coded HSL literals  (wave:M (colour wave))
  [medium  ] B4-07 `.cartoon-card` dead-class shim resurrects a removed glass-ui recipe  (wave:M (glass wave); coupled to glass-ui 4.0 adoption CHRONIC)
  [medium  ] B4-08 Legend overlay occludes the harmonic curves at the right edge  (wave:M (layout wave))
  [medium  ] B4-10 Per-coefficient hover popover is mouse-only and absolutely positioned, not a glass overlay primitive  (wave:M (glass-overlay wave))
  [medium  ] B4-12 Convergence plot grid is flat grey with no mathematical landmarks (no π ticks, no axis labels)  (wave:M (math/grid wave))
  [low     ] B4-09 Inconsistent independent-variable symbol: equation card uses f(t), the rest uses f(x)  (wave:M (math-correctness wave))
  [low     ] B4-11 Two divergent serif stacks for math glyphs — brand-serif fragmentation  (wave:M (typography wave))
  [low     ] B4-13 Energy MetricBadge is buried in a hover-card — the convergence headline number is hidden  (wave:M (layout/motion wave))
  glassui_asks:
    - canvas-anchored-overlay: A way to anchor glass-ui 4.0's top-layer/anchored popover to an arbitrary canvas-relative point (a cursor or a sampled curve coordinate) rather than only to a DOM element — e.g. a lightweight 'virtual anchor' (a synthetic getBoundingClientRect rect) accepted by the popover/anchor API.
    - cartoon-card-or-stamped-surface-recipe: Restore (or provide a current-vocab equivalent for) the offset-stamp-shadow + 2px-border + hover-lift 'cartoon' card decoration that glass-ui removed at C.W5, as a first-class 4.0 surface variant.
    - viz-amber-light-contrast-rebaseline: Rebaseline the upstream light-mode --viz-amber token to clear WCAG AA against --background (fourier currently overrides hsl(35 70% 42%)≈3.54:1 → hsl(35 76% 35%)≈4.6:1 locally).

### B5 — B5 — Per-Pane Design Audit: MORPH (/morph)
VERDICT: The morph pane is the single most motion-themed surface in the app — its entire purpose is to author a 3-phase transition (settle-out → cross-fade → settle-in) — yet it is the LEAST glass/motion-suffused pane, and it ships ZERO prefers-reduced-motion handling while 10 sibling components honour it (an a11y/INP regression and a direct breach of the standing PROPORTION directive). The control hierarchy is flat and timing-centric (three identical duration cards) rather than expressing the morph as a single authored motion-curve; the harmonic grid is a cramped horizontal scroller; and the easing vocabulary is value.js polynomials/back-curves only, despite keyframes 4.2 shipping a first-class springTimingFunction + SpringProgress and glass-ui 4.0 shipping useSpring/useSpringPress/useReducedMotion/useViewTransition. The deps are 1–2 majors stale (glass-ui ^3.1 vs 4.0, keyframes ^2.2 vs 4.2), so the morph engine is built on the pre-spring world. This is the highest-leverage pane for the M tranche's motion-language thesis: re-author it as a spring-native, reduced-motion-aware, glass-gridded "motion studio."
findings: 10
  [critical] B5-01 Morph pane has ZERO prefers-reduced-motion handling — the motion pane is the only one that ignores motion preference  (wave:M-wave (motion-language) — first move; also a J-blocking a11y gap)
  [high    ] B5-02 Morph engine is pre-spring; easing vocab is value.js polynomials/back only — keyframes 4.2 springTimingFunction + glass-ui useSpring unused  (wave:M-wave (motion-language) — depends on dep-bump wave CHRONIC)
  [high    ] B5-03 Control hierarchy is flat & timing-centric: three identical duration cards instead of one authored morph-curve  (wave:M-wave (control-pane hierarchy))
  [medium  ] B5-04 Harmonic-level grid is a cramped horizontal scroller — buries the core MATH idea of the page  (wave:M-wave (grid/MATH suffusion))
  [medium  ] B5-05 Glass suffusion is near-absent — page leans on the .cartoon-card resurrection shim, not glass surfaces  (wave:M-wave (glass suffusion); coordinates with in-flight 4.0 class rename CHRONIC)
  [medium  ] B5-06 Typography is timid — h1 caps at 2rem; no audacious display on the motion pane  (wave:M-wave (typography pops))
  [medium  ] B5-07 Colour pops are monochrome-red; misses the audacious palette directive  (wave:M-wave (colour pops) — leans on value.js 0.12)
  [low     ] B5-08 Hardcoded non-token colours break the design-token discipline  (wave:M-wave (token hygiene) — trivial, fold into B5-04)
  [low     ] B5-09 Export/Reset buttons are bespoke-styled, not glass-ui Button variants — inconsistent with the rest of the app  (wave:M-wave (glass suffusion))
  [low     ] B5-10 No View-Transition / FLIP on shape swap — glass-ui 4.0 useViewTransition unused for the actual morph button  (wave:M-wave (motion-language) DEFERRED)
  glassui_asks:
    - Spring-curve thumbnail / springTimingFunction surface helper: A small helper (or documented recipe) to render a spring's value-over-time curve as an SVG path — analogous to fourier's easingCurvePath — so spring presets can show their overshoot in a Select. Ideally glass-ui exposes a curve-sampling util over keyframes springTimingFunction.
    - Filled 'foreground' Button variant: A first-class glass-ui Button variant matching the high-contrast filled CTA (background:var(--foreground), color:var(--background)) that fourier currently hand-rolls as .btn-export.

### B6 — (NULL / failed agent)

### B7 — B7 — Per-Pane Design Audit: /demo/shape-extractor
VERDICT: The shape-extractor route (/demo/shape-extractor) is a Playwright harness page masquerading as UI: a raw div with inline styles, a `<pre>` tag dumping JSON, a `setTimeout` race, a `document.getElementById` imperative reach, and a `window.__fourierShapeData` global — all hallmarks of a debug scaffolding that was never promoted into the design language of the app it lives inside. It carries zero glass/grid/math vocabulary, no route meta, no navigation context, and renders the two SVG shapes in 200×200 bordered boxes with `1px solid #ccc`. The correct architectural move is a FULL transposition: promote it to a genuine internal tool page that suffuses the glass-card surface language, exposes the pipeline steps as animated stages (sample → contour-extract → Fourier compute), adopts `onMounted` + `nextTick` instead of `setTimeout`, replaces the `document.getElementById` reach with a Vue ref, and strips the `window.__fourierShapeData` global in favour of a Playwright-accessible `data-testid` attribute. The page is not linked from the nav, has no route meta title/description, and is only reachable by direct URL, so it is also missing discoverability context for developers.
findings: 15
  [critical] SE-10 `@mkbabb/glass-ui` pinned at `^3.1.0` — page cannot adopt glass-ui 4.0 primitives (glass-card, glass-wash, veil-surface) until the dep is bumped  (wave:K CHRONIC DEFERRED)
  [high    ] SE-02 Inline styles throughout — bypasses all design tokens  (wave:M.W1)
  [high    ] SE-06 Page carries no glass/grid/math design language — raw `<h1>` and `<h2>` with browser defaults  (wave:M.W1)
  [medium  ] SE-03 `setTimeout(200)` race to await SVG render — brittle and semantically wrong  (wave:M.W1)
  [medium  ] SE-04 `document.getElementById('output')` imperative DOM reach instead of Vue ref  (wave:M.W1)
  [medium  ] SE-05 `window.__fourierShapeData` global — test coupling via ambient global is an antipattern  (wave:M.W1)
  [medium  ] SE-07 Two-column SVG layout clips on mobile — Moon SVG partially off-screen at 375px  (wave:M.W1)
  [medium  ] SE-09 Pipeline has no step/stage animation — the three phases (render → extract → dump) are invisible to the user  (wave:M.W2)
  [medium  ] SE-11 SVG border uses hardcoded `#ccc` — not a design token, breaks in dark mode  (wave:M.W1)
  [medium  ] SE-14 Typography: `<h1>` / `<h2>` carry no serif font family — visual register disconnected from every other page  (wave:M.W1)
  [medium  ] SE-15 Audacious colour pops absent — accent-red is used on SVG strokes but nowhere in the page chrome  (wave:M.W2)
  [low     ] SE-01 Route has no meta title/description — Playwright captures it as 'Fourier Analysis' default  (wave:M.W1)
  [low     ] SE-08 Extract button uses `variant='default'` glass-ui Button with no visual annotation that it auto-fires on mount  (wave:M.W1)
  [low     ] SE-12 No `content-visibility: auto` on the JSON `<pre>` output — large serialized contour data blocks layout  (wave:M.W1)
  [low     ] SE-13 Page not linked from any navigation surface — developer discoverability is zero  (wave:M.W1)
  glassui_asks:
    - glass-ui 4.0 dep bump (fourier-local K.W4 corrected target): Bump fourier's `@mkbabb/glass-ui` to `^4.0.0` (and keyframes `^4.0.0`, value.js `^0.12.0`). After bump: delete the `cartoon-card` shim at web/src/style.css:97-110 — glass-ui 4.0 `cartoon-surface` supersedes it. Run `vue-tsc --build` to surface any 3.x→4.x breaking API changes at the type level before fixing.

### C1 — C1 — Design-hierarchy system across control panes (title demarcation, spacing rhythm, dividers, internal structure)
VERDICT: The control panes still speak FIVE dialects against ONE platform instrument — but the world has shifted decisively under the prior (3.1.0-era) audit: glass-ui 4.0.0 has SHIPPED every primitive that audit booked as a glass-ui-ASK. A-1 (inter-row divider) is now the `dividers` prop on ConfiguratorLayer; A-2 (title bound to a token rung) is the `.configurator-section-label` reading `--configurator-section-{size,weight}` plus the section→row→token three-rung label hierarchy; A-3 (asideSide) ships. The density axis ships with a container-style-query companion. So the WHOLE hierarchy system is now ADOPT-ONLY, fourier-local, zero glass-ui asks remaining for the four R-rules — it collapses to one chassis pass + one `^3.1.0→^4.0.0` major bump. The unified system: (1) every pane titled by `ConfiguratorLayer label/sub` — retire CollapsibleSection, the ImageUpload `<h3>` mimic, the morph `config-card-title`, the EquationPanel sans-label; (2) one rhythm = the shipped density axis + the φ ladder (NOT the prior audit's `--space-2/4` — that scale does not exist in glass-ui 4.0.0; the idiomatic axis is `density` + `--space-phi-*`); (3) one divider = the `dividers` prop for inter-row + the `.chassis-divider`/`bezel-line` groove for explicit sub-section boundaries — retire `.advanced-divider`/`.divider-line` and the equation `border-t`; (4) one two-tier altitude rule (one amber primary control per pane, secondary on `glass-quiet`). The biggest single regression is that the equation + morph routes never consume the Configurator chassis at all — they remain hand-rolled `.eq-grid`/`.config-grid` walls of `cartoon-card`s.
findings: 10
  [critical] C1-02 Equation + morph routes never consume the Configurator chassis — they are hand-rolled `.eq-grid`/`.config-grid` walls of `cartoon-card`s  (wave:M.W2 (chassis propagation; gates the title/rhythm/divider rules) CHRONIC)
  [high    ] C1-01 The four prior glass-ui-ASKs (A-1/A-2/A-3 + density) all SHIPPED in glass-ui 4.0.0 — the entire ASK partition collapses to an ADOPT-ONLY bump  (wave:M.W1 (the dep bump is the keystone; all C1 findings gate on it) CHRONIC)
  [high    ] C1-03 FIVE title idioms; the morph `text-lg/400` serif title is HEAVIER than workspace pane titles — hierarchy inverted  (wave:M.W2 (title rule, with the chassis pass) CHRONIC)
  [high    ] C1-04 ImageUpload is the lone titled outlier in the empty workspace — an `<h3>` mimic floating on a `cartoon-card`, not a ConfiguratorLayer  (wave:M.W2 (can land first; workspace already has the chassis) CHRONIC)
  [high    ] C1-05 No unified spacing rhythm — three hand-rolled systems; the shipped density axis sits entirely unused; section-gap ≈ control-gap so grouping reads only from borders  (wave:M.W3 (rhythm rule) CHRONIC)
  [high    ] C1-07 ZERO platform dividers in the control panes; four off-system separator idioms coexist — including hand-rolled `.divider-line`s INSIDE a Configurator pane  (wave:M.W4 (divider rule) CHRONIC)
  [medium  ] C1-06 BasisSelector value-row void — the numeric input is split into a bespoke `mb-1.5 justify-end` band above the slider, orphaning the value from its control  (wave:M.W3 (rhythm rule; the most visible single rhythm fix))
  [medium  ] C1-08 No two-tier altitude rule — every group reads at one altitude/weight with no entry point; amber primary action is not disciplined to one-per-pane  (wave:M.W5 (hierarchy/altitude rule; consumes asideSide) CHRONIC)
  [medium  ] C1-09 `cartoon-card` is a resurrected dead-class shim hosting the non-chassis panes — the legacy substrate the dialects ride on  (wave:M.W2 (chassis pass retires the pane sites) + M.W6 cleanup (delete the shim once 0 sites remain) CHRONIC DEFERRED)
  [low     ] C1-10 The in-flight glass-subtle→glass-quiet rename is a partial-vocabulary touch with no committed bump — risks pinning fourier to a half-migrated 4.0.0 class vocab  (wave:M.W1 (fold into the dep bump))

### C2 — Visual Incongruence — Cross-Page Surface, Border, Colour, Type, and Shadow Consistency
VERDICT: The codebase carries at least seven distinct classes of cross-page visual incongruence. The most systemic are: (1) a rampant 1.5 px non-standard border weight (24 sites) that has no counterpart in any Tailwind or glass-ui token; (2) two competing card-surface idioms — `cartoon-card` (13+ sites) and bare `border-2 border-foreground/15 rounded-xl` (4 sites) — that do not converge on a single glass-ui `<Card>` primitive; (3) at least six divergent inline-spinner recipes across four pages with inconsistent size, border-width, and accent token; and (4) `rgba(0,0,0,N)` bespoke box-shadows scattered across 10+ components that bypass the glass-ui `--shadow-*` token family. Secondary clusters: raw `text-red-N` used for error states instead of `--destructive`; raw `border-radius: <rem>` values authored outside the `--radius-*` token ladder; and `transition-all` still alive in ImageUpload.vue. No single page is immune; every route (/, /w, /gallery, /equation, /morph, /paper, /demo/shape-extractor) carries at least one failing.
findings: 14
  [high    ] C2-01 Epidemic 1.5 px border weight — 24 sites, zero token backing  (wave:M W1)
  [high    ] C2-02 Two-headed card idiom: `cartoon-card` vs bare `border-2 border-foreground/15 rounded-xl`  (wave:M W1 CHRONIC)
  [medium  ] C2-03 Six divergent inline-spinner recipes across four pages  (wave:M W1)
  [medium  ] C2-04 Bespoke `rgba(0,0,0,N)` box-shadows bypassing glass-ui `--shadow-*` token family  (wave:M W1)
  [medium  ] C2-05 Error states use raw `text-red-400 / border-red-500` instead of `--destructive` semantic token  (wave:M W1)
  [medium  ] C2-06 Raw `border-radius: <rem>` values throughout CSS blocks, bypassing `--radius-*` token ladder  (wave:M W1)
  [medium  ] C2-07 Glass surface tier mismatch: `glass-quiet` used as a floating popover (EquationPanel) where `glass-floating` is semantically correct  (wave:M W1)
  [medium  ] C2-14 PaperSearch.vue hand-rolls a search input field + results dropdown instead of consuming glass-ui `<Input>` + `<Command>` / `<Popover>`  (wave:M W2)
  [low     ] C2-08 `cartoon-card` on non-card elements causes redundant `rounded-lg` override (cartoon-card already sets rounded via cartoon-surface)  (wave:M W1)
  [low     ] C2-09 `transition-all` still alive in ImageUpload.vue (three violations)  (wave:M W1)
  [low     ] C2-10 Paper cluster uses `calc(var(--radius) - 2px/4px)` arithmetic instead of named token rungs  (wave:M W1)
  [low     ] C2-11 HarmonicLevelGrid uses hardcoded `#60a5fa` (blue-400) for bound-slot highlight instead of a token  (wave:M W2)
  [low     ] C2-12 ConvergenceLegend dashed `border: 2px dashed rgba(180,180,180,0.6)` for the original-function dot is light-mode only  (wave:M W1)
  [low     ] C2-13 GalleryDraftsSection uses `border-[1.5px] border-foreground/8` while GalleryCard uses `border-2 border-foreground/15` — same semantic level, different weight and opacity  (wave:M W1)
  glassui_asks:
    - shadow-glow-rainbow token: A `--shadow-glow-rainbow` custom property (or a `@utility shadow-glow-rainbow` class) that encodes the chromatic multi-layer glow used by AnimationControls.vue's play-btn hover state: `0 4px 20px rgba(200,100,255,0.2), 0 2px 12px rgba(100,180,255,0.15)`. The play-button is the highest-energy UI element in the app; its hover state is intentional audacious design and deserves a named, themeable token rather than hardcoded rgba.
    - cartoon-card as a Card `surface` preset: Promote the `cartoon-card` pattern to a first-class `<Card surface="cartoon">` variant in glass-ui's Card primitive, so the 2px-border + offset-stamp-shadow recipe is owned upstream, not shimmed locally via fourier's style.css:107-111.

### C3 — C3 — Glass design-language suffusion (style.css glass utilities + glass-ui 4.0 glass primitives; depth hierarchy)
VERDICT: fourier's glass story is incoherent against glass-ui 4.0 because the entire web tree is still authored to a pre-4.0 (≈v0.7/v0.8) class vocabulary that 4.0 renamed and restructured. The in-flight uncommitted rename `glass-subtle→glass-quiet` is itself WRONG (subtle's 4.0 successor is `glass-wash`, the ~0.30α rung — the rename over-weights every touched overlay by a full rung) and is also incomplete (it leaves `glass-elevated`, a class glass-ui 4.0 DELETED, live at two sites; leaves `glass-scrubber`, a Slider variant 4.0 never shipped, at ~9 sites). Glass is simultaneously UNDER-applied (the primary content surfaces are all `cartoon-card`/Memphis-sticker, not glass — the home/workspace/equation panels read as paper stickers, no translucency, no specular, no depth) and INCONSISTENTLY hand-rolled (EquationPanel, PaperView, the legend/toggle overlays bolt raw `box-shadow`/`blur(8px)`/`color-mix` recipes onto, or instead of, ladder rungs). 4.0 ships exactly the primitives this needs — the 5-rung `{wash,quiet,resting,floating,overlay}` ladder, the orthogonal `[data-surface]={glass,veil,opaque}` decoration axis, native `cartoon-surface`/`Card surface="cartoon"`, `veil-surface` text-plates, the shipped `GlassTimeline`/`Slider` — making fourier's local shims (the `.cartoon-card` @utility in style.css) and reimplementations dead legacy under the no-shims/no-legacy directive. The right move is a single M wave that retires the v0.8 vocabulary, maps fourier onto a 3-band glass DEPTH hierarchy tied to the design-hierarchy system, and deletes every hand-rolled glass recipe.
findings: 10
  [critical] C3-03 glass-scrubber Slider variant does not exist in glass-ui 4.0 — ~9 consumer sites pass a phantom variant  (wave:M (slider/timeline reconciliation wave) CHRONIC)
  [high    ] C3-01 The uncommitted glass-subtle→glass-quiet rename is semantically wrong — subtle's 4.0 successor is glass-wash, one rung lighter  (wave:M (glass-vocabulary-migration wave))
  [high    ] C3-02 glass-elevated is a DELETED 4.0 class still live at two overlay sites  (wave:M (glass-vocabulary-migration wave) CHRONIC)
  [high    ] C3-04 The .cartoon-card @utility shim in style.css is now dead legacy — 4.0 ships cartoon-surface + Card surface="cartoon" natively  (wave:M (shim-retirement wave) CHRONIC)
  [high    ] C3-05 Primary content surfaces are sticker-cards, not glass — the GLASS design language is essentially absent from the main panels  (wave:M (glass-suffusion CORE wave))
  [high    ] C3-08 No glass DEPTH hierarchy tied to the design-hierarchy system — rung choice is ad hoc per surface  (wave:M (glass-suffusion CORE wave))
  [medium  ] C3-06 Hand-rolled glass recipes bypass the ladder — raw box-shadow/blur/color-mix bolted onto or instead of rungs  (wave:M (glass-vocabulary-migration wave))
  [medium  ] C3-07 fourier reimplemented GlassTimeline/ScrubberTimeline locally instead of consuming the shipped 4.0 primitives  (wave:M (slider/timeline reconciliation wave) CHRONIC)
  [medium  ] C3-09 Overlay/text-plate surfaces don't use the veil register — boxed glass reads as a dividing line on text plates  (wave:M (glass-vocabulary-migration wave))
  [medium  ] C3-10 Adaptive bright-bucket / specular catch-light unused — glass over the bright math canvas can't self-darken and reads inert  (wave:M (glass-suffusion CORE wave) DEFERRED)
  glassui_asks:
    - useSpecular composable (or documented pointer-write contract): A tiny exported composable that wires a glass surface's `--mouse-x`/`--mouse-y` from pointermove (rAF-throttled, reduced-motion-aware) and composes `.glass-specular-track`, so a consumer gets the moving catch-light with one binding instead of hand-writing the inline-style pointer math. If one already exists internally for the dock controls, just export + document it.
    - Slider scrubber/spectrum variant clarity + a single-track timeline export contract: Confirm/document the canonical path for a single-track normalized scrubber: is it `<Slider variant="standard">`, `<GlassTimeline variant="scrubber">`, or `<ScrubberTimeline>`? The three coexist and fourier has all three reimplemented locally against a non-existent `glass-scrubber` variant. A short 'use X for a value slider, Y for a media scrubber' note in the slider/timeline index would let fourier delete its local wrappers with confidence.
    - Depth-hierarchy / rung-semantics doc surfaced for consumers: Surface the rung→semantics table (wash=hairline chrome, quiet=ambient, resting=content, floating=popover, overlay=modal-over-modal) that currently lives only in Card.vue's prop docstring as a top-level design-language note (e.g. a `docs/glass-depth.md` or the ladder.css header). Include the veil/opaque axis and the bright-bucket opt-in.

### C4 — GRID DESIGN-LANGUAGE — dot-grid/lattice motif, mathematical chrome vs dead space
VERDICT: The grid vocabulary is split across four incoherent implementations that never cross-reference or share tokens: a Canvas2D line-grid in the workspace placeholder that reads as dead background noise; a live mathematical coordinate-grid drawn on every populated BasisCanvas frame that is legitimately valuable but visually anemic (rgba(150,150,150,0.09), unthemed); a CSS repeat-linear-gradient grid on the ContourEditorCanvas (28px pitch, themed via color-mix but at 5% opacity — barely perceptible); and an identical CSS grid on the GalleryCard image-frame (16px pitch, 4% opacity — invisible in practice). The two CSS grids are contextually appropriate as engineering-graph-paper chrome but lack identity; the Canvas2D placeholder grid is pure dead space that was already flagged in the D-audit (DA-design-A2-05) and the J-postimpl audit (D4-crosspage-hierarchy:66) without being resolved; and the coordinate-grid on the populated canvas is the one site where a mathematical lattice genuinely reinforces the MATH theme, yet it draws hardcoded anonymous grey instead of resolved CSS tokens, so it flips mode-blindly. No site uses a dot lattice. No grid communicates the project's actual mathematical identity — phase-space coordinate axes, tick-marks at integer multiples of π, unit-circle markers — the things that distinguish a Fourier-analysis canvas from a generic plotting widget.
findings: 9
  [high    ] C4-G1 Workspace placeholder grid — dead space, not mathematical chrome  (wave:M.W1 (empty-state redesign + placeholder mathematical chrome) CHRONIC)
  [high    ] C4-G2 Canvas coordinate-grid — hardcoded anonymous grey, mode-blind  (wave:M.W1 (canvas token discipline))
  [medium  ] C4-G3 Placeholder grid and populated grid are disconnected — identity break at load  (wave:M.W1 (canvas unification))
  [medium  ] C4-G4 ContourEditorCanvas CSS grid — correct intent, wrong token discipline and invisible opacity  (wave:M.W2 (CSS grid token discipline))
  [medium  ] C4-G6 Three separate grid pitch values with no shared token — incoherent lattice language  (wave:M.W2 (CSS grid token discipline))
  [medium  ] C4-G7 No dot-lattice / tick-mark mathematical identity at any coordinate intersection  (wave:M.W3 (mathematical lattice identity))
  [low     ] C4-G5 GalleryCard image-frame grid — decorative dead space, indistinguishable from empty  (wave:M.W2 (CSS grid token discipline))
  [low     ] C4-G8 ConvergencePlot grid duplicates grid.ts logic in a separate module — split brain  (wave:M.W1 (canvas unification))
  [low     ] C4-G9 HarmonicLevelGrid — the legitimate grid-as-UI has no mathematical lattice identity  (wave:M.W3 (mathematical lattice identity))
  glassui_asks:
    - canvas-grid-tokens: Expose --canvas-grid-minor-color and --canvas-grid-axis-color as light-dark-aware CSS custom properties in the token layer (scale-paper.css or color-radius.css), analogous to how --constellation-* tokens are already defined. These should be plain-hsl leaves per the Canvas2D light-dark() constraint (same proof:constellation-tokens clause c). The consumer (fourier grid.ts) reads them via getComputedStyle and caches with resolveVizColors().
    - graph-paper-surface-utility: A @utility graph-paper (analogous to .paper-texture) that applies a CSS repeating-linear-gradient crosshatch at --grid-pitch using --foreground at 8% opacity. Parameterisable via --graph-paper-opacity and --graph-paper-pitch. The ContourEditorCanvas and GalleryCard image-frame are both fourier-local consumers but the pattern is general (any drawing-surface card that wants graph-paper chrome).

### C5 — C5 — Math-motif suffusion (mathematical notation, glyphs, lattices, epicycle iconography as intentional chrome)
VERDICT: The math motif is half-built and unevenly applied: it is strong where it is content (KaTeX Σ equation card, the canvas epicycle render, the script-ℱ logo, NotationPills sin/eⁱ/Ae glyphs, the basis-icon set, the genuinely elegant DarkModeToggle that morphs a sun↔moon as a live Fourier epicycle path) but completely absent from the chrome that frames the app — empty states, loading spinners, dividers, section markers, and page-level ambient surface. The single largest gap is architectural, not cosmetic: five per-page Fourier-path assets (equation/gallery/morph/paper/visualize.json, ~270KB each) were pre-computed and shipped but wired into ZERO consumers, while the exact rendering machinery to display them (svg-fourier.ts + FourierMorphSvg + useFourierMorph) already ships and is proven by the toggle. Eight near-identical generic CSS border-spin spinners stand in where a partial-sum-convergence or epicycle-trace loader would be both signature and thematically literal. The remedy is a single M wave that builds one decorative epicycle/lattice chrome primitive, consumes the orphaned assets as per-route ambient signatures, and replaces the duplicated spinner — all WITHIN PROPORTION (one watermark per surface, PRM-gated, INP-safe), turning math from decoration-in-the-content into the app's quiet signature.
findings: 7
  [high    ] C5-1 Five pre-computed per-page Fourier-path assets are orphaned — generated but wired to zero consumers  (wave:M (math-chrome wave) — keystone finding)
  [medium  ] C5-2 Eight duplicated generic border-spin spinners where an epicycle/partial-sum loader would be signature and literal  (wave:M (math-chrome wave))
  [medium  ] C5-3 Empty states use generic Lucide icons + plain prose instead of math iconography  (wave:M (math-chrome wave))
  [low     ] C5-4 Section dividers and panel markers are plain rules — no math-glyph chrome  (wave:M (math-chrome wave))
  [low     ] C5-5 No app-level ambient math lattice/watermark — global surface is flat  (wave:M (math-chrome wave))
  [low     ] C5-6 Morph (and other) page header is plain text with no ℱ signature despite being the most epicycle-native surface  (wave:M (math-chrome wave))
  [low     ] C5-7 Equation 'info' icon hand-rolled as inline SVG instead of the consistent icon/glyph system  (wave:M (math-chrome wave) — or fold into any equation-touching wave)
  glassui_asks:
    - decorative-epicycle / ambient-signature primitive: A reusable glass-ui decorative primitive that renders a single-stroke Fourier/epicycle path as an ambient watermark or signature (takes a path/point-array, exposes opacity + stroke + optional PRM-gated draw-on-enter trace). Fourier currently hand-rolls this with svg-fourier.ts + decorative/FourierMorphSvg.vue + useFourierMorph.
    - EpicycleSpinner / themable loading indicator: A loading-indicator primitive accepting a motif (default ring, pluggable to nested concentric circles / epicycles), PRM-aware, INP-safe (pure CSS transform). Fourier needs the epicycle variant; the generic ring covers everyone else.

### C6 — C6 — Typography Audacity (display face, type scale, hierarchy-via-type, hero/section heads, KaTeX)
VERDICT: fourier's typography is the single most under-realized axis against the "large and audacious" directive, and the glass-ui 4.0 re-grounding makes the fix a clean gestalt transposition rather than new bespoke work. glass-ui 4.0 ships a complete φ-ladder display system (`--type-display-1..audacious` clamps to a 352px peak, `text-display-*` / `text-hero` / `text-pane-title` / `text-title|heading|subheading` utilities, `useStaggerReveal` + `.char-stagger` kinetic) and a brand display face (Plus Jakarta Sans) — fourier consumes NONE of it. Worse, two latent breakages exist under the pending 4.0 bump: fourier vendors Fraunces (woff2, variable 100-900) yet never references it (a dead ~30KB+ font load), and `.cm-serif` — which paints the paper hero, the brand mark, and every math label — resolves via glass-ui 4.0 to `var(--font-serif-math, serif)`, a token fourier never sets, so the entire Computer Modern display voice silently degrades to system Times. The idiomatic move: rebind `--font-display` → the already-vendored Fraunces (the editorial-mathematical display voice the J design wanted, which glass-ui's Plus-Jakarta default is wrong for), set `--font-serif-math` → Computer Modern for the math/body voice, then adopt the φ-ladder utilities for a real hero + section ramp. This is the prior J-typo vision, corrected for the 4.0 reality (no Fraunces/WONK in glass-ui; it's a fourier-local display brand).
findings: 8
  [critical] C6-F1 `.cm-serif` silently degrades to system Times under glass-ui 4.0 — the paper hero, brand mark, and all math labels lose Computer Modern  (wave:M.W1 (the glass-ui 4.0 dep-bump + token-reconciliation foundation wave; must land WITH the bump or the bump breaks the paper voice))
  [high    ] C6-F2 Fraunces is vendored, preloaded-adjacent, and fetched — but referenced NOWHERE: a dead display-font payload  (wave:M.W1 (foundation: bind `--font-display`→Fraunces in the same token-reconciliation that fixes F1) CHRONIC DEFERRED)
  [high    ] C6-F3 The φ-display ladder is entirely unused — every heading is a raw ad-hoc Tailwind size, capping at ~3.25rem; nothing is audacious  (wave:M.W2 (hierarchy-via-type: cascade the ladder across paper + the 7 page heads))
  [high    ] C6-F4 No hero register anywhere — gallery and equation pages have NO page heading at all; the brand mark is the only large type  (wave:M.W2 (hero + page-head adoption))
  [medium  ] C6-F5 Mono micro-labels are hand-rolled magic-number sizes instead of the `text-admin-label`/`text-mono-caption` rungs  (wave:M.W2 (ladder cascade — mono tier))
  [medium  ] C6-F6 KaTeX sizing is a hand-tuned em-stack disconnected from the type ladder; display math doesn't scale with the φ-display register  (wave:M.W2 (math register joins the ladder))
  [medium  ] C6-F7 Zero kinetic typography — the only entrance is a 0.18s tab slide; glass-ui ships `useStaggerReveal` + `.char-stagger` unused  (wave:M.W3 (kinetic-type entrance; pairs with the broader motion wave))
  [low     ] C6-F8 Canvas-drawn type (grid/labels/placeholder) is a parallel hardcoded font system that won't follow the display rebrand  (wave:M.W2 (canvas-type reconciliation; or defer to a follow-up if W2 is tight) DEFERRED)
  glassui_asks:
    - GA-C6-1 — display-face register knob so a consumer can run a serif display voice through the φ-ladder without forking every `text-display-*` utility: glass-ui's `text-display-*`/`text-hero`/`text-pane-title` utilities all hardcode `font-family: var(--font-display)` AND `font-weight: 600`/`font-optical-sizing: auto` tuned for Plus Jakarta Sans (a sans). A consumer like fourier that rebinds `--font-display` to a characterful serif (Fraunces, WONK/SOFT axes) gets the right family but inherits sans-tuned weight/optical defaults. Expose per-register tuning knobs — `--display-weight`, `--display-wonk` (→ `font-variation-settings`), `--display-optical` — on the display utilities so a consumer can dial the variable-axis register from tokens, not by re-authoring the utilities. This is the corrected, 4.0-era successor to the J design's stale 'glass-ui reserves Fraunces WONK=1' premise: glass-ui ships Plus Jakarta as display; the Fraunces voice is fourier-local; glass-ui should make the display ladder voice-agnostic.
    - GA-C6-2 — A-2 (ConfiguratorLayer label/sub bound to the text-title/text-heading ladder) — re-affirm against 4.0: Pre-existing ask A-2 (ADOPTION-ASKS.md:171) booked the ConfiguratorLayer pane-title/label typography to the `text-title`/`text-heading` ladder at the component root, originally targeted at glass-ui AT/3.3.0. With 4.0 shipped, re-verify whether the label/sub slots now read the ladder rungs or still carry magic literals (`text-sm font-semibold`). If still literal, the fix lands in 4.x.

### C7 — C7 — Colourful audacious POPS (from the icon palette)
VERDICT: The icon palette IS audacious — favicon.svg is a six-stop rainbow epicycle (R-O-Y-G-B-V: #ff4444 #ff8800 #ffcc00 #44bb44 #4488ff #8844ff) and colors.ts carries a softened twin (STATIC.rainbow). But that chroma is quarantined to a single chart legend (the equation page harmonic series). The entire app chrome — home, gallery, morph, workspace, paper — renders near-monochrome cream/foreground with exactly ONE pop: the orange dark-mode gear-sun in the corner. The nav 'active' state collapses all five distinctly-iconed routes onto one undifferentiated --viz-amber accent. Worse, the spectrum-hue formula `(1-x)*300` is hand-rolled in sRGB-HSL and copy-pasted across four files while value.js 0.12.0 ships exactly the OKLCH palette engine (mixColorsN, interpolateHue, safeAccentColor) that fourier never calls — it imports only easeInOutSine. The proportioned move is to (1) promote the icon rainbow to a single canonical OKLCH ramp generated by value.js, (2) key per-route accents off it so each page carries its own hue, and (3) re-route the four duplicated spectrum functions through that one source. Audacious where attention lives (active state, data viz, key CTAs, hover), restrained on body surfaces.
findings: 8
  [high    ] C7-1 Icon rainbow palette is quarantined — chrome is monochrome but the brand mark is six-colour  (wave:M (new tranche) — Wave 'pops/palette-source')
  [high    ] C7-2 Nav active state flattens five distinct routes onto one amber accent  (wave:M (new tranche) — Wave 'pops/nav-identity')
  [medium  ] C7-3 Spectrum-hue formula `(1-x)*300` hand-rolled in sRGB-HSL and duplicated in four files  (wave:M (new tranche) — Wave 'pops/palette-source' (co-lands with C7-1) CHRONIC)
  [medium  ] C7-4 value.js 0.12.0 colour engine is present but unused — only easeInOutSine is imported  (wave:M (new tranche) — Wave 'pops/palette-source' (the dep bump is the foundation move; pairs with K's dep-bump posture) CHRONIC)
  [medium  ] C7-5 Morph page is red-monochrome — the one page literally about shape blending carries no chroma  (wave:M (new tranche) — Wave 'pops/motion-chroma')
  [low     ] C7-6 Gallery tier tokens (featured gold / saved blue) exist upstream but the gallery shows no colour  (wave:M (new tranche) — Wave 'pops/nav-identity' (gallery surface); overlaps J's gallery wiring (J.W5/W6) DEFERRED)
  [low     ] C7-7 Hover/interaction feedback is monochrome alpha-mix — no hue-shift pop on interactive elements  (wave:M (new tranche) — Wave 'pops/nav-identity')
  [low     ] C7-8 favicon.svg and colors.ts STATIC.rainbow are divergent hand-maintained twins of the same palette  (wave:M (new tranche) — Wave 'pops/palette-source')
  glassui_asks:
    - icon-ramp-as-token-recipe: A glass-ui token recipe for a consumer-supplied N-stop perceptual brand ramp: given an ordered list of seed colours, emit --brand-ramp-0..N (display) and --brand-ramp-soft-0..N (calmed) tokens in OKLCH, gamut-mapped, with per-stop contrast-safe variants against --background. fourier would feed it the six favicon stops.
    - per-route-accent-channel: A glass-ui convention/utility for per-route (or per-section) accent identity: a documented --route-accent custom-property channel that nav/tab/dock active-state recipes read, so a host app can assign each route a distinct hue and have active indicators, drop-shadows, and hover tints pick it up uniformly.

### D1 — D1 — glass-ui 4.0.0 idioms to adopt
VERDICT: fourier's ^3.1.0 pin is two majors behind the shipped 4.0.0 (glass-ui), two majors behind 4.2.0 (keyframes), and two minors behind 0.12.0 (value.js). The RUN-BOARD's E1 gate ("glass-ui 3.2.0 on npm blocked") was cleared long ago — the real blocker is now a ^4.0.0 bump. Nine concrete 4.0.0 idioms have ZERO fourier adoption sites: Configurator asideSide left-flip, useTextHighlight (replaces v-html highlight injection in search), useId (two dock VT e2e breakages root-caused here), inert a11y on collapsed subtrees, useCanvas2D substrate (replaces the hand-rolled useCanvasSetup + ResizeObserver), the glass-top-layer @starting-style native popover grammar, startViewTransition from /motion-core (router calls document.startViewTransition raw), UnderlineTabs→SegmentedTabs variant="underline" rename (UnderlineTabs no longer exported), and MetricBadge :amount → :value (BA breaking rename across 11 call sites).
findings: 14
  [critical] D1-01 Package pins 3+ versions stale — glass-ui ^3.1.0 vs shipped 4.0.0  (wave:K.W4 (owns the dep bump — per tranche_k.md W4 glass-ui-3.2.0 adoption, now retargeted to 4.0.0) CHRONIC)
  [critical] D1-02 UnderlineTabs import no longer exported — dead named export  (wave:K.W4)
  [critical] D1-03 MetricBadge :amount prop — BA breaking rename to :value across 11 call sites  (wave:K.W4)
  [critical] D1-04 useId() not used for VT name uniqueness — two dock view-transition-name collisions break e2e  (wave:K.W4 CHRONIC)
  [high    ] D1-05 Configurator lacks asideSide="left" — controls aside is always right, DEC-2 violation  (wave:M.W1)
  [high    ] D1-06 useTextHighlight not used — search hit highlight injects v-html with <mark> DOM mutation  (wave:M.W2)
  [high    ] D1-07 useCanvas2D substrate not adopted — BasisCanvas/ConvergencePlot use hand-rolled ResizeObserver + rAF without park/freeze  (wave:M.W3)
  [high    ] D1-08 inert a11y not applied to collapsed subtrees — keyboard focus leaks into closed panels  (wave:M.W2)
  [medium  ] D1-09 startViewTransition called raw on document — should use glass-ui /motion-core substrate  (wave:M.W1)
  [medium  ] D1-10 MobileFloatingToc dropdown uses v-if + Transition — should be native [popover] + glass-top-layer  (wave:M.W2)
  [medium  ] D1-11 Coefficient popover in EquationView is a positioned div — should be native popover or HoverPopover  (wave:M.W2)
  [medium  ] D1-12 FullscreenViewer uses hand-rolled focus trap — glass-ui inert pattern suffices  (wave:M.W2)
  [medium  ] D1-13 View transition name on viz canvas is a static string — two co-mounted canvases will collide  (wave:M.W1)
  [low     ] D1-14 FadingScroll not adopted — CoefficientsPanel and paper sidebar scroll without edge-fade  (wave:M.W2)
  glassui_asks:
    - useCanvas2D on /canvas subpath — confirm export shape: Verify that `@mkbabb/glass-ui/canvas` (src/subpaths/canvas.ts) exports `useCanvas2D` and `useCanvasLifecycle` at the public surface so fourier can import them without reaching into internal paths.
    - FadingScroll on /fading-scroll — confirm BA scroll-fade migration note for consumers: Confirm MIGRATION.md or CHANGELOG carries a consumer-facing note that `.scroll-fade-*` utility classes are retired and `<FadingScroll>` is the canonical replacement, so fourier's M tranche can book the clean migration without re-deriving the rationale.

### D2 — D2 — glass-ui GAPS: fourier-hand-rolled primitives to ABSTRACT INTO glass-ui (NET-NEW asks)
VERDICT: fourier hand-rolls at least five families that already (or should) live in glass-ui as refined primitives. The single largest gap is a TONAL ACCENT recipe — `--pill-color` + `color-mix(in srgb, <tone> N%, transparent)` — repeated ~57 times across NotationPills, EquationModeToggle, BasisSelector, InfoCard, HarmonicLevelGrid; this is the colour-pop accent system the user wants suffused, yet it has no glass-ui home (IconChip covers icon-glyph tone, NOT background/border/text accent fills). Second: two distinct segmented/pill toggles (NotationPills, EquationModeToggle) re-implement what glass-ui SHIPS as SegmentedTabs (PILL+UNDERLINE materials) — fourier never imports it. Third: a math-serif section heading (`cm-serif text-sm font-semibold tracking-tight`) appears 6+ times — Section has a `label` tone but no math/serif tone. Fourth: ConvergenceLegend is a hand-rolled colour swatch-list legend with no glass-ui equivalent. Fifth: the J-CORE atom-diff viewer (when wired in J.W6) should be born as a glass-ui primitive, not hand-rolled. These are glassui_asks; fourier books them, does not write glass-ui.
findings: 7
  [high    ] D2-01 Tonal accent recipe (`--pill-color` + color-mix) hand-rolled ~57x — no glass-ui home  (wave:M (glass-ui-adoption wave) — book the ask now; adopt after glass-ui ships CHRONIC)
  [high    ] D2-02 Two segmented/pill toggles re-implement glass-ui SegmentedTabs (never imported)  (wave:M (adopt SegmentedTabs))
  [high    ] D2-05 Atom-diff / remix-highlight viewer (J-CORE) has no frontend home — born it as a glass-ui primitive, not hand-rolled  (wave:J.W6 (consume) — but the PRIMITIVE is an M-booked glassui_ask DEFERRED)
  [medium  ] D2-03 Math-serif section heading (`cm-serif text-sm font-semibold tracking-tight`) hand-rolled 6+x  (wave:M (book ask + adopt Section))
  [medium  ] D2-04 ConvergenceLegend is a hand-rolled colour swatch-list legend — no glass-ui legend primitive  (wave:M (book ask))
  [low     ] D2-06 Math-glyph chrome (serif Σ / a+b / notation icon inside a control) hand-rolled inline  (wave:M (book ask))
  [low     ] D2-07 Hairline divider-rule hand-rolled (`color-mix(... foreground 6%)` 1px) instead of glass-ui Separator  (wave:M (adopt Separator; small ask for hairline variant))
  glassui_asks:
    - Tonal-accent recipe (accent-tone primitive): A single library recipe that mints a contrast-floored three-channel tonal accent — `--accent-fill` (~12-16% tone over transparent), `--accent-edge` (~30-40%), `--accent-ink` (solid tone) — from ONE `--tone` input, exposed as a `.accent-tone` utility and/or a `tone` arm on the pill/badge family. Reuse IconChip's existing oklch-aware section-color mixing; floor light/dark contrast once in the library (as fourier currently hand-does for --viz-amber).
    - Legend / SwatchList primitive: A floating glass list of `{swatch, label}` rows with swatch variants (solid / ring / dashed) reusing status-dot's dot engine, plus an optional `v-model:hovered` channel that two-way-syncs row highlight with an external plot.
    - AtomDiff / diff-highlight viewer: A three-tone (added / removed / changed) keyed row renderer over a canonical diff shape (the J atomdiff vocab), reusing the tonal-accent recipe (green/red/amber) for the highlight fills and glass-ui's existing View-Transitions/@starting-style motion floor for row enter/leave.
    - Section `tone="math"` (serif section-heading rung): A math/serif tone on the existing `<Section>` (and a `.text-section-serif` typography ladder rung) baking `cm-serif + body-sm + semibold + tracking-tight`.
    - MathGlyph chrome (centered single-glyph affordance): A `<MathGlyph variant=serif|mono>` (or a glyph slot on icon-chip/metric-pill) that owns the optically-centered single-glyph box: cm-serif/Fira, fixed advance width, leading-none, baseline-trimmed — sized as a control affordance and slot-compatible with SegmentedTabs segments.
    - Separator `variant="hairline"` (quiet rule weight): A quiet/hairline tone on the existing `<Separator>` — one library-tuned low-alpha rule weight for light + dark, instead of consumers guessing an alpha.

### D3 — D3 — glass-ui component-internal refinements (refine-within-an-existing-component asks)
VERDICT: The glass-ui 4.0.0 re-ground INVALIDATES this dimension's entire prior ledger: A-1 (inter-row divider) shipped as ConfiguratorLayer's `dividers` prop, A-2 (label/sub ladder) shipped as the `.configurator-section-label` token hook reading `--configurator-section-{size,weight}`, and A-3 (asideSide) shipped — all three "BOOK-to-AT" asks are SATISFIED-UPSTREAM and must be struck, not carried. What remains are FOUR genuinely-open component-internal refinements fourier's surfaces actually need, each evidenced by a hand-rolled workaround in the fourier tree: (1) ConfiguratorLayer has no header trailing/actions slot — the single highest-value refine, since it is the sole reason fourier maintains a parallel CollapsibleSection.vue and pushes reset buttons into the layer body at two sites; (2) DockIconButton ships no active/pressed toggle-state contract, so 7 `:class="{is-active}"` sites ride a class with NO backing paint in either tree; (3) Card `surface="cartoon"` shipped, retiring the 14-site `.cartoon-card` shim (fourier-local adoption); (4) DockSeparator shipped orientation-aware, retiring fourier's 4-site axis-blind `.dock-separator` hairline. The bump from `^3.1.0` to `^4.0.0` is the prerequisite that turns all of these from asks into adoptions.
findings: 6
  [high    ] D3-01 A-1 / A-2 / A-3 are SATISFIED-UPSTREAM in glass-ui 4.0.0 — strike the stale BOOK-to-AT asks  (wave:M (the dep-bump wave) unblocks the adoption; the consumption lands with the J.W5 control-pane pass CHRONIC)
  [high    ] D3-02 ConfiguratorLayer has NO header trailing/actions slot — the sole reason fourier keeps a parallel CollapsibleSection + body-top reset hacks  (wave:glassui_ask A-3-new (book to glass-ui); fourier consumes + retires CollapsibleSection at M / J.W5)
  [high    ] D3-03 DockIconButton ships no active/pressed toggle-state contract — 7 `is-active` sites ride a class with NO backing paint  (wave:glassui_ask DOCK-ACTIVE (book to glass-ui); fourier adopts at M)
  [medium  ] D3-04 Card surface="cartoon" shipped in 4.0.0 — the 14-site `.cartoon-card` dead-class shim is now retirable  (wave:M (fourier-local, with the bump); optional glassui_ask only if cartoon-surface needs the token binding CHRONIC)
  [medium  ] D3-05 DockSeparator shipped orientation-aware — fourier's 4-site axis-blind `.dock-separator` hairline is retirable, and reveals a real fourier need the component already serves  (wave:M (fourier-local, with the bump))
  [low     ] D3-06 Recurring hand-rolled dock count-badge — candidate for a DockBadge / DockIconButton badge slot refine  (wave:glassui_ask DOCK-BADGE (book, low priority); is_deferred until a second consumer corroborates DEFERRED)
  glassui_asks:
    - A-3-new — ConfiguratorLayer header `trailing`/`actions` slot: Add a named `trailing` (or `actions`) slot to the ConfiguratorLayer trigger header, rendered right-aligned just before the chevron, inside a `@click.stop` boundary so a slotted control (e.g. a reset button) does not toggle the section. Makes ConfiguratorLayer a strict superset of a label+sub+chevron+collapsible+actions section.
    - DOCK-ACTIVE — first-class active/pressed toggle contract on DockIconButton (and Button): Add an `active?: boolean` toggle prop to DockIconButton (and ideally Button) that stamps `aria-pressed` + `data-active` and drives a selected/pressed visual register in dock.css/button styling (tinting at the existing `--dock-selected-accent` token).
    - DOCK-BADGE — dock count-pill / icon-button indicator slot (low priority, deferred): Either a `<DockBadge>` inline tabular-nums pill primitive for dock transport-count annotations, or a `badge`/`indicator` named slot on DockIconButton positioning a corner pip/count within the button box (the specular-tracking host already establishes the relative box).

### D4 — D4 — Platform-Over-Library (inv-30)
VERDICT: Fourier duplicates platform/glass-ui capability across seven categories: a `cartoon-card` shim that belongs as a direct `cartoon-surface` adoption, a hand-rolled rAF animation clock that should be replaced by keyframes.js `NumericAnimation`, a bespoke position-tracked coefficient popover that should use `HoverPopover` from glass-ui, two bespoke two-column panel grids (`.eq-grid`, `.config-grid`) that duplicate the `Configurator` chassis, a fullscreen viewer with a hand-rolled focus trap that should use glass-ui's `Dialog`, nine inline `animate-spin` spinners that should use glass-ui's `Pulse` (variant="ring"), and a tab-panel slide animation in style.css that glass-ui 4.0 does not yet carry but is a standing glass-ui ask. The dep pins are a major version behind (glass-ui ^3.1.0, keyframes.js ^2.2.0, value.js ^0.10.0 vs. shipped 4.0.0 / 4.2.0 / 0.12.0), blocking all glass-ui 4.x and keyframes.js 4.x improvements.
findings: 14
  [critical] D4-12 Dep pins two major versions stale: glass-ui ^3.1.0 vs 4.0.0, keyframes.js ^2.2.0 vs 4.2.0  (wave:M.W0 CHRONIC DEFERRED)
  [high    ] D4-01 cartoon-card shim: local @utility re-wiring a removed glass-ui class  (wave:M.W1 CHRONIC DEFERRED)
  [high    ] D4-02 Hand-rolled rAF clock in animation store: duplicates keyframes.js NumericAnimation  (wave:M.W2)
  [high    ] D4-03 Coefficient popover: mouse-tracked absolute div duplicates glass-ui HoverPopover  (wave:M.W2)
  [high    ] D4-06 FullscreenViewer: hand-rolled focus trap + Teleport duplicates glass-ui Dialog  (wave:M.W3)
  [medium  ] D4-04 .eq-grid: bespoke two-column panel grid duplicating Configurator chassis  (wave:M.W3)
  [medium  ] D4-05 .config-grid: morph page hand-rolls a 3-column responsive grid  (wave:M.W1)
  [medium  ] D4-07 9 inline animate-spin spinners: duplicates glass-ui Pulse ring variant  (wave:M.W1)
  [medium  ] D4-09 EquationModeToggle: bespoke pill toggle duplicates glass-ui ToggleGroup  (wave:M.W2)
  [medium  ] D4-10 MobileFloatingToc dropdown + GallerySearchBar filter panel: bespoke absolutely-positioned overlays vs. native popover  (wave:M.W3)
  [medium  ] D4-11 AnimationControls play button: bespoke rainbow glass pill bypasses glass-ui material system  (wave:M.W2)
  [low     ] D4-08 Tab-panel slide animation in style.css: local carry of an upstream glass-ui ask  (wave:M.W1 CHRONIC DEFERRED)
  [low     ] D4-13 useOffsetPagination: local fork of a retired glass-ui subpath  (wave:M.W1 DEFERRED)
  [low     ] D4-14 PaperView teleport-overlay: bespoke fixed-position radial gradient backdrop instead of glass-ui Dialog backdrop  (wave:M.W1)
  glassui_asks:
    - tab-panel-animation: Ship `[data-state="active"][role="tabpanel"]` slide-in animation natively inside the Tabs primitive's stylesheet (or via an `animations.css` rule keyed on the same data-attributes reka-ui's TabsContent sets). Token: `var(--ease-standard)` for the easing, `0.18s` or `var(--duration-fast)` for duration. Include `@media (prefers-reduced-motion: reduce) { animation: none; }` guard. Once shipped, the identical local carry in `fourier/web/src/style.css:83-96` is deleted.
    - usePagedLoader: Ship a `usePagedLoader<T>` composable (subpath `@mkbabb/glass-ui/pagination` or merged into the main barrel) with the active-loader contract: `fetchFn: (limit, offset) => Promise<{ data: T[]; total: number }>`, `pageSize?`, and a `loadPage(p?)` imperative. This is the shape `fourier/web/src/composables/useOffsetPagination.ts` forks from glass-ui v0.9.3.
    - viz-amber-token-contrast: Rebaseline the light-mode `--viz-amber` token from `hsl(35 70% 42%)` (≈3.54:1, WCAG AA fail) to `hsl(35 76% 35%)` (≈4.6:1, AA pass). Dark-mode stays `hsl(37 73% 67%)` (already compliant). fourier carries a `:root { --viz-amber: hsl(35 76% 35%); }` override in style.css (D.W4.d) with an explicit comment calling it a local stop-gap until glass-ui rebaselines.

### E1 — E1 — Mathematical-Animation Opportunities (the Fourier signature motion language)
VERDICT: The app is motion-CORRECT on the platform axes (View-Transitions route-morph, IntersectionObserver rAF-gating, ping-pong convergence loop) but motion-ABSENT on the orchestration/signature axis — exactly the thesis the J.WC motion spec already wrote ("Convergence is the motion signature… used at ZERO sites"). For a Fourier project the single most under-exploited brand asset is the math itself as motion: the partial-sum settling onto its target, the epicycle chain drawing its own trace, harmonics accumulating one beat at a time, spectral bars rising in amplitude order. The renderers already COMPUTE every quantity these reveals need (precomputed trail, partial_sums per level, amplitude-sorted spectrum, harmonicProgress sweep) — they are simply snapped to a flat appear or driven by raw performance.now() oscillators with no reduced-motion guard. The siblings now ship the precise primitives (keyframes 4.2.0 DrawSVG + MotionPath; glass-ui 4.0 useStaggerReveal/useCountup/useSpring) and fourier consumes essentially none of them. The highest-leverage M wave is a single "convergence-reveal" motion grammar applied at first-paint of every canvas + spectrum, plus harmonic-accumulation staggers — audacious within proportion, PRM-honoured, INP-safe.
findings: 8
  [high    ] E1-01 No signature reveal: every canvas/spectrum snaps to a flat appear — the partial sum never visibly converges onto its target  (wave:M (signature-motion wave) — the headline; feeds E6 DEFERRED)
  [high    ] E1-02 keyframes.js 4.2.0 ships DrawSVG + MotionPath — the two primitives built for epicycle tracing — and fourier consumes neither  (wave:M (signature-motion wave) DEFERRED)
  [medium  ] E1-03 Two canvas oscillators pulse forever off raw performance.now() with NO reduced-motion guard (tip glow + golden shimmer)  (wave:M (signature-motion wave) — PRM/hygiene sub-task CHRONIC)
  [medium  ] E1-04 Spectral bars 'pop in' via flat CSS transition-all instead of rising in amplitude order (harmonic-accumulation stagger)  (wave:M (signature-motion wave) DEFERRED)
  [medium  ] E1-05 Convergence numeric readouts (energy %, N=k/total) jump discretely — no convergence-as-counter motion  (wave:M (signature-motion wave) DEFERRED)
  [medium  ] E1-06 Morph route defaults to linear easing and never traces the shape drawing-on — the most animation-forward page is the least expressive  (wave:M (signature-motion wave) DEFERRED)
  [low     ] E1-07 No first-impression epicycle reveal: '/' redirects to a saved tab, so there is no hero where the Fourier signature greets the visitor  (wave:M (signature-motion wave) — optional flourish; or defer to a J-shell reveal DEFERRED)
  [low     ] E1-08 ConvergencePlot runs its own private rAF clock + easing, divorced from the shared animation store — two convergence engines that cannot share a heartbeat  (wave:M (signature-motion wave) — substrate/prereq for E1-01)
  glassui_asks:
    - convergence-reveal motion preset / directive: A reusable 'partial-sum settle' motion preset (spring curve + orchestration helper, e.g. a vConvergence directive or a useConvergenceReveal composable) that drives a 0→1 'accumulation' clock with PRM-collapse-to-terminal built in — the canonical 'series settling onto target' grammar.
    - canvas-aware motion-phase source (PRM + paused gating): A tiny shared 'motion phase' provider that yields an oscillation phase (or steady-state value) honouring prefers-reduced-motion and an external 'active' flag, suitable for Canvas2D draw loops (not just DOM/CSS).

### E2 — Hero items + scroll-driven animation (paper + UI)
VERDICT: The frontend is animation-starved at exactly the surfaces the user wants to elevate: the paper opens on a plain centered h1 with zero kinetic or scroll-entry treatment, there is NO home/landing hero at all (`/` redirects to a saved tab), and the equation/morph/gallery views lead with weak or absent titles. Meanwhile glass-ui 4.0 already ships the entire substrate fourier needs — `.scroll-progress` (adopted), the unconsumed `[data-scroll-reveal]` view-timeline recipe, kinetic typography (`.text-pane-title`, `.char-stagger`, `.text-engraved`), `--spring-*` linear() springs, and the `useStaggerReveal`/`useScrollProgress` composables — but the deps still pin `@mkbabb/glass-ui ^3.1.0`, so all of it is dark. The bump to `^4.0.0` is the load-bearing prerequisite; once landed, hero + scroll-reveal are mostly token/recipe adoption, not net-new engineering. The one real hazard is fourier-specific: the paper's virtual section window + `content-visibility: auto` + teleport jumps will make a naive scroll-reveal re-fire on every re-entry, so reveals must be once-disciplined.
findings: 8
  [critical] E2-01 Dep pin ^3.1.0 darkens the entire glass-ui 4.0 kinetic + scroll-reveal substrate (M prerequisite)  (wave:M.W0 CHRONIC)
  [high    ] E2-02 Paper title is an inert h1 — no hero, no kinetic treatment, no entry animation  (wave:M.W1)
  [high    ] E2-03 No mathematical home/landing hero exists — `/` redirects straight into a tab  (wave:M.W2)
  [high    ] E2-04 glass-ui's [data-scroll-reveal] view-timeline substrate is shipped but entirely unconsumed  (wave:M.W3)
  [medium  ] E2-05 Paper section reveals collide with the virtual window + content-visibility + teleport — needs once-discipline  (wave:M.W3)
  [medium  ] E2-06 Equation, morph, and gallery views lack hero typography — weak or absent titles  (wave:M.W4)
  [low     ] E2-07 Reading-progress bar is the only scroll-driven UI — no parallax math-lattice / depth  (wave:M.W5)
  [low     ] E2-08 glass-ui ships kinetic recipes but no JS helper to set --char-index / split text (recurring gap)  (wave:M.W1 CHRONIC DEFERRED)
  glassui_asks:
    - useCharStagger / SplitChars kinetic primitive: A first-class glass-ui composable or render component that splits a string into per-glyph spans, sets --char-index, and preserves an accessible full-text label — the JS partner to the existing .char-stagger>.char CSS recipe (typography.css). Should expose stagger-step + spring-token props and a PRM-aware no-op terminal state.
    - data-scroll-reveal once-discipline hook for virtualized/teleporting scrollers: An opt-in mode on the [data-scroll-reveal] recipe (e.g. a [data-scroll-reveal=once] variant or a documented data-revealed latch) that reveals a child exactly once and renders terminal-state on re-entry — so the recipe is safe inside content-visibility:auto virtual windows and after programmatic teleport scrolls.

### E3 — keyframes.js 4.2.0 leverage — 2x-major migration audit (E3)
VERDICT: fourier pins @mkbabb/keyframes.js ^2.2.0, two full major versions behind the local 4.2.0 library. The entire orchestration tier — RAFPlayback, SpringProgress, stagger, flip/flipShared, drag/Draggable, decay, Sequence, springLinearStops/springTimingFunction, createNativeTimeline, animate(), DrawSVG, MotionPath — landed between v3.0.0 and v4.2.0 and is entirely absent from fourier. The codebase carries at least seven hand-rolled rAF loops that are direct duplicates of what RAFPlayback already does correctly (generation guard, bind-proof, IntersectionObserver gate, reduced-motion snap). The easing catalog is backed by raw value.js function imports rather than the typed Easing surface the 4.x engines consume natively, leaving the spring linear() compositor path permanently dark. Every finding below maps to a concrete fourier animation site and proposes its idiomatic 4.x transposition; none are cosmetic — each closes a correctness, performance, or architectural gap.
findings: 16
  [critical] E3-01 Package pin is two majors stale — ^2.2.0 vs local 4.2.0  (wave:M W1 — the bump is the prerequisite gate for every subsequent finding CHRONIC)
  [high    ] E3-02 animation store: bespoke rAF loop duplicates RAFPlayback exactly  (wave:M W2)
  [high    ] E3-03 useCurveTransition: bare rAF loop with hand-rolled easing instead of RAFPlayback.play()  (wave:M W2)
  [high    ] E3-04 ConvergencePlot: third independent rAF loop — identical ping-pong pattern as animation store  (wave:M W2)
  [high    ] E3-05 useCanvasHover: two independent rAF loops (scale lerp + shimmer) should be SpringProgress + RAFPlayback.drive()  (wave:M W2)
  [high    ] E3-06 useFourierMorph: three-phase morph driven by Animation as a bare progress ticker — replace with Sequence  (wave:M W3)
  [high    ] E3-07 Easing catalog backed by raw value.js function imports — blocks the spring linear() compositor path  (wave:M W2)
  [medium  ] E3-08 usePointDrag: bespoke pointer-event state machine — replace with Draggable  (wave:M W3)
  [medium  ] E3-09 GalleryMarquee: CSS-keyframe marquee is not stagger-aware — stagger() enables per-card reveal on entry  (wave:M W4)
  [medium  ] E3-10 PaperView scroll navigation: six raw rAF calls in useScrollNavigation — ScrollTimeline opportunity  (wave:M W4)
  [medium  ] E3-11 springLinearStops / springTimingFunction not used anywhere — --spring-* tokens are dark  (wave:M W4)
  [medium  ] E3-12 FourierMorphSvg SVG path draw-in should use DrawSVG instead of a timed CSS stroke-dashoffset  (wave:M W4)
  [medium  ] E3-13 GalleryCard panel open/close uses CSS transition without spring easing — flip() opportunity  (wave:M W4)
  [medium  ] E3-14 animate() single-call front door not used — every animation site constructs Animation by hand  (wave:M W3)
  [medium  ] E3-16 No reduced-motion handling on any non-CSS animation site  (wave:M W2 CHRONIC)
  [low     ] E3-15 PaperView reading progress uses raw rAF + scroll listener instead of ScrollTimeline  (wave:M W4)
  glassui_asks:
    - spring-scrubber-integration: Expose a SpringProgress-driven variant of the Slider scrub thumb — when the user releases the scrub thumb mid-drag, the thumb should spring-settle to the dropped position rather than snapping. This requires the glass-ui Slider to accept an optional SpringProgress instance for the value-settle phase.

### E4 — E4 — value.js 0.12.0 leverage: colour + motion adoption surface
VERDICT: fourier pins @mkbabb/value.js ^0.10.0 (installed 0.10.0); the published library is 0.12.0 — two major-minor releases ahead. The gap contains six high-leverage capabilities fourier does not use: perceptual-space colour mixing (mixColorsN), OKLCH-safe contrast helpers (computeSafeAccent / safeAccentColor), per-frame zero-alloc WAAPI colour serialisation (toAnimationString + cssColorInterpKeyword), a cusp-aware gamut-mapping upgrade, and the computed-endpoint cache perf tier. Fourier's colour stack is a bespoke island of hand-rolled HSL templating, per-file spectrumColor quadruplication, and a 118-line custom CSS-var-to-hex parser — all of which value.js 0.12.0 supplants cleanly. Bumping the pin unlocks perceptually-uniform harmonic-colour ramps, OKLab-interpolated dark-mode toggle, contrast-safe swatch accents, and a forward adoption seam for the planned sampleColorRamp (0.13.0). Zero new science required; all leverage is composition over shipped kernels.
findings: 10
  [high    ] E4-F1 Version pin ^0.10.0 leaves two minor releases of leverage on the table  (wave:M.W1 (version-bump wave, prerequisite of all other E4 findings) CHRONIC)
  [high    ] E4-F2 spectrumColor quadruplication — four independent HSL-template implementations across the codebase  (wave:M.W2 (colour system transposition) CHRONIC)
  [high    ] E4-F3 118-line bespoke CSS-var-to-hex parser in colors.ts duplicates value.js 0.12.0 parseCSSColor  (wave:M.W2 (colour system transposition))
  [high    ] E4-F6 mixColorsN (0.12.0) enables harmonic-weighted colour sweeps absent in current fourier — the colourful-pops unlock  (wave:M.W3 (colourful-pops implementation wave))
  [medium  ] E4-F4 DarkModeToggle lerpColor uses raw sRGB channel arithmetic — perceptual mudding on warm-to-cool transition  (wave:M.W2 (colour system transposition))
  [medium  ] E4-F5 energyColor and notation.ts use HSL threshold literals — not tied to the token system or perceptual scale  (wave:M.W2 (colour system transposition))
  [medium  ] E4-F7 computeSafeAccent / safeAccentColor (0.12.0) absent — no contrast-safe accent logic on any coloured swatch  (wave:M.W3 (colourful-pops implementation wave))
  [medium  ] E4-F8 toAnimationString (0.12.0) absent — WAAPI colour keyframes still emit legacy sRGB strings  (wave:M.W3 (colourful-pops implementation wave))
  [low     ] E4-F9 gamutMap wide-gamut egress (0.12.0) absent — canvas epicycle colours cannot safely target Display P3  (wave:M.W3 (colourful-pops implementation wave))
  [low     ] E4-F10 sampleColorRamp (planned 0.13.0) — forward adoption seam must be booked now to unblock M.W3's harmonic-sweep design  (wave:M.W3 (book-with-kill-date = value.js 0.13.0 publish) DEFERRED)

### E5 — E5 — Animation Inventory + Proportion Gate
VERDICT: Fourier runs six distinct animation surfaces: (1) a manual rAF ping-pong clock in the animation store driving the epicycle canvas at 60 fps, (2) a separate manual rAF loop in ConvergencePlot (equation page) that is an independent and architecturally redundant twin of the store clock, (3) a third manual rAF tween engine in useCurveTransition for coefficient transitions, (4) two concurrent hover-scale + shimmer rAF sub-loops inside useCanvasHover that fire on mousemove outside the main clock, (5) a CSS-only surface (six @keyframes across four components plus View Transitions and Transition wrappers), and (6) the keyframes.js Animation engine used narrowly for the Fourier morph sequence. The rAF budget is high: at peak (epicycle canvas with hover active) four concurrent rAF loops run simultaneously on the main thread. The store clock is the only one gated by IntersectionObserver; the ConvergencePlot loop has no visibility gate. PRM coverage has three gaps: rainbow-slide in ImageUpload, golden-shimmer in ContourEditorCanvas, and the main-thread shimmer rAF in useCanvasHover which fires unconditionally. Proportionality is broadly maintained but the play-button rainbow-drift, the ConvergencePlot's always-on auto-play, and performance.now-based shimmer sampling during the main 60 fps draw call each push toward gaudy or INP-risk territory.
findings: 14
  [high    ] E5-01 ConvergencePlot: autonomous rAF clock duplicates animation store, no visibility gate  (wave:M.W1)
  [high    ] E5-02 useCanvasHover: two concurrent rAF sub-loops (hover-scale + shimmer) independent of main clock  (wave:M.W2)
  [high    ] E5-03 ConvergencePlot canvas: canvas resized (width/height set) on every draw frame at 60 fps  (wave:M.W1)
  [medium  ] E5-04 useCurveTransition: third bespoke 500ms rAF tween alongside two existing rAF clock architectures  (wave:M.W2)
  [medium  ] E5-05 ImageUpload: rainbow-slide infinite CSS animation has no prefers-reduced-motion guard  (wave:M.W3)
  [medium  ] E5-06 ContourEditorCanvas: golden-shimmer infinite CSS animation has no prefers-reduced-motion guard  (wave:M.W3)
  [medium  ] E5-07 performance.now() shimmer sampling runs on every 60 fps canvas draw call — no clock memoization  (wave:M.W2)
  [medium  ] E5-08 ConvergencePlot rAF loop has no IntersectionObserver gate — burns CPU when scrolled off-screen  (wave:M.W1)
  [medium  ] E5-09 keyframes.js pinned at ^2.2.0 — two major versions behind 4.2.0; morph composable uses legacy Animation class API  (wave:M.W0 CHRONIC)
  [medium  ] E5-10 play-btn rainbow-drift: `background-size: 300% 300%` background-position CSS animation — INP boundary risk on low-end devices  (wave:M.W3)
  [low     ] E5-11 Scattered bare `ease` keyword in transitions across 10+ components — inconsistent with canonical token vocabulary  (wave:M.W3 CHRONIC)
  [low     ] E5-12 FullscreenViewer `<Transition name='fs'>`: no View Transition integration — misses the native compositor path  (wave:M.W4)
  [low     ] E5-13 useScrollNavigation: rAF correction loop for virtual-section scroll — up to 20 frames of main-thread positioning  (wave:M.W4)
  [low     ] E5-14 ConvergencePlot auto-plays on mount unconditionally — no user-intent gate  (wave:M.W3)
  glassui_asks:
    - CSS @property shimmer token: Ship a `@property --shimmer-alpha` animation in glass-ui's animations.css: a CSS custom-property keyframe oscillating 0.85→1.0 at a configurable period (default 200ms). Components consuming the shimmer read `var(--shimmer-alpha)` in Canvas 2D `globalAlpha` or SVG `opacity` without any JS `performance.now()` call.
    - Collapsible PRM guard in animations.css: Add `@media (prefers-reduced-motion: reduce) { [data-state='open'] [data-radix-collapsible-content], [data-state='closed'] [data-radix-collapsible-content] { animation: none; } }` to the canonical `collapsible-open` / `collapsible-close` keyframe definitions in glass-ui/src/styles/animations.css.

### E6 — E6 — Animation-Architecture Transposition (gestalt, no-legacy): consolidate fourier's fragmented motion into one idiomatic architecture over keyframes.js 4.2.0 + value.js 0.12.0 + glass-ui 4.0.0 motion primitives + native scroll-driven CSS + View-Transitions
VERDICT: fourier's motion is fragmented across at least 8 independent hand-rolled rAF loops, 7 bespoke @keyframes, two duplicated ping-pong clocks, raw value.js easing imports, and a manual scroll-progress writer — while the sibling libraries now ship the exact canonical replacements: glass-ui 4.0.0 exposes a complete `/motion` + `/motion-core` composable suite (`useRAFLoop`, `useScrollProgress`, `useViewTransition`, `useIntersectionPause`, `useSpring`, `useAnimatedNumber`, `useStaggerReveal`) plus `scroll-driven.css`/`view-transition.css`, and keyframes.js 4.2.0 ships a value.js-free light tier (`RAFPlayback`, `NumericAnimation`, `SpringProgress`, `SmoothProgress`, `Timeline`/`ScrollTimeline`, `springTimingFunction`). fourier imports ZERO of these. The gestalt M-motion CORE is a three-layer architecture — CSS-first (native scroll/VT/@keyframes, compositor) over glass-ui motion composables (the Vue-reactive managed tier) over keyframes.js light primitives (the canvas/data-driven tier) — with value.js owning only colour/easing curves. This retires the manual rAF store (the named legacy), collapses the two ping-pong clocks into one, and removes ~20 raw requestAnimationFrame call-sites. Everything proposed is no-legacy: each hand-rolled loop is DELETED, not wrapped.
findings: 11
  [critical] E6-9 Dependency pins are 1-2 majors behind the shipped siblings — the bump is the hard predecessor of the entire motion CORE  (wave:M-W1 (THE BUMP — front of chain) CHRONIC)
  [high    ] E6-1 Manual rAF store is the named legacy — transpose the epicycle clock onto keyframes.js RAFPlayback + glass-ui useIntersectionPause  (wave:M-W2 (motion CORE — the keystone transposition) CHRONIC)
  [high    ] E6-2 ConvergencePlot duplicates the store's ping-pong clock verbatim — collapse the two clocks into one driver  (wave:M-W2 (motion CORE))
  [high    ] E6-3 useFourierMorph builds tween animations on the HEAVY keyframes engine via a string-px hack — transpose to the light NumericAnimation + Sequence tier  (wave:M-W3 (morph + curve-transition tier))
  [medium  ] E6-4 useCurveTransition hand-rolls a third rAF lerp clock — fold into the shared light tier  (wave:M-W3 (morph + curve-transition tier))
  [medium  ] E6-5 Canvas hover-scale + shimmer are two more hand-rolled rAF loops with a magic-number exponential lerp — transpose to SpringProgress/SmoothProgress  (wave:M-W4 (canvas micro-motion tier))
  [medium  ] E6-6 PaperView keeps a manual rAF scroll-progress writer behind a hand-rolled @supports gate — adopt glass-ui useScrollProgress / .scroll-progress recipe  (wave:M-W5 (scroll-driven CSS adoption) CHRONIC)
  [medium  ] E6-7 Seven bespoke @keyframes re-implement glass-ui motion idioms — retire to glass-ui tokens/recipes or scroll-driven CSS  (wave:M-W5 (scroll-driven CSS) + M-W6 (token/recipe consolidation))
  [medium  ] E6-8 View-Transitions are wired ad-hoc in the router with raw double-rAF — adopt glass-ui useViewTransition + the shipped view-transition.css substrate  (wave:M-W7 (View-Transition consolidation) — interlocks with J e2e close)
  [low     ] E6-10 value.js easing is imported raw and twice-cataloged — unify on the keyframes.js easing boundary + spring twins, with value.js as the curve authority  (wave:M-W6 (token/easing consolidation))
  [low     ] E6-11 The teleport correction loop in paper navigation is a manual stabilize-rAF — leave as platform scroll, do NOT migrate to a motion primitive  (wave:M-W0 (charter — boundary doc; not a migration) DEFERRED)
  glassui_asks:
    - fourier-shaped-playhead-orchestrator (confirm useRAFLoop covers the ping-pong/alternate case): Confirm whether glass-ui's useRAFLoop (or a motion composable composition) cleanly expresses an infinite forward/reverse ping-pong playhead with reactive eased t, scrub-seek, and speed-scaling — the epicycle/convergence clock shape. If not, a small usePlayhead-style composable wrapping keyframes.js NumericAnimation(direction:'alternate') + RAFPlayback + useIntersectionPause would serve every fourier viz and likely other media consumers.
    - marquee-as-scroll-driven recipe: Whether glass-ui ships (or should ship) an infinite-marquee recipe in scroll-driven.css / animations.css backed by a compositor loop with hover-pause and a reduced-motion carve — the GalleryMarquee shape.
    - canvas-targeted spring guidance (DOM composables vs keyframes light primitives): Confirm the intended division of labour: glass-ui useSpring/useSpringPress for DOM/Vue-reactive transform targets, keyframes.js SpringProgress/SmoothProgress (light tier) for canvas/imperative numeric targets such as fourier's hover-scale and shimmer phase.