# Constellation deploy standardization — the adoption-ask ledger

**Status**: COORDINATION DOC (the per-repo, maintainer-owned adoption asks). **Authored**: 2026-05-28; refreshed 2026-05-29 at fourier-analysis tranche-F thread ζ, wave W12 (ζ.4). **Companion to**: `DEPLOY-STANDARDIZATION-DESIGN.md` (§2 the standard patterns + §4 the tranche fold) and the 4-lane current-state survey at `../audits/runs/2026-05-28-constellation-survey/SURVEY-FINDINGS.md`.

## §0 — What this ledger is (and is NOT)

This is the canonical register of the **cross-repo adoption asks** that flow OUTWARD from the constellation deploy standardization. Each ask is coordinated FROM fourier-analysis (the design substrate lives here) but **executed by the target repo's own maintainer** in that repo's own tree.

**inv-16 — the cross-repo source boundary — is load-bearing here.** fourier-F commits NONE of these asks to any other repo. fourier-F's write surface this tranche was `fourier-analysis/**` + `deploy/**` (the new `mkbabb/deploy` repo fourier-F owns) ONLY. Every line below that says "adopt X into repo Y" is a request, not a change fourier performed. Each is therefore booked as a **maintainer-owned named residual** subject to the F-close 30-day stale-watch (§4).

This ledger is a COORDINATION DOC under `fourier-analysis/docs/`. It mutates no host state and edits no other repo's source.

## §1 — What fourier-F actually completed (the standing-on this ledger sits atop)

So the asks below read against reality, the foundation laid this tranche:

- **The constellation webhook chain was restored.** The chronic ~2-month regression — the GitHub webhook secret was MISSING on all 5 repos, silently severing the auto-deploy chain — was diagnosed and fixed. (This was the root of survey C8 / the long-latent deploy-trigger failure.)
- **The chain was hardened to per-repo URLs + per-repo HMAC secrets.** Each repo now signs against `deploy.babb.dev/hooks/<repo>` with its OWN secret; the single shared HMAC (survey S4) is RETIRED. One repo's compromise no longer re-signs payloads for all five.
- **`mkbabb/deploy` now versions the spine + the standard templates.** The deploy spine that lived only as un-versioned host state is captured; the templates the asks below reference (`deploy-hook.sh`, `docker-compose.hardening.yml`, `ci.yml`, `env.example`, the `cf/` recipes) are authored there as the canonical reference shapes.
- **`dispatch.sh` is RETAINED on the host — deliberately.** The 4 non-fourier app repos (words/floridify, speedtest, value.js/palette-api, csp-solver) still route through the multiplex dispatcher because they have NOT adopted the hardened per-repo `scripts/deploy-hook.sh` + direct `hooks.json` arm. Adopting that, per repo, is exactly the maintainer-owned ask in Ask 2. **The dispatcher's deletion is GATED on ALL FOUR migrating** — deleting it sooner would 404 their deploy path.

fourier-analysis itself already conforms to the full standard (it IS the reference shape for the hardened hook). The asks therefore target the other five repos.

## §2 — The reference templates (in `mkbabb/deploy`)

Every ask names its reference template. The canonical homes (per the `deploy/README.md` contract):

| Reference | Path in `mkbabb/deploy` | What it is |
|---|---|---|
| Hardened deploy hook | `templates/deploy-hook.sh` | flock serialization + record-rollback-SHA + `git reset --hard origin/master` + `docker compose build && up -d` + real health-gate + rollback-on-rollback + post-gate auto-migration |
| Docker hardening floor | `templates/docker-compose.hardening.yml` | `read_only: true` + `tmpfs /tmp` + `cap_drop: ALL` + `security_opt: no-new-privileges` + resource limits + json-file log rotation + backup svc for stateful apps |
| CI template | `templates/ci.yml` | GitHub Actions: lint → typecheck → build → test on push/PR to master; backends add a live-service test job; libraries add a release/publish job |
| `.env` discipline | `templates/env.example` | the `:?` mandatory-prod pattern; `.env` gitignored everywhere |
| Per-repo webhook entry | `host/hooks.json.template` | the per-repo `deploy.babb.dev/hooks/<repo>` entry with `${HMAC_<REPO>}` interpolation (the direct arm that retires the dispatcher route) |
| CF Pages recipe | `cf/pages-deploy.sh` | the standard `wrangler pages deploy` recipe (promoted from speedtest, the reference) |
| DNS-as-code | `cf/dns-cf-sync.sh` | constellation-wide DNS-as-code (promoted from fourier-only) |

## §3 — The adoption asks (per-repo, maintainer-owned)

Acceptance shape = the observable evidence the maintainer's PR is complete. Priority = relative urgency for the F-close stale-watch (P1 highest).

### Ask 1 — CI adoption (no CI today)

- **Targets**: words/floridify, speedtest, csp-solver.
- **The change**: add `.github/workflows/ci.yml` to each repo, copied from `deploy/templates/ci.yml` and parameterized for the repo's stack (backends — words, csp-solver — add the live-service test job; speedtest's frontend/edge build is the build stage). csp-solver's stale `deploy.yml.disabled` is removed in the same PR (superseded by the standard CI + the webhook deploy).
- **Reference template**: `deploy/templates/ci.yml`.
- **Acceptance shape**: a green CI run visible on a push/PR to `master` in each of the three repos; the `Actions` tab shows lint → typecheck → build → test stages; csp-solver no longer carries `deploy.yml.disabled`.
- **Priority**: **P2.** These three repos have NO CI today (survey §1) — a real maturity gap, but not a live-correctness regression. Hygiene, not fire.

### Ask 2 — Hardened deploy-hook + per-repo direct webhook arm (retire the dispatcher route)

- **Targets**: words/floridify, speedtest, csp-solver. *(value.js/palette-api is the same migration but carries an extra precondition — see Ask 3.)*
- **The change**: each repo adds `scripts/deploy-hook.sh` from `deploy/templates/deploy-hook.sh` (flock + health-gate + rollback-on-rollback + auto-migration, parameterized for the repo's compose project + health endpoint). The host then gets a per-repo `hooks.json` entry routing `deploy.babb.dev/hooks/<repo>` DIRECT to that repo's hook (the `deploy/host/hooks.json.template` shape), retiring that repo's arm of the multiplex `dispatch.sh`.
- **Reference template**: `deploy/templates/deploy-hook.sh` + `deploy/host/hooks.json.template`.
- **Acceptance shape**: a push to the repo's `master` fires `deploy.babb.dev/hooks/<repo>` → the repo's own `deploy-hook.sh` runs → the health-gate passes (HTTP 200 from the repo's health endpoint) → the rollback path is exercised in a deliberate failed-deploy drill. The repo's arm is GONE from `dispatch.sh`.
- **Dispatcher retirement gate**: once ALL FOUR non-fourier repos (words, speedtest, csp-solver, AND value.js per Ask 3) have migrated, `dispatch.sh` can finally be DELETED from the host. Until the last of the four lands, the dispatcher stays — deleting it sooner 404s the un-migrated repos' deploy path. The deletion is a single host op that the maintainer performs (or authorizes) once the fourth migration's acceptance is green.
- **Priority**: **P1 (chain), P2 (per-repo).** The chain restoration + hardening is done (§1); the remaining per-repo migration is the long pole that unblocks deleting `dispatch.sh`. value.js (Ask 3) is the hardest of the four and the true critical-path item.

### Ask 3 — value.js / palette-api: convert rsync deploy-dir to a git checkout, then adopt the hardened hook (the N1 real fix)

- **Target**: value.js (palette-api backend).
- **The change**: the palette-api deploy directory is `~/Programming/palette-api` — an **rsync target, NOT a git checkout** (survey §2 deploy-dir divergence). Its dispatcher arm is therefore **latent-broken**: the standard hook does a `git fetch` / `git reset --hard origin/master`, which fails outright on a non-git directory. The ask is two-step: (a) convert the deploy dir to a proper git checkout under the canonical root (`/srv/constellation/palette-api` or the chosen canonical `<app>` dir per `deploy/host/deploy-dir-layout.md`); then (b) adopt `scripts/deploy-hook.sh` + a per-repo `hooks.json` direct arm exactly as Ask 2. This is the **N1 "value.js dispatcher arm" real fix** — not a workaround, the actual repair of the latent-broken arm.
- **Reference template**: `deploy/host/deploy-dir-layout.md` (the canonical-root map) + `deploy/templates/deploy-hook.sh` + `deploy/host/hooks.json.template`.
- **Acceptance shape**: `git -C <canonical palette-api dir> rev-parse HEAD` succeeds (it is a git checkout); a push to value.js `master` fires `deploy.babb.dev/hooks/value-js` → `deploy-hook.sh` performs a successful `git reset --hard` + rebuild + health-gate; the rsync `deploy.sh` step is retired. value.js's arm leaves `dispatch.sh`.
- **Priority**: **P1.** This is the one currently-broken arm (latent — it fails the instant the dispatcher tries to run it). It is also the gating fourth migration for deleting `dispatch.sh` (Ask 2). Highest real urgency of the deploy-trigger asks.

### Ask 4 — Docker hardening level-up to the floor

- **Targets**: words/floridify, speedtest, csp-solver, fourier-analysis. *(value.js/api is already AT the floor — it is the reference shape; keyframes.js is a pure npm lib with no backend container.)*
- **The change**: each backend's `docker-compose` levels up to the `deploy/templates/docker-compose.hardening.yml` baseline — `read_only: true` + `tmpfs /tmp` + `cap_drop: ALL` + `security_opt: no-new-privileges` + resource limits + json-file log rotation; stateful apps add the backup service. (fourier-analysis is currently moderate — USER app + limits + Mongo TLS — and levels up the same way; this is the one item where fourier-F could have moved its OWN repo, but it is recorded here for symmetry with the constellation floor and folds into fourier's own backlog rather than this cross-repo ledger.)
- **Reference template**: `deploy/templates/docker-compose.hardening.yml`.
- **Acceptance shape**: `docker inspect <container>` on each app shows `ReadonlyRootfs: true`, `CapDrop: ["ALL"]`, `SecurityOpt` containing `no-new-privileges`; the app still passes its health-gate (read_only is the most likely to break a service that writes outside `/tmp` — the acceptance must confirm functional parity, not just the flags).
- **Priority**: **P3.** Defense-in-depth posture improvement; no live exposure. Lowest urgency.

### Ask 5 — Frontend-hosting convergence to CF Pages

- **Targets**: value.js, keyframes.js.
- **The change**: both currently deploy their frontends via **GitHub Pages → CF CNAME (peaceiris GH Action)**; the D-era declared target is **CF Pages** (the speedtest reference). Converge each to the `deploy/cf/pages-deploy.sh` wrangler recipe (and the corresponding GitHub Actions secret for the CF API token, per the §2.5 secrets discipline), retiring the peaceiris gh-pages step. This resolves the single biggest "is the documented topology the live topology?" drift the survey surfaced.
- **Reference template**: `deploy/cf/pages-deploy.sh`.
- **Acceptance shape**: `color.babb.dev` (value.js) and `keyframes.babb.dev` serve from CF Pages (a `cf-ray` header + the Pages deployment visible in the CF dashboard / `wrangler pages deployment list`); the peaceiris `gh-pages` workflow step is removed; the CNAME→GH-Pages path is gone.
- **Priority**: **P3.** A working frontend served two ways is a consistency/drift item, not a correctness or security one. Converge when the maintainer touches those repos' deploy.

### Ask 6 — csp-solver: register the missing solve / openapi / docs routes (N4)

- **Target**: csp-solver (sudoku repo).
- **The change**: the backend is live at `/api/v1/health` but `solve`, `openapi.json`, and `docs` routes 404 — a **route-registration regression** in the sudoku repo (the router is defined but not mounted). The ask is a **1-line `app.include_router(...)`** (plus FastAPI's docs/openapi being enabled) so the documented API surface is reachable. This is the N4 residual; per FA2 §3 it is an external-repo route-registration bug, ASK-only — fourier holds no lever into csp-solver source.
- **Reference template**: none (this is a repo-local correctness fix, not a deploy-spine template adoption) — the acceptance is the canonical FastAPI surface, not a standard file.
- **Acceptance shape**: `curl https://api.sudoku.babb.dev/api/v1/solve` (or the documented solve path) returns a real solve response (not 404); `curl https://api.sudoku.babb.dev/openapi.json | jq .info.title` returns the API title; `/docs` serves Swagger UI.
- **Priority**: **P2.** A real (if low-traffic) prod API-surface regression — the documented endpoints don't resolve. Above pure hygiene, below the broken deploy arm.

### Ask 7 — floridify: Mongo-bind upstream hardening (N7)

- **Target**: words/floridify.
- **The change**: harden the floridify Mongo bind upstream so the database is not reachable beyond its intended internal surface (the D-era constellation Mongo-exposure close was applied to the fourier-side Mongos; floridify's bind is the remaining upstream item). The exact fix is the maintainer's call — bind to the internal docker network only / `127.0.0.1` + firewall, mirroring the D.W1 close — but the ask is to bring floridify's Mongo to the same internal-only posture the rest of the constellation holds.
- **Reference template**: the D-era Mongo-exposure close pattern (internal-only bridge network; no published Mongo port) — documented in `../tranches/D/coordination/CONSTELLATION-DEPLOY.md`; no new `mkbabb/deploy` template (it is a compose-bind posture, captured implicitly by the `docker-compose.hardening.yml` network discipline).
- **Acceptance shape**: an external port scan / `docker port <mongo-container>` shows NO published Mongo port; the Mongo is reachable only on the app's internal bridge network; floridify's services still connect (functional parity confirmed).
- **Priority**: **P2.** A data-exposure-adjacent hardening item — higher than cosmetic hygiene, though the survey did not flag a confirmed live exposure (it is upstream-hardening, not an open breach). Treat as P2 pending the maintainer's confirmation of the current bind.

## §4 — Withdrawn flags + the named-residual stale-watch

**S1 + S2 are WITHDRAWN — nothing to rotate.** The 4-lane survey initially flagged committed credentials in floridify (`pk_*` Clerk keys) and speedtest (`cfat_*` CF token). The maintainer determination (2026-05-28) is that neither is a live exposure: Clerk `pk_*` is a **publishable key** (public by design — it ships in the frontend bundle), and the speedtest `cfat_*` is **not a sensitive/active credential**. No rotation, no history-purge, no remediation — in or out of F scope. Recorded resolved in `SURVEY-FINDINGS.md §3` and `DEPLOY-STANDARDIZATION-DESIGN.md §3`. These appear here only to forestall their re-flagging.

**The named-residual stale-watch.** Each ask above is a **maintainer-owned named residual** entered into the F-close 30-day stale-watch (inherited from `E/FINAL.md §5`, re-triggered at F.W13). The watch's purpose is not to force execution — these are the maintainer's to schedule — but to keep them from silently rotting unbooked. At each 30-day review, an ask is either (a) closed (the maintainer's PR landed; acceptance green), (b) re-affirmed as open with an unchanged owner, or (c) reclassified (e.g. a priority change, or folded into a successor tranche).

**Re-triggered at fourier-G thread ζ (G.W8, 2026-05-30).** The 30-day watch is re-stamped. One material change: **Ask 4's fourier portion is now LANDED** — G.W7 leveled fourier's `docker-compose.prod.yml` to the hardening floor (backend FULL: `read_only`+`tmpfs`+`cap_drop: ALL`+`no-new-privileges`, verified live via `docker inspect` + functional smoke; frontend/mongo/nginx `no-new-privileges` with `read_only`/`cap_drop` booked as per-image staging-test residuals). The other six asks remain maintainer-owned and OPEN. **New coordination note (G.ζ): the `api.color.babb.dev` inv-22 partial conformance** (serves only `/`→200; `/health`/`/docs`/`/openapi.json`→404) is a value.js-owned vhost item — see `INVARIANTS.md §2.7` for the honest `F-Inv 22*` scoping. It rides alongside Ask 5 (value.js deploy convergence) as a value.js maintainer concern; fourier holds no lever (inv-16).

| Ask | Target repo(s) | Change (one line) | Priority | Owner | Stale-watch status |
|---|---|---|---|---|---|
| 1 | words, speedtest, csp-solver | adopt `deploy/templates/ci.yml` (no CI today) | P2 | each repo's maintainer | OPEN — re-affirmed G.W8 |
| 2 | words, speedtest, csp-solver | adopt hardened `deploy-hook.sh` + per-repo direct `hooks.json` arm (retire dispatcher route; deletion gated on all four) | P1/P2 | each repo's maintainer | OPEN — re-affirmed G.W8 |
| 3 | value.js (palette-api) | rsync deploy-dir → git checkout under canonical root, then adopt `deploy-hook.sh` (the N1 real fix; gating 4th migration) | P1 | value.js maintainer | OPEN — re-affirmed G.W8 |
| 4 | words, speedtest, csp-solver, ~~fourier~~ | level up to `deploy/templates/docker-compose.hardening.yml` floor | P3 | each repo's maintainer | **fourier portion LANDED G.W7** (backend full floor live; others no-new-priv + booked); 3 external repos OPEN — re-affirmed G.W8 |
| 5 | value.js, keyframes.js | converge GH-Pages→CF-CNAME to CF Pages per `deploy/cf/pages-deploy.sh` | P3 | each repo's maintainer | OPEN — re-affirmed G.W8 |
| 6 | csp-solver | 1-line `app.include_router` — register missing solve/openapi/docs routes (N4) | P2 | csp-solver maintainer | OPEN — re-affirmed G.W8 |
| 7 | words/floridify | Mongo-bind upstream hardening to internal-only (N7) | P2 | floridify maintainer | OPEN — re-affirmed G.W8 |
| inv-22-color | value.js (palette-api vhost) | bring `api.color` to the 4-endpoint vhost contract (`/health`/`/docs`/`/openapi.json` currently 404) — honest `F-Inv 22*` scope | P3 | value.js maintainer | OPEN — booked G.W8 (new) |
| glass-ui-a11y | glass-ui (`ConfiguratorLayer`) | apply `inert` to the collapsed `ConfiguratorLayer` body — it sets `aria-hidden="true"` but keeps focusable children, an axe `aria-hidden-focus` **serious** violation. Surfaced by fourier **H.W1**'s e2e a11y keystones (workspace-default AND Configurator-open: any closed sibling layer carries the defect); both app `test.fixme`'d pending a glass-ui release + the guarded `^2→^3` bump | P2 | glass-ui maintainer | **SATISFIED-UPSTREAM / ADOPT-NOW** (2026-06-04) — `inert` SHIPPED **glass-ui 3.1.1** (`ConfiguratorLayer.vue:144` `:inert="!internalOpen \|\| undefined"` + `:143` `:aria-hidden`), closing the `aria-hidden-focus` violation at the source. On the `^3.2.0` bump, **un-`test.fixme`** the 2 a11y keystones (`visualization-crud.spec.ts:630`, `visualization-ux.spec.ts:110`) — they flip to live asserts. Kill-date = the bump commit |
| cascade-vjs | value.js | bump `unplugin-vue-markdown ^29.2.0→^32.0.0` (vite `^8` peer) + `@mkbabb/{glass-ui,keyframes.js}` `file:`→`^published` + regen lockfile — clears the `npm ci` ERESOLVE (cascade HEAD) | P2 | value.js maintainer | OPEN — booked **H.W6** (new); precise fix in `H/waves/W6-W7-epsilon-booking.md §2A` |
| cascade-kf | keyframes.js | `@mkbabb/glass-ui file:`→`^3.0.0` + `npm install` lockfile regen — clears EUSAGE/@popperjs + TS2307 (after cascade-vjs) | P2 | keyframes.js maintainer | OPEN — booked **H.W6** (new); §2B |
| cascade-gui | glass-ui | regenerate `package-lock.json` so `@mkbabb/{keyframes.js^2.1.1,value.js^0.10.0}` resolve from the registry (no package.json change; pure lockfile drift; after cascade-kf) | P2 | glass-ui maintainer | OPEN — booked **H.W6** (new); §2C |
| words-spa | words/floridify | add `deploy-pages.yml` + `scripts/pages-deploy.sh` (fourier δ model) + commit `frontend/package-lock.json` to publish the `frontend/` SPA (the genuine 404 outage); register the CF-Pages project/CNAME | P1 | words maintainer | OPEN — booked **H.W7** (new); §2D |
| valuejs-J-atomdiff | value.js (palette-api) | EXTEND `forkPalette`→`remixPalette` (records the diff); `PaletteVersion` gains `atom_diff`; add `GET /:slug/diff?from=`; author the `lib/crud/atomdiff.ts` per-language PATTERN twin (NOT a shared package — inv-16/inv-26); bind to the canonical repo-neutral `docs/tranches/J/design/J-diff-shape.md` shape doc so the `/diff` envelope is byte-isomorphic at the J cross-repo parity probe | P2 | value.js-J maintainer | **DONE-in-sibling** (2026-06-04) — value.js EXECUTED + GREEN in J (`api/src/lib/crud/atomdiff.ts`, the canonical name; `PaletteVersion.atomDiff`; `POST /:slug/remix`; `GET /:slug/diff`; conformance probe binds `J-diff-shape.md §3/§4`), folded into K, carried through **L CLOSED** (`66dcd68`). Residual = fourier's OWN `/diff` envelope PARITY confirm (fourier-local Python probe; K.W6) |
| valuejs-J-publish | value.js (palette-api) | add the symmetric `POST /:slug/{publish,unpublish}` idempotent in-place flag-flip (per `docs/tranches/J/design/J.W1c-publish-visibility.md`) — never a new row, the guard's first live caller (inv-I-2); **AND in the SAME change the [P0] fix it names**: `listPalettes` (`crud-list.ts:85`) is MISSING the `visibility="public"` filter, so private/unlisted palettes currently LEAK into the public browse list — the `f.visibility = "public"` clause MUST land together with the publish verb (inv-15 name-the-consumer; without it the op is substrate-without-a-consumer) | P1 | value.js-J maintainer | **DONE-in-sibling** (2026-06-04) — value.js shipped `POST /:slug/{publish,unpublish}` (the inv-I-2 guard's first live caller) AND the [P0] public-view filter (`crud-list.ts:114` `visibility="public"`), then DEEPENED in **L** (`Palette.sessionToken` + 4-state `status` excised; filter transposed to `(visibility, tier)`). **L CLOSED** (`66dcd68`). Residual = fourier's OWN publish envelope PARITY confirm (fourier-local; K.W6) |
| ~~glass-ui-P5-inner-rounding~~ | ~~glass-ui (`ConfiguratorLayer`)~~ | **KILLED-AS-PHANTOM** (2026-06-04) — STRUCK. The "round the inner section dividers at the component root" ask is a phantom **twice over**: user-REJECTED ("the rounding is controlled by the container") AND container-owned BY DESIGN upstream. glass-ui's `Configurator.vue:130` (`rounded-panel … overflow-hidden`) owns the radius at the container-root clip; commit `779fed7` deliberately REVERTED per-section radius after adversarial verification found it geometrically inert + divider-deforming; glass-ui's own `AS/FINAL.md:113-118` calls fourier's ledger a "misdiagnosis." The inner sections are CORRECTLY square by design — there is nothing for glass-ui to add. The "NOT satisfied until the inner sections round" acceptance was itself the phantom. **NEVER re-book** (the 3-tranche I→J→K inertia is the lesson). | — | — | **KILLED-AS-PHANTOM** — see §10 |
| glass-ui-dock-vt-name | glass-ui (`GlassDock` / `dock.js`) | replace the setup-local `view-transition-name` counter (`let n` in `setup()` → every instance mints `glass-dock-1`) with `useId()`-derived unique-per-mount VT-names, so two co-mounted docks no longer collide; this is the SINGLE root cause of fourier's e2e red (~13 specs trip the "no console errors" probe on "Unexpected duplicate view-transition-name: glass-dock-1", pre-existing — prior master run 26831216131 identical) AND a real prod regression (the editing dock geometry-morph silently fails when the duplicate-named snapshot is dropped during `startViewTransition`) | P1 | glass-ui maintainer | **SATISFIED-UPSTREAM / ADOPT-NOW** (2026-06-04) — the useId fix SHIPPED **glass-ui 3.1.1** (`GlassDock.vue:137` `glass-dock-${useId()}`, `DockLayerGroup.vue:69`; a `proof:vt-names` static gate makes the collision class structurally impossible) and is present in 3.2.0. The fourier unblock is the one-line `^3.1.0`→`^3.2.0` caret bump + `npm install` — it greens fourier e2e AT THE SOURCE. Kill-date = the bump commit. **No console-filter bridge, no self-deleting probe, no allow-entry** (the W6 bridge was born-legacy — the upstream shipped; building it would mask a non-existent error). fourier holds no lever on the upstream (inv-16); the bump is fourier-local |

## §5 — Provenance + boundary attestation

- **Asks 1–5** derive from `DEPLOY-STANDARDIZATION-DESIGN.md §2` (the standard patterns) + `SURVEY-FINDINGS.md §1–§2` (the current-state heterogeneity).
- **Ask 3** is the N1 "value.js dispatcher arm" real fix (the latent-broken non-git deploy dir).
- **Ask 6** is N4 (csp-solver route-registration regression; FA2 §3).
- **Ask 7** is N7 (floridify Mongo-bind upstream; F.md §9).
- **glass-ui-a11y** is surfaced by fourier H.W1 (the e2e a11y keystones in `visualization-ux.spec.ts`: "workspace default" AND "ContourSettings Configurator-open"): a real `aria-hidden-focus` **serious** violation in glass-ui's `ConfiguratorLayer` collapsed body (it sets `aria-hidden="true"` but keeps focusable children, omitting `inert`). Because the workspace stacks several independent `ConfiguratorLayer`s, ANY closed sibling carries the defect — so opening one layer cannot clear it; both keystones hit it. fourier holds no lever (the app consumes the published `@mkbabb/glass-ui@^2.0.0`); both keystones are `test.fixme`'d as honest, booked baselines pending the glass-ui `inert` fix + the guarded `^2→^3` bump (an inv-16′ sweep candidate). The app's own co-located defect — a `button-name` **critical** on the ContourSettings Strategy `SelectTrigger` — was fixed in-tree (added `aria-label`), so the residual is purely vendored.
- **inv-16 attestation**: this ledger is a coordination doc under `fourier-analysis/docs/`. Authoring it edited NO other repo's source and mutated NO host state. Every ask is a request the named maintainer executes in their own tree. fourier-F's tranche write surface remained `fourier-analysis/**` + `deploy/**` only.

## §6 — H.ε booking (inv-16′; BOOK-ALL, 2026-06-01)

H opened inv-16′ (authorized-cross-repo-sweep) to let fourier *execute* constellation CI fixes, since the user owns all `mkbabb/*` and asked to "perfect the constellation." At H.ε the working-tree recon found **all five sibling repos mid-flight with unpushed local backlogs** (value.js in-sync; keyframes +19, glass-ui +111, words +11, speedtest +563 unpushed) — and a fix-push would also publish that backlog. Asked to choose, the user selected **"Book all precise fixes — touch no sibling repo"** (2026-06-01). So H.ε executed **nothing cross-repo**: it authored the *exact, file-verified* fixes (the cascade `cascade-vjs`/`-kf`/`-gui`, `words-spa`, `speedtest` CI = Ask 1, `api.color` = inv-22-color, `api.sudoku` = Ask 6, `glass-ui-a11y`) so each owner applies on a clean checkout with zero further investigation. The bounded nine-commit list + the per-repo diffs live in `../tranches/H/waves/W6-W7-epsilon-booking.md`. **inv-16′ governed: enabled, named, ledgered — and, here, deliberately not compelled.** fourier-H's write surface for ε was `fourier-analysis/docs/**` only. The **speedtest = `speedtest.friday.institute`** correction (not babb.dev) is recorded in the booking doc §4 and reconciled across the docs at H.ζ.

## §7 — J-era bookings (inv-16′; J deep-audit fold, 2026-06-02)

The J deep-audit (`../audits/runs/2026-06-02-J-deep-audit/J-deep-audit.md` + reports A1–A6) folded three discrete cross-repo asks that the J authoring asserted in prose but had NOT yet ledgered. Each is a NAMED, file-verified, per-repo-green-CI-gated inv-16′ row (added to the §4 table above). fourier-J's write surface for these is `fourier-analysis/docs/**` only — each is the named maintainer's to execute on their own clean checkout (inv-16 held; the constellation was mid-flight at authoring per H.ε / `../tranches/H/waves/W6-W7-epsilon-booking.md`).

- **valuejs-J-atomdiff** is the ATOM-DIFF adoption the fourier-J plan already *claims* exists as "a NAMED, file-verified `ADOPTION-ASKS.md` entry" (`J/J.md:82,94-96`) — but which was absent until now (the **A6-3** finding: the grep of this ledger returned nothing; the claim was forward-tense without an artifact, an inv-27/inv-16′ honesty gap). It books value.js EXTENDING `forkPalette`→`remixPalette` (records the per-stop atom-diff), `PaletteVersion` gaining `atom_diff`, the new `GET /:slug/diff?from=`, and the `lib/crud/atomdiff.ts` per-language PATTERN twin — the **canonical** module name (A6-4: reconcile `lib/crud/atomdiff` vs `lib/crud/remix` vs `services/palette/diff.ts` to the one name `atomdiff`). It is a shared PATTERN, NOT a shared package (inv-16) and NOT codegen (inv-26). The ask **binds to the canonical repo-neutral diff-shape doc** `docs/tranches/J/design/J-diff-shape.md` (FA1's canonical name; authored at J.W1): both repos' `/diff` envelopes assert against the shape doc, not against each other — so the cross-repo parity probe (the J close-gate) compares two impls to one contract, the inv-26 spirit. The value.js fork machinery already exists (`api/src/services/palette/forks.ts`); the ask is the diff layer + the publish facet, not the substrate.

- **valuejs-J-publish** is the PUBLISH/VISIBILITY peer — the symmetric counterpart to the orphaned-CORE fourier doc `../tranches/J/design/J.W1c-publish-visibility.md`. It books value.js adding the dedicated agent-legible verb pair `POST /:slug/{publish,unpublish}`: an idempotent in-place `$set` flag-flip on the SAME `{slug}` row (NEVER a new document — the structural anti-duplication guarantee that draws the bright line against remix), the FIRST live caller of value.js's dead `inv-I-2` visibility transition guard, owner-gated from the session (inv-14), If-Match ETag-guarded, RFC 9457 problem+json (inv-26). **The [P0] it names lands in the SAME change** (inv-15 name-the-consumer, made executable): `listPalettes` (`api/src/services/palette/crud-list.ts:85`) filters ONLY `f.deletedAt = null` — there is **no** `f.visibility = "public"` clause anywhere, so `private`/`unlisted` palettes currently LEAK into the public browse list. The publish op is substrate-without-a-consumer until that filter lands; the conjoined public-view predicate must become `{ deletedAt: null, visibility: "public" }`, shipped together with the publish verb. The one legitimate per-repo difference is the private-state NAME (fourier `draft`, value.js `private`) and the `unpublish` landing state (fourier `unlisted` — its contract forbids `public→draft`; value.js `private` — all 9 `(visibility, tier)` tuples legal); both satisfy "not in public view." Verified at the J close via the §6 response-envelope-parity round-trip.

- **glass-ui-P5-inner-rounding** is the literal user-reported P5 defect, distinct from the already-booked `glass-ui-a11y` row. P5 ("glass-ui must be **fully rounded by default, at the root**") is **booked-not-shipped** (A5): the drafted `glass-ui-root-rounding.patch` + glass-ui's committed `b6d6cf4` fixed only the OUTER `Configurator` container's `rounded-card`→`rounded-panel` axis. The actual defect — the inner `ConfiguratorLayer` sections rendering squared (`border-b` dividers, no radius) — is **byte-identical in 2.x and 3.x** (`../audits/runs/2026-06-01-constellation-ui/style-audit.md:10,266`): the `^2→^3` bump does NOT fix it; it needs a code change at the `ConfiguratorLayer` root, which is glass-ui-owned (fourier holds no lever, inv-16). The ask is to round the inner section dividers at the component root and to **confirm against shipped glass-ui post-`b6d6cf4`**; P5 is NOT marked satisfied until the inner sections round. Gated on a glass-ui release.

**inv-16 attestation (J-era)**: §7 authored `fourier-analysis/docs/**` only; edited NO other repo's source; mutated NO host state. Each row is a request the named maintainer executes in their own tree, per-repo-green-CI-gated. The cross-link to the deep-audit run dir is the provenance.

## §8 — K-era bookings (inv-16′; J-postimpl-audit fold, 2026-06-04)

The J post-implementation audit (`../audits/runs/2026-06-04-J-postimpl-audit/SYNTHESIS.md` + the 6 dimension reports A1–A6) folded ONE discrete cross-repo ask that the prior bookings had NOT yet ledgered: the `glass-ui-dock-vt-name` row added to the §4 table above. It is a NAMED, file-verified, per-repo-green-CI-gated inv-16′ row, and it is the **single root cause** of fourier's e2e red — verified absent from §7 at the audit (the §7 J-era bookings end at `glass-ui-P5-inner-rounding`; the grep returned nothing, an inv-27/inv-16′ honesty gap A4 closes). fourier-J/K's write surface for this is `fourier-analysis/docs/**` only — the row is the glass-ui maintainer's to execute on their own clean checkout (inv-16 held).

- **glass-ui-dock-vt-name** is the e2e/CI chronic the deep-audit run's **A4** report root-caused and pinned to source. The briefing's "module counter" framing is WRONG and the correction is load-bearing: `GlassDock` mints its `view-transition-name` from a **setup-local** counter (`node_modules/@mkbabb/glass-ui/dist/dock.js:228` — `let n = 0` *inside* `setup()`), so the counter resets to `0` on every instance and `++n` deterministically yields `glass-dock-1` for the first (and only) dock of *each* instance. Two co-mounted docks (fourier's `CanvasControlsDock` + `EditorControlsDock`, both live when `isEditing && store.contour && hasData`) therefore ALWAYS collide on `glass-dock-1` — there is no instance that ever mints `glass-dock-2`. The idiomatic fix is `useId()` (Vue's SSR-safe unique id, already imported in `dock.js:6`), NOT "fix the counter" — a module counter would still be order-dependent and SSR-fragile. The `props` block exposes no VT-name prop, so a fourier consumer cannot pass a unique name: this is a hard upstream defect, glass-ui-owned, fourier holds no lever (inv-16).

- **This row is DUAL-NATURED — an inv-27 e2e blocker AND a real prod visual regression.** The warning is NOT cosmetic (A4 §3): the browser logs "Unexpected duplicate view-transition-name: glass-dock-1" *during* a `startViewTransition` capture, and fourier opens exactly such a transition on `visualization`↔`workspace` route morphs (`router/index.ts:131-147`). During that capture the browser drops the duplicate-named snapshot, so the dock geometry-morph **silently fails in production** while editing. One symptom, two defects: the e2e probe surfaced a real visual regression, not noise. ~13 specs trip the "no console errors" probe on the duplicate-VT string; the failure is pre-existing (prior master run `26831216131` is byte-identical), so the suite has been red since before this session — the exact inv-27 corrosion the invariant exists to prevent.

- **The upstream fix SHIPPED (re-ground 2026-06-04): it is now ADOPT-NOW, not 3.2.0-gated.** The `useId()` fix landed **glass-ui 3.1.1** (`GlassDock.vue:137` `glass-dock-${useId()}`, `DockLayerGroup.vue:69`; the `proof:vt-names` static gate makes the collision class structurally impossible) and is present in 3.2.0. So fourier's "wait on a future glass-ui 3.2.0 ship" frame is FALSE — the fourier unblock is a one-line `^3.1.0`→`^3.2.0` caret bump + `npm install` against an ALREADY-PUBLISHED peer (`npm latest=3.2.0`), which greens fourier e2e AT THE SOURCE. The bump belongs at the FRONT of the executable chain (it is the hard predecessor of J.W6's green close + the inv-28 API-arm gate's pass-condition + the DEC-2/control-pane lens), NOT a K-deploy-sequenced wait. The kill-date is the bump commit. fourier executes only the *adoption* (the bump + consume); the *upstream fix* was the glass-ui maintainer's and is DONE (inv-16). See §10.

- **fourier DECLINED a local `!important` workaround (NO-LEGACY, per the user) — and the W6 console-filter bridge is now KILLED-BEFORE-BIRTH.** A `!important` cannot rename a `view-transition-name` that glass-ui sets via an inline style binding keyed off the minted name, and wrapping each dock in a parent with its own VT-name does not help (the duplicate is on the *child* dock element, not the wrapper). The only correct fix is the glass-ui-owned `useId()` change — which SHIPPED (3.1.1). The previously-proposed **W6 kill-dated exact-string console-filter + self-deleting satisfaction probe is STRUCK as born-legacy** (it was authored-only, never built — `_probes.ts` absent, 0 grep hits): the upstream shipped, so building a bridge now would mask a console error that NO LONGER EXISTS — the exact NO-LEGACY violation the directive forbids when the real fix is one bump away. The honest path is the bump; the e2e probe then passes on real silence (a plain green assertion). The legitimate filter-DRY consolidation (the existing 401/404/412/429 whitelist → a `_probes.ts` helper) may still proceed as a pure helper — but with NO dock-VT allow-entry and NO self-deleting probe.

**inv-16 attestation (K-era)**: §8 authored `fourier-analysis/docs/**` only; edited NO other repo's source (touched NEITHER `dock.js` NOR any sibling tree); mutated NO host state. The `glass-ui-dock-vt-name` row is a request the glass-ui maintainer executes in their own tree, per-repo-green-CI-gated; fourier's only act is the 3.2.0 adoption (K.W4) once the upstream fix ships. The cross-link to the 2026-06-04 J-postimpl-audit run dir (A4) is the provenance.

## §9 — Control-pane hierarchy asks (inv-16′; the 2026-06-04 4-auditor control-pane audit)

The control-pane design-hierarchy audit (`../audits/runs/2026-06-04-control-pane-hierarchy/SYNTHESIS.md` + the 4 dimension reports D1-D4 → the WC lens `../tranches/J/design/WC-design-hierarchy.md`) found the panes speak FIVE title idioms / three rhythm systems / four divider treatments, only one of which is the platform instrument (the workspace `Configurator`). The fix is propagation, not invention — and it partitions cleanly: **H1-H10 are fourier-local** (consume the SHIPPING glass-ui 3.1.0 primitives; land J.W5; a net code DECREASE), and **three discrete glass-ui asks** are net-new (carried by K.W4's glass-ui-3.2.0 adoption bundle alongside `glass-ui-dock-vt-name` + `glass-ui-P5-inner-rounding` + `glass-ui-a11y`):

- **A-1 — `ConfiguratorLayer`/`ConfiguratorRow` inter-row divider-rule opt-in.** The twin-line hairline groove (catch-light over under-shadow, dark-aware) ships TODAY only on `.instrument-rail` (`dist/styles/instrument-rail.css:68-110`, auto sibling-rule); `.configurator-layer` emits no divider and `.configurator-row` emits only gap/padding (grep-confirmed; **re-verified ABSENT at 3.2.0** — `ConfiguratorLayer.vue:100` still emits a flat `border-b`). fourier cannot retrofit the rail's sibling-rule onto the Configurator chassis, and hand-rolling a hairline between rows is the forbidden NO-LEGACY workaround. The ask: glass-ui adds the opt-in to the Configurator chassis. **Satisfaction:** adjacent `ConfiguratorRow`s render the machined groove (not flush, not a flat line) at every breakpoint. Owner: glass-ui maintainer. fourier holds no lever (inv-16). **BOOK-with-kill-date = glass-ui AT/3.3.0** (genuinely absent at 3.2.0; glass-ui SELF-BOOKED it to its AT successor, `AS/FINAL.md:146-152`). Non-blocking; fourier rides rhythm + explicit `.chassis-divider` meanwhile. [P2]
- **A-2 — `ConfiguratorLayer` `label`/`sub` bound to the `text-title`/`text-heading` ladder at the component root.** Today the title size/weight is a magic literal at every consumer site (`text-sm font-semibold`, etc. — **re-verified at 3.2.0**, `ConfiguratorLayer.vue:118`) and `style.css` carries NO section-title token — so "promote the title to the ladder" (WC-typo #1) has no single lever. Binding the `label`/`sub` typography to the glass-ui ladder at the component root makes one rung change restyle every pane title. fourier setting the utility on its OWN headers is fourier-local; the component-internal token hook is this ask (reaching inside `ConfiguratorLayer` to restyle its label is an inv-16 internal override). **Satisfaction:** one ladder-token change visibly restyles every pane title across all three routes. Owner: glass-ui maintainer. **BOOK-with-kill-date = glass-ui AT/3.3.0** (genuinely absent at 3.2.0; glass-ui SELF-BOOKED it to its AT successor, `AS/FINAL.md:153-155`). Non-blocking; fourier sets the ladder utility on its OWN headers meanwhile. [P2]
- **A-3 — `Configurator asideSide` prop** — the controls-LEFT (authoring-rail) lever for DEC-2. **SHIPPED glass-ui 3.2.0 / ADOPT-NOW** (`Configurator.vue:85/101/162`, default `right`; "left" flips via grid-column placement + border-side swap, NO DOM reorder — zero a11y regression; the `asideWidth` token band also shipped). The earlier "does not exist at 3.1.0" gate is DISSOLVED. fourier consumes `aside-side="left"` at J.W5 and DELETES the 3 `grid-template-columns` overrides (`VisualizationView.vue:322/326/329`). [P0/keystone]

**`glass-ui-P5-inner-rounding` is KILLED-AS-PHANTOM (re-ground 2026-06-04)** — STRUCK from the §7 ledger; this control-pane audit's prior "CONFIRMED (not re-booked)" line is SUPERSEDED. The squared inner sections (`_seed-workspace-configurator.png`) are CORRECT by design — the radius is owned at the container-root clip (`Configurator.vue:130` `rounded-panel … overflow-hidden`); per-section radius was deliberately REVERTED upstream (`779fed7`) as geometrically inert + divider-deforming; glass-ui's `AS/FINAL.md:113-118` calls fourier's ledger a "misdiagnosis." NEVER re-book. See §10.

**inv-16 attestation (control-pane audit)**: §9 authored `fourier-analysis/docs/**` only; edited NO glass-ui source, NO sibling tree, NO host state. A-1/A-2 are requests the glass-ui maintainer executes in their own tree (now BOOK-to-AT, glass-ui-self-booked); A-3 is DONE-in-3.2.0 and ADOPT-NOW. fourier's only act is the 3.2.0 adoption bump. The fourier-local H1-H10 touch only fourier surfaces (J.W5), consume the shipping glass-ui primitives, and never hand-roll or `!important`-override a glass-ui internal (NO-LEGACY). Provenance: the 2026-06-04 control-pane-hierarchy run dir.

## §10 — glass-ui-3.2.0 re-ground (2026-06-04)

The glass-ui-reground deep audit (`../audits/runs/2026-06-04-glassui-reground-deep-audit/SYNTHESIS.md`, 6 dimension auditors against the SIBLINGS' ACTUAL HEADS) re-grounded every glass-ui/value.js row in this ledger against the reality each sibling had already shipped. **The headline: the "gated on a future glass-ui 3.2.0 release" scaffolding the §7/§8/§9 bookings carried is FALSE — glass-ui shipped 3.2.0 (`npm latest=3.2.0`, HEAD `06b35d9`), value.js closed J/K/L, keyframes shipped 3.0.0. The whole cohort of gated asks collapses to a one-line `^3.1.0`→`^3.2.0` caret bump — the FIRST executable move, ADOPT-NOW, not a wait.** The bump is the hard predecessor of J.W6's green close, the inv-28 API-arm gate's pass-condition, and the DEC-2/control-pane lens — so it belongs at the FRONT of the executable chain.

**The re-grounded verdicts (this note supersedes the dated prose in §7/§8/§9 where it conflicts; the dated records are not falsified, just superseded):**

- **DONE-in-glass-ui-3.2.0 / ADOPT-NOW (4 asks dissolve on the bump):**
  - `glass-ui-dock-vt-name` — useId fix shipped **3.1.1** (`GlassDock.vue:137`); the bump greens fourier e2e at source. The W6 console-filter bridge is KILLED-BEFORE-BIRTH (born-legacy — the upstream shipped; building it would mask a non-existent error). Kill-date = the bump commit.
  - `glass-ui-a11y` (`inert`) — shipped **3.1.1** (`ConfiguratorLayer.vue:144`); un-`test.fixme` the 2 a11y keystones on the bump.
  - **A-3 `asideSide`** — shipped **3.2.0** (`Configurator.vue:85/101/162`); fourier consumes `aside-side="left"` for DEC-2 and deletes the 3 grid overrides.
  - **`useTextHighlight`** — shipped **3.2.0 on `/dom`** (NOT `/motion-core` — fix the doc drift), multi-instance-safe + a `supported` flag (the inv-29 progressive-enhancement floor for free). fourier imports from `@mkbabb/glass-ui/dom` for the equation-hover + diff-viewer consumers.

- **KILLED-AS-PHANTOM (struck, never re-book):**
  - `glass-ui-P5-inner-rounding` — user-REJECTED AND container-owned BY DESIGN. The radius lives at the container-root clip (`Configurator.vue:130` `rounded-panel … overflow-hidden`); per-section radius was REVERTED upstream (`779fed7`) as geometrically inert + divider-deforming; glass-ui's `AS/FINAL.md:113-118` calls fourier's ledger a "misdiagnosis." The inner sections are CORRECTLY square. The "NOT satisfied until the inner sections round" acceptance was itself the phantom. The 3-tranche I→J→K inertia is the lesson: a book is not a fact until re-verified against the sibling's actual close.

- **DONE-in-value.js-L (the cohort peers — flip the OPEN rows):**
  - `valuejs-J-atomdiff` + `valuejs-J-publish` — value.js EXECUTED both in J, folded into K, hardened through **L CLOSED** (`66dcd68`); the [P0] public-view filter shipped (`crud-list.ts:114`). The only residual is fourier's OWN `/diff` + publish ENVELOPE PARITY confirm (fourier-local Python probe, K.W6) — value.js owes the cohort nothing further.

- **BOOK-with-kill-date = glass-ui AT/3.3.0 (the only surviving glass-ui asks):**
  - **A-1** inter-row divider-rule + **A-2** label/sub→ladder token hook — genuinely ABSENT at 3.2.0; glass-ui SELF-BOOKED both to its AT successor (`AS/FINAL.md:146-155`). Non-blocking for the bump; fourier rides rhythm + explicit `.chassis-divider` + a fourier-local title ladder meanwhile.

**inv-16 attestation (§10 re-ground)**: this note authored `fourier-analysis/docs/**` only; edited NO sibling repo's source, NO glass-ui/value.js/keyframes tree, NO host state, NO code. Every cross-repo claim cites the sibling's own committed artifact (file:line / commit / FINAL.md / CHANGELOG), read-only. fourier's only forward act is the fourier-local `^3.2.0` bump + consume. Provenance: the 2026-06-04 glass-ui-reground deep-audit run dir.

End of ADOPTION-ASKS.md.
