#!/usr/bin/env bash
set -euo pipefail

# Conformance skeleton — C1.1 (CRUD-CONTRACT §1 / CONFORMANCE-MATRIX.md).
# W3: assert no content-hash-shaped fragment ([0-9a-f]{32,}) appears in any
# client-side URL pattern / route path under web/src/ or api/. Exit 0 iff zero
# matches; emit "OK: no hash-shaped URL fragments" on success. Placeholder until W3.

exit 0
